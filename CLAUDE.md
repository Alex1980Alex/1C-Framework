# CLAUDE.md — PDF Vector & Graph Framework

> AI agent instructions are in [AGENTS.md](AGENTS.md). This file is project context for humans and AI.

## Project Overview

Framework for PDF document processing: semantic search (RAG), knowledge graphs, hybrid retrieval. Production-ready with REST API, CLI, MCP server, Streamlit UI.

## Tech Stack

- **Python 3.11+**, async-first
- **LangChain / LangGraph** — agents, tools, chains, orchestration
- **Qdrant / ChromaDB / FAISS** — vector stores
- **Neo4j / NetworkX** — graph stores
- **FastAPI** — REST API
- **MCP** — Model Context Protocol server
- **Typer** — CLI

## Project Structure

```
src/
  pdf_framework/        # Core library
    loaders/            # PDF loading (pymupdf, pdfplumber)
    processing/         # Splitting, cleaning, metadata
    embeddings/         # Embedding providers
    vector_store/       # Vector DB (base + providers)
    graph_store/        # Graph DB (base + providers)
    search/             # Hybrid search, strategies, reranking
    agents/             # LangGraph agents (RAG, analytical, research)
    indexing/           # Indexing pipelines, hybrid loader
    tools/              # LangChain tools
    chains/             # LangChain chains
    schemas/            # Pydantic data models
    config/             # Settings modules
    evaluation/         # RAGAS, benchmarks
    guardrails/         # Input/output validation
    observability/      # Logging, tracing
    analytics/          # Cost tracking, audits
    feedback/           # User feedback
    multitenancy/       # Multi-tenant support
    optimization/       # Performance tuning
    knowledge_base/     # Knowledge base management
    callbacks/          # Event callbacks
    utils/              # Utilities
  api/                  # REST API (FastAPI)
  cli/                  # CLI (Typer)
  mcp_server/           # MCP server for Claude Code
  ui/                   # Streamlit web UI
  workers/              # Background task workers
```

## Development Rules

- Store implementations extend abstract base classes in `*/base.py`
- Pydantic models from `schemas/` as data contracts
- Async-first: all I/O operations are `async`
- Configuration via `pydantic-settings` (`.env` file)
- Provider pattern: swap Vector/Graph/Embedding providers without changing logic

## Output Rules

- File references in responses must be clickable markdown: `[file.py](.claude/hooks/file.py)` or `[file.py:42](.claude/hooks/file.py#L42)`

## BSL Development (1C Enterprise)

- **BSL код**: `src/bsl/` (semantic_search, mcp_integration, sonar, finetuning; `mcp_server` ретирован 2026-07-24 — ADR-056, был мёртвым vendored-близнецом upstream, продакшн = сабмодуль `external/1c_mcp`)
- **Инструменты**: `tools/auto-documenter/` (Node.js), `tools/bsl-debugger/` (Node.js), `tools/bsl-debug-server/` (Python — live RDBG wrapper, **HMR-enabled через `mcp_hmr_proc.py`**), `tools/ast-grep-mcp/`, `tools/bsl-semantic-diff/`
- **MCP серверы**: bsl-semantic-search, bsl-platform-context, auto-documenter, bsl-debugger, **`1c-debug` + `1c-debug-hmr`** (live BSL отладка через RDBG, HMR-вариант reload'ит при edit без потери session — `.active.json` persistence + unified `ping()` dispatch, добавлен 2026-05-10; см. [`docs/framework documentation/3_ИНСТРУМЕНТЫ/3.5_AUTONOMOUS_DEBUG_CONTROL/36.7_HMR_Subprocess_Wrapper.md`](docs/framework%20documentation/3_ИНСТРУМЕНТЫ/3.5_AUTONOMOUS_DEBUG_CONTROL/36.7_HMR_Subprocess_Wrapper.md) + skill [`1c-debug-hmr`](.claude/skills/1c-debug-hmr/SKILL.md)), ast-grep-mcp, bsl-semantic-diff
- **Memory**: `src/memory/` (ai_memory, vector_memory, skill_learning, librarian, orchestrator, infrastructure). P0-P4 DONE, P5 in progress. Ключевые хуки: `session-memory-save.py` (Stop → SQLite + L5 drafts promote), `pattern-reinforce-stop.py` (Stop → §22 reinforce surfaced-паттернов; выделен 2026-06-09 из session-memory-save, где за early-return'ами дедупа не срабатывал НИКОГДА — roadmap 260609 P0.1; первый production-reinforce applied=10 2026-06-09; **P1 2026-06-10** — write-contract `content_hash`+dedup+`record_ingest` во всех прямых писателях [общий `content_hash.point_id`], честный `route_and_save` [`failed_targets`], surfacing cache-key top-K salient + TTL 900/empty-180, см. roadmap 260609 §18), `memory-first-hook.py` (UPS semantic surfacing + confidence-gating + RRF k=60 + execution-cache [top-K salient key] + per-stage trace-log), `patterns-harvester.py`/`skills-harvester.py` (Stop ingestion в `learned_patterns`/`skill_library` + epoch.bump P1.5). Skill: `memory-unified`. **Полная история §22–§27** (confidence lifecycle = decayed Beta(7,3) posterior `vector_memory/confidence.py`+`reinforce.py`; §23 auto-cache pipeline; §24 surfacing hybrid arms → RRF + confidence-gating `memory-first-hook`; §25 self-tuning golden-set `scripts/tune_memory_surfacing.py`; §26 ingestion/sync + Q1 ADR (experience/conversation collections dropped); §27 full observability — 8 sinks, `infrastructure/trace_log.py`, `scripts/memory_observability_{query,report}.py`) **вынесена в** [27.13 Memory Subsystem Changelog](docs/framework%20documentation/5_ПАМЯТЬ/5.1_UNIFIED_MEMORY/27.13_Memory_Subsystem_Changelog.md) + главы [27.9–27.12](docs/framework%20documentation/5_ПАМЯТЬ/5.1_UNIFIED_MEMORY/) + roadmaps 260523/260601/260602/260603/260605. **Honest-failure контракт (260611, governance-проводка)**: handler/adapter-фейлы НЕ глотаются — `propagate_update` отдаёт `failed_entities{entity→reason}` + named breakers `propagation:<source>` в `memory_circuit_status` (OPEN → `failed:circuit_open` fail-fast); `unified_search` кладёт упавшее vector-плечо в `sources_failed[]`; `memory_ttl_cleanup` исполняет до store'ов (vector archive / memory-ai delete + `store_actions`/`failed`); versioning wire-minimal (ADR-V: orchestrator-mediated мутации снапшотятся, rollback пишет обратно в store); promote идемпотентен (`promoted_to` + log-dedup); reinforce_miss → `missing`, не `errors` — [260611](docs/roadmap/260611_ROADMAP_MEMORY_GOVERNANCE_WIRING.md). **Skill-learning revival (260612)**: pending/saved/rejected = живой карантинный конвейер — atomic `_write_jsonl`, rejected-dedup (`dup_rejected`), stats derive-on-read, `route_and_save`→pending (`auto_confirm` opt-out); вход: протокольный `capture_pattern` после verify PASS (task-protocol шаг 6 / code-verify) + авто-карантин low-confidence lessons (`quarantine_items`, порог 0.8); выход: SessionStart-баннер `skill-learning-pending-on-start.py` + плечо `skill_learning` в memory-first-hook (pending ×0.7 + метка) + RU-стемминг адаптера; модерация: `review_pending` TTL-30d auto-reject в maintenance-каденсе + confirm→детач-harvest (идемпотентный point_id + epoch.bump) — [260611 revival](docs/roadmap/260611_ROADMAP_SKILL_LEARNING_REVIVAL.md). **Payload contract (260716 P0, 2026-07-16):** payload из store = **недоверенный вход** (8+ писателей, одна точка с `pattern_type:'requirements'` роняла весь `search_patterns` + vector-плечо `unified_search` в `sources_failed`). Три слоя: (1) **единый коэрсер** `PatternType.coerce`/`normalize_pattern_type`/`coerce_float|int|dt` в [`vector_memory/models.py`](src/memory/vector_memory/models.py) — `_PATTERN_TYPE_ALIASES` ЕДИНСТВЕННАЯ alias-карта (`CATEGORY_MAP` в `normalize_light_patterns`/`reflection` теперь производные), дефолт `workflow-pattern` (не инвариант → мусор архивируем); (2) **писатели нормализуют на границе** (`pattern_harvest._build_payload`+`quarantine_items`, `route_and_save` обе ветки +clamp confidence, `capture_pattern` enum+coerce; провенанс `metadata.original_pattern_type`), ⚠ `save_pattern` намеренно строгий fail-closed (запинен тестом); (3) **per-item изоляция** в 6 циклах (search/list/decay/plan_forget/vector-плечо/skill-learning-плечо/каскад) — счётчики `items_skipped`/`errors`/`unreadable`; изоляция ПЛЕЧА (infra down → `sources_failed`) ≠ изоляция ТОЧКИ. Ловушка: `.get(k, default)` НЕ покрывает явный `null` ([[feedback-payload-untrusted-input]]). Миграция — [`scripts/migrate_pattern_types.py`](scripts/migrate_pattern_types.py) (dry-run+backup, идемпотентен). 36 unit + [260716](docs/roadmap/260716_ROADMAP_MEMORY_PAYLOAD_HARDENING.md) + [27.13](docs/framework%20documentation/5_ПАМЯТЬ/5.1_UNIFIED_MEMORY/27.13_Memory_Subsystem_Changelog.md). **P1 hardening (260716 P1, 2026-07-17)** — тот же класс на ВЫХОДЕ (пути, которые не падают, а тихо врут): единый seed `LEGACY_SEED_CONFIDENCE` выведен из Beta-prior (read-path занижал легаси-точки: 0.643 вместо 0.70; blast radius замерен — 0/153); `_cascade_confidence` перешёл на относительные импорты (абсолютный `memory.*` при запуске `-m src.memory…` давал ВТОРОЙ экземпляр `link_registry` либо тихий no-op до первого `_get_embedding`); `PatternRecord.extra` + сохранение непарсящихся строк в `merge_patterns` (каденс-джоб перезаписывает силос → «skip при чтении» = «delete с диска», архивные воскресали); RRF не дилюирует пустыми плечами (упавшие в знаменатель и не попадали — асимметрия); `SourceError.error_type` + `sources_failed_detail` в trace + breakers `search:<source>` на общем с propagation реестре; **`search_pattern_points` экспортирована из `vector_memory/server.py` — ЕДИНСТВЕННЫЙ владелец политики поиска** (плечо `unified_search` дублировало её и разошлось: prefilter по stored confidence [не safe superset под count-decay] + не исключало archived → всплывало то, что прямой поиск прячет); процессный Qdrant-клиент в `reinforce.py` (было 10 соединений/сессию → WinError 10048); M6 `_importance_expr` — ⚠ **диагноз аудита был перевёрнут**: TEXT-importance не выпадает, а захватывает топ ([[reference-sqlite-text-outranks-real]]); `dedupe_learned_patterns.py` стал link-aware (канон из `mirrors` > эвристики, re-point рёбер, fail-closed) — **запрет `--apply` снят, но на живой коллекции не запускался** ([[reference-dedupe-learned-patterns-unsafe]]). 2325 unit + саботаж-харнесс 15/15 ([[feedback-sabotage-check-tests]]). **⚠ Операционка:** правки кода MCP-серверов (`src/memory/vector_memory/*`) требуют `/mcp reconnect` ([[feedback-mcp-stale-code-reconnect]]); хуки пишут stdout UTF-8-байтами, не `print()` ([[feedback-windows-hook-stdout-cp1251]]); в хуках с импортом `shared.*`(hooks-local)+`memory.*`(src) добавлять `src` через `append`, НЕ `insert(0)` ([[feedback-hook-src-shared-collision]]). **Курируемая память (ADR-011, 2026-06-12):** синхронизация с store'ами = read-time merge + manual promotion (репликация — non-goal); новые `memory/*.md` записи несут **citations** (`file:line`/commit) — verify-at-read дешевле (паттерн Copilot agentic memory); входящий конвейер store→канон — SessionStart-баннер `memory-curation-candidates-on-start.py` (eff_conf≥0.85 & apps≥5, не покрытые каноном; cooldown 20h, opt-out `MEMORY_CURATION_BANNER_DISABLE=1`).
- **LLM Rotation**: `src/shared/llm_rotation/` (sonnet-first ротация, 6 MCP-тулов вкл. `llm_route_explain`; **2026-07-26 fix-llm-rotation-model-routing**: per-server `timeout` в `.mcp.json` СИЛЬНЕЕ env `MCP_TOOL_TIMEOUT` [60000 рвал вызовы на 60с → 300000], сквозной бюджет ротации `total_budget_seconds=240`+`primary_budget_share=0.6` [фоллбэку гарантировано время], model-aware выбор `resolve_model_for_provider`, priority>adaptive в sort_key, per-call лог + JSON-ошибки + абсолютный completions-лог; скилл `llm-delegation` [бывш. z-ai-delegation]; ⚠ рантайм после `/mcp reconnect`). **Строгое совпадение тира (мандат 2026-07-26):** явная `model` сверяется со СВОИМ набором провайдера (`default_model`+`models`) одинаково для всех форматов — claude-cli технически запускает любую claude-модель, но исполнять чужой тир «за компанию» запрещено (так `claude-cli-haiku` исполнял opus — исходный симптом расследования). `model="haiku"`→`claude-cli-haiku` (primary-sonnet скипается), `model="opus"`→**никто**, честный `RuntimeError`, называющий лечение («завести провайдера тира в `DEFAULT_PROVIDERS`»); списки `models` сужены до своего тира у всех, включая платный `anthropic-sonnet` (иначе явный opus молча ушёл бы на самый дорогой платный тир). **Фиксы по живой верификации (2026-07-26):** советчик [`posttooluse-delegation-tracker.py`](.claude/hooks/posttooluse-delegation-tracker.py) получил `_latency_profile` (claude-cli 25-150с — норма, единый порог 15с помечал «медленным» КАЖДУЮ успешную делегацию и советовал уйти с sonnet-first); `primary_retries`→`primary_attempts` (инкремент ДО вызова: чистый успех читался как «был ретрай»); изоляция llm-rotation-синков перенесена в [`tests/conftest.py`](tests/conftest.py) — пофайловая фикстура протекла мимо `test_backoff.py`, а `llm_health.is_provider_down()` читает тот же completions-лог и трактует тестовые `provider="none"`/`error~"down"` как провал ⇒ мусор от pytest на 30 мин разоружал `z-ai-write-guard` ([[feedback-test-log-pollution-enforcement-hole]]); продовый лог вычищен 946→769 (123 неатрибутируемые записи оставлены осознанно)
- **Субагенты**: `.claude/agents/` (learning-loop — 5-phase self-learning pipeline, monitor). **Живой прогон A3 (2026-06-13, roadmap 260612):** learning-loop SEARCH→FETCH(3 источника)→EXECUTE→VERIFY(adversarial reviewer-субагент, PASS)→CREATE произвёл skill `duckdb-analytics` (DuckDB-аналитика над JSONL/Parquet логами; артефакт EXECUTE — [`scripts/skill_ingest_trend.py`](scripts/skill_ingest_trend.py)) + bundle в `skill-router-config.json` (66 bundles; +13 роутируемых скиллов и ключ `_unrouted_intentional` для slash/делегация-скиллов — аудит 2026-07-06); verify-гейт обязателен перед регистрацией в каталог
- **Паттерны**: `docs/architecture/PATTERNS.md` (15 arch + 13 automation), skill `framework-patterns`
- **Профили**: `.mcp/pdf.json`, `.mcp/bsl.json`, `.mcp/full.json`, `.mcp/lazy-mcp.json`
- **Lazy MCP**: `infra/lazy-mcp/` (proxy, 11 категорий, 27 on-demand серверов)
- **Qdrant коллекции (после Phase 8 + 9.1, 2026-04-30; config defaults aligned 2026-05-01 §2.1; experience/conversation dropped 2026-06-03)**: 8 коллекций × **Qwen3-Embedding-8B 4096d** через TEI Docker (`pdf-rag-tei`). Production retrieval: `bsl_code_v4_late` (alias → `bsl_code_v4_late_hybrid` 37 668 pts, named `{dense:4096d, bm25:sparse IDF}` since 2026-05-22; prev physical `_v2` retained as backup; default query mode = **hybrid RRF** (2026-06-20 ре-верифицировано — прежний «pure BM25 / dense 18%» оказался артефактом устаревшего eval; dense здоров [L 0.72/S 0.42], hybrid > BM25-first на L +10pp / S +18pp recall@10; правка `_call_qdrant_search`, [[feedback-bsl-sparse-bm25-dominance]] СУПЕРСЕДЕД; query-adaptive fusion — next) — см. [chapter 31.7](docs/framework%20documentation/2_КОНТЕКСТ/2.8_QWEN3_RETRIEVAL_PRODUCTION/31.7_BSL_Hybrid_Sparse_BM25.md) + memory `feedback_bsl_sparse_bm25_dominance`), `bsl_code_v4` (24 455 std), `framework_code_v1` (21 277+, auto-reindex on commit), `graph_embeddings` (6 694), `wiki_pages_v1` (3 073), `pdf_documents` (830), `skill_library` (98, Phase 9.1), `learned_patterns` (124; +73 promoted из pending-карантина 2026-07-06). **`experience_embeddings`/`conversation_memory` dropped 2026-06-03** (§26 Q1 ADR — 0-writer, роль покрыта episodic `memory_ai.db` + `learned_patterns`; surfacing-arms убраны, см. [260603_ADR_Q1](docs/roadmap/260603_ADR_Q1_EXPERIENCE_CONVERSATION_COLLECTIONS.md)). Code-side defaults: `EmbeddingSettings.provider="tei"`, `model="Qwen/Qwen3-Embedding-8B"`, `dimensions=4096`; `VectorStoreSettings.dimensions=4096`; regression: `tests/unit/test_config.py::test_phase8_invariants`. Подробности: [chapter 31 Qwen3 Retrieval Production](docs/framework%20documentation/2_КОНТЕКСТ/2.8_QWEN3_RETRIEVAL_PRODUCTION/31.1_Обзор.md). Legacy `bsl_code_v2` (768d nomic), `_e5_legacy`, `bsl_code_v3` (1024d E5), `visual_grounding` (768d nomic) — **dropped 2026-04-30**, см. roadmap §27/§32.5
- **BSL hook**: `bsl-tool-router.py` — routes BSL/1C queries to bsl-development skill
- **Auto-reports (2026-05-22, chapter [`28_1_AUTO_REPORTS`](docs/framework%20documentation/2_КОНТЕКСТ/2.7_AUTO_REPORTS/))**: после каждого `category=run_end` в [`data/indexing-progress.jsonl`](data/indexing-progress.jsonl) Stop-хук [`post-indexing-analyzer.py`](.claude/hooks/post-indexing-analyzer.py) детачит [`scripts/analyze_run.py`](scripts/analyze_run.py) — пишет deep отчёт в `data/reports/{indexing,graph}/` (Markdown + JSON sidecar + `_latest_<subject>.md`). Indexing: alias-resolved Qdrant introspection (`framework_code_v1` → `*_mrl_1024`), quantization detect, L2 norm + self-recall@1 probes, diff vs prev. Graph (3 sub-источника): `sqlite` (`cache/bsl_call_graph.db` — 33k symbols/80k calls/2k modules, dangling-call detector), `neo4j` (default `NEO4J_URI` / `NEO4J_USER` / `NEO4J_PASSWORD` env vars), `qdrant_graph` (collection `graph_embeddings`, entity-vs-relation split). Cold-start: на первом запуске seed'ит existing run_ids без анализа (защита от dump'а истории). FIFO dedup state в `.claude/cache/post-indexing-analyzer-state.json` (cap 500), atomic write через `os.replace`. Cap 5 spawn'ов/Stop. Skill: [`post-indexing-analysis`](.claude/skills/post-indexing-analysis/SKILL.md). Smoke: [`tests/integration/test_analyze_run.py`](tests/integration/test_analyze_run.py) (3 теста, Qdrant-independent). Mapping в [`docs-change-enforcer.py`](.claude/hooks/docs-change-enforcer.py) CODE_TO_DOMAIN: `scripts/analyzers/`, `scripts/analyze_run.py`, `.claude/hooks/post-indexing-analyzer.py` → `28_1_AUTO_REPORTS` / `post-indexing-analysis`. Ручной запуск: `python scripts/analyze_run.py --mode {indexing|graph} --run-id <id>` или для графа `--source {sqlite|neo4j|qdrant_graph}`.
- **Tool observability (roadmap [260713](docs/roadmap/260713_ROADMAP_TOOL_OBSERVABILITY_AUDIT.md), 2026-07-14)**: аудит логирования инструментов/MCP + замкнутый цикл «лог → анализ → вердикт → действие». **P0 корректность лога (commit b575a2dc3):** B1 `base/protocol.py::detected_event` детект PostToolUse по `tool_response` (был только `tool_result` → все Post мисклассифицировались в Pre в fallback-ветке; обход из [`mcp-invocation-logger.py`](.claude/hooks/mcp-invocation-logger.py) удалён); B2 `HookInput.agent_id` прокинут в `log_invocation` (аддитивно; платформа сейчас не шлёт); B4 `BaseHook.run()` подавляет дубль-автолог mcp__ тула при `outcome=allow` (канон у mcp-логгера, убрана инфляция `audit_query top-tools`); **B3/B5 новый [`tool-invocation-logger.py`](.claude/hooks/tool-invocation-logger.py)** — канонический `category="tool_call"` для built-in тулов (Pre+Post, `tool_call_id`+`args_hash`, `_NoAutoLogToolLogger` подавляет автолог), зеркалит mcp-логгер; потребители ([`tool_usage_report.py`](scripts/tool_usage_report.py), [`audit_query.py`](scripts/audit_query.py) `top-tools`) обобщены на `_CANONICAL_CATEGORIES={mcp_call,tool_call}` (built-in получили реальную Pre→Post латентность + дедуп). **P1.1 decision layer (§6):** [`scripts/analyze_tool_health.py`](scripts/analyze_tool_health.py) (stdlib-only) — за окно 14д per-tool success-rate/p50-p95/repeats/abandonment → **вердикты** broken/degraded/ineffective/unused/healthy (§6.1; **repeats НЕ движут вердикт** — idempotent-polling [ping/targets] = healthy, был FP; ineffective = только abandonment; `DEGRADED_MIN_CALLS=3`) → `data/reports/tools/_latest.{md,json}` + `verdicts.jsonl` (история) + ratchet `baseline.json`; Stop-хук [`tool-health-analyzer-stop.py`](.claude/hooks/tool-health-analyzer-stop.py) (cooldown 24ч, detached, паттерн post-indexing-analyzer) + SessionStart-баннер [`tool-health-banner-on-start.py`](.claude/hooks/tool-health-banner-on-start.py) (сюрфейсит alerts; broken → авто-задача диагностики через `task_master` с 72ч-дедупом; degraded — за человеком; молчит при healthy; **авто-фикса нет**). Opt-out `TOOL_HEALTH_ANALYZER_DISABLE=1`/`TOOL_HEALTH_BANNER_DISABLE=1`. 45 unit (P0+P1.1) + code-verify PASS ×2. `data/reports/tools/*` gitignored. **P1.2 проактивный MCP health-probe (B7):** [`scripts/probe_mcp_health.py`](scripts/probe_mcp_health.py) — `health_check`-tools серверов из хука недостижимы (их исполняет клиент, серверы тяжёлые), поэтому пробим их **зависимости**: Qdrant `GET /healthz` (env `QDRANT_URL`), TEI `POST /embed`, memory-ai `memory_ai.db` + bsl-code-search `bsl_call_graph.db` (SQLite ro) → `data/mcp-health.jsonl` + `_mcp_health.json` (down+карта «что деградирует»); SessionStart-хук [`mcp-health-probe-on-start.py`](.claude/hooks/mcp-health-probe-on-start.py) (inline, баннер только при down, opt-out `MCP_HEALTH_PROBE_DISABLE=1`); RDBG не пробится (on-demand). 9 unit + code-verify PARTIAL→FIXED. **P1.4 memory-sinks regression в каденс (B8):** [`memory-maintenance-cadence.py`](.claude/hooks/memory-maintenance-cadence.py) при фаере синхронно (<1с, read-only) прогоняет `memory_observability_report.py`, парсит `[REGRESSION]`-маркер и сюрфейсит замолчавшие синки в баннер (раньше freshness-детектор запускался только вручную); живая находка — 3 stale-синка propagation/circuit/links >7д. 6 unit + e2e. **P2.3 внутренний per-call лог MCP-серверов (B9):** shared stdlib-only [`scripts/mcp_call_log.py`](scripts/mcp_call_log.py) (`log_mcp_call`+контекст `track_call` — таймер/auto-лог ok/error_type/ре-райз; ротация 2MB `os.replace`, fail-soft, opt-out `MCP_CALL_LOG_DISABLE=1`) → `.claude/cache/mcp-<server>-calls.jsonl` = второй источник истины при падении stdio до Post-хука. Обёрнуты **memory-orchestrator** (защитный импорт + `call_tool`→`_dispatch_tool`, error-ветки метят `state`) и **1c-mcp-crud** (монки-патч `OneCClient.call_tool` в лаунчере [`scripts/mcp_1c_stdio_launcher.py`](scripts/mcp_1c_stdio_launcher.py), сабмодуль `external/1c_mcp/` НЕ тронут, idempotent `_mcp_call_logged`). ⚠ **регрессия поймана+исправлена:** `sys.path.append(project_root)` тащил регулярный `<root>/src/` в скан → шедоуил namespace-`external/1c_mcp/src/` → ломал `from src.py_server.main` — фикс: scripts-dir на path + bare `import mcp_call_log`. Рантайм-эффект gated на `/mcp reconnect` ([[feedback-mcp-stale-code-reconnect]]). 11 unit + code-verify PASS. **P2.1 gen_ai.*-алиасы полей:** [`invocation_logger`](.claude/hooks/shared/invocation_logger.py) пишет аддитивные dotted-алиасы `gen_ai.tool.name`/`gen_ai.tool.call.id`/`error.type` (дословно дублируют плоские поля, единый `err_type` — нет дрейфа; `additionalProperties:true` → валидация цела; будущий OTel-экспорт = переименование). **P2.2 rule-слой эффективности:** дублирующие функции (pair-duration/percentile/retry-abandonment) вынесены в single-source [`scripts/tool_effectiveness.py`](scripts/tool_effectiveness.py) (stdlib-only), [`analyze_tool_health.py`](scripts/analyze_tool_health.py)+[`tool_usage_report.py`](scripts/tool_usage_report.py) делегируют (алиасы `_pct`/`_pair_duration_list` сохранены, behavior-preserving); добавлен **per-server rollup** (Tool Success Rate + Step Efficiency = % retry-вызовов + abandonment) в отчёт P1.1 → секция «Эффективность (rule-слой, per-server)» + `servers` в sidecar. 90 unit (73 существующих + 17 новых) + code-verify PASS; живая находка `edt-mcp` retry-rate 14%. **Хардинг по адверсариальному аудиту реализованного (commit 89dcd4547):** 3 параллельных ревьюера → 20+ дефектов исправлены. P1.1 [`analyze_tool_health.py`](scripts/analyze_tool_health.py): tz-crash `iter_window_rows` (одна aware-ts роняла весь прогон TypeError'ом → `_to_naive_local`), degraded-ветки baseline под `DEGRADED_MIN_CALLS` (calls=1 er=100% светил degraded), `p95=0.0` при `paired=0` не ратчетится + self-heal + CLI `--reset-baseline` (выход из «degraded навсегда»), `last_seen`+TTL-prune 30д (unused не вечен), `window_incomplete` флаг (ротация срезала хвост), ротация `verdicts.jsonl`; хуки: spawn-fail → warning+retry (не тихая смерть на 24ч), эскалация broken 72ч→336ч. P0: `hookSpecificOutput`→`outcome=message` (не глотался P0.4-подавлением), `agent_id` в канонический mcp_call, гонка ротации `hook-invocations.jsonl` → `os.replace`, Bash dict `exit_code`≠0/`interrupted`→error (вердикты видят фейлы built-in), [`base/base.py`](.claude/hooks/base/base.py) доведён до зеркала protocol. P1.2/P1.4/P2.3: `_instance_slug` из `MCP_ONEC_URL` (5 ИБ-инстансов 1c-mcp-crud больше не сливаются в один per-call лог), каденс-вызов отчёта `--print --since 14d` (read-only+bounded), маркер `[REGRESSION]` в `--print`, mkstemp+ротация probe. 129 unit (116+13) + re-review PASS. Регресс-тесты [`tests/unit/test_tool_obs_adversarial_fixes.py`](tests/unit/test_tool_obs_adversarial_fixes.py). **Ядро роадмапа закрыто** (P0/P1.1/P1.2/P1.4/P2.1/P2.2/P2.3). **NEXT — реализация 260718 ([roadmap](docs/roadmap/260718_ROADMAP_TOOL_OBSERVABILITY_NEXT.md), 2026-07-18):** аудит реализованного 260713 + верификация 3 агентами вскрыл, что метрики СНОВА врали → досборка. **N-P0 честность данных:** (P0.1) дамп формы `tool_response` установил корень NB1 — **платформа НЕ шлёт PostToolUse на неуспешный built-in вызов** (Bash non-zero exit / Edit «строка не найдена» / Read miss: Pre есть, Post нет), значит error-rate из Post ≡ 0 структурно; честный провал = **непарный Pre** ([`tool_effectiveness.completion_stats`](scripts/tool_effectiveness.py)); живой эффект Bash 0→3%, **Agent 20%** (провалы делегирования были невидимы); ⚠ **прежнее «блоки гардов сюда не попадают — enforcer `exit 2` преемптит canonical-логгер, Write unpaired=0 при живом блоке» ОПРОВЕРГНУТО 2026-07-25**: часть энфорсеров блокирует через `decision:"block"` (правка против обрыва хода), при котором Pre-цепочка доходит до логгера → из 49 непарных Write **36** совпали с block-записью (native OTel о них не знает — тул не исполнялся), реальных провалов Write в окне 0; вычет через `guard_blocks_by_tool` + `completion_stats(blocked=…)` → счётчик `blocked`, Write 14%→1% degraded→healthy ([260725 ретро](docs/roadmap/260725_ROADMAP_SESSION_RETRO.md)); gated shape-dump в [`tool-invocation-logger.py`](.claude/hooks/tool-invocation-logger.py) off-by-default против рецидива. (P0.2) каскадная ротация `.1→.N` (`HOOK_LOG_ARCHIVES=12`, [`invocation_logger`](.claude/hooks/shared/invocation_logger.py)) + `_all_logs` — окно 14д реально покрывается (держался 1 архив → 2 дня; ⚠ NB3 «архив не читался» был неверен — читался, проблема в single-retention). (P0.3) p95-degraded off у **content-variable** тулов (Bash/Grep/execute_code — латентность от контента) + пол 500мс → 6 живых p95-FP стали 0; оставшиеся degraded реальны (error-rate + genuine latency). **N-P1 §6 decision layer:** (P1.1) `mcp_down_deps`(≥2д)+`stale_sinks`([REGRESSION]) → единый `infra_alerts`; (P1.2) [`scripts/tool_verdict_history.py`](scripts/tool_verdict_history.py) — **читатель** verdicts.jsonl (был write-only): повтор broken 30д→p1, healed-детект, тренд; (P1.4) мёртвый `tool-effectiveness.jsonl` ретирован. **N-P2:** (2.1) probe +6× 1c-mcp-crud reachability (severity=degraded — dev-инфобазы флапают)+skill-learning; (2.2) per-call лог **vector-memory/skill-learning/memory-ai** (`mcp_call_log` wrap); (2.3) doc-sync §1.3 + `CANONICAL_CATEGORIES` single-source; (2.4) разрез делегирования per-agent (`agent_id` течёт). **N-P3 = [ADR-051](.claude/skills/architecture-research/adr/051-tool-observability-heavy-path-gating.md):** тяжёлый путь (OTel→Langfuse+LLM-judge) отложен за явные триггеры (инфру не поднимаем в код-сессии); дешёвый предшественник — захват `duration_ms` из Post-payload (найден в N-P2.3). 32 новых unit + 158 tool-obs зелёные + 330 gate-parity; коммиты c225fc257/86686dedf. ⚠ MCP-серверные правки — рантайм после `/mcp reconnect`. **Тяжёлый путь OTel→Langfuse ([roadmap 260718](docs/roadmap/260718_ROADMAP_OTEL_LANGFUSE_HEAVY_PATH.md) H-P0..H-P4, [ADR-051](.claude/skills/architecture-research/adr/051-tool-observability-heavy-path-gating.md)→[ADR-052](.claude/skills/architecture-research/adr/052-otel-langfuse-topology-retention-security.md), 2026-07-18):** мандат активировал отложенный ADR-051. Расширил СУЩЕСТВУЮЩУЮ инфру ADR-022 P2 (коллектор запущен, `enable_claude_otel.py`, эмиссия верифицирована 2026-06-17). **H-P0.1** `duration_ms`-захват: платформа кладёт настоящую длительность тула top-level в PostToolUse-payload (эмпирика — дамп tool-response-shapes) — точнее пэйринга (пара врала до ~10с); `extract_duration_ms`+`tool_durations` (предпочитает прямое, behavior-preserving), live-contract Bash 2057мс vs elapsed_ms ~20мс. **H-P1** [`otel-collector.yaml`](docker/otel-collector.yaml) += `file`-exporter → `data/otel/*.jsonl` (gitignored bind-mount; debug-ветка ADR-022 цела) — расширил живой коллектор (порт 4318 занят → параллельный compose = конфликт). **H-P3 (главная ценность)** [`scripts/otel_crosscheck.py`](scripts/otel_crosscheck.py): native `claude_code.tool_result` ↔ hook unpaired-Pre по `tool_use_id` (эмпирически 100% overlap) → FN/FP/TP + latency; секция в `_latest.md`. ⚠ **живые данные поймали критбаг, что unit пропустил:** реальный Claude Code кодирует `success`/`duration_ms` как `stringValue` («false»/«3879»), не bool/int → наивный `bool("false")`==True читал бы ВСЕ провалы как успех (NB1-класс на OTel-парсинге); фикс `_coerce_bool`/`_coerce_int`. Результат: детектор unpaired-Pre согласован с native (0 FN/0 FP/1 TP), latency native↔hook-direct дельта **0мс** (H-P0.1 точен бит-в-бит), native↔пэйринг max **9888мс** (ошибка FIFO-пары). **H-P4** [`scripts/tool_llm_judge.py`](scripts/tool_llm_judge.py): сэмпл 5-10% (все провалы + хеш-детерминизм) → рубрики Argument Correctness/Task Completion → jsonl + опц. Langfuse Scores; advisory-only, content off by default. **H-P2** [`docker-compose.langfuse.yml`](docker/docker-compose.langfuse.yml) (эталонный, порты на 127.0.0.1) + [`langfuse_up.py`](scripts/otel/langfuse_up.py) (генерит секреты в gitignored `.env.otel`, up, health, пересоздаёт коллектор с fan-out — file-путь H-P3 цел) + overlay `otel-collector-langfuse.yaml`; **opt-in, НЕ default** (RAM ~3ГБ, H-P3 работает без Langfuse; ADR-052). 37 unit (H-P0.1+H-P3+H-P4) + 154 tool-obs зелёные. Пайплайн `pipeline/otel-langfuse-heavy-path/`. **Known-issues слой «xfail для инструментов» ([ADR-055](.claude/skills/architecture-research/adr/055-tool-health-known-issues-suppression.md), 2026-07-19):** тул со сломом по ИЗВЕСТНОЙ внешней причине → вердикт `known-issue` (🔕) вместо `broken` (config [`data/reports/tools/known_issues.json`](data/reports/tools/known_issues.json) `{tool,reason,ref,review_by}`): виден в отчёте/баннере, но НЕ плодит mandatory-задачу (убирает whack-a-mole). Форма best-practice (Alertmanager silences + pytest `xfail(strict)`, кеш `architecture-research/cache/tool-alert-suppression-known-issues-2026.md`): `_apply_known_issues` overlay после infra в [`analyze_tool_health.py`](scripts/analyze_tool_health.py) → sidecar `known_issues`/`recovered_known_issues`; `review_by` обязателен (missing → fail-closed, нет вечного mute); **xpass-детект** (known-issue снова healthy → баннер «сними suppression») + expiry авто-всплывают; `tool_verdict_history` тренд: broken→known-issue НЕ «вылечено». Зарегистрированы codepilot1c `qa_run`/`qa_generate` (корень W13 — апстрим getThickClientInfo + `infra_error`-при-успехе; реальный фикс внешний → отдельная задача, нота в roadmap 260718). Fail-soft (нет config → прежнее поведение); 14 unit + code-verify PASS. **Клиентский потолок ≠ провал инструмента (N-P4.3, 2026-07-25):** третий класс ложного провала после `blocked` — Post не пришёл, потому что **клиент** перестал ждать (~60с), а сервер довёл операцию до УСПЕХА (журнал EDT: `delete_project` 134/203с, `update_database` до 52 мин — все `outcome=ok`). Новый stdlib-парсер [`scripts/edt_journal.py`](scripts/edt_journal.py) (`<ws>/.metadata/.log` + архивы; `Completed tools/call: <t> in Nms, outcome=` → `start = end − ms`; `Failed tools/call: … - <текст>` = ЕДИНСТВЕННЫЙ носитель текста отказа, в hook-логе и native OTel его нет) → `server_ok_by_tool` (фильтр `outcome=ok` ∧ `ms ≥ CLIENT_CAP_MS=55_000`, env `TOOL_HEALTH_CLIENT_CAP_MS`) → `completion_stats(server_ok=…)` → счётчик **`client_timeout`** вне `calls`/`errors` (тот же расходуемый 1:1 приём, что `blocked`; **парный Pre забирает свою запись первым**, допуск `SERVER_OK_MATCH_SEC=2` из замера Δ +0.065…+0.548с; порог стоит в пустой полосе между максимумом парного вызова 13.4с и минимумом оборванного 60.3с → ниже порога fail-closed). Отчёт получил секцию **«Серверные исходы (журнал EDT)»**: вычтенное видно (`no silent caps`) + причины отказов по alert-тулам (`0ms` = аргумент отклонён до работы ⇒ ошибка вызывающего). Живой эффект: вычтено 7 вызовов, **broken 4→3** (`delete_project` broken→healthy), `update_database` 43%→40% (остаток честный). ⚠ **native OTel `success=false` ≠ «тул сломан»** — native смотрит глазами клиента; арбитр исхода только серверный журнал. Не покрыто: `codepilot1c` (пишет не в журнал EDT), `1c-mcp-crud-*` (непарные = вызов не доехал до сервера — видно по per-call логу, вычет по отсутствию записи опасен: сервер down выглядит так же), `llm-rotation` (нет серверного лога). 22 unit ([`test_tool_health_client_timeout.py`](tests/unit/test_tool_health_client_timeout.py)) + саботаж 5/5 + [260718 §18](docs/roadmap/260718_ROADMAP_TOOL_OBSERVABILITY_NEXT.md). **LLM-judge заведён в каденс ([roadmap 260725](docs/roadmap/260725_ROADMAP_LLM_JUDGE_CADENCE_SETFIT_DATASET.md) J-P0…J-P3, 2026-07-26, пайплайн `pipeline/llm-judge-cadence/`):** H-P4-судья был написан, но запускался ТОЛЬКО вручную — семантический слой не жил, тогда как rule-слой уже в каденсе. ⚠ **допущение плана опровергнуто живой проверкой:** в `.claude/cache/mcp-*-calls.jsonl` только `{error_type,ms,ok,server,tool,ts}` — args/result отсутствуют ПО КОНТРАКТУ («metadata only» в докстринге `log_mcp_call`), судить по имени тула и длительности нельзя (те же данные, что у rule-слоя), адаптер выдавал бы пустой батч молча. Замер: 650 вызовов, 640 `ok=true` (98%) — судья работает именно по ним, где правила слепы (класс «`ok=true` + пустой результат из-за неверного поля»). **J-P0.1** [`mcp_call_log.py`](scripts/mcp_call_log.py) += `args_digest`/`result_digest` под `MCP_CALL_LOG_CONTENT=1` (**off по умолчанию**, запись бит-в-бит прежняя — запинено `test_record_shape_identical_to_legacy`); редакция секретов **в два эшелона**: рекурсивно по КЛЮЧАМ структуры (`_redact_struct`) ДО `json.dumps` + regex по строке — regex по сериализованному тексту принципиально не ловит вложенный объект как значение (`{"secret":{"k":"v"}}`: ветка `\S+` рвётся на пробеле, хвост утекал сырым — code-verify Р1); fail-closed (нередактируемое → поле не пишется); обвязаны vector-memory/orchestrator/skill-learning, **`1c-mcp-crud` осознанно НЕ обвязан** (аргументы = запросы к боевым базам). **J-P0.2** адаптер native OTel (единственный путь судить built-in тулы и чужие серверы; переиспользует `_coerce_bool` — `stringValue "false"` иначе читался бы как успех). **J-P0.3** `--source {mcp-calls,otel,jsonl,auto}` + **курсор** `.claude/cache/llm-judge-cursor.json` (каденс фаерит нерегулярно → окно «24ч от сейчас» теряло бы записи) + **fail-loud** `content_disabled` (выключенный слой ≠ «нарушений нет»). **J-P1** [`memory-maintenance-cadence.py`](.claude/hooks/memory-maintenance-cadence.py) спавнит судью детачем со СВОИМ cooldown 24ч (ключ `last_judge`, НЕ счётчик сессий — раздельные лимиты), opt-out `TOOL_LLM_JUDGE_CADENCE_DISABLE=1`, провайдер down → пропуск с пометкой. **J-P2** `read_judge_verdicts` в [`analyze_tool_health.py`](scripts/analyze_tool_health.py) → секция «Семантика (LLM-judge, advisory)» + ключ `llm_judge` в sidecar; расхождение = rule healthy ∧ median task_completion<0.5 ∧ ≥3 оценок; **вердикты broken/degraded НЕ меняются** (инвариант запинен, gate-parity зелёный). **J-P3** [`scripts/tool_judge_validation.py`](scripts/tool_judge_validation.py) — пороги зафиксированы В КОДЕ до окна (precision ≥0.5 при n≥5), промоут ПО КАТЕГОРИЯМ провалов (deepeval-таксономия), **judgy-поправка** на ошибку самого судьи (`(raw−FPR)/(TPR−FPR)`; сырая доля подтверждений смещена, TPR==FPR → «не знаю»); промоут в hard — только вручную. 66 новых unit + 439 зелёных; **code-verify FAIL→fixed** (Р1 утечка вложенного секрета · Р2 падение одного источника уносило второй в `auto` · Р3 `cap` резал выборку, а курсор уезжал на конец файла → срезанные вызовы, включая приоритетные провалы, не судились бы НИКОГДА · Р4 тесты оставались зелёными при удалении проводки из `execute()` · Р5 путь в выводе/половинчатая калибровка); саботаж-проверки Р1/Р3 краснят целевые тесты. Активация на живых данных = `MCP_CALL_LOG_CONTENT=1` + `/mcp reconnect`; окно замера J-P3 требует ручной разметки `judge-review.jsonl` (сейчас честно `insufficient-data`). Часть 2 (автосбор GT для SetFit, S-P0…S-P2) — не начиналась.
- **1С Pipeline** (slash-команды): `/analyze-1c-task` → `/implement-1c-task` → `/write-1c-tests` → `/run-1c-tests` (цепочный прогон VA BDD с resume, pre-scenario TestDB check, `.run-state.json`). Skills: `va-bdd-testing` v1.1 (Stage 4a). Runner: `tools/vanessa/run-bdd.ps1 -OutputJson -RunId`. Подробности: **[гл. 43 Пайплайн 1С](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.1_ОБЗОР_И_ПАРАДИГМА.md)** (обзор+парадигма / [43.2 команды+режимы](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.2_КОМАНДЫ_И_РЕЖИМЫ.md) / [43.3 маршрутизация+хуки+инструментирование](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md)); тестирование (этап 4) — [17.5](docs/framework%20documentation/7_ПРОВЕРКА/7.4_ТЕСТИРОВАНИЕ_1С/17.5_КОМАНДЫ_ПАЙПЛАЙНА.md). **4-этапная проводка (ADR-019 B′ F-1, 2026-06-15):** `analyze-1c-task-preflight`/`implement-1c-task-preflight` авто-заводят `pipeline/<slug>/.pipeline-state.json` (helper [`shared/pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py) — JIRA-slug, best-effort) → 1С-слэш-маршрут проходит `pipeline-protocol-stop` (ADR-018) БЕЗ ручного пайплайна (G3 закрыт). **F-1.5** ([`pipeline-1c-advance.py`](.claude/hooks/pipeline-1c-advance.py) PostToolUse + `advance_for_artifact`): запись ANALYSIS-REPORT→этапы 1,2 done, IMPLEMENTATION-PROGRESS→3 (guard по title-метке). **F-2** ([`pipeline-gate.py`](.claude/hooks/pipeline-gate.py) += `/implement-1c-task` через `gate_1c_implement`): хард-блок реализации до approve дизайна (G4). **F-3**: нота «4-этапная парадигма» в analyze/implement SKILL.md (G5/G2). **F-1.6**: `advance_test_done` — этап 4 done по ДОКАЗАТЕЛЬСТВУ теста: `.run-state.json` все passed (BDD/YAxUnit) **ЛИБО** (ADR-050, 2026-07-19) IMPLEMENTATION-PROGRESS с секцией «Тестирование на реальных данных» и чистым PASS (чистая `_live_data_test_passed`: скоуп секции + PASS-токен + гард FAIL/SKIP/STATIC-ASSUMPTION; консервативно — неоднозначность→не закрываем). Делает этап 4 доказуемым БЕЗ обязательного BDD (закрыл дыру «done без тест-артефакта», группа C аудита W13); caller `pipeline-1c-advance` зовёт `advance_for_artifact`+`advance_test_done` (не `or`) → live-PASS двигает этап 3 И 4 одной записью. 11 unit + smoke. **W**: [`scripts/tool_usage_report.py`](scripts/tool_usage_report.py) (`--run-id`/`--session`→отчёт по `hook-invocations.jsonl` + `data/tool-effectiveness.jsonl`; `--rollup`). **input-ingestion (V.6)**: [`onec-task-input.py`](.claude/hooks/onec-task-input.py) UPS + `classify_1c_task` → протокол дизамбигуации (T1/T2/T3, ASK/folder/prior-load) при 1С-задаче из чата. Всё — [roadmap 260614](docs/roadmap/260614_ROADMAP_1C_COMMANDS_4STAGE_ALIGNMENT.md) §18; методика 1С не тронута (проводка в хуках, ADR-019 B′). **AUTO-режим (2026-06-15):** команда [`/run-1c-task <вход>`](.claude/commands/run-1c-task.md) + skill [`run-1c-task`](.claude/skills/run-1c-task/SKILL.md) — оркестратор analyze→**авто-approve**→implement→test БЕЗ паузы на ревью первого этапа (отличие от гейтованного `/analyze`+`/implement`); вход (JIRA / описание из чата / путь к папке ТЗ) детектится `resolve_task_input` ([`pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py)); хард-правило «AUTO ≠ игнор блокеров» (критическая неоднозначность → СТОП). Методики (analyze-1c-task-v2/implement-1c-task) не дублируются — скилл их делегирует. Пайплайн `pipeline/impl-1c-run-task/`. **Классификатор сложности + маршрутизация (2026-06-15):** при 1С-задаче ИЗ ЧАТА [`onec-task-input.py`](.claude/hooks/onec-task-input.py) зовёт `route_1c_task` ([`pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py)) — `classify_1c_task` (1С-детект+тип) + `estimate_effort` (эвристика трудозатрат по keyword-сигналам, config `_EFFORT_CFG` тюнится: light/modify/heavy_obj/cross/multi/folder → simple≤2/medium 3–5/complex≥6; **light = downgrade-only при pos==0**, не counterweight — иначе имя реквизита занижает medium→simple) → `flow`: **простая→`/run-1c-task` (AUTO), средняя→спросить, сложная→гейтованный `/analyze`+`/implement`**; не-1С→молча, слабый-1С-сигнал-без-JIRA→`ask_1c` (подтвердить). Решение маршрута — пользователь (2026-06-15); эвристика < правило «сомнение в 1С/потоке → спросить» (хвост-нота в инъекции). `code-skill-enforcer` `_is_test_file` → tests/ exempt от content-уровней A/A.1 (фикстуры легитимно несут доменные токены). Пайплайн `pipeline/impl-1c-task-router/`. **Recall-расширение детектора (2026-06-15, заземлено на ~35 реальных задач `configuration/260304…/docs/`):** `classify_1c_task` многоуровнев — `_1C_DEFINITIVE` (`гкс_`/`configuration/` → 1С даже без глагола) + **`_1C_CODE`** (литеральный 1С/BSL-синтаксис: `Менеджер.Член` ед./мн. с member-guard `\.(?:[А-ЯЁ]|гкс_)` · события `ПередЗаписью`/`ОбработкаПроведения` · закрывающие `КонецЕсли`/`КонецПроцедуры` · токены запроса `ЕСТЬNULL`/`СГРУППИРОВАТЬ ПО`/`СрезПоследних` · БСП `ОбщегоНазначения.` → **1С БЕЗ глагола И `confident_1c=True`**, наличие кода = ключевой признак [фидбэк 2026-06-17]; case-sensitive `re.U` без `re.I`, БЕЗ неоднозначных `ManagerModule`/`.bsl` — code-путь короткозамыкает на `auto`-прогон, поэтому code-FP дороже domain-FP [code-review FAIL→fix]; `pipeline/1c-detector-expand/`) + `(_1C_SIGNAL доменные термины ∪ _1C_STRONG CamelCase/объект.точка) AND _TASK_VERB`; словари `_TASK_VERB`/`_1C_SIGNAL` расширены (АРМ/РС/РН/печатн/обмен/движени/предопредел/учт-«не учтено» T3 и т.д., `\b`-границы против substring-FP). **Инвариант безопасности:** term+verb без сильного маркера → `confident_1c=False` → `ask_1c` (не авто-маршрут) — recall↑, ложные деградируют в вопрос. CamelCase-регекс в `_1C_STRONG` строго `re.U` БЕЗ `re.I` (иначе hump-класс матчит любые 3 кириллицы). **Решение «это 1С» — за Claude (хук advisory), сомнение → спросить** ([[feedback-1c-detection-judgment-mine]]). Пайплайн `pipeline/impl-1c-detector-recall/`. **Единый task-completion gate (2026-06-15):** Stop-хук [`onec-task-completion-stop.py`](.claude/hooks/onec-task-completion-stop.py) (после `pipeline-protocol-stop`) — ЕДИНЫЙ gate 1С-задачи: один проход по транскрипту → консолидированный чеклист всех обязательных петель (без 3-каскада). Hard: **recall** (`unified_search`/`search_patterns`) + **capture** (`capture_pattern`/`route_and_save`/`.claude/…/memory/*.md`) + **research** (`WebSearch`/`WebFetch` — Infostart + GitHub); info: **skill** (1С-методика — уже enforced `code-skill-enforcer` на Write). Блок ОДИН раз со всеми ✗ сразу; детект по фактическим tool_use (не raw-text); 1С-сигнал `is_1c_task_title` (paren-anchored единый предикат в мосте, N4); opt-out `ONEC_TASK_GATE_DISABLE=1`, graceful, no-sentinel. **Заменил** `memory-protocol-stop` + `research-protocol-stop` (удалены — консолидация по адверсариальному анализу). Усиления тонких мест N1–N12 (tail 8МБ N3, единый предикат N4, capture-`.claude` N6, audit `approved_by=auto` N5, opt-out audit N10) — `pipeline/impl-thin-spots-hardening/`. 7 unit + e2e block(чеклист)/allow + live exempt + code-verify PASS. Док — [гл. 43.5 Сквозная карта](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.5_СКВОЗНАЯ_КАРТА.md). Пайплайн `pipeline/impl-onec-task-gate/`. **ADR-035 Фаза 1 (advisory T1-T2, 2026-06-22):** в чеклист добавлены **рекомендательные** (НЕ блок) high-leverage инструменты — T1 `bsl_impact_analysis` (impact «кто сломается» перед правкой экспортного метода) + `1c-debug-hmr`/`1c-debug` live BP-trace (детект сужен до содержательных `set_breakpoint`/`stack_trace`/`variables`/`evaluate`/`step`/`logpoint`, НЕ connection-проверок ping/health/targets), T2 `bsl_search`/`bsl_hybrid_search`/`bsl_similar` (эталон на Планировании); маркер `•` в block-сообщении + строки в LOOPS.md, **блок-решение НЕ изменено** (строго recall/capture/research, инвариант code-verify PASS); event-log [`.claude/cache/onec-toolgate-events.jsonl`](.claude/cache/) пишется на КАЖДОЙ 1С-задаче (allow+block) для замера follow_rate/FP перед промоутом в hard. **Измерительный контур (2026-06-22, code-verify PASS):** валидатор [`scripts/onec_toolgate_validation.py`](scripts/onec_toolgate_validation.py) (presence_rate per-tool → `promote-candidate`/`keep-advisory`, **caveat survivorship-bias** — presence ≠ безопасность hard) + SessionStart-баннер [`onec-toolgate-validation-on-start.py`](.claude/hooks/onec-toolgate-validation-on-start.py) (раз/день, окно 06-22→07-06) + FIFO-ротация лога (`ADVISORY_LOG_CAP`). Фаза 2 (hard-промоут **по вердикту валидатора + ручная проверка применимости** per-tool, НЕ авто-флип + контекст-классификация типа задачи через `pipeline_1c_bridge`) — после окна валидации; см. [ADR-035](.claude/skills/architecture-research/adr/035-mandatory-high-leverage-1c-tools.md). **Семантический слой ② детектора — SetFit-гейт (ADR-025, 2026-06-18, opt-in каркас):** обучаемый `is_1c`-классификатор [`onec_setfit_gate.py`](.claude/hooks/shared/onec_setfit_gate.py) встаёт ПЕРЕД TF-IDF (`onec_semantic_fallback`) в `route_1c_task` через `_semantic_signal` — **opt-in (`ONEC_SETFIT_ENABLE`), по умолчанию СПИТ**: `setfit_prob`→`None` при нет-флага/нет-`setfit`/нет-модели/ошибка → graceful откат на TF-IDF, поведение при OFF побитово прежнее (+аддитивный ключ `semantic_source∈{setfit,tfidf,skipped}`). Инвариант безопасности сохранён — мягкий сигнал промоутит ТОЛЬКО в `ask_1c` (не confident/auto). Бинарный (сложность остаётся за `estimate_effort`); старт `rubert-tiny2`, апгрейд `multilingual-e5-small` (MiniLM-L12-v2 отклонён). Обучение [`scripts/train_onec_setfit.py`](scripts/train_onec_setfit.py) (`--dry-run`/`--model`, поверх `data/1c-detector-ground-truth.json`, GT=68 — мало), калибровка [`eval_1c_detector.py --setfit`](scripts/eval_1c_detector.py); **активация ⟸ разметка ~150–300 + обученная модель**. Зеркалит opt-in лестницу: ADR-023 (TEI откл, коллапс) → ADR-024 (LLM-tail net-neutral) → **ADR-025 (SetFit, масштабный путь)**. **ОБУЧЕНО (Этап 3, 2026-06-18): backend ST+LR — `setfit` несовместим с transformers 5.x (`default_logdir`/`config_setfit.json`), поэтому `SentenceTransformer` (rubert-tiny2) + LR-голова напрямую (frozen RU-эмбеддинги НЕ коллапсируют на NL-тайтлах ≠ Qwen3 на BSL-коде); GT 68→230 ([`bootstrap_1c_gt.py`](scripts/bootstrap_1c_gt.py)); интегрированный каскад is_1c F1 0.883→0.974 / Recall 0.813→0.978 (FN 25→3, precision цел), CV F1 0.94; 20 gate-тестов; модель gitignored (регенер. `train_onec_setfit.py`), gate opt-in OFF. **Warm-serve:** in-process в хуке = ~6с/промпт (>5с-таймаут → негоден), поэтому [`scripts/onec_setfit_serve.py`](scripts/onec_setfit_serve.py) — CPU-сервис (stdlib http.server, 0 VRAM, idle-shutdown 30мин), гейт зовёт по HTTP с авто-стартом detached → **6040мс→~25мс**; включение = `ONEC_SETFIT_ENABLE=1` + сервис авто-стартуется, code-verify PASS. Пайплайн `pipeline/onec-setfit-gate/` **Гейт «действие не названо» (Находка 3, 2026-06-18):** `route_1c_task` для УВЕРЕННОГО 1С-входа (гкс_/код/JIRA) БЕЗ названного действия — нет таск-глагола (`_TASK_VERB`) И нет work-сигналов (`signals==[]`) — больше НЕ уходит в `auto` (запуск `/run-1c-task`), а понижается в `ask_flow` (спросить «что сделать?»). Корень: детектор смешивал «в тексте есть 1С-код» и «дана 1С-задача к исполнению» (живой пример: вставленный путь к `ManagerModule` + строка BSL + предупреждение анализатора + вопрос «Это 1С задача?» → раньше `flow=auto`). Гейт достижим ТОЛЬКО в simple-полосе (verb-less ∧ zero-signal ⇒ points≤2), medium/complex не затронуты; инвариант безопасности «confident сохранён» цел (гейт меняет лишь `flow`+ключ `actionless`). Потребитель `onec-task-input.py` — тейлоред-сообщение для `actionless`. Заодно `_TASK_VERB` расширен частыми глаголами (`замен|помен|переписа|перепиш|передела|обнов|переимен|поправ`, start-`\b` против substring-FP `взамен`/`переписка`/`переделка`); известный тех-долг — стем ловит отглагольные сущ. (`обновление`/`заменитель`), blast-radius мал. 8 unit + code-verify PASS. Пайплайн `pipeline/fix-1c-route-actionless-gate/` **Hardening маршрутизации C1-C4 (2026-06-20, аудит 43.5, пайплайн `pipeline/1c-route-logic-hardening/`):** **C1** — actionless получил отдельное `flow="ask_action"` (раньше перегружал `ask_flow`; **обновляет «Находка 3» выше** — actionless теперь `ask_action`, `ask_flow` = только medium); enum самодостаточен, `onec-task-input` диспетчеризует по значению (доп-ключ `actionless` оставлен для наблюдаемости). **C4** — non-1C veto иммунен ТОЛЬКО при `confidence ≥ 0.9` (JIRA/код/гкс_/configuration); CamelCase-кириллица (`_1C_STRONG`, 0.7) теперь **ветируется** тех-контекстом («функция ПолучитьЭмбеддинг в langchain» → none) — раньше любой confident обходил veto. **C2/C3** — новый `resolve_active_1c_slug` (JIRA-код → иначе CURRENT-если-1С) = ЕДИНЫЙ резолвер для `gate_1c_implement` (G4) и `ensure_pipeline_1c(implement)`; `/implement` цепляется к пайплайну `/analyze` через CURRENT → **G4 надёжен для не-JIRA** (раньше `gate` искал по `derive_slug(implement_prompt)` ≠ analyze-slug → молча no-op / ложный блок). 87 unit + code-verify reviewer PASS; гл. 43 синхронизирована (ask_action / veto-тиры / G4-не-JIRA). **Sonar-гейт изменённого/добавленного 1С-кода ([ADR-037](.claude/skills/architecture-research/adr/037-mandatory-sonar-rescan-changed-1c-code.md), 2026-06-23, мандат пользователя):** в `onec-task-completion-stop` добавлена **hard**-проверка `sonar_rescan` — при `config_edit` (правка/добавление `.bsl`) блок завершения, если изменённый код не прогнан через SonarQube с чистой дельтой (0 BLOCKER/CRITICAL на затронутых файлах). Контракт [`shared/sonar_rescan_state.py`](.claude/hooks/shared/sonar_rescan_state.py) (детект `.bsl` осн.репо+сабмодули, фильтр `/src/` — scratch-`.bsl` в docs исключены; **`configuration/<JIRA>` исключён ЦЕЛИКОМ вкл. `/src/`-дампы** — ADR-048 approve 2026-07-06: ведение задач, не код, Sonar их не сканирует → детект без исключения дал бы dead-lock «не проанализирован»; `evaluate`: missing/stale/stale-scan/fail/uncovered/edits-after/ok — DRY-контракт детекта общий с verify) + verify [`scripts/sonar_rescan_verify.py`](scripts/sonar_rescan_verify.py) (reachability→дельта по Sonar API [component exists + BLOCKER/CRITICAL + scan свежее правок]→пишет `.claude/cache/onec-sonar-rescan-state.json`). **Default-ON** (не opt-in-env как ADR-035/036); anti-deadlock: opt-out `ONEC_SONAR_GATE_DISABLE=1`, Sonar-down→verify пишет `skipped`→ok, graceful→ok, выход = прогнать verify (ВСЕГДА пишет state). Операционно = Clean-as-You-Code на СВОИ изменения (общий QG soft — ADR-033/034 R6 — легаси не гейтим). git-timeout 5s, только при `config_edit`. 12 unit ([`test_sonar_rescan_state.py`](tests/unit/test_sonar_rescan_state.py), все ветки evaluate + фильтр /src/) + code-verify reviewer PASS (R1 session_start=None ложный-allow / R2 stale-scan статус / R3 git-timeout — fixed). Память [[feedback-1c-changed-code-sonar-mandatory]]. Пайплайн `pipeline/sonar-mandatory-gate/`. **tz-фикс `parse_dt` (2026-07-02, commit c384f0a5d):** `shared/sonar_rescan_state.py::parse_dt` срезал tzinfo у UTC-даты Sonar (`+0000`) → `.timestamp()` трактовал UTC как локаль → в TZ ахед UTC анализ выглядел на offset ч «старше» mtime → **ложный `stale-scan`** (гейт локально не закрывался никогда). Фикс: НЕ срезать tzinfo (aware-вход Sonar-даты → корректный epoch в `scan_stale`; naive локальный state-`ts` от `datetime.now()` остаётся naive → прямое `ts<session_start` в `evaluate` не ломается). 12/12 unit + code-verify PASS. Обнаружено при закрытии гейта GKSTCPLK-2597 (присоединение файлов из АРМ). **mode=changed-lines (2026-07-03, разбор «почему GKSTCPLK-2637 закрывался часами»):** verify гейтит BLOCKER/CRITICAL по пересечению issue-строк с `git diff -w -U0` изменённых `.bsl` (`changed_line_ranges`/`parse_hunk_new_ranges` в контракте; untracked → все строки, fail-closed) — сервер-независимый CaYC: вырожденный new-code baseline (первый скан → все легаси-issue «новые», new≈total 71k) детектится (`baseline_degenerate` в state) и НЕ гейтит; issues тянутся per-file (кириллический 500 не воспроизводится на CB 26.x) с ленивым project-wide fallback (ES-cap 10k → `issues_truncated`); сервер: ветка `main` → `SPECIFIC_ANALYSIS` на пре-2637 анализ (принимается ТОЛЬКО branch-level, project-level set = тихий no-op — проверять `/list`). Парный фикс ADR-036-формата: `bsl_lint --format` селективен (`_selective_format` — формат только строк, изменённых vs HEAD; EOL исходника сохраняется — устранены CRLF-флип и легаси-churn, ломавший SCM-атрибуцию) + `.gitattributes` в сабмодуле SVETLY; правила «правки .bsl батчем одним вызовом», «док-комментарии сразу», «один скан на задачу» — в implement-1c-task SKILL (Этап 3/4/чеклист). Память [[reference-sonar-changed-lines-gate]] + [[feedback-bsl-batch-edit-format-hook]], пайплайн `pipeline/sonar-gate-process-improvements/`. **Надёжность SQ-контура + скоуп (2026-07-06, [roadmap 260706](docs/roadmap/260706_ROADMAP_SONARQUBE_SCAN_RELIABILITY.md) + [ADR-048](.claude/skills/architecture-research/adr/048-sonar-project-split-per-configuration.md)):** разбор 5 инцидентов прогона → **P0 реализован**: verify получил `wait_ce()` (poll `ce/activity_status` до idle, `--wait-ce` 120с, 404→fast-path) + freshness=max(`project_analyses`, `project_branches/list`) — закрыта гонка финализации Compute Engine (exit 0 сканера ≠ анализ дописан → был ложный `scan_stale`); `run-sonar-analysis.ps1` — fail-fast (якорь `$PSScriptRoot\..` вместо cwd-зависимого rev-parse [из сабмодуля был тихий no-op exit 0 БЕЗ скана], FATAL exit 2 на токен/sources/helper'ы, маркер `SCAN-SKIPPED`, venv-python) + **UTF-8 BOM** (без BOM PS 5.1 читал cp1251: `0x94` из `—` в двойных строках = типографская `”`-делимитер → parse error; байтовый тест-инвариант, [[feedback-ps1-utf8-bom-smart-quotes]]). **Скоуп (approve-поправки):** `configuration/<JIRA>` исключён из скана (`sonar_sources.py` `GROWING_PARENTS=[]`) И детекта гейта (`_is_config_bsl` префикс-фильтр) — пара «скан-скоуп ↔ гейт-детект» = один инвариант; **A7 решён (вар.а): `MFM/Конфигурация` = проект `utp-mfm`** (`STABLE_ROOTS += MFM`, детектится — сканируется, слепых зон нет). **P3 split принят** (реестр [`scripts/sonar_projects.py`](scripts/sonar_projects.py) = utp-ib/utp-svetly/utp-mfm ✅ реализован (**+utp-kat** 2026-07-23: 4-я конфигурация `TransportManagementDevelop_KAT/Конфигурация` — в ГЛАВНОМ репо, не сабмодуль; провижинена+baseline-скан 2503 файла EXECUTION SUCCESS; правки KAT-`.bsl` под `/src/` детектил path-shaped `_is_config_bsl`, но без записи в реестре split-verify fail-closed'ил их «вне реестра» → неустранимый блок гейта — закрыт добавлением в `sonar_projects.PROJECTS`+`sonar_sources.STABLE_ROOTS`); dual-mode `SONAR_SPLIT_PROJECTS`, per-project `scan_stale`, ожидаемая починка SCM blame; фазы A/B/C — **все ✅ 2026-07-06**: A0+A.registry+A.wiring [ps1 `-Project`/`SONAR_SPLIT_PROJECTS` split-цикл + verify `verify_project`/`_run_split` per-project, mono бит-в-бит, 46 unit] + **B** [3 проекта provisioned+валидированы: EXECUTION/CE SUCCESS, `_match_component` резолвит `<proj>:src/…`, baseline пропинан, A9 heap OK] + **C** [локальный флип `SONAR_SPLIT_PROJECTS=1` в `.env` → гейт split-mode; реверс=убрать строку, mono сохранён; repo-wide код-дефолт+CI+архивация mono — отдельно]). **P1 ✅** (CE-handoff по `report-task.txt` в ps1, `-LogFile` UTF-8-лог native-safe, verify `--show-file` диагностика по файлу, venv-python). Не редиректить stderr ps1 (`2>&1` + EAP=Stop = падение на первом варнинге сенсора). Пайплайн `pipeline/sonar-scan-reliability/`, 27 unit, code-verify PASS ×2. **SDD↔1С-пайплайн мост (ADR-041 R1-R4, [`adr/041`](.claude/skills/architecture-research/adr/), 2026-06-25):** устраняет трение гл.24 (SDD/OpenSpec) ↔ гл.43 (1С-пайплайн) — `pipeline_1c_bridge.py` `sync_approval` (идемпотентная one-way проекция OpenSpec-approval → `.pipeline-state.json`, R2) + lineage-консистентность ANALYSIS-REPORT↔proposal/specs (advisory, R3) + SDD-aware `route_1c_task(use_sdd)` (R4); проводка в `pipeline-gate.py`/`onec-task-input.py`. **YAxUnit unit-трек этапа-4 (план floofy-popping-candle, 2026-06-26):** полноценный unit-трек рядом с BDD — расширение [`src/bsl/exts/UnitTests/`](src/bsl/exts/UnitTests/) (рукописные assert-тесты, дискавери `ЮТТ_ЮТПодключаемыеМодули→Модульные`, шаблон Smoke; deploy Designer `LoadConfigFromFiles -Extension UnitTests`+`UpdateDBCfg`), методичка [17.6](docs/framework%20documentation/7_ПРОВЕРКА/7.4_ТЕСТИРОВАНИЕ_1С/17.6_НАПИСАНИЕ_YAXUNIT_ТЕСТОВ.md), команды `/write-1c-unit-tests`+`/run-1c-unit-tests`, скилл `yaxunit-unit-testing`; прогон `mcp-onec-test-runner`→JUnit→`.run-state.json` (`chain[].type=yaxunit`)→`advance_test_done`. ⚠ раннер `.env ONEC_TEST_CONN` дефолт `Srvr="KOMPUTER";Ref="TestDB"` недоступен → локально `Srvr="DESKTOP-TNU600C";Ref="ИБTransportManagementDevelop"`. **Live-данные обязательны ([ADR-050](.claude/skills/architecture-research/adr/050-mandatory-live-data-verification-1c-pipeline.md), мандат 2026-07-13):** Фаза 2.5 Runtime Trace — **default-ON** для задач с исполняемым BSL-кодом (analyze-1c-task-v2 v4.5.0; `--no-trace` opt-out, `--trace` — форс, SKIP только: нет кода / debug env down — с фиксацией) + Этапы 5.x/6 на **реальных данных** (implement-1c-task v2.9.0; кандидаты `execute_query`, параметры функциями вызывающего — Шаблон 7) + обязательные главы отчётов: ANALYSIS-REPORT **§10 «Проверка на реальных данных»** / IMPLEMENTATION-PROGRESS **«Тестирование на реальных данных»** (база, идентификаторы данных, инструменты, PASS/FAIL); advisory-контроль `lint_1c_artifacts.py` (+2 core-секции, lenient «живых данных»; 12 unit). Закрыт долг [[feedback-live-bp-trace-mandatory]] («память ≠ enforcement» — мандаты проводить в методики сразу). Пайплайн `pipeline/impl-1c-live-data-mandate/`. **Аудит 1С-tooling В0-В5 ([roadmap 260718_1C_TOOLING_AUDIT](docs/roadmap/260718_ROADMAP_1C_TOOLING_AUDIT.md), 2026-07-18):** W1 главная слабость = high-leverage тулы не используются (impact 1.8%/BP-trace 6.3%/эталоны 0.6% на 331 задачу); **честный applicable-знаменатель** для безопасного hard-промоута — [`shared/onec_change_scope.py`](.claude/hooks/shared/onec_change_scope.py) `edits_exported_method` (правка экспортного метода: пересечение changed-lines с export-спанами, `Экспорт` только в сигнатуре [code-verify поймал FP на комменты тела], async учтён) → `impact_applicable` в toolgate-event (`onec-task-completion-stop`) → `impact_rate_on_applicable` в валидаторе (ADR-035 §19); методики-мандаты: эталон-поиск (analyze Ф3) + impact-чек экспортного (implement Э3); **YAxUnit-смоук серверной логики (рек.3, 2026-07-19)** — advisory (opt-in default, НИКОГДА не блок) на том же applicable-сигнале `edits_exported_method` (тестируемая = экспортная поверхность, POST-change комплемент impact-чека), ЕДИНСТВЕННЫЙ маршрут `mcp-onec-test-runner` (`run_all_tests`/`run_module_tests`; НЕ edt/codepilot дубли, НЕ BDD); сюрфейс `•`-буллет+LOOPS.md, метрика `onec_toolgate_validation.yaxunit_rate_on_applicable` (тот же честный знаменатель); прогон пишет `.run-state.json`→авто-Этап-4 (F-1.6), иначе секция live-данных (ADR-050); 4 unit + parity-harness (256 комбо, advisory не влияет на hard); test-post-в-транзакции портирован (implement Э6, [ADR-053](.claude/skills/architecture-research/adr/053-1c-tooling-v3-new-tools.md) + bsl-lsp ADOPT user-gated + formsserver ADOPT-lazy); RU-синонимы `expand_queries` (W8); Agent/Task unpaired-Pre ≠ провал (`ASYNC_UNPAIRED_UNRELIABLE`, W10); ретирмент В5 = документированное НЕ-действие (crud = per-инфобаза); **W13 ЗАКРЫТ (2026-07-18)**: тест-контур этапа 4 заявлял 5 стеков — применялся ~0 (не мёртв, а НЕ использовался). Плагин `codepilot1c` обновлён headless p2 director'ом (`com.codepilot1c.feature.feature.group` 0617→0714, release-ассет `com.codepilot1c.update-*.zip`=p2-site); **EDT-runtime путь `qa_run` сломан на EDT 2025.2.6.4** (`IRuntimeComponentManager.getThickClientInfo` в API отсутствует — апстрим-баг, доказано javap+`.log`), **binary-путь proven working** (MFM смоук `junit tests=1 failures=0 errors=0`): `edt.use_runtime=false` + `platform.bin_path=…\1cv8c.exe` (exe!) + `test_manager`/`test_clients[].ib_connection` c `Usr=`; `.epf` из git LFS (`git lfs pull`); `status:infra_error` при exit 0 ложный (истина — junit); слоты dev-лицензии → `rac session terminate` зависших клиентов. BDD-через-MCP задокументирован ([17.1](docs/framework%20documentation/7_ПРОВЕРКА/7.4_ТЕСТИРОВАНИЕ_1С/17.1_ОБЗОР_ТЕСТИРОВАНИЯ.md)/[43.9.8](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.9.8_ТЕСТИРОВАНИЕ.md) + скиллы codepilot1c/va-bdd-testing + память [[reference-codepilot1c-qa-run-binary-path]]/[[reference-codepilot1c-plugin-source]]). Развилка default-исполняемости этапа 4 + 9 «done» без `.run-state` артефактов — за пользователем.
- **Hooks Infrastructure**: `.claude/hooks/docs-change-enforcer.py` → `SKIP_PATTERNS` для инфра-файлов (`.gitmodules`/`.gitignore`/`.gitattributes`, `tools/`, `scripts/`, `tests/`, `features/`, `.run-state.json`, `pyproject.toml`, `.mcp.json`, `src/projects/`, `src/bsl/`, `logs/`, `skills-lock.json` [вендоренный лок `skills` CLI — паттерн `.lock` его НЕ ловит: имя через дефис `-lock.json`], `agents.md` [близнец `claude.md`: корневые инструкции агентам, самодокументирующийся; без него правка `AGENTS.md` всплывала как `UNMAPPED` и жёстко блокировала сессию — энфорсер требовал документацию ДЛЯ документации, 2026-07-20], `.claude/worktrees/` [stale agent-worktrees = снапшоты старых веток, зеркалится и в `docs-change-tracker.py`; 2026-07-04] — runtime artefacts). При добавлении нового типа инфра-файла — добавить в `SKIP_PATTERNS`, иначе `UNMAPPED` блокировка. EDT-воркспейсы-сабмодули в корне (`ИБTransportManagementDevelop/`, `TransportManagementDevelop_SVETLY/` — submodule 2026-06-29, `MFM/` [УправлениеМатериальнымиПотоками] — submodule 2026-06-30, remote `UpravlenieMaterialnymiPotokami-Configuration`, `TransportManagementDevelop_KAT/` — workspace одноимённой инфобазы, 2026-07-23) тоже в `SKIP_PATTERNS`: submodule-gitlink = указатель, не product-код. **Existence filter** (added 2026-04-15): `get_session_files()` отбрасывает файлы, которых уже нет в working tree (удалены в пределах 6-часового окна `git log`), чтобы стрей-артефакты не блокировали завершение сессии. `code-skill-patterns.json` — конфиг `code-skill-enforcer`, правила `{pattern, skill, label, domain}`; `skill` должен существовать в каталоге `.claude/skills/`, иначе phantom-блокировка. **Routing-fixes (2026-05-29)**: `FastAPI|APIRouter` и `pytest\.fixture|@pytest|conftest\.py` перенесены из `research_protocol` (Level A.1 → форсил `learning-loop`) в `patterns.mappings` (Level A) → `framework-api` / `evaluation-benchmark` соответственно (профильные скиллы уже существовали). `z-ai-write-guard.py` `_EXEMPT_PREFIXES` дополнен `tests/` (docstring уже его перечислял — устранён code/doc drift; тест-код = precision-работа, не делегируемая генерация). **BSL исключён из гарда (2026-07-20):** `.bsl`/`.os` убраны из `_CODE_EXTENSIONS` — гард требовал делегировать 1С-код, что прямо противоречило стоячему правилу «Z.AI — исполнитель для всех языков КРОМЕ 1С; BSL пишет Opus, Z.AI галлюцинирует API платформы». Все обходы были хуже правки: делегировать BSL = выдуманный API в живой конфигурации; резать на куски <15 строк = гонка с PostToolUse-автоформатом (`bsl_lint --format`, правки `.bsl` обязаны идти ОДНИМ батчем); ждать 30-мин окна `has_llm_delegation` = исход зависит от давности постороннего `llm_complete`. Регресс [`tests/unit/test_z_ai_guard_bsl_exempt.py`](tests/unit/test_z_ai_guard_bsl_exempt.py) пинит **обе** стороны инварианта (BSL проходит / Python блокируется) + саботаж-проверкой подтверждено, что откат правки краснит ровно 2 теста; ⚠ ловушки тестирования гарда: файлы **вне репозитория** освобождаются до проверки расширения, а `tests/`/`docs/`/`data/`/`.claude/` — по `_EXEMPT_PREFIXES` (тест на `tmp_path` был бы зелёным по ложной причине), плюс окно делегирования требует заглушек `sys.modules` для `shared.session_state`/`shared.llm_health`. Синхронно обновлён skill [`llm-delegation`](.claude/skills/llm-delegation/SKILL.md) (бывш. z-ai-delegation) (ревью BSL обязателен, делегирование — нет). **Отчёты 1С-пайплайна исключены (2026-07-22):** `_MD_EXEMPT_BASENAMES = {analysis-report.md, implementation-progress.md}` в скоупе `configuration/` (якорь на границу сегмента — `my_configuration/` НЕ освобождается). Тот же класс, что BSL-исключение: артефакт несёт выверенные факты (номера строк модулей, UUID объектов 1С, дословные выдержки ИТС), делегирование их переписывает и тихо искажает — а на них потом опирается `/implement-1c-task`; плюс тот же конфликт энфорсеров, что закрыл `pipeline/` (`onec-task-completion-stop`/`pipeline-1c-advance` ТРЕБУЮТ файл, гард запрещал). Регресс [`tests/unit/test_z_ai_guard_1c_reports_exempt.py`](tests/unit/test_z_ai_guard_1c_reports_exempt.py) (6 тестов, обе стороны инварианта + lookalike-каталог). ⚠ **ловушка тестирования гарда:** без заглушек `shared.llm_health`/`shared.session_state` (`_no_delegation`) тест зависит от состояния машины — любой `llm_complete` в 30-мин окне пропускает запись мимо гарда, и позитивные кейсы зеленеют по ложной причине (поймано code-verify FAIL→fix). Smoke/behavioral покрытие orphan-модулей `processing/`: [`tests/unit/test_import_smoke.py`](tests/unit/test_import_smoke.py) (авто-discovery всех `src.pdf_framework` модулей, marker `unit` → CI `test-unit`) + [`tests/unit/test_processing_behavior.py`](tests/unit/test_processing_behavior.py) (исполняет методы через tmp_path+моки+fitz-PDF, ловит `self._format`/missing-await класс багов). **Roadmap §18 trigger-detection (2026-05-29)**: новый Stop-хук [`roadmap-progress-enforcer.py`](.claude/hooks/roadmap-progress-enforcer.py) реализует §19.3 P1 ([roadmap 260523](docs/roadmap/260523_ROADMAP_FULL_DEV_LIFECYCLE_ANALYSIS.md)) — парсит transcript на Bash `tool_use` с `gh pr merge`/`git merge` (парсинг tool_use, НЕ raw-text → чтение роадмапа не false-positive'ит); при merge-milestone без правки `docs/roadmap/` → **block-once per session** (sentinel `.claude/cache/roadmap-progress-enforcer-state.json`, soft — не deadlock). Opt-out `ROADMAP_PROGRESS_NO_ENFORCE=1`. Зарегистрирован в Stop-chain после `docs-change-enforcer`. **§19.3 полностью закрыт (предрассветно):** + CI-lint [`scripts/roadmap_progress_log.py`](scripts/roadmap_progress_log.py) (`lint` structural+freshness в `ci.yml` lint-job, `append` skeleton-генератор, `links` wikilink-валидация против memory store) + `docs/roadmap/` exempt в обоих auto-save хуках (`auto-git-save.py` `IGNORE_PATH_PREFIXES` + `posttooluse-auto-git-save.py` `SKIP_PATTERNS`) → §18-правки получают осознанный `docs(roadmap):` коммит вместо `chore: auto-save` preempt (safety net — `git-commit-enforcer` watches `docs/`). **Amend-absorb (2026-06-12):** все 3 auto-save хука амендят свой незапушенный auto-save HEAD того же prefix'а (`shared/auto_save_core.get_amendable_head`: prefix word-boundary, `branch -r --contains HEAD` пуст, нет merge/rebase/revert) → незапушенный хвост = один коммит, поглощаемый осмысленным `git commit --amend`; серии «5× chore: auto-save» устранены; регресс `tests/unit/test_auto_save_amend.py`, детали skill `auto-git-save` §1.5. **Ruff-format перед auto-commit (2026-06-19):** все 3 auto-save-пути перед коммитом зовут `shared/auto_save_core.format_staged_python()` — ruff-format'ят staged `.py` под `[tool.ruff] line-length` (`--line-length` передаётся ЯВНО: голый `ruff format <file>` ширину не подхватывает, с флагом == pre-commit; python из `.venv`), затем `git add` заново; best-effort (нет ruff/timeout → коммит НЕ блокируется), пропуск vendored-деревьев (`tools/`/`external/`/`infra/`/`src/bsl/`, зеркало pre-commit `exclude`); устраняет рецидив красноты CI-джоба **Pre-commit Hooks** (auto-commit идёт `--no-verify`/кастомный `core.hooksPath`, в обход pre-commit → неотформатированный `.py` попадал в master; memory `project-ci-precommit-red-autocommit-noverify`); детали skill `auto-git-save` §1.6. **Stale index.lock guard (2026-07-24):** все 3 коммит-пути перед первой git-командой зовут `auto_save_core.clear_stale_index_lock()` — осиротевший `.git/index.lock` старше 10 мин снимается (живой инцидент: 0-байтовый лок 4.5ч от упавшего процесса блокировал ВСЕ коммиты), свежий не трогается (fail-safe на OSError/будущий mtime); регресс [`tests/unit/test_auto_save_stale_lock.py`](tests/unit/test_auto_save_stale_lock.py), детали skill `auto-git-save` §1.7 + [roadmap 260724](docs/roadmap/260724_ROADMAP_1C_TOOLING_RELIABILITY.md). **Regression-hardening (2026-06-02, R1-R4):** после merge/autofix-регрессии CI-lint расширен `src/` → `ruff check src/ scripts/ .claude/hooks/` ([`ci.yml`](.github/workflows/ci.yml)) — broken-код в `scripts/`/`.claude/hooks/` раньше не линтился; добавлен Compile-smoke step (`compileall scripts .claude/hooks`, dep-free, ловит структурную порчу; undefined-name класс ловит F821 в расширенном lint). Включены `RUF100`+`PGH004` в ruff `select` (noqa-гигиена: запрет «голого» `# noqa` и мёртвых директив — корень регрессии: `# noqa: F821` маскировал удалённые символы); потребовало вычистить ~116 мёртвых noqa. Guard [`tests/unit/test_git_hooks_present.py`](tests/unit/test_git_hooks_present.py) (`core.hooksPath`-каталог был тихо удалён → отключил весь авто-reindex). **Bugfix `docs-change-enforcer.py`:** `get_session_files()` строил `since_arg`+`grep_args` (session-bound окно + исключение `^chore: auto-save`/`auto-format` коммитов), но НЕ передавал их в `git log` (dead code → over-firing на косметических коммитах); проводка восстановлена + добавлены `^style:`/`^style(` паттерны (style/format/noqa-коммиты = comment/whitespace-only). ⚠ `git log --grep "^style("` требует basic-regex (без `-E`, иначе `fatal: Unmatched (`). **Bugfix `--semantic-suggest` (2026-06-12, roadmap 260612 pdf-docs P2.B3):** `semantic_fallback_suggest` слал TEI 4096d-вектор в `framework_code_v1` (alias → MRL 1024d) → Qdrant 400 глотался `except` → «(no confident match)» на ЛЮБОЙ файл с момента MRL-миграции; добавлен truncate+renorm до dim коллекции (alias-aware introspection); закреплено live-тестом [`tests/integration/test_pdf_docs_chains_live.py`](tests/integration/test_pdf_docs_chains_live.py). **Skill Router Honest Eval (2026-06-13, roadmap [260613](docs/roadmap/260613_ROADMAP_SKILL_ROUTER_HONEST_EVAL.md)):** [`skill-router.py`](.claude/hooks/skill-router.py) Layer A2 = 1С-сигналы по форме текста (CamelCase-кириллица/`гкс_`/`Документ.`/`Srvr=`) + буквальное имя скилла **только как целое слово** (`\b…\b`, F3 — подстрочный `in` форсил `deployment`/`autoresearch`); [`eval-skill-router.py`](scripts/eval-skill-router.py) считает honest **pooled `action_f1`** (action-only, без silence-padding) + диагностический train/test split, гейт acceptance читает его (НЕ in-sample padded macro-F1); GT [`data/skill-router-ground-truth.jsonl`](data/skill-router-ground-truth.jsonl) под провенанс-контрактом (`source`/`split`, 22 leakage-кандидата `transcript-router` → `quarantine` вне гейта, см. [README](data/skill-router-ground-truth.README.md)) + blocking-lint [`scripts/lint_skill_router_gt.py`](scripts/lint_skill_router_gt.py) (CI-job `skill-router-gt-lint`, инвариант «transcript-router ⇒ quarantine»); [`skill_system_acceptance.py`](scripts/skill_system_acceptance.py) критерий `gate_representative` (quarantined==0) держит вердикт честно-PROVISIONAL пока hard-когорта в карантине. `docs-change-enforcer.py` `SKIP_PATTERNS` дополнен `skill-router-ground-truth` (eval-dataset, не product-code). **Фаза B core (B1/B3/B4):** A2-веса роутера вынесены в config `a2_signals` (tunable без правки кода, behavior-preserving — дефолты == хардкод); eval `--split {train\|test\|all}` + `--cv K` (k-fold action_f1 mean±std). Оптимизатор весов (B5) — следующий срез, осмыслен после A7 (ре-верификация 22 карантинных кейсов → репрезентативный гейт). **Tooling Adoption (260613):** карта внедрения ecosystem-инструментов [260613_ROADMAP_TOOLING_ADOPTION.md](docs/roadmap/260613_ROADMAP_TOOLING_ADOPTION.md) + ADR-012..015 (`architecture-research/adr/`) фиксируют adopt/skip по 4 SDLC-шагам (ADOPT Context7 lazy-load + tdd-guard opt-in; SKIP Spec-Kit/BMAD/Figma/Sentry/plugin-маркетплейс — N6 конфликт с hook-архитектурой); `SKIP_PATTERNS` в docs-change-enforcer дополнен `/adr/` (ADR = самодокументирующиеся decision-артефакты, не product-код). 4-шаговый SDLC (Планирование→Дизайн→Кодирование→Тестирование) + авто-ADR без переспросов закреплён в памяти `feedback-sdd-4step-auto-adr`. **tdd-guard opt-in (260613 Этап 4.2/4.3, [ADR-015](.claude/skills/architecture-research/adr/015-testing-adopt-tddguard-optin-eval-playwright.md)):** [`tdd-guard.py`](.claude/hooks/tdd-guard.py) — PreToolUse:Write\|Edit, **default OFF=no-op**; `TDD_GUARD_ENABLE=1` + правка `src/**.py` с новым def/class без `tests/**/test_<mod>.py` → advisory system_message (`continue:true`, **НИКОГДА не блок**) + кеш события в `.claude/cache/tdd-guard-events.jsonl`. Авто-валидация: [`tdd_guard_validation.py`](scripts/tdd_guard_validation.py) (окно 06-13→06-20 = **7 дней**) считает advisory/follow_rate → авто-решение hard-block (follow_rate≥0.5 + low-noise) vs keep-advisory; SessionStart-баннер [`tdd-guard-validation-on-start.py`](.claude/hooks/tdd-guard-validation-on-start.py) раз/день. Включено **ЛОКАЛЬНО** (`settings.local.json` gitignored) — team `settings.json` НЕ тронут. **Banner-fix:** общий [`shared/acceptance_watch.py`](.claude/hooks/shared/acceptance_watch.py) `acceptance_banner` параметризован `window_days` (default **14** → memory-ai/skill-learning/skill-system без изменений; tdd-guard передаёт **7**) — раньше знаменатель «день N/14» хардкодился и врал для 7-дневного окна. **Generic 4-stage pipeline ([ADR-017](.claude/skills/architecture-research/adr/017-generic-4stage-pipeline-slash-state.md), 2026-06-13):** домен-агностичный пайплайн Планирование→Дизайн→Кодирование→Тестирование (**НЕ путать** с 1С-цепочкой `/analyze-1c-task→…`). Команды [`pl-plan`/`pl-design`/`pl-code`/`pl-test`](.claude/commands/) → **артефакт-на-этап** `pipeline/<task>/0N-*.md` + [`shared/pipeline_state.py`](.claude/hooks/shared/pipeline_state.py) (state `.pipeline-state.json` + указатель `CURRENT`, stdlib CLI `init|done|approve|status|gate`, паттерн из `.run-state.json`). Каждый этап **делегирует существующему скиллу** (architecture-research/implementer/code-verify) — каркас их оркестрирует, не дублирует. Единственный **hard-гейт** — перед Кодированием: дизайн (02) должен быть `approved`; [`pipeline-gate.py`](.claude/hooks/pipeline-gate.py) (UserPromptSubmit) блокирует `/pl-code` до одобрения (аналог `approval-gate.py`), `pl-design`/`pl-test` — advisory. `pipeline/`+`.pipeline-state.json` в docs-enforcer `SKIP_PATTERNS`. Опция позже — `/pl-run-all` (Workflow-скрипт, те же команды/артефакты, автономный прогон). **Обязательная авто-парадигма ([ADR-018](.claude/skills/architecture-research/adr/018-mandatory-auto-pipeline-protocol.md), 2026-06-13):** пайплайн теперь **mandatory** и **self-driven** — Claude сам ведёт этапы (не ждёт `/pl-*`). [`pipeline-protocol.py`](.claude/hooks/pipeline-protocol.py) (UPS) инъектит обязательную инструкцию; [`pipeline-protocol-stop.py`](.claude/hooks/pipeline-protocol-stop.py) (Stop) **hard-блокирует** завершение, если в сессии были Write/Edit (сигнал «была задача» из `data/hook-invocations.jsonl` **ИЛИ** git-fallback `_git_session_edit` — незакоммиченный файл, изменённый за сессию [`git status --ignore-submodules=all` + `st_mtime >= старт сессии`]; закрывает MultiEdit/NotebookEdit без PreToolUse-матчера НЕ трогая settings.json; anti-deadlock — mtime-bound + denylist `docs/wiki/log.md` + fail-safe; регресс `tests/unit/test_pipeline_protocol_git_signal.py`, 2026-06-14) без использования пайплайна (`pipeline/<slug>/.pipeline-state.json` не обновлён за сессию). Охват — **все задачи вкл. trivial** (trivial→компактный `pipeline.md`; medium/complex→`01..04`+approval); жёсткость — **hard сразу** (выбор пользователя, НЕ фазовый rollout). **Carve-out (обязателен):** чистые вопросы без Write — exempt (иначе блок даже ответа/deadlock). Safety: opt-out `PIPELINE_PROTOCOL_DISABLE=1` + graceful degradation + Write-gating (нет ложных блоков на разговоре). Реверс — снять 2 записи из `settings.json`. **Gate-policy слой ([ADR-034](.claude/skills/architecture-research/adr/034-pattern-pipeline-evolution-bestpractices.md) R3, 2026-06-21):** [`shared/gate_policy.py`](.claude/hooks/shared/gate_policy.py) — composable **fail-closed** `evaluate_gates(context, policies)` (любой deny/исключение policy → общий deny) + `log_decision` → `data/gate-decisions.jsonl` (decision-log/аудит, OPA-вдохновлено; путь заякорен на корень проекта — до 2026-07-03 относительный путь мусорил jsonl внутрь сабмодулей конфигураций); единая точка композиции гейтов вместо независимых конкурирующих Stop-хуков (натяжение T3). **3 живых гейта мигрированы** (`pipeline-protocol-stop`/`onec-task-completion-stop`/`pipeline-gate`) — логируют deny/allow-решения через `gate_policy` → единый `data/gate-decisions.jsonl` (defensive import с graceful fallback, behavior-preserving — поведение блокировок не изменено); регресс [`tests/unit/test_gate_policy.py`](tests/unit/test_gate_policy.py) (4 теста) + import-smoke 3 хуков. **Полная композиция (opt-in):** [`shared/gate_policies.py`](.claude/hooks/shared/gate_policies.py) (`build_context` один проход + `pipeline_policy`/`onec_completion_policy`) + оркестратор [`gate-orchestrator-stop.py`](.claude/hooks/gate-orchestrator-stop.py) — при `GATE_ORCHESTRATOR_ENABLE=1` (default OFF) заменяет 2 раздельных Stop-хука одним `evaluate_gates`-проходом → ОДИН блок со всеми ✗ (старые хуки уступают early-return); регресс [`tests/unit/test_gate_policies.py`](tests/unit/test_gate_policies.py) (6 тестов). **Ре-синхронизация single-source (2026-07-03, roadmap [260703](docs/roadmap/260703_ROADMAP_CH43_1C_PIPELINE_AUDIT.md) P0.1, инцидент G-1):** оркестратор фактически **ВКЛЮЧЁН** (`GATE_ORCHESTRATOR_ENABLE=1` в `settings.local.json`), но его политики реплицировали лишь recall/capture/research → Sonar-hard (ADR-037), LOOPS.md и advisory-event ADR-035 при ON молча не исполнялись (окно валидации потеряно). Фикс: решение+side-effects вынесены в единый [`onec-task-completion-stop.evaluate_completion`](.claude/hooks/onec-task-completion-stop.py) — зовут ОБА пути (живой `main()` И `build_context` оркестратора, `apply_side_effects=True`); `onec_completion_policy` читает его `hard_ok`/rich-reason (legacy-fallback для старых тестов); parity-тест [`tests/unit/test_gate_parity.py`](tests/unit/test_gate_parity.py) закрепляет инвариант «вердикт политики == `evaluate_completion.hard_ok`» (матрица recall/capture/research/sonar). Заодно P0.3 `_find_jira` (denylist акронимов UTF/SHA/GPT + allowlist `ONEC_JIRA_PREFIXES`; «UTF-8»→none, generic-JIRA→0.7 veto-able) + P0.4 research засчитывает Bash `ecosystem_scan.py`/`onec_search.py`. **P1 (та же дата, 7 фиксов):** `_owning_1c_slug` — владелец артефакта по глубочайшему state_dir-предку/JIRA пути (не CURRENT → артефакт задачи B не двигает задачу A); age-bound `_incomplete_onec_pipeline` (env `ONEC_INCOMPLETE_MAX_AGE_DAYS`=14, tie-break свежайший); G4 advisory при no-op; SetFit-порог из `meta.json` (`_setfit_threshold_safe`); `run_id` в `BaseHook.run()`-логе; allow-лог + `_log_degraded` (симметрия/fail-open decision-log); runtime-verify V1-V7 9/9. P2: глава 43 (27 файлов) синхронизирована с кодом (см. roadmap §18). **P3 (2026-07-04, [ADR-044](.claude/skills/architecture-research/adr/044-gate-pipeline-robustness-leader-patterns.md), паттерны лидеров):** P3.4 parity-harness [`test_gate_parity_harness.py`](tests/unit/test_gate_parity_harness.py) (256-комбинационная композиция `evaluate_gates == AND живых Stop-хуков`) + CI-шаг «Gate parity (isolated)» → рассинхрон политики с хуком валит CI (повтор G-1 невозможен); P3.5 doc-drift линтер [`scripts/lint_ch43_sync.py`](scripts/lint_ch43_sync.py) (5 инвариантов, факты из кода ast+regex, `--json`/`--strict`) + CI lint advisory-джоб; P3.1 `_fix_recipe` в `onec-task-completion-stop` (block-сообщение = copy-paste команды только для незакрытых ✗-петель, koto {{gate_output}}); P3.2 `pipeline_state.bump_attempt`/`set_needs_human` + CLI (bounded AUTO: env `ONEC_MAX_FIX_ITERATIONS`=4, эскалация needs-human p0/p1/p2); P3.3 критики дизайна перед авто-approve + P3.6 compound-learning — в `run-1c-task` SKILL. Полный роадмап эволюции pattern↔pipeline (R1 child-workflow / R4 SARIF-контракт `sonar_issues_pull.py --format sarif` / R5 `remediation_class` / R6 Clean-as-You-Code `sonar_quality_gate_check.py` opt-in `SONAR_QG_HARD` / R7 evaluator-optimizer / R8 таксономия PATTERN 2.14 «Findings-to-Pipeline») — в ADR-034 + кеш `architecture-research/cache/pattern-pipeline-orchestration-2026.md`; описание слоистости (каталог→пайплайн→оркестратор + резолв T1-T5) — [гл. 43.8](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.4_ПАЙПЛАЙН_1С/43.8_СЛОИСТОСТЬ_ПАТТЕРНЫ_ПАЙПЛАЙН_ОРКЕСТРАТОРЫ.md). **docs-change-tracker `/adr/` exempt (2026-06-22):** [`docs-change-tracker.py`](.claude/hooks/docs-change-tracker.py) (PostToolUse, плодит «обнови доки/скиллы» задачи) теперь зеркалит ADR-исключение энфорсера — `_SKIP_PATTERNS += "architecture-research/adr/" + "/adr/"`; правка ADR-файла больше НЕ матчит общий `.claude/skills/`-prefix (`_CODE_TO_DOCS_SKILLS`) → нет ложной mandatory-задачи (ADR = самодокументирующийся артефакт + реестр `_index.json`). Skip проверяется ПОСЛЕ `_try_complete_tasks` → авто-закрытие задач не затронуто. code-verify PASS; регресс — прямой импорт модуля (ADR skip=True; `src/`/обычный `SKILL.md` skip=False); `/adr/` слеш-ограничен (нет FP на `quadrant`/`adraft`). Пробел был: энфорсер игнорил `/adr/`, трекер — нет (ADR-035 правка плодила ложную задачу). **Enforcement-надёжность (2026-06-30, commit 160950f54, pipeline `fix-enforcement-reliability`):** новый `shared/llm_health.py` `is_provider_down()` (читает `data/llm-rotation-*.jsonl`, окно 30мин, порог ≥2 записи & ≥50% провалов, tz-нормализация naive/aware) → `z-ai-write-guard.py` graceful-пропускает большие code-write при недоступном Z.AI-провайдере (детект через invocation-лог отвергнут — MCP-таймаут логируется `outcome=allow success=True`); `shared/session_state.py` `_save_state` переведён на atomic `tempfile.mkstemp`+`os.replace` (гонка ~19 UPS-хуков повреждала `session-skills.json` строкой пробелов → `get_task_protocol`=idle → `TaskProtocolEnforcer` ложно блокировал легитимную запись). 9 unit (`test_llm_health.py` 6 + `test_session_state_atomic.py` 3) + code-verify PASS (инлайн+subagent); `llm_health.py` = shared-helper (не зарегистрированный хук, как `auto_save_core`), settings.json не тронут **Block без обрыва хода (2026-07-18, мандат пользователя «сессия не должна прерываться»):** `HookOutput.block()` ([`base/protocol.py`](.claude/hooks/base/protocol.py)) ставил `continue: false` — по контракту Claude Code это поле приоритетнее `decision` и останавливает ВЕСЬ ход («PreToolUse hook stopped continuation»): reason уходил пользователю, не модели → каждый SKILL REQUIRED/TASK PROTOCOL-блок замораживал сессию до ручного «продолжай». Фикс: block = только `decision:"block"`+`reason` (вызов отклонён, reason видит МОДЕЛЬ, скилл активируется в том же ходе); для Stop-хуков та же пара = enforcer-«продолжай». Все 10 блокирующих хуков шли через эту единственную точку. 4 unit + live-pipe-пин [`test_hook_block_no_continue_stop.py`](tests/unit/hooks/test_hook_block_no_continue_stop.py) (саботаж: возврат строки → 2 красных); `base/base.py` невиновен (exit-2/stderr-путь). Память [[feedback-hook-block-no-continue-false]]. **Блок гарда ≠ правка кода (2026-07-26, pipeline `fix-blocked-write-not-edit`):** отклонённый `Write` оставляет в `hook-invocations.jsonl` **12 Pre-записей** (по одной на хук цепочки — блокирующий отдаёт `decision:"block"`, при котором цепочка доходит до логгера), и два энфорсера читали их как ФАКТ правки: `pipeline-protocol-stop._session_writes_and_start` (→ ложный `[GATE-ORCHESTRATOR] правки кода без пайплайна` в сессии с чистым git-деревом; тот же сигнал у живого оркестратора через `gate_policies.build_context`, поэтому правка одной функции лечит оба пути) и `code-verify-reminder` (mandatory-задача «проверь изменённый код» на `PreToolUse`, т.е. на намерение). Фикс: (1) предикат блокировки вынесен в single-source `is_guard_block` + публичный `take_block` в [`scripts/tool_effectiveness.py`](scripts/tool_effectiveness.py), `analyze_tool_health.guard_blocks_by_tool` делегирует — report- и enforcement-слой больше не разъезжаются; (2) правка = **canonical** Pre (`category="tool_call"`), не объяснённая block-записью того же тула в той же сессии (±`BLOCK_MATCH_SEC=5с`, расходуемое 1:1), **fail-closed** — нет canonical / битый `ts` / нет хелпера → считаем правкой как раньше (гейт не ослабляем из-за пробела в телеметрии); (3) `code-verify-reminder` создаёт задачу только на `PostToolUse` (Post приходит лишь у исполнившегося вызова; Pre-регистрация была workaround #6305 «Post фаерит 1/день» — замер 2026-07-26 по логу: Post 1460 vs Pre 1540 = **95% доставки**, workaround устарел). Тот же вычет, что `blocked` в tool-health (260718 N-P4.3), но в enforcement-слое. 16 unit ([`test_blocked_write_not_edit.py`](tests/unit/test_blocked_write_not_edit.py)) + 349 gate/pipeline + 180 tool-obs зелёные, саботаж краснит ровно целевые тесты. **Фолбэк протокола на факт из транскрипта (2026-07-26, pipeline `fix-task-protocol-transcript-fallback`):** `task-protocol-enforcer` блокировал Write «Skills not checked» ПОСЛЕ успешной активации — имя скилла легло в `activated_skills`, а мутация `task_protocol` не сохранилась (`phase` остался `classified`, `subtask_count` 0 при 4 фактических `TaskCreate`). ⚠ **атрибуция уточнена по логу:** UPS-цепочка в окне НЕ фаерила (`data/hook-invocations.jsonl`: фаеры 13:16 и 13:47, активация 13:25, блок 13:32) ⇒ это ПОТЕРЯ записи мутации (класс session-state v2.1 ниже), а не законный сброс на новом промпте; первая редакция этой ноты винила UPS на системно-инъектированном ходе — неверно, снято. Фикс: скан транскрипта вынесен в общий [`shared/transcript_skills.py`](.claude/hooks/shared/transcript_skills.py) — `skill_in_transcript` (конкретный скилл, потребитель `code-skill-enforcer`) + `skill_checked_after_last_prompt` (любой скилл текущей задачи, потребитель `task-protocol-enforcer`), единые предикаты «настоящий промпт пользователя» (`type=user` ∧ content-**строка** ∧ не `isMeta` — форма снята с живого транскрипта: результаты инструментов приходят той же ролью списком блоков, инъекции Stop-фидбэка помечены `isMeta`) и «вызов Skill». Энфорсер протокола пропускает правку при фактическом вызове Skill **после последнего промпта** + self-heal state. **Якорь обязателен** (без него скилл прошлой задачи навсегда открывал бы гейт — это обход, не фолбэк) и **fail-closed** (нет якоря / транскрипта / модуля → обычный блок). 13 unit ([`test_task_protocol_transcript_fallback.py`](tests/unit/test_task_protocol_transcript_fallback.py)) + 4 существующих `code-skill-enforcer` зелёные, live-контракт на реальном транскрипте проверен, саботаж якоря краснит 3 целевых теста. **Session-state v2.1 — межпроцессные мутации (2026-07-17, pipeline `fix-session-state-skill-race`):** инцидент «Skill() успешен, а `code-skill-enforcer` продолжает блокировать» — активация терялась: (A) `os.replace` на Windows отдаёт `PermissionError`, пока параллельный hook-процесс держит `session-skills.json` открытым на чтение (на `PreToolUse:Skill` стартуют 4 хука; observer глотал исключение), (B) латентный lost-update — мутаторы делали read-modify-write поверх процессного кэша без межпроцессного лока (atomic-фикс 2026-06-30 закрывал только torn-write). Фикс [`shared/session_state.py`](.claude/hooks/shared/session_state.py): ретрай `os.replace` (6×20мс на PermissionError) + `_mutate()` — единая точка мутаций (атомарный `O_CREAT|O_EXCL` lock-файл `session-skills.json.lock`, wait ≤0.6с fail-open, stale>5с взламывается, свежее чтение с диска мимо кэша); ⚠ `shared/hook_lock.py` для этого НЕ годится — его acquire сам read-check-write (TOCTOU). Defense-in-depth в [`code-skill-enforcer.py`](.claude/hooks/code-skill-enforcer.py): `_skill_activated` = state ИЛИ transcript-fallback (скан хвоста ≤2МБ на фактический `tool_use Skill` с требуемым именем → self-heal записи в state) — класс «ложный SKILL REQUIRED после успешной активации» закрыт даже при будущих сбоях записи. Пост-ревью (code-verify PASS): **read-путь никогда не персистит** — recovery-ветка `_load_state` при ошибке чтения писала пустой state и стирала чужие активации (reader-side erase, тот же класс со стороны читателя); теперь read-retry 3×20мс + деградация только в памяти, файл лечит первая мутация. 11 unit (`test_session_state_race.py` 7 + `test_code_skill_enforcer_fallback.py` 4) + сабботаж-проверки на pre-fix коммите красные. ⚠ Сабботаж через `git stash` тут не работает — auto-save уже коммитит фикс в HEAD, брать версии из pre-fix коммита ([[feedback-sabotage-stash-autosave-trap]]). **Потеря записи session_state стала видимой (2026-07-26, pipeline `fix-state-write-observability`):** `_save_state` честно ре-райзит провал `os.replace`, но ВСЕ вызывающие (наблюдатель, `task-protocol-enforcer`, `code-skill-enforcer`, `skill-router`, `skill-eval-enforcer-shell`) глушат исключение `except Exception: pass` — факт умирал там, класс лечился только фолбэками, а частота была неизвестна. Теперь единая точка `_mutate` перед ре-райзом пишет крошку в `.claude/cache/session-state-failures.jsonl` (`event=mutation_lost` + `op`/`error_type`/`locked`/`session_id`; метка `op` берётся из фрейма вызывающего — 13 внутренних мутаторов не подписываются вручную, и новый не забудет), плюс отдельное `event=lock_fail_open` — **ведущий** индикатор класса lost-update (лок не взят за отведённое ожидание, мутация прошла без взаимного исключения), тогда как потеря записи — запаздывающий. Ротация 256КБ половиной файла (приём `mcp_call_log`), opt-out `SESSION_STATE_FAILLOG_DISABLE=1`, **fail-soft** — провал записи крошки не подменяет исходное исключение (запинено тестом). Читатель [`shared/session_state_failures.py`](.claude/hooks/shared/session_state_failures.py) (`--print`/`--json --since 7d`) + строка `[STATE-WRITE]` в баннере `memory-maintenance-cadence` (инлайн, read-only, паттерн P1.4 `_check_regressions`), чтобы сток не стал write-only как `verdicts.jsonl` до N-P1.2; одинокий `lock_fail_open` в баннер НЕ идёт (характеристика нагрузки, не поломка). Заодно два дефекта того же корня: `task-protocol-observer` разнёс `record_skill_checked` и `add_activated_skill` в РАЗНЫЕ try (падение первой уносило вторую, хотя они питают разные энфорсеры — ровно это и наблюдалось в инциденте), а `_ipc_lock` создаёт каталог состояния перед `O_CREAT` (на несуществующем каталоге ПЕРВАЯ мутация сессии тихо шла в fail-open — поймано новым тестом). 14 unit ([`test_session_state_failure_log.py`](tests/unit/test_session_state_failure_log.py)) + 426 в затронутых наборах; live-контракт в изолированном процессе (успех молчит, сломанный `os.replace` → крошка + исключение + строка баннера); саботаж краснит ровно целевые тесты. **Окно свежести LLM-делегирования (2026-07-18):** `has_llm_delegation` был one-shot флагом без TTL, а state переживает сессии → один вчерашний `llm_complete` навсегда выключал `z-ai-write-guard` (живьём: 141 allow/0 block за день, «оркестратор не делегирует»); фикс — флаг годен только в окне `LLM_DELEGATION_FRESH_MINUTES` (default 30 мин, fail-closed на missing/непарсимой отметке; патч сгенерирован claude-cli-sonnet через `llm_complete` — демонстрация целевого цикла Opus план → sonnet exec → Opus review). Обрывы длинных `llm_complete` (спавн claude-cli 25-150с выглядел «50% error-rate») закрыты `MCP_TOOL_TIMEOUT=240000` в `settings.local.json` + правило `timeout=120` в skill `llm-delegation` (актуализирован: провайдеры sonnet-first, цикл оркестрации, таймауты). 8 unit `test_llm_delegation_freshness.py` + синтетический probe (окно истекло → BLOCK, живое → ALLOW). Память [[feedback-llm-delegation-cycle-freshness]]. **TEI keep-alive / UPS-таймаут (2026-06-30):** UPS-хуки `memory-first-hook` и `shared/prework_dispatcher.py` — таймаут в `settings.json` поднят 5→10 c (корень диагностированного «UserPromptSubmit hook timed out after 5s»: `memory-first-hook` avg ~3.6 c / max ~4.7 c у 5-сек лимита — TEI-эмбед + мультиколлекционный Qdrant RRF + lexical arms). Новый SessionStart-хук [`tei-warmup-on-start.py`](.claude/hooks/tei-warmup-on-start.py) детачем спавнит [`scripts/tei_keepalive.py`](scripts/tei_keepalive.py) — single-instance keep-alive (heartbeat-lock `.claude/cache/tei-keepalive.lock`, bounded 6ч, пинг `POST localhost:8080/embed` каждые 240 c) держит TEI-эмбеддер тёплым (warm ~36 мс vs cold ~385 мс), убирая cold-start джиттер; opt-out `TEI_KEEPALIVE_DISABLE=1`, env `TEI_KEEPALIVE_INTERVAL`/`_DURATION`. Память [[reference-tei-keepalive-ups-timeout]] **Read-only ревью + process-guard (2026-06-30):** субагент-ревьюер `code-verify` ушёл вразнос — вместо статического разбора выполнил `Stop-Process` и убил keep-alive демон. Фикс из 2 слоёв: (1) **read-only контракт ревьюера** в [`code-verify/SKILL.md`](.claude/skills/code-verify/SKILL.md) (Уровень 2 + все 4 шаблона: только Read/Grep/Glob, БЕЗ запуска команд/процессов/`Stop-Process`/Write; рантайм-проверки — за оркестратором, НЕ ревьюером; anti-trap «`.venv` = 2 процесса [шим+реальный] — норма, не дубль»); (2) **caller-agnostic PreToolUse-гейт** [`process-guard.py`](.claude/hooks/process-guard.py) (matcher **`Bash|PowerShell`** — `Stop-Process` = PowerShell-командлет, НЕ покрытый Bash-матчерами; `agent_id` в hook-stdin недоступен → защита не по «субагенту», а по цели) блокирует деструктив (`Stop-Process`/`taskkill`/`kill`/`pkill`/`killall`) против защищённых демонов: pid из lock-файлов (`_PROTECTED_LOCKFILES`, читает `.claude/cache/*.lock`) + name-токен (`_PROTECTED_NAME_TOKENS`) + broad python-kill; opt-out `PROCESS_GUARD_DISABLE=1`; graceful→allow; 8 pipe-тестов PASS. Память [[feedback-subagent-reviewer-readonly]] **Skills-audit tooling (2026-07-06, аудит 260705 гл. 9_НАВЫКИ, roadmap [260705_ROADMAP_SKILLS_DOCS_AUDIT.md](docs/roadmap/260705_ROADMAP_SKILLS_DOCS_AUDIT.md)):** 5 самодостаточных тулов — [`scripts/lint_skills.py`](scripts/lint_skills.py) (официальные бюджеты 2026: BODY500/DESC1024/TRUNC1536/NODESC/NAMEDUP/NAMEDIR/DEADLINK errors + advisory G5 `BADMATURITY`/G6 `OVERCONSTRAINED`; **CI-job `skill-lint` blocking** `--strict` на errors, `ci.yml`; ⚠ резолвит ссылки ТОЛЬКО после `unquote` — `%20`-пути иначе false-positive), [`fix_skill_deadlinks.py`](scripts/fix_skill_deadlinks.py) (авто-ремедиация битых relative-ссылок в скиллах после перенумерации глав — индекс имён .md, dry-run default; закрыл класс [[project-docs-renumbering-broke-enforcement]]), [`gen_hooks_catalog.py`](scripts/gen_hooks_catalog.py) (каталог хуков из `settings.json` single-source — 106 регистраций на 2026-07-25, `--check UPS=19,...` для CI; убил 4×-дрейф 13.2; экранирует `|` в matcher-ячейках — иначе multi-matcher строки рвут markdown-таблицу), [`eval_skills.py`](scripts/eval_skills.py) (per-skill baseline-delta через `.claude/skills/<n>/evals.yaml`; 🟡 live-baseline требует project-unaware провайдера — llm-rotation fallback'ит на claude-cli с авто-CLAUDE.md → delta=0), [`tune_skill_descriptions.py`](scripts/tune_skill_descriptions.py) (G3 self_recall собственных триггеров через роутер → слабые описания). 29 unit. Конвенции авторства (frontmatter-2026/maturity/plateau) — в skill `doc-to-skill`. **Bugfix `docs-change-enforcer.py:111`:** `src/memory/` целил `0_ВВЕДЕНИЕ/0.1_ОБЗОР` вместо `5_ПАМЯТЬ/5.1_UNIFIED_MEMORY` (корень рекуррентного docs-enforcer-шума). Прогрессивное раскрытие: va-bdd-testing 1457→495 + implement-1c-task 1048→491 (детали в `references/`, нулевое удаление). **Audit-precision + Diátaxis-скоуп (2026-07-19, [ADR-054](.claude/skills/architecture-research/adr/054-doc-skill-coverage-diataxis-scoping.md)):** баннер `[AUDIT-COVERAGE]` был ~98% шумом (89 doc + 52 skill) — 4 бага экстрактора [`audit_docs_skills.py`](scripts/audit_docs_skills.py): (1) `_extract_router_prefix` не читал `APIRouter(prefix=)` из модуля (/openai_compat→/v1); (2) мангл акронимов GraphRAGAuto→graph_r_a_g_auto; (3) узкий allowlist 2-4 файла → whole-tree fallback `_all_docs_text`/`_all_skills_text`; (4) класс-как-фича → фильтр по `__all__`. + скоуп skill-gap по **Diátaxis** (скиллы=how-to, exhaustive-справочники=доки): `SKILL_COVERAGE_CATEGORIES={agent,strategy,mcp_tool,cli_command,wiki_component}`, справочные (config_var/endpoint/hook/memory/bsl) → `coverage_skills=None`, трек только по докам — чинит корень `--update`-pollution. Баннер 89+52 → **22+0**; 9 протёкших `--update`-таблиц в скиллах вычищены; 22 unit + code-verify PASS ([[project-audit-docs-banner-precision-fix]], [[reference-diataxis-reference-vs-howto]]). **Roadmap-placement helper** [`scripts/roadmap_place.py`](scripts/roadmap_place.py) — воспроизводит «куда привязать отложенную задачу» (греп docs/roadmap → attach/create по СУБЪЕКТУ, не механизму; 10 unit; [[reference-roadmap-place-helper]]). known-issues слой tool-health (ADR-055) — см. bullet Tool observability выше.
- **PR-automation (`post-task-push-pr.py` P0-P3 batch, landed via roadmap 260522)**: PostToolUse:TaskUpdate хук — на `TaskUpdate(status=completed)` собирает per-task ветку, push на origin, открывает / переиспользует PR, опционально ждёт checks и мержит. 18 items из [40.4 Дорожная карта](docs/framework%20documentation/7_ПРОВЕРКА/7.8_PR_AUTOMATION/40.4_Дорожная_карта.md): label-driven activation, conflict-aware push, existing-PR reuse, pre-commit pre-push gate, SMTP уведомления, merge wait-for-checks, auto-rebase, dry-run, per-task SHA tracking, CODEOWNERS reviewer assignment, cross-PR blocked-by labels, cherry-pick branch model через git worktree (P3.2), post-merge auto-revert script ([`scripts/pr_check_post_merge.py`](scripts/pr_check_post_merge.py) P3.4), GitHub merge queue alternative `AUTO_PR_MERGE_QUEUE=1` (P3.3). Companion helpers: [`shared/pr_helpers.py`](.claude/hooks/shared/pr_helpers.py) (git/gh primitives + `cherry_pick_range_to_branch`), [`shared/pr_notifier.py`](.claude/hooks/shared/pr_notifier.py) (SMTP), dashboard [`scripts/pr_automation_dashboard.py`](scripts/pr_automation_dashboard.py) (P1.5), orphan cleanup [`scripts/cleanup_orphan_branches.py`](scripts/cleanup_orphan_branches.py) (P2.4), GitHub webhook receiver [`src/api/routes/github_webhooks.py`](src/api/routes/github_webhooks.py) (P3.1), Mergify template [`.mergify.yml`](.mergify.yml) (P3.3). **Master switch:** `AUTO_PR_ENABLED=1` (default `0` после миграции). Base ветка: `AUTO_PR_BASE=master` (после reconciliation 2026-05-23, roadmap 260523 §11; dev-master deleted). Полный env reference + smoke checks + operator wiring guide — [40.4 §«Реализация 2026-05-22 — P3»](docs/framework%20documentation/7_ПРОВЕРКА/7.8_PR_AUTOMATION/40.4_Дорожная_карта.md) и [40.5 Pipeline Workflow](docs/framework%20documentation/7_ПРОВЕРКА/7.8_PR_AUTOMATION/40.5_Pipeline_Workflow.md). **Зависимость:** хук требует modernized `base/protocol.py` (`hook_event_name` приоритет, `transcript_path` fallback) — портирован в этой же миграции
- **`base/protocol.py` modernization (landed 2026-05-22 в составе PR-automation migration)**: `HookInput.detected_event` теперь читает `hook_event_name` payload-поле как авторитативный источник (Claude Code 2.x), `transcript` падает на `transcript_path` (snake_case современный) с легаси-фоллбэком на `transcript`. Без этой модернизации все PostToolUse-хуки misclassify-ят modern Claude Code события как "Unknown" и не срабатывают. Обратная совместимость со старым payload-форматом сохранена
- **F821 hotfix stubs (2026-05-24, hotfix/ci-green-master / PR #10)**: pre-existing F821 errors в `.claude/hooks/` после PR #2 merge — helper functions удалены, calls остались. На `session-memory-save.py` восстановлены 3 stub: `save_to_wiki_log`, `try_promote_patterns`, `_emit_langfuse_span` (все return None/False, no-op). На `memory-first-hook.py` добавлен `HOOK_NAME` constant (`"memory-first-hook"`). Полная реализация (Langfuse spans, L5 wiki promote) deferred до master CI stabilization. Stubs предотвращают `NameError` при runtime, ratchet down ruff F821 errors в `.claude/hooks/` с 16 до 9. См. PR #10 commits `a54b3ebe3`, `ab44af483`
- **Тот же класс, найден 2026-07-26 (`z-ai-delegation-enforcer`)**: ruff F821 ловит удалённое ИМЯ, но не удалённый АТРИБУТ — `execute()` звал `self._delegation_level(...)`, а мерж около 2026-05-23 удалил весь helper-блок из четырёх методов (`_bandit_level`/`_router_level`/`_should_canary`/`_delegation_level`, добавлены в `ca61a74b0` §4.5.3 A/B canary). `BaseHook.run()` гасил `AttributeError` → отказ бесшумный: **2607 записей** в `.claude/hooks/cache/hook-errors.log` за два месяца при `outcome=error` в `hook-invocations.jsonl`, хук числился рабочим и просто не советовал делегирование (ветка бандита мертва, `mode='AUTONOMOUS'`). Восстановлено из `ca61a74b0` с двумя правками: (1) `sys.path.insert(0, repo_root)`→`append` ([[feedback-hook-src-shared-collision]]); (2) **langfuse-span убран под `DELEGATION_ROUTING_SPAN=1`, по умолчанию OFF** — замер на здоровом Langfuse: `_get_langfuse_client()` 740мс + `client.flush()` 1132мс ⇒ хук занимал **2.2с при таймауте 3с** (`flush=False` не помогает — SDK выгружает буфер на atexit; амортизировать нечем, на каждый промпт стартует новый процесс), после гейта **187-226мс**; канареечный A/B (`_ROUTER_CANARY_PCT`) и так OFF, так что при выключенном span записывать нечего кроме `method="linucb"`. Регресс [`test_delegation_enforcer_helpers.py`](tests/unit/hooks/test_delegation_enforcer_helpers.py) (8 тестов) пинит обе стороны; саботаж: удаление метода краснит 5, снятие гейта — ровно 1. ⚠ **Урок наблюдаемости**: `outcome=error` копился в логе два месяца и никем не читался — сток есть, читателя не было (тот же класс, что write-only `verdicts.jsonl` до N-P1.2)
- **§14 Pre-Work dispatcher (PR #9, 2026-05-23)**: новый UPS hook [`shared/prework_dispatcher.py`](.claude/hooks/shared/prework_dispatcher.py) реализует ADR-D1 (asyncio.gather parallel orchestrator из [roadmap 260523 §17.1](docs/roadmap/260523_ROADMAP_FULL_DEV_LIFECYCLE_ANALYSIS.md)) + aggregator [`shared/prework_aggregator.py`](.claude/hooks/shared/prework_aggregator.py) (top-K + ~500 tokens cap, ADR-D2 MVP) + first worker [`prework-architecture.py`](.claude/hooks/prework-architecture.py) (rapidfuzz match на `architecture-research/cache/_index.json`). Worker contract: stdin `{prompt}` → stdout `{items: [{title, body, score}]}`. Регистрация в `settings.json` UPS chain после `memory-first-hook` (timeout 5s). Smoke: real prompt → top-3 cached topics с score 1.0/1.0/0.889. Per-worker timeout via `asyncio.wait_for`, orphan subprocess killed на `TimeoutError`. Reversibility: single rollback = remove dispatcher entry. Future workers (P1, separate PRs): prework-similar-code (Qdrant `framework_code_v1`), prework-github-bp (cache-first WebSearch), prework-stackoverflow (UPS + PostToolUse:Bash reactive). Code-verify PASS via subagent aa124b154f2739a13. Memory entry `feedback_prework_dispatcher_works` будет saved после prod observation 24h
- **§14 P1 prework-similar-code worker (PR #12, 2026-05-24)**: второй worker [`prework-similar-code.py`](.claude/hooks/prework-similar-code.py) — TEI Qwen3 embed `Instruct:/Query:` prefix → MRL truncate 4096→1024d → Qdrant search в `framework_code_v1` collection (31k chunks) → top-5 hits с `score >= 0.35`. Smoke "refactor the search manager to use httpx" → 5 hits (top score 0.697 для search_tool.py). Registered в `WORKERS` registry [`shared/prework_dispatcher.py`](.claude/hooks/shared/prework_dispatcher.py) с timeout 2s (TEI cold-start padding). Graceful degradation: TEI/Qdrant down → empty items, dispatcher продолжает.
- **§14 P1 workers 2/3 + 3/3 (PR #46, 2026-05-25)**: финальные prework workers — [`prework-github-bp.py`](.claude/hooks/prework-github-bp.py) (cache-first GitHub best-practices, §14.4 Option B; фильтр `architecture-research/cache/` по `github_repos_count > 0` + freshness ≤365d + rapidfuzz score ≥60, timeout 4s) + [`prework-stackoverflow.py`](.claude/hooks/prework-stackoverflow.py) (cache-first SO/error-context, §14.5 Option C UPS half; мерж `tech-research` dict-schema + `architecture-research` list-schema caches, SO_SIGNALS keyword filter + freshness ≤540d, timeout 1s). Оба зарегистрированы в `WORKERS` registry. End-to-end smoke: 4 секции (ARCH + CODE + GH + SO) в unified `systemMessage`, truncated по `MAX_TOTAL_CHARS=2000` cap per ADR-D2. PostToolUse:Bash reactive SO worker (Option C complete) — отдельная итерация
- **Maximum CI tier landed (PR #19, 2026-05-24/25)**: `.github/workflows/codeql.yml` (Python+JS Security analysis, weekly schedule + PR triggers) + `.github/workflows/claude.yml` (Anthropic bot auto-review на pull_request:opened, skip bot authors per `user.type != 'Bot'`) + [`post-merge-revert-stop.py`](.claude/hooks/post-merge-revert-stop.py) Stop-hook (detached `pr_check_post_merge.py --apply`, 10min cooldown, gated by `AUTO_PR_ENABLED`) + Stop chain extended в `settings.json` + `dependabot.yml` `directories:` explicit list (excluded private submodules). Companion docs chapters 40.6-40.8 + 42 Monitor CI + roadmap 260524 1C CI. Code-verify PASS via subagent abb4be7715b25be63. **Post-merge fixes:** claude.yml bot filter (`66e71fdf1`) для skip Dependabot auto-review failures, retry.py `raise None → RuntimeError` (`037fe6228`) для CodeQL py/illegal-raise alert #1349 — verify PASS via subagent ac929a17a7847c182
- **CI Autopilot Phase 2 — Notifications + Digest (2026-05-26)**: [`scripts/ci_digest.py`](scripts/ci_digest.py) — markdown summary за окно `--since 24h/7d/...` (failures top-10 по нормализованным error_hash + open PRs + merged + auto-issues + GitHub notifications via `gh api notifications`). [`.claude/hooks/gh-notif-intake-on-start.py`](.claude/hooks/gh-notif-intake-on-start.py) — SessionStart hook auto-summarize unread GH notifications в systemMessage `[GH-NOTIF] N unread (ci-fail=X, ...)`. [`shared/gh_notif_classifier.py`](.claude/hooks/shared/gh_notif_classifier.py) — subject+reason → category+priority (P0 security / P1 ci-fail+pr-review / P2 dependabot / P3 merged). Code-verify subagent `aac7e35e5ca3f47e2` → PARTIAL → 4 fixes applied (timezone-naive crash → coerce UTC, `claude` skip_re tightened к `Claude Code Review\|claude\[bot\]`, hook timeout 5→8s, Dependabot `^bump .. from .. to ..` regex). Phase 2 item #2 (`workflow_run` server-side webhook) — deferred, см. [260525_ROADMAP_CI_AUTOPILOT_PHASE2.md §1](docs/roadmap/260525_ROADMAP_CI_AUTOPILOT_PHASE2.md). Doc: [42.5 GH Notifications Intake](docs/framework%20documentation/7_ПРОВЕРКА/7.10_MONITOR_CI/42.5_GH_Notifications_Intake.md), [42.6 Daily Digest](docs/framework%20documentation/7_ПРОВЕРКА/7.10_MONITOR_CI/42.6_Daily_Digest.md)
- **CI monitoring auto-catchup (PR #54, 2026-05-25 evening)**: closes gap где `.claude/cache/ci-failures.jsonl` stale между Claude сессиями (Monitor `bkozphbx9` session-bound — между сессиями нет auto-trigger). Два корневых fix: (1) [`scripts/ci_failure_cache.py:analyze_failure`](scripts/ci_failure_cache.py) — bug где `refs/pull/{pr}/head` использовался как branch name (gh не принимает) → заменено на `_resolve_pr_head()` (gh pr view → headRefName + headRefOid) + предпочтение `--commit <sha>` над `--branch <name>`. (2) Новый mode `--catchup [LIMIT=20]` — `gh run list --status failure --limit N` → filter run_ids уже в jsonl → process each (idempotent). (3) Новый SessionStart hook [`ci-catchup-on-start.py`](.claude/hooks/ci-catchup-on-start.py) — detached subprocess `Popen` (CREATE_NEW_PROCESS_GROUP на Windows) спавнит `ci_failure_cache.py --catchup 20` при каждом session start. Opt-out `CI_CATCHUP_DISABLE=1`, override `CI_CATCHUP_LIMIT=N`. Backfill результат: cache `1 → 48 entries, 31 unique hashes` (несколько count=2 близко к auto-issue threshold=3). Code-verify subagent `a152803afc173a0bb` → PASS с 2 minor recs applied (open→all PR lookup ordering + stderr error logging). Doc: [42.4 §SessionStart auto-catchup](docs/framework%20documentation/7_ПРОВЕРКА/7.10_MONITOR_CI/42.4_Failure_Caching.md). True 24/7 (GitHub webhook trigger) — deferred
- **§14.5 reactive SO + §20 P2 + §15 P0 foundation (PR #47, 2026-05-25 late PM)**: [`posttooluse-stackoverflow-on-error.py`](.claude/hooks/posttooluse-stackoverflow-on-error.py) — PostToolUse:Bash hook, on non-zero exit + Traceback/pip ERROR/npm ERR!/fatal: signature → emit `[SO-ON-ERROR]` advisory с WebSearch suggestion (handles dict-shape `tool_response` через `_normalize_response` extract stdout/stderr keys; explicit exit_code authoritative). Pairs с UPS-time [`prework-stackoverflow.py`](.claude/hooks/prework-stackoverflow.py) — §14 Pre-Work pipeline теперь 100% complete. Bonus hotfix: [`posttooluse-bash-errors.py:37`](.claude/hooks/posttooluse-bash-errors.py) pre-existing 4-tuple→3-tuple unpack bug на `Found N errors` pattern. §20 P2: [`scripts/triage_codeql_alerts.py`](scripts/triage_codeql_alerts.py) semi-automated triage (vendored/tests/intentional/review), **applied 174/261 dismissed** (vendored 43 + tests 6 + intentional 125, 87 review manual). §15 P0 (3/4 subtasks): [`shared/invocation_logger.py`](.claude/hooks/shared/invocation_logger.py) — CloudEvents v1.0 envelope (`specversion/id/source/type/time/datacontenttype/correlationid/causationid`) + W3C traceparent (`00-{32hex}-{16hex}-01`, deterministic trace_id из run_id для cross-event correlation) + new `causation_id` arg на `log_invocation()`; backward-compat preserved (all flat fields). [`scripts/audit_query.py`](scripts/audit_query.py) — DuckDB SQL layer над `hook-invocations.jsonl` (5 views: recent/latency-p95/error-rate/top-tools/hooks-per-session) + `--view causation-chain --correlation-id` DAG traversal + raw `--sql`; `union_by_name=true, ignore_errors=true` handles schema drift + legacy malformed lines. **Deferred (subtask 4):** crypto-shredding per-session-key — отдельный PR per §15.6 P1. Code-verify FAIL → Ralph Wiggum iter 1 → 7/7 smoke PASS via subagent `a4396858021c471fc`

## Research

При вопросах про 1С: документация 8.3.27 — первоисточник. Внешние источники (its.1c.ru, infostart.ru) — только дополнение. Каждый факт с атрибуцией. Протокол: skill `1c-doc-research`.

**Архитектура 1С-задачи** (Шаг 2 Дизайн): skill `1c-solution-architecture` (bundle `1c-arch`) + [гл.45](docs/framework%20documentation/4_МЫШЛЕНИЕ/4.5_АРХИТЕКТУРА_1С/45.1_Архитектура_1С_решений.md) — decision-tree расширение↔изменение типовой, матрица выбора регистра, БП-моделирование, чек-лист стандартов. **Свежий/доверенный 1С-поиск** (Infostart+web, relevance×trust(домен)×recency): [`scripts/onec_search.py`](scripts/onec_search.py) (ADR-040, [гл.44.6](docs/framework%20documentation/2_КОНТЕКСТ/2.10_ECOSYSTEM_SCAN/44.6_1C_RU_Search_onec_search.md); инфра `docker/docker-compose.search.yml` SearXNG+TEI-reranker).

**GitHub-поиск (ОБЯЗАТЕЛЬНО)**: репо/библиотеки/best-practices — через `python scripts/ecosystem_scan.py "<query>" --top 8` (ADR-039, [гл.44](docs/framework%20documentation/2_КОНТЕКСТ/2.10_ECOSYSTEM_SCAN/44.1_Обзор.md), engagement×relevance), НЕ голый WebSearch. Хард-энфорсер [`github-search-via-ecosystem-scan.py`](.claude/hooks/github-search-via-ecosystem-scan.py) (PreToolUse:WebSearch блокирует GitHub-намеренный поиск; opt-out `GITHUB_ECOSYSTEM_SCAN_DISABLE=1`). 1С/RU-веб → `onec_search` (гл.44.6).

**ИТС deep-fetch** (дословный контент its.1c.ru под подпиской): skill `its-research` + [`scripts/its_fetch.py`](scripts/its_fetch.py) — Playwright storageState SSO + trafilatura→markdown; креды в gitignored `.env.its`; [гл.44.7](docs/framework%20documentation/2_КОНТЕКСТ/2.10_ECOSYSTEM_SCAN/44.7_ITS_Deep_Fetch.md). **Поиск по докам Claude Code**: `/cc-docs` + skill `cc-docs` + [`scripts/cc_docs_search.py`](scripts/cc_docs_search.py) — sitemap→trafilatura→TEI→Qdrant `cc_docs` + freshness (`check`/`index --incremental`); [гл.44.8](docs/framework%20documentation/2_КОНТЕКСТ/2.10_ECOSYSTEM_SCAN/44.8_CC_Docs_Search.md).

При вопросах про RAG/ML/Python: протокол в skill `tech-research`.
