# CLAUDE.md — PDF Vector & Graph Framework

> AI agent instructions are in [AGENTS.md](AGENTS.md). This file is project context for humans and AI.

## Project Overview

Framework for PDF document processing: semantic search (RAG), knowledge graphs, hybrid retrieval. Production-ready with REST API, CLI, MCP server, Streamlit UI.

## Tech Stack

- **Python 3.11+**, async-first
- **LangChain / LangGraph** — agents, tools, chains, orchestration
- **Qdrant / ChromaDB / FAISS** — vector stores
- **Neo4j / NetworkX** — graph stores
- **FastAPI** — REST API
- **MCP** — Model Context Protocol server
- **Typer** — CLI

## Project Structure

```
src/
  pdf_framework/        # Core library
    loaders/            # PDF loading (pymupdf, pdfplumber)
    processing/         # Splitting, cleaning, metadata
    embeddings/         # Embedding providers
    vector_store/       # Vector DB (base + providers)
    graph_store/        # Graph DB (base + providers)
    search/             # Hybrid search, strategies, reranking
    agents/             # LangGraph agents (RAG, analytical, research)
    indexing/           # Indexing pipelines, hybrid loader
    tools/              # LangChain tools
    chains/             # LangChain chains
    schemas/            # Pydantic data models
    config/             # Settings modules
    evaluation/         # RAGAS, benchmarks
    guardrails/         # Input/output validation
    observability/      # Logging, tracing
    analytics/          # Cost tracking, audits
    feedback/           # User feedback
    multitenancy/       # Multi-tenant support
    optimization/       # Performance tuning
    knowledge_base/     # Knowledge base management
    callbacks/          # Event callbacks
    utils/              # Utilities
  api/                  # REST API (FastAPI)
  cli/                  # CLI (Typer)
  mcp_server/           # MCP server for Claude Code
  ui/                   # Streamlit web UI
  workers/              # Background task workers
```

## Development Rules

- Store implementations extend abstract base classes in `*/base.py`
- Pydantic models from `schemas/` as data contracts
- Async-first: all I/O operations are `async`
- Configuration via `pydantic-settings` (`.env` file)
- Provider pattern: swap Vector/Graph/Embedding providers without changing logic

## Output Rules

- File references in responses must be clickable markdown: `[file.py](.claude/hooks/file.py)` or `[file.py:42](.claude/hooks/file.py#L42)`

## BSL Development (1C Enterprise)

- **BSL код**: `src/bsl/` (semantic_search, mcp_server, mcp_integration, sonar, finetuning)
- **Инструменты**: `tools/auto-documenter/` (Node.js), `tools/bsl-debugger/` (Node.js), `tools/bsl-debug-server/` (Python — live RDBG wrapper, **HMR-enabled через `mcp_hmr_proc.py`**), `tools/ast-grep-mcp/`, `tools/bsl-semantic-diff/`
- **MCP серверы**: bsl-semantic-search, bsl-platform-context, auto-documenter, bsl-debugger, **`1c-debug` + `1c-debug-hmr`** (live BSL отладка через RDBG, HMR-вариант reload'ит при edit без потери session — `.active.json` persistence + unified `ping()` dispatch, добавлен 2026-05-10; см. [`docs/framework documentation/36_AUTONOMOUS_DEBUG_CONTROL/36.7_HMR_Subprocess_Wrapper.md`](docs/framework%20documentation/36_AUTONOMOUS_DEBUG_CONTROL/36.7_HMR_Subprocess_Wrapper.md) + skill [`1c-debug-hmr`](.claude/skills/1c-debug-hmr/SKILL.md)), ast-grep-mcp, bsl-semantic-diff
- **Memory**: `src/memory/` (ai_memory, vector_memory, skill_learning, librarian, orchestrator, infrastructure). P0-P4 DONE, P5 in progress. Ключевые хуки: `session-memory-save.py` (Stop → SQLite + L5 drafts promote), `pattern-reinforce-stop.py` (Stop → §22 reinforce surfaced-паттернов; выделен 2026-06-09 из session-memory-save, где за early-return'ами дедупа не срабатывал НИКОГДА — roadmap 260609 P0.1; первый production-reinforce applied=10 2026-06-09; **P1 2026-06-10** — write-contract `content_hash`+dedup+`record_ingest` во всех прямых писателях [общий `content_hash.point_id`], честный `route_and_save` [`failed_targets`], surfacing cache-key top-K salient + TTL 900/empty-180, см. roadmap 260609 §18), `memory-first-hook.py` (UPS semantic surfacing + confidence-gating + RRF k=60 + execution-cache [top-K salient key] + per-stage trace-log), `patterns-harvester.py`/`skills-harvester.py` (Stop ingestion в `learned_patterns`/`skill_library` + epoch.bump P1.5). Skill: `memory-unified`. **Полная история §22–§27** (confidence lifecycle = decayed Beta(7,3) posterior `vector_memory/confidence.py`+`reinforce.py`; §23 auto-cache pipeline; §24 surfacing hybrid arms → RRF + confidence-gating `memory-first-hook`; §25 self-tuning golden-set `scripts/tune_memory_surfacing.py`; §26 ingestion/sync + Q1 ADR (experience/conversation collections dropped); §27 full observability — 8 sinks, `infrastructure/trace_log.py`, `scripts/memory_observability_{query,report}.py`) **вынесена в** [27.13 Memory Subsystem Changelog](docs/framework%20documentation/27_UNIFIED_MEMORY/27.13_Memory_Subsystem_Changelog.md) + главы [27.9–27.12](docs/framework%20documentation/27_UNIFIED_MEMORY/) + roadmaps 260523/260601/260602/260603/260605. **Honest-failure контракт (260611, governance-проводка)**: handler/adapter-фейлы НЕ глотаются — `propagate_update` отдаёт `failed_entities{entity→reason}` + named breakers `propagation:<source>` в `memory_circuit_status` (OPEN → `failed:circuit_open` fail-fast); `unified_search` кладёт упавшее vector-плечо в `sources_failed[]`; `memory_ttl_cleanup` исполняет до store'ов (vector archive / memory-ai delete + `store_actions`/`failed`); versioning wire-minimal (ADR-V: orchestrator-mediated мутации снапшотятся, rollback пишет обратно в store); promote идемпотентен (`promoted_to` + log-dedup); reinforce_miss → `missing`, не `errors` — [260611](docs/roadmap/260611_ROADMAP_MEMORY_GOVERNANCE_WIRING.md). **Skill-learning revival (260612)**: pending/saved/rejected = живой карантинный конвейер — atomic `_write_jsonl`, rejected-dedup (`dup_rejected`), stats derive-on-read, `route_and_save`→pending (`auto_confirm` opt-out); вход: протокольный `capture_pattern` после verify PASS (task-protocol шаг 6 / code-verify) + авто-карантин low-confidence lessons (`quarantine_items`, порог 0.8); выход: SessionStart-баннер `skill-learning-pending-on-start.py` + плечо `skill_learning` в memory-first-hook (pending ×0.7 + метка) + RU-стемминг адаптера; модерация: `review_pending` TTL-30d auto-reject в maintenance-каденсе + confirm→детач-harvest (идемпотентный point_id + epoch.bump) — [260611 revival](docs/roadmap/260611_ROADMAP_SKILL_LEARNING_REVIVAL.md). **⚠ Операционка:** правки кода MCP-серверов (`src/memory/vector_memory/*`) требуют `/mcp reconnect` ([[feedback-mcp-stale-code-reconnect]]); хуки пишут stdout UTF-8-байтами, не `print()` ([[feedback-windows-hook-stdout-cp1251]]); в хуках с импортом `shared.*`(hooks-local)+`memory.*`(src) добавлять `src` через `append`, НЕ `insert(0)` ([[feedback-hook-src-shared-collision]]). **Курируемая память (ADR-011, 2026-06-12):** синхронизация с store'ами = read-time merge + manual promotion (репликация — non-goal); новые `memory/*.md` записи несут **citations** (`file:line`/commit) — verify-at-read дешевле (паттерн Copilot agentic memory); входящий конвейер store→канон — SessionStart-баннер `memory-curation-candidates-on-start.py` (eff_conf≥0.85 & apps≥5, не покрытые каноном; cooldown 20h, opt-out `MEMORY_CURATION_BANNER_DISABLE=1`).
- **LLM Rotation**: `src/shared/llm_rotation/` (5 провайдеров, fallback)
- **Субагенты**: `.claude/agents/` (learning-loop — 5-phase self-learning pipeline, monitor). **Живой прогон A3 (2026-06-13, roadmap 260612):** learning-loop SEARCH→FETCH(3 источника)→EXECUTE→VERIFY(adversarial reviewer-субагент, PASS)→CREATE произвёл skill `duckdb-analytics` (DuckDB-аналитика над JSONL/Parquet логами; артефакт EXECUTE — [`scripts/skill_ingest_trend.py`](scripts/skill_ingest_trend.py)) + bundle в `skill-router-config.json` (45 bundles); verify-гейт обязателен перед регистрацией в каталог
- **Паттерны**: `docs/architecture/PATTERNS.md` (15 arch + 13 automation), skill `framework-patterns`
- **Профили**: `.mcp/pdf.json`, `.mcp/bsl.json`, `.mcp/full.json`, `.mcp/lazy-mcp.json`
- **Lazy MCP**: `infra/lazy-mcp/` (proxy, 11 категорий, 27 on-demand серверов)
- **Qdrant коллекции (после Phase 8 + 9.1, 2026-04-30; config defaults aligned 2026-05-01 §2.1; experience/conversation dropped 2026-06-03)**: 8 коллекций × **Qwen3-Embedding-8B 4096d** через TEI Docker (`pdf-rag-tei`). Production retrieval: `bsl_code_v4_late` (alias → `bsl_code_v4_late_hybrid` 37 668 pts, named `{dense:4096d, bm25:sparse IDF}` since 2026-05-22; prev physical `_v2` retained as backup; default query mode = **hybrid RRF** (2026-06-20 ре-верифицировано — прежний «pure BM25 / dense 18%» оказался артефактом устаревшего eval; dense здоров [L 0.72/S 0.42], hybrid > BM25-first на L +10pp / S +18pp recall@10; правка `_call_qdrant_search`, [[feedback-bsl-sparse-bm25-dominance]] СУПЕРСЕДЕД; query-adaptive fusion — next) — см. [chapter 31.7](docs/framework%20documentation/31_QWEN3_RETRIEVAL_PRODUCTION/31.7_BSL_Hybrid_Sparse_BM25.md) + memory `feedback_bsl_sparse_bm25_dominance`), `bsl_code_v4` (24 455 std), `framework_code_v1` (21 277+, auto-reindex on commit), `graph_embeddings` (6 694), `wiki_pages_v1` (3 073), `pdf_documents` (830), `skill_library` (80, Phase 9.1), `learned_patterns` (44). **`experience_embeddings`/`conversation_memory` dropped 2026-06-03** (§26 Q1 ADR — 0-writer, роль покрыта episodic `memory_ai.db` + `learned_patterns`; surfacing-arms убраны, см. [260603_ADR_Q1](docs/roadmap/260603_ADR_Q1_EXPERIENCE_CONVERSATION_COLLECTIONS.md)). Code-side defaults: `EmbeddingSettings.provider="tei"`, `model="Qwen/Qwen3-Embedding-8B"`, `dimensions=4096`; `VectorStoreSettings.dimensions=4096`; regression: `tests/unit/test_config.py::test_phase8_invariants`. Подробности: [chapter 31 Qwen3 Retrieval Production](docs/framework%20documentation/31_QWEN3_RETRIEVAL_PRODUCTION/31.1_Обзор.md). Legacy `bsl_code_v2` (768d nomic), `_e5_legacy`, `bsl_code_v3` (1024d E5), `visual_grounding` (768d nomic) — **dropped 2026-04-30**, см. roadmap §27/§32.5
- **BSL hook**: `bsl-tool-router.py` — routes BSL/1C queries to bsl-development skill
- **Auto-reports (2026-05-22, chapter [`28_1_AUTO_REPORTS`](docs/framework%20documentation/28_1_AUTO_REPORTS/))**: после каждого `category=run_end` в [`data/indexing-progress.jsonl`](data/indexing-progress.jsonl) Stop-хук [`post-indexing-analyzer.py`](.claude/hooks/post-indexing-analyzer.py) детачит [`scripts/analyze_run.py`](scripts/analyze_run.py) — пишет deep отчёт в `data/reports/{indexing,graph}/` (Markdown + JSON sidecar + `_latest_<subject>.md`). Indexing: alias-resolved Qdrant introspection (`framework_code_v1` → `*_mrl_1024`), quantization detect, L2 norm + self-recall@1 probes, diff vs prev. Graph (3 sub-источника): `sqlite` (`cache/bsl_call_graph.db` — 33k symbols/80k calls/2k modules, dangling-call detector), `neo4j` (default `NEO4J_URI` / `NEO4J_USER` / `NEO4J_PASSWORD` env vars), `qdrant_graph` (collection `graph_embeddings`, entity-vs-relation split). Cold-start: на первом запуске seed'ит existing run_ids без анализа (защита от dump'а истории). FIFO dedup state в `.claude/cache/post-indexing-analyzer-state.json` (cap 500), atomic write через `os.replace`. Cap 5 spawn'ов/Stop. Skill: [`post-indexing-analysis`](.claude/skills/post-indexing-analysis/SKILL.md). Smoke: [`tests/integration/test_analyze_run.py`](tests/integration/test_analyze_run.py) (3 теста, Qdrant-independent). Mapping в [`docs-change-enforcer.py`](.claude/hooks/docs-change-enforcer.py) CODE_TO_DOMAIN: `scripts/analyzers/`, `scripts/analyze_run.py`, `.claude/hooks/post-indexing-analyzer.py` → `28_1_AUTO_REPORTS` / `post-indexing-analysis`. Ручной запуск: `python scripts/analyze_run.py --mode {indexing|graph} --run-id <id>` или для графа `--source {sqlite|neo4j|qdrant_graph}`.
- **1С Pipeline** (slash-команды): `/analyze-1c-task` → `/implement-1c-task` → `/write-1c-tests` → `/run-1c-tests` (цепочный прогон VA BDD с resume, pre-scenario TestDB check, `.run-state.json`). Skills: `va-bdd-testing` v1.1 (Stage 4a). Runner: `tools/vanessa/run-bdd.ps1 -OutputJson -RunId`. Подробности: **[гл. 43 Пайплайн 1С](docs/framework%20documentation/43_ПАЙПЛАЙН_1С/43.1_ОБЗОР_И_ПАРАДИГМА.md)** (обзор+парадигма / [43.2 команды+режимы](docs/framework%20documentation/43_ПАЙПЛАЙН_1С/43.2_КОМАНДЫ_И_РЕЖИМЫ.md) / [43.3 маршрутизация+хуки+инструментирование](docs/framework%20documentation/43_ПАЙПЛАЙН_1С/43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md)); тестирование (этап 4) — [17.5](docs/framework%20documentation/17_ТЕСТИРОВАНИЕ_1С/17.5_КОМАНДЫ_ПАЙПЛАЙНА.md). **4-этапная проводка (ADR-019 B′ F-1, 2026-06-15):** `analyze-1c-task-preflight`/`implement-1c-task-preflight` авто-заводят `pipeline/<slug>/.pipeline-state.json` (helper [`shared/pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py) — JIRA-slug, best-effort) → 1С-слэш-маршрут проходит `pipeline-protocol-stop` (ADR-018) БЕЗ ручного пайплайна (G3 закрыт). **F-1.5** ([`pipeline-1c-advance.py`](.claude/hooks/pipeline-1c-advance.py) PostToolUse + `advance_for_artifact`): запись ANALYSIS-REPORT→этапы 1,2 done, IMPLEMENTATION-PROGRESS→3 (guard по title-метке). **F-2** ([`pipeline-gate.py`](.claude/hooks/pipeline-gate.py) += `/implement-1c-task` через `gate_1c_implement`): хард-блок реализации до approve дизайна (G4). **F-3**: нота «4-этапная парадигма» в analyze/implement SKILL.md (G5/G2). **F-1.6**: `advance_test_done` — `.run-state.json` все passed → этап 4 done. **W**: [`scripts/tool_usage_report.py`](scripts/tool_usage_report.py) (`--run-id`/`--session`→отчёт по `hook-invocations.jsonl` + `data/tool-effectiveness.jsonl`; `--rollup`). **input-ingestion (V.6)**: [`onec-task-input.py`](.claude/hooks/onec-task-input.py) UPS + `classify_1c_task` → протокол дизамбигуации (T1/T2/T3, ASK/folder/prior-load) при 1С-задаче из чата. Всё — [roadmap 260614](docs/roadmap/260614_ROADMAP_1C_COMMANDS_4STAGE_ALIGNMENT.md) §18; методика 1С не тронута (проводка в хуках, ADR-019 B′). **AUTO-режим (2026-06-15):** команда [`/run-1c-task <вход>`](.claude/commands/run-1c-task.md) + skill [`run-1c-task`](.claude/skills/run-1c-task/SKILL.md) — оркестратор analyze→**авто-approve**→implement→test БЕЗ паузы на ревью первого этапа (отличие от гейтованного `/analyze`+`/implement`); вход (JIRA / описание из чата / путь к папке ТЗ) детектится `resolve_task_input` ([`pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py)); хард-правило «AUTO ≠ игнор блокеров» (критическая неоднозначность → СТОП). Методики (analyze-1c-task-v2/implement-1c-task) не дублируются — скилл их делегирует. Пайплайн `pipeline/impl-1c-run-task/`. **Классификатор сложности + маршрутизация (2026-06-15):** при 1С-задаче ИЗ ЧАТА [`onec-task-input.py`](.claude/hooks/onec-task-input.py) зовёт `route_1c_task` ([`pipeline_1c_bridge.py`](.claude/hooks/shared/pipeline_1c_bridge.py)) — `classify_1c_task` (1С-детект+тип) + `estimate_effort` (эвристика трудозатрат по keyword-сигналам, config `_EFFORT_CFG` тюнится: light/modify/heavy_obj/cross/multi/folder → simple≤2/medium 3–5/complex≥6; **light = downgrade-only при pos==0**, не counterweight — иначе имя реквизита занижает medium→simple) → `flow`: **простая→`/run-1c-task` (AUTO), средняя→спросить, сложная→гейтованный `/analyze`+`/implement`**; не-1С→молча, слабый-1С-сигнал-без-JIRA→`ask_1c` (подтвердить). Решение маршрута — пользователь (2026-06-15); эвристика < правило «сомнение в 1С/потоке → спросить» (хвост-нота в инъекции). `code-skill-enforcer` `_is_test_file` → tests/ exempt от content-уровней A/A.1 (фикстуры легитимно несут доменные токены). Пайплайн `pipeline/impl-1c-task-router/`. **Recall-расширение детектора (2026-06-15, заземлено на ~35 реальных задач `configuration/260304…/docs/`):** `classify_1c_task` многоуровнев — `_1C_DEFINITIVE` (`гкс_`/`configuration/` → 1С даже без глагола) + **`_1C_CODE`** (литеральный 1С/BSL-синтаксис: `Менеджер.Член` ед./мн. с member-guard `\.(?:[А-ЯЁ]|гкс_)` · события `ПередЗаписью`/`ОбработкаПроведения` · закрывающие `КонецЕсли`/`КонецПроцедуры` · токены запроса `ЕСТЬNULL`/`СГРУППИРОВАТЬ ПО`/`СрезПоследних` · БСП `ОбщегоНазначения.` → **1С БЕЗ глагола И `confident_1c=True`**, наличие кода = ключевой признак [фидбэк 2026-06-17]; case-sensitive `re.U` без `re.I`, БЕЗ неоднозначных `ManagerModule`/`.bsl` — code-путь короткозамыкает на `auto`-прогон, поэтому code-FP дороже domain-FP [code-review FAIL→fix]; `pipeline/1c-detector-expand/`) + `(_1C_SIGNAL доменные термины ∪ _1C_STRONG CamelCase/объект.точка) AND _TASK_VERB`; словари `_TASK_VERB`/`_1C_SIGNAL` расширены (АРМ/РС/РН/печатн/обмен/движени/предопредел/учт-«не учтено» T3 и т.д., `\b`-границы против substring-FP). **Инвариант безопасности:** term+verb без сильного маркера → `confident_1c=False` → `ask_1c` (не авто-маршрут) — recall↑, ложные деградируют в вопрос. CamelCase-регекс в `_1C_STRONG` строго `re.U` БЕЗ `re.I` (иначе hump-класс матчит любые 3 кириллицы). **Решение «это 1С» — за Claude (хук advisory), сомнение → спросить** ([[feedback-1c-detection-judgment-mine]]). Пайплайн `pipeline/impl-1c-detector-recall/`. **Единый task-completion gate (2026-06-15):** Stop-хук [`onec-task-completion-stop.py`](.claude/hooks/onec-task-completion-stop.py) (после `pipeline-protocol-stop`) — ЕДИНЫЙ gate 1С-задачи: один проход по транскрипту → консолидированный чеклист всех обязательных петель (без 3-каскада). Hard: **recall** (`unified_search`/`search_patterns`) + **capture** (`capture_pattern`/`route_and_save`/`.claude/…/memory/*.md`) + **research** (`WebSearch`/`WebFetch` — Infostart + GitHub); info: **skill** (1С-методика — уже enforced `code-skill-enforcer` на Write). Блок ОДИН раз со всеми ✗ сразу; детект по фактическим tool_use (не raw-text); 1С-сигнал `is_1c_task_title` (paren-anchored единый предикат в мосте, N4); opt-out `ONEC_TASK_GATE_DISABLE=1`, graceful, no-sentinel. **Заменил** `memory-protocol-stop` + `research-protocol-stop` (удалены — консолидация по адверсариальному анализу). Усиления тонких мест N1–N12 (tail 8МБ N3, единый предикат N4, capture-`.claude` N6, audit `approved_by=auto` N5, opt-out audit N10) — `pipeline/impl-thin-spots-hardening/`. 7 unit + e2e block(чеклист)/allow + live exempt + code-verify PASS. Док — [гл. 43.5 Сквозная карта](docs/framework%20documentation/43_ПАЙПЛАЙН_1С/43.5_СКВОЗНАЯ_КАРТА.md). Пайплайн `pipeline/impl-onec-task-gate/`. **Семантический слой ② детектора — SetFit-гейт (ADR-025, 2026-06-18, opt-in каркас):** обучаемый `is_1c`-классификатор [`onec_setfit_gate.py`](.claude/hooks/shared/onec_setfit_gate.py) встаёт ПЕРЕД TF-IDF (`onec_semantic_fallback`) в `route_1c_task` через `_semantic_signal` — **opt-in (`ONEC_SETFIT_ENABLE`), по умолчанию СПИТ**: `setfit_prob`→`None` при нет-флага/нет-`setfit`/нет-модели/ошибка → graceful откат на TF-IDF, поведение при OFF побитово прежнее (+аддитивный ключ `semantic_source∈{setfit,tfidf,skipped}`). Инвариант безопасности сохранён — мягкий сигнал промоутит ТОЛЬКО в `ask_1c` (не confident/auto). Бинарный (сложность остаётся за `estimate_effort`); старт `rubert-tiny2`, апгрейд `multilingual-e5-small` (MiniLM-L12-v2 отклонён). Обучение [`scripts/train_onec_setfit.py`](scripts/train_onec_setfit.py) (`--dry-run`/`--model`, поверх `data/1c-detector-ground-truth.json`, GT=68 — мало), калибровка [`eval_1c_detector.py --setfit`](scripts/eval_1c_detector.py); **активация ⟸ разметка ~150–300 + обученная модель**. Зеркалит opt-in лестницу: ADR-023 (TEI откл, коллапс) → ADR-024 (LLM-tail net-neutral) → **ADR-025 (SetFit, масштабный путь)**. **ОБУЧЕНО (Этап 3, 2026-06-18): backend ST+LR — `setfit` несовместим с transformers 5.x (`default_logdir`/`config_setfit.json`), поэтому `SentenceTransformer` (rubert-tiny2) + LR-голова напрямую (frozen RU-эмбеддинги НЕ коллапсируют на NL-тайтлах ≠ Qwen3 на BSL-коде); GT 68→230 ([`bootstrap_1c_gt.py`](scripts/bootstrap_1c_gt.py)); интегрированный каскад is_1c F1 0.883→0.974 / Recall 0.813→0.978 (FN 25→3, precision цел), CV F1 0.94; 20 gate-тестов; модель gitignored (регенер. `train_onec_setfit.py`), gate opt-in OFF. **Warm-serve:** in-process в хуке = ~6с/промпт (>5с-таймаут → негоден), поэтому [`scripts/onec_setfit_serve.py`](scripts/onec_setfit_serve.py) — CPU-сервис (stdlib http.server, 0 VRAM, idle-shutdown 30мин), гейт зовёт по HTTP с авто-стартом detached → **6040мс→~25мс**; включение = `ONEC_SETFIT_ENABLE=1` + сервис авто-стартуется, code-verify PASS. Пайплайн `pipeline/onec-setfit-gate/` **Гейт «действие не названо» (Находка 3, 2026-06-18):** `route_1c_task` для УВЕРЕННОГО 1С-входа (гкс_/код/JIRA) БЕЗ названного действия — нет таск-глагола (`_TASK_VERB`) И нет work-сигналов (`signals==[]`) — больше НЕ уходит в `auto` (запуск `/run-1c-task`), а понижается в `ask_flow` (спросить «что сделать?»). Корень: детектор смешивал «в тексте есть 1С-код» и «дана 1С-задача к исполнению» (живой пример: вставленный путь к `ManagerModule` + строка BSL + предупреждение анализатора + вопрос «Это 1С задача?» → раньше `flow=auto`). Гейт достижим ТОЛЬКО в simple-полосе (verb-less ∧ zero-signal ⇒ points≤2), medium/complex не затронуты; инвариант безопасности «confident сохранён» цел (гейт меняет лишь `flow`+ключ `actionless`). Потребитель `onec-task-input.py` — тейлоред-сообщение для `actionless`. Заодно `_TASK_VERB` расширен частыми глаголами (`замен|помен|переписа|перепиш|передела|обнов|переимен|поправ`, start-`\b` против substring-FP `взамен`/`переписка`/`переделка`); известный тех-долг — стем ловит отглагольные сущ. (`обновление`/`заменитель`), blast-radius мал. 8 unit + code-verify PASS. Пайплайн `pipeline/fix-1c-route-actionless-gate/` **Hardening маршрутизации C1-C4 (2026-06-20, аудит 43.5, пайплайн `pipeline/1c-route-logic-hardening/`):** **C1** — actionless получил отдельное `flow="ask_action"` (раньше перегружал `ask_flow`; **обновляет «Находка 3» выше** — actionless теперь `ask_action`, `ask_flow` = только medium); enum самодостаточен, `onec-task-input` диспетчеризует по значению (доп-ключ `actionless` оставлен для наблюдаемости). **C4** — non-1C veto иммунен ТОЛЬКО при `confidence ≥ 0.9` (JIRA/код/гкс_/configuration); CamelCase-кириллица (`_1C_STRONG`, 0.7) теперь **ветируется** тех-контекстом («функция ПолучитьЭмбеддинг в langchain» → none) — раньше любой confident обходил veto. **C2/C3** — новый `resolve_active_1c_slug` (JIRA-код → иначе CURRENT-если-1С) = ЕДИНЫЙ резолвер для `gate_1c_implement` (G4) и `ensure_pipeline_1c(implement)`; `/implement` цепляется к пайплайну `/analyze` через CURRENT → **G4 надёжен для не-JIRA** (раньше `gate` искал по `derive_slug(implement_prompt)` ≠ analyze-slug → молча no-op / ложный блок). 87 unit + code-verify reviewer PASS; гл. 43 синхронизирована (ask_action / veto-тиры / G4-не-JIRA).
- **Hooks Infrastructure**: `.claude/hooks/docs-change-enforcer.py` → `SKIP_PATTERNS` для инфра-файлов (`.gitmodules`/`.gitignore`/`.gitattributes`, `tools/`, `scripts/`, `tests/`, `features/`, `.run-state.json`, `pyproject.toml`, `.mcp.json`, `src/projects/`, `src/bsl/`, `logs/` — runtime artefacts). При добавлении нового типа инфра-файла — добавить в `SKIP_PATTERNS`, иначе `UNMAPPED` блокировка. **Existence filter** (added 2026-04-15): `get_session_files()` отбрасывает файлы, которых уже нет в working tree (удалены в пределах 6-часового окна `git log`), чтобы стрей-артефакты не блокировали завершение сессии. `code-skill-patterns.json` — конфиг `code-skill-enforcer`, правила `{pattern, skill, label, domain}`; `skill` должен существовать в каталоге `.claude/skills/`, иначе phantom-блокировка. **Routing-fixes (2026-05-29)**: `FastAPI|APIRouter` и `pytest\.fixture|@pytest|conftest\.py` перенесены из `research_protocol` (Level A.1 → форсил `learning-loop`) в `patterns.mappings` (Level A) → `framework-api` / `evaluation-benchmark` соответственно (профильные скиллы уже существовали). `z-ai-write-guard.py` `_EXEMPT_PREFIXES` дополнен `tests/` (docstring уже его перечислял — устранён code/doc drift; тест-код = precision-работа, не делегируемая генерация). Smoke/behavioral покрытие orphan-модулей `processing/`: [`tests/unit/test_import_smoke.py`](tests/unit/test_import_smoke.py) (авто-discovery всех `src.pdf_framework` модулей, marker `unit` → CI `test-unit`) + [`tests/unit/test_processing_behavior.py`](tests/unit/test_processing_behavior.py) (исполняет методы через tmp_path+моки+fitz-PDF, ловит `self._format`/missing-await класс багов). **Roadmap §18 trigger-detection (2026-05-29)**: новый Stop-хук [`roadmap-progress-enforcer.py`](.claude/hooks/roadmap-progress-enforcer.py) реализует §19.3 P1 ([roadmap 260523](docs/roadmap/260523_ROADMAP_FULL_DEV_LIFECYCLE_ANALYSIS.md)) — парсит transcript на Bash `tool_use` с `gh pr merge`/`git merge` (парсинг tool_use, НЕ raw-text → чтение роадмапа не false-positive'ит); при merge-milestone без правки `docs/roadmap/` → **block-once per session** (sentinel `.claude/cache/roadmap-progress-enforcer-state.json`, soft — не deadlock). Opt-out `ROADMAP_PROGRESS_NO_ENFORCE=1`. Зарегистрирован в Stop-chain после `docs-change-enforcer`. **§19.3 полностью закрыт (предрассветно):** + CI-lint [`scripts/roadmap_progress_log.py`](scripts/roadmap_progress_log.py) (`lint` structural+freshness в `ci.yml` lint-job, `append` skeleton-генератор, `links` wikilink-валидация против memory store) + `docs/roadmap/` exempt в обоих auto-save хуках (`auto-git-save.py` `IGNORE_PATH_PREFIXES` + `posttooluse-auto-git-save.py` `SKIP_PATTERNS`) → §18-правки получают осознанный `docs(roadmap):` коммит вместо `chore: auto-save` preempt (safety net — `git-commit-enforcer` watches `docs/`). **Amend-absorb (2026-06-12):** все 3 auto-save хука амендят свой незапушенный auto-save HEAD того же prefix'а (`shared/auto_save_core.get_amendable_head`: prefix word-boundary, `branch -r --contains HEAD` пуст, нет merge/rebase/revert) → незапушенный хвост = один коммит, поглощаемый осмысленным `git commit --amend`; серии «5× chore: auto-save» устранены; регресс `tests/unit/test_auto_save_amend.py`, детали skill `auto-git-save` §1.5. **Ruff-format перед auto-commit (2026-06-19):** все 3 auto-save-пути перед коммитом зовут `shared/auto_save_core.format_staged_python()` — ruff-format'ят staged `.py` под `[tool.ruff] line-length` (`--line-length` передаётся ЯВНО: голый `ruff format <file>` ширину не подхватывает, с флагом == pre-commit; python из `.venv`), затем `git add` заново; best-effort (нет ruff/timeout → коммит НЕ блокируется), пропуск vendored-деревьев (`tools/`/`external/`/`infra/`/`src/bsl/`, зеркало pre-commit `exclude`); устраняет рецидив красноты CI-джоба **Pre-commit Hooks** (auto-commit идёт `--no-verify`/кастомный `core.hooksPath`, в обход pre-commit → неотформатированный `.py` попадал в master; memory `project-ci-precommit-red-autocommit-noverify`); детали skill `auto-git-save` §1.6. **Regression-hardening (2026-06-02, R1-R4):** после merge/autofix-регрессии CI-lint расширен `src/` → `ruff check src/ scripts/ .claude/hooks/` ([`ci.yml`](.github/workflows/ci.yml)) — broken-код в `scripts/`/`.claude/hooks/` раньше не линтился; добавлен Compile-smoke step (`compileall scripts .claude/hooks`, dep-free, ловит структурную порчу; undefined-name класс ловит F821 в расширенном lint). Включены `RUF100`+`PGH004` в ruff `select` (noqa-гигиена: запрет «голого» `# noqa` и мёртвых директив — корень регрессии: `# noqa: F821` маскировал удалённые символы); потребовало вычистить ~116 мёртвых noqa. Guard [`tests/unit/test_git_hooks_present.py`](tests/unit/test_git_hooks_present.py) (`core.hooksPath`-каталог был тихо удалён → отключил весь авто-reindex). **Bugfix `docs-change-enforcer.py`:** `get_session_files()` строил `since_arg`+`grep_args` (session-bound окно + исключение `^chore: auto-save`/`auto-format` коммитов), но НЕ передавал их в `git log` (dead code → over-firing на косметических коммитах); проводка восстановлена + добавлены `^style:`/`^style(` паттерны (style/format/noqa-коммиты = comment/whitespace-only). ⚠ `git log --grep "^style("` требует basic-regex (без `-E`, иначе `fatal: Unmatched (`). **Bugfix `--semantic-suggest` (2026-06-12, roadmap 260612 pdf-docs P2.B3):** `semantic_fallback_suggest` слал TEI 4096d-вектор в `framework_code_v1` (alias → MRL 1024d) → Qdrant 400 глотался `except` → «(no confident match)» на ЛЮБОЙ файл с момента MRL-миграции; добавлен truncate+renorm до dim коллекции (alias-aware introspection); закреплено live-тестом [`tests/integration/test_pdf_docs_chains_live.py`](tests/integration/test_pdf_docs_chains_live.py). **Skill Router Honest Eval (2026-06-13, roadmap [260613](docs/roadmap/260613_ROADMAP_SKILL_ROUTER_HONEST_EVAL.md)):** [`skill-router.py`](.claude/hooks/skill-router.py) Layer A2 = 1С-сигналы по форме текста (CamelCase-кириллица/`гкс_`/`Документ.`/`Srvr=`) + буквальное имя скилла **только как целое слово** (`\b…\b`, F3 — подстрочный `in` форсил `deployment`/`autoresearch`); [`eval-skill-router.py`](scripts/eval-skill-router.py) считает honest **pooled `action_f1`** (action-only, без silence-padding) + диагностический train/test split, гейт acceptance читает его (НЕ in-sample padded macro-F1); GT [`data/skill-router-ground-truth.jsonl`](data/skill-router-ground-truth.jsonl) под провенанс-контрактом (`source`/`split`, 22 leakage-кандидата `transcript-router` → `quarantine` вне гейта, см. [README](data/skill-router-ground-truth.README.md)) + blocking-lint [`scripts/lint_skill_router_gt.py`](scripts/lint_skill_router_gt.py) (CI-job `skill-router-gt-lint`, инвариант «transcript-router ⇒ quarantine»); [`skill_system_acceptance.py`](scripts/skill_system_acceptance.py) критерий `gate_representative` (quarantined==0) держит вердикт честно-PROVISIONAL пока hard-когорта в карантине. `docs-change-enforcer.py` `SKIP_PATTERNS` дополнен `skill-router-ground-truth` (eval-dataset, не product-code). **Фаза B core (B1/B3/B4):** A2-веса роутера вынесены в config `a2_signals` (tunable без правки кода, behavior-preserving — дефолты == хардкод); eval `--split {train\|test\|all}` + `--cv K` (k-fold action_f1 mean±std). Оптимизатор весов (B5) — следующий срез, осмыслен после A7 (ре-верификация 22 карантинных кейсов → репрезентативный гейт). **Tooling Adoption (260613):** карта внедрения ecosystem-инструментов [260613_ROADMAP_TOOLING_ADOPTION.md](docs/roadmap/260613_ROADMAP_TOOLING_ADOPTION.md) + ADR-012..015 (`architecture-research/adr/`) фиксируют adopt/skip по 4 SDLC-шагам (ADOPT Context7 lazy-load + tdd-guard opt-in; SKIP Spec-Kit/BMAD/Figma/Sentry/plugin-маркетплейс — N6 конфликт с hook-архитектурой); `SKIP_PATTERNS` в docs-change-enforcer дополнен `/adr/` (ADR = самодокументирующиеся decision-артефакты, не product-код). 4-шаговый SDLC (Планирование→Дизайн→Кодирование→Тестирование) + авто-ADR без переспросов закреплён в памяти `feedback-sdd-4step-auto-adr`. **tdd-guard opt-in (260613 Этап 4.2/4.3, [ADR-015](.claude/skills/architecture-research/adr/015-testing-adopt-tddguard-optin-eval-playwright.md)):** [`tdd-guard.py`](.claude/hooks/tdd-guard.py) — PreToolUse:Write\|Edit, **default OFF=no-op**; `TDD_GUARD_ENABLE=1` + правка `src/**.py` с новым def/class без `tests/**/test_<mod>.py` → advisory system_message (`continue:true`, **НИКОГДА не блок**) + кеш события в `.claude/cache/tdd-guard-events.jsonl`. Авто-валидация: [`tdd_guard_validation.py`](scripts/tdd_guard_validation.py) (окно 06-13→06-20 = **7 дней**) считает advisory/follow_rate → авто-решение hard-block (follow_rate≥0.5 + low-noise) vs keep-advisory; SessionStart-баннер [`tdd-guard-validation-on-start.py`](.claude/hooks/tdd-guard-validation-on-start.py) раз/день. Включено **ЛОКАЛЬНО** (`settings.local.json` gitignored) — team `settings.json` НЕ тронут. **Banner-fix:** общий [`shared/acceptance_watch.py`](.claude/hooks/shared/acceptance_watch.py) `acceptance_banner` параметризован `window_days` (default **14** → memory-ai/skill-learning/skill-system без изменений; tdd-guard передаёт **7**) — раньше знаменатель «день N/14» хардкодился и врал для 7-дневного окна. **Generic 4-stage pipeline ([ADR-017](.claude/skills/architecture-research/adr/017-generic-4stage-pipeline-slash-state.md), 2026-06-13):** домен-агностичный пайплайн Планирование→Дизайн→Кодирование→Тестирование (**НЕ путать** с 1С-цепочкой `/analyze-1c-task→…`). Команды [`pl-plan`/`pl-design`/`pl-code`/`pl-test`](.claude/commands/) → **артефакт-на-этап** `pipeline/<task>/0N-*.md` + [`shared/pipeline_state.py`](.claude/hooks/shared/pipeline_state.py) (state `.pipeline-state.json` + указатель `CURRENT`, stdlib CLI `init|done|approve|status|gate`, паттерн из `.run-state.json`). Каждый этап **делегирует существующему скиллу** (architecture-research/implementer/code-verify) — каркас их оркестрирует, не дублирует. Единственный **hard-гейт** — перед Кодированием: дизайн (02) должен быть `approved`; [`pipeline-gate.py`](.claude/hooks/pipeline-gate.py) (UserPromptSubmit) блокирует `/pl-code` до одобрения (аналог `approval-gate.py`), `pl-design`/`pl-test` — advisory. `pipeline/`+`.pipeline-state.json` в docs-enforcer `SKIP_PATTERNS`. Опция позже — `/pl-run-all` (Workflow-скрипт, те же команды/артефакты, автономный прогон). **Обязательная авто-парадигма ([ADR-018](.claude/skills/architecture-research/adr/018-mandatory-auto-pipeline-protocol.md), 2026-06-13):** пайплайн теперь **mandatory** и **self-driven** — Claude сам ведёт этапы (не ждёт `/pl-*`). [`pipeline-protocol.py`](.claude/hooks/pipeline-protocol.py) (UPS) инъектит обязательную инструкцию; [`pipeline-protocol-stop.py`](.claude/hooks/pipeline-protocol-stop.py) (Stop) **hard-блокирует** завершение, если в сессии были Write/Edit (сигнал «была задача» из `data/hook-invocations.jsonl` **ИЛИ** git-fallback `_git_session_edit` — незакоммиченный файл, изменённый за сессию [`git status --ignore-submodules=all` + `st_mtime >= старт сессии`]; закрывает MultiEdit/NotebookEdit без PreToolUse-матчера НЕ трогая settings.json; anti-deadlock — mtime-bound + denylist `docs/wiki/log.md` + fail-safe; регресс `tests/unit/test_pipeline_protocol_git_signal.py`, 2026-06-14) без использования пайплайна (`pipeline/<slug>/.pipeline-state.json` не обновлён за сессию). Охват — **все задачи вкл. trivial** (trivial→компактный `pipeline.md`; medium/complex→`01..04`+approval); жёсткость — **hard сразу** (выбор пользователя, НЕ фазовый rollout). **Carve-out (обязателен):** чистые вопросы без Write — exempt (иначе блок даже ответа/deadlock). Safety: opt-out `PIPELINE_PROTOCOL_DISABLE=1` + graceful degradation + Write-gating (нет ложных блоков на разговоре). Реверс — снять 2 записи из `settings.json`. **Gate-policy слой ([ADR-034](.claude/skills/architecture-research/adr/034-pattern-pipeline-evolution-bestpractices.md) R3, 2026-06-21):** [`shared/gate_policy.py`](.claude/hooks/shared/gate_policy.py) — composable **fail-closed** `evaluate_gates(context, policies)` (любой deny/исключение policy → общий deny) + `log_decision` → `data/gate-decisions.jsonl` (decision-log/аудит, OPA-вдохновлено); единая точка композиции гейтов вместо независимых конкурирующих Stop-хуков (натяжение T3). **Additive** — существующие гейты (`pipeline-protocol-stop`/`onec-task-completion-stop`/`pipeline-gate`) НЕ тронуты, миграция approval/scope/completion на policy-слой инкрементальна; регресс [`tests/unit/test_gate_policy.py`](tests/unit/test_gate_policy.py) (4 теста). Полный роадмап эволюции pattern↔pipeline (R1 child-workflow / R4 SARIF-контракт `sonar_issues_pull.py --format sarif` / R5 `remediation_class` / R6 Clean-as-You-Code `sonar_quality_gate_check.py` opt-in `SONAR_QG_HARD` / R7 evaluator-optimizer / R8 таксономия PATTERN 2.14 «Findings-to-Pipeline») — в ADR-034 + кеш `architecture-research/cache/pattern-pipeline-orchestration-2026.md`
- **PR-automation (`post-task-push-pr.py` P0-P3 batch, landed via roadmap 260522)**: PostToolUse:TaskUpdate хук — на `TaskUpdate(status=completed)` собирает per-task ветку, push на origin, открывает / переиспользует PR, опционально ждёт checks и мержит. 18 items из [40.4 Дорожная карта](docs/framework%20documentation/40_PR_AUTOMATION/40.4_Дорожная_карта.md): label-driven activation, conflict-aware push, existing-PR reuse, pre-commit pre-push gate, SMTP уведомления, merge wait-for-checks, auto-rebase, dry-run, per-task SHA tracking, CODEOWNERS reviewer assignment, cross-PR blocked-by labels, cherry-pick branch model через git worktree (P3.2), post-merge auto-revert script ([`scripts/pr_check_post_merge.py`](scripts/pr_check_post_merge.py) P3.4), GitHub merge queue alternative `AUTO_PR_MERGE_QUEUE=1` (P3.3). Companion helpers: [`shared/pr_helpers.py`](.claude/hooks/shared/pr_helpers.py) (git/gh primitives + `cherry_pick_range_to_branch`), [`shared/pr_notifier.py`](.claude/hooks/shared/pr_notifier.py) (SMTP), dashboard [`scripts/pr_automation_dashboard.py`](scripts/pr_automation_dashboard.py) (P1.5), orphan cleanup [`scripts/cleanup_orphan_branches.py`](scripts/cleanup_orphan_branches.py) (P2.4), GitHub webhook receiver [`src/api/routes/github_webhooks.py`](src/api/routes/github_webhooks.py) (P3.1), Mergify template [`.mergify.yml`](.mergify.yml) (P3.3). **Master switch:** `AUTO_PR_ENABLED=1` (default `0` после миграции). Base ветка: `AUTO_PR_BASE=master` (после reconciliation 2026-05-23, roadmap 260523 §11; dev-master deleted). Полный env reference + smoke checks + operator wiring guide — [40.4 §«Реализация 2026-05-22 — P3»](docs/framework%20documentation/40_PR_AUTOMATION/40.4_Дорожная_карта.md) и [40.5 Pipeline Workflow](docs/framework%20documentation/40_PR_AUTOMATION/40.5_Pipeline_Workflow.md). **Зависимость:** хук требует modernized `base/protocol.py` (`hook_event_name` приоритет, `transcript_path` fallback) — портирован в этой же миграции
- **`base/protocol.py` modernization (landed 2026-05-22 в составе PR-automation migration)**: `HookInput.detected_event` теперь читает `hook_event_name` payload-поле как авторитативный источник (Claude Code 2.x), `transcript` падает на `transcript_path` (snake_case современный) с легаси-фоллбэком на `transcript`. Без этой модернизации все PostToolUse-хуки misclassify-ят modern Claude Code события как "Unknown" и не срабатывают. Обратная совместимость со старым payload-форматом сохранена
- **F821 hotfix stubs (2026-05-24, hotfix/ci-green-master / PR #10)**: pre-existing F821 errors в `.claude/hooks/` после PR #2 merge — helper functions удалены, calls остались. На `session-memory-save.py` восстановлены 3 stub: `save_to_wiki_log`, `try_promote_patterns`, `_emit_langfuse_span` (все return None/False, no-op). На `memory-first-hook.py` добавлен `HOOK_NAME` constant (`"memory-first-hook"`). Полная реализация (Langfuse spans, L5 wiki promote) deferred до master CI stabilization. Stubs предотвращают `NameError` при runtime, ratchet down ruff F821 errors в `.claude/hooks/` с 16 до 9. См. PR #10 commits `a54b3ebe3`, `ab44af483`
- **§14 Pre-Work dispatcher (PR #9, 2026-05-23)**: новый UPS hook [`shared/prework_dispatcher.py`](.claude/hooks/shared/prework_dispatcher.py) реализует ADR-D1 (asyncio.gather parallel orchestrator из [roadmap 260523 §17.1](docs/roadmap/260523_ROADMAP_FULL_DEV_LIFECYCLE_ANALYSIS.md)) + aggregator [`shared/prework_aggregator.py`](.claude/hooks/shared/prework_aggregator.py) (top-K + ~500 tokens cap, ADR-D2 MVP) + first worker [`prework-architecture.py`](.claude/hooks/prework-architecture.py) (rapidfuzz match на `architecture-research/cache/_index.json`). Worker contract: stdin `{prompt}` → stdout `{items: [{title, body, score}]}`. Регистрация в `settings.json` UPS chain после `memory-first-hook` (timeout 5s). Smoke: real prompt → top-3 cached topics с score 1.0/1.0/0.889. Per-worker timeout via `asyncio.wait_for`, orphan subprocess killed на `TimeoutError`. Reversibility: single rollback = remove dispatcher entry. Future workers (P1, separate PRs): prework-similar-code (Qdrant `framework_code_v1`), prework-github-bp (cache-first WebSearch), prework-stackoverflow (UPS + PostToolUse:Bash reactive). Code-verify PASS via subagent aa124b154f2739a13. Memory entry `feedback_prework_dispatcher_works` будет saved после prod observation 24h
- **§14 P1 prework-similar-code worker (PR #12, 2026-05-24)**: второй worker [`prework-similar-code.py`](.claude/hooks/prework-similar-code.py) — TEI Qwen3 embed `Instruct:/Query:` prefix → MRL truncate 4096→1024d → Qdrant search в `framework_code_v1` collection (31k chunks) → top-5 hits с `score >= 0.35`. Smoke "refactor the search manager to use httpx" → 5 hits (top score 0.697 для search_tool.py). Registered в `WORKERS` registry [`shared/prework_dispatcher.py`](.claude/hooks/shared/prework_dispatcher.py) с timeout 2s (TEI cold-start padding). Graceful degradation: TEI/Qdrant down → empty items, dispatcher продолжает.
- **§14 P1 workers 2/3 + 3/3 (PR #46, 2026-05-25)**: финальные prework workers — [`prework-github-bp.py`](.claude/hooks/prework-github-bp.py) (cache-first GitHub best-practices, §14.4 Option B; фильтр `architecture-research/cache/` по `github_repos_count > 0` + freshness ≤365d + rapidfuzz score ≥60, timeout 4s) + [`prework-stackoverflow.py`](.claude/hooks/prework-stackoverflow.py) (cache-first SO/error-context, §14.5 Option C UPS half; мерж `tech-research` dict-schema + `architecture-research` list-schema caches, SO_SIGNALS keyword filter + freshness ≤540d, timeout 1s). Оба зарегистрированы в `WORKERS` registry. End-to-end smoke: 4 секции (ARCH + CODE + GH + SO) в unified `systemMessage`, truncated по `MAX_TOTAL_CHARS=2000` cap per ADR-D2. PostToolUse:Bash reactive SO worker (Option C complete) — отдельная итерация
- **Maximum CI tier landed (PR #19, 2026-05-24/25)**: `.github/workflows/codeql.yml` (Python+JS Security analysis, weekly schedule + PR triggers) + `.github/workflows/claude.yml` (Anthropic bot auto-review на pull_request:opened, skip bot authors per `user.type != 'Bot'`) + [`post-merge-revert-stop.py`](.claude/hooks/post-merge-revert-stop.py) Stop-hook (detached `pr_check_post_merge.py --apply`, 10min cooldown, gated by `AUTO_PR_ENABLED`) + Stop chain extended в `settings.json` + `dependabot.yml` `directories:` explicit list (excluded private submodules). Companion docs chapters 40.6-40.8 + 42 Monitor CI + roadmap 260524 1C CI. Code-verify PASS via subagent abb4be7715b25be63. **Post-merge fixes:** claude.yml bot filter (`66e71fdf1`) для skip Dependabot auto-review failures, retry.py `raise None → RuntimeError` (`037fe6228`) для CodeQL py/illegal-raise alert #1349 — verify PASS via subagent ac929a17a7847c182
- **CI Autopilot Phase 2 — Notifications + Digest (2026-05-26)**: [`scripts/ci_digest.py`](scripts/ci_digest.py) — markdown summary за окно `--since 24h/7d/...` (failures top-10 по нормализованным error_hash + open PRs + merged + auto-issues + GitHub notifications via `gh api notifications`). [`.claude/hooks/gh-notif-intake-on-start.py`](.claude/hooks/gh-notif-intake-on-start.py) — SessionStart hook auto-summarize unread GH notifications в systemMessage `[GH-NOTIF] N unread (ci-fail=X, ...)`. [`shared/gh_notif_classifier.py`](.claude/hooks/shared/gh_notif_classifier.py) — subject+reason → category+priority (P0 security / P1 ci-fail+pr-review / P2 dependabot / P3 merged). Code-verify subagent `aac7e35e5ca3f47e2` → PARTIAL → 4 fixes applied (timezone-naive crash → coerce UTC, `claude` skip_re tightened к `Claude Code Review\|claude\[bot\]`, hook timeout 5→8s, Dependabot `^bump .. from .. to ..` regex). Phase 2 item #2 (`workflow_run` server-side webhook) — deferred, см. [260525_ROADMAP_CI_AUTOPILOT_PHASE2.md §1](docs/roadmap/260525_ROADMAP_CI_AUTOPILOT_PHASE2.md). Doc: [42.5 GH Notifications Intake](docs/framework%20documentation/42_MONITOR_CI/42.5_GH_Notifications_Intake.md), [42.6 Daily Digest](docs/framework%20documentation/42_MONITOR_CI/42.6_Daily_Digest.md)
- **CI monitoring auto-catchup (PR #54, 2026-05-25 evening)**: closes gap где `.claude/cache/ci-failures.jsonl` stale между Claude сессиями (Monitor `bkozphbx9` session-bound — между сессиями нет auto-trigger). Два корневых fix: (1) [`scripts/ci_failure_cache.py:analyze_failure`](scripts/ci_failure_cache.py) — bug где `refs/pull/{pr}/head` использовался как branch name (gh не принимает) → заменено на `_resolve_pr_head()` (gh pr view → headRefName + headRefOid) + предпочтение `--commit <sha>` над `--branch <name>`. (2) Новый mode `--catchup [LIMIT=20]` — `gh run list --status failure --limit N` → filter run_ids уже в jsonl → process each (idempotent). (3) Новый SessionStart hook [`ci-catchup-on-start.py`](.claude/hooks/ci-catchup-on-start.py) — detached subprocess `Popen` (CREATE_NEW_PROCESS_GROUP на Windows) спавнит `ci_failure_cache.py --catchup 20` при каждом session start. Opt-out `CI_CATCHUP_DISABLE=1`, override `CI_CATCHUP_LIMIT=N`. Backfill результат: cache `1 → 48 entries, 31 unique hashes` (несколько count=2 близко к auto-issue threshold=3). Code-verify subagent `a152803afc173a0bb` → PASS с 2 minor recs applied (open→all PR lookup ordering + stderr error logging). Doc: [42.4 §SessionStart auto-catchup](docs/framework%20documentation/42_MONITOR_CI/42.4_Failure_Caching.md). True 24/7 (GitHub webhook trigger) — deferred
- **§14.5 reactive SO + §20 P2 + §15 P0 foundation (PR #47, 2026-05-25 late PM)**: [`posttooluse-stackoverflow-on-error.py`](.claude/hooks/posttooluse-stackoverflow-on-error.py) — PostToolUse:Bash hook, on non-zero exit + Traceback/pip ERROR/npm ERR!/fatal: signature → emit `[SO-ON-ERROR]` advisory с WebSearch suggestion (handles dict-shape `tool_response` через `_normalize_response` extract stdout/stderr keys; explicit exit_code authoritative). Pairs с UPS-time [`prework-stackoverflow.py`](.claude/hooks/prework-stackoverflow.py) — §14 Pre-Work pipeline теперь 100% complete. Bonus hotfix: [`posttooluse-bash-errors.py:37`](.claude/hooks/posttooluse-bash-errors.py) pre-existing 4-tuple→3-tuple unpack bug на `Found N errors` pattern. §20 P2: [`scripts/triage_codeql_alerts.py`](scripts/triage_codeql_alerts.py) semi-automated triage (vendored/tests/intentional/review), **applied 174/261 dismissed** (vendored 43 + tests 6 + intentional 125, 87 review manual). §15 P0 (3/4 subtasks): [`shared/invocation_logger.py`](.claude/hooks/shared/invocation_logger.py) — CloudEvents v1.0 envelope (`specversion/id/source/type/time/datacontenttype/correlationid/causationid`) + W3C traceparent (`00-{32hex}-{16hex}-01`, deterministic trace_id из run_id для cross-event correlation) + new `causation_id` arg на `log_invocation()`; backward-compat preserved (all flat fields). [`scripts/audit_query.py`](scripts/audit_query.py) — DuckDB SQL layer над `hook-invocations.jsonl` (5 views: recent/latency-p95/error-rate/top-tools/hooks-per-session) + `--view causation-chain --correlation-id` DAG traversal + raw `--sql`; `union_by_name=true, ignore_errors=true` handles schema drift + legacy malformed lines. **Deferred (subtask 4):** crypto-shredding per-session-key — отдельный PR per §15.6 P1. Code-verify FAIL → Ralph Wiggum iter 1 → 7/7 smoke PASS via subagent `a4396858021c471fc`

## Research

При вопросах про 1С: документация 8.3.27 — первоисточник. Внешние источники (its.1c.ru, infostart.ru) — только дополнение. Каждый факт с атрибуцией. Протокол: skill `1c-doc-research`.

При вопросах про RAG/ML/Python: протокол в skill `tech-research`.
