#Область ОписаниеПеременных

&НаКлиенте
Перем ИнтервалАвтообновления;

&НаКлиенте
Перем ТекущаяАктивнаяСтраница;

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ИнициализироватьРеквизитыФормы();
	Элементы.НастройкиУсиленногоКонтроляКачества.Обновить();
	Элементы.НастройкиОтключенияВходногоКонтроляКачества.Обновить();
	Элементы.НастройкиНоменклатурыКонтроляУдостоверенияКачества.Обновить();
	УстановитьВидимостьДоступность();
	
	ИнициализироватьНастройкиАвтообновления();
	
КонецПроцедуры 

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УправлениеФормой();
	
	// Подключение автообновления настроек каждую минуту по аналогии
	ИнтервалАвтообновления = 60;
	ПодключитьОбработчикОжидания("АвтообновлениеНастроек", ИнтервалАвтообновления, Ложь);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ОтборАктивностьПриИзменении(Элемент)
	УправлениеФормой();
	ПрименитьФильтрАктивностиДляАктивнойСтраницы();
КонецПроцедуры

&НаКлиенте
Процедура ВидПеревозкиПриИзменении(Элемент)
	УправлениеФормой(); 
	УстановитьВидимостьДоступность();
КонецПроцедуры

&НаКлиенте
Процедура ПЛКПриИзменении(Элемент)
	УправлениеФормой();
КонецПроцедуры

&НаКлиенте
Процедура ГруппаНастройкиПриСменеСтраницы(Элемент, ТекущаяСтраница)
	
	ТекущаяАктивнаяСтраница = ТекущаяСтраница;
	
	ПрименитьФильтрАктивностиДляАктивнойСтраницы();
	
	ОтключитьОбработчикОжидания("АвтообновлениеНастроек");
	ПодключитьОбработчикОжидания("АвтообновлениеНастроек", ИнтервалАвтообновления, Ложь);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыНастройкиЗапретаВскрытияПоНоменклатуре

&НаКлиенте
Процедура НастройкиЗапретаВскрытияПоНоменклатуреВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
		СтандартнаяОбработка = Ложь;
	ТекущиеДанные = Элементы.НастройкиЗапретаВскрытияПоНоменклатуре.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	КлючЗаписи = ПолучитьКлючЗаписиЗапретаВскрытияПоНоменклатуре(
		ТекущиеДанные.Номенклатура, ТекущиеДанные.ДопускКВскрытию, ТекущиеДанные.ДатаНачала,
		ТекущиеДанные.ВидПеревозки, ТекущиеДанные.ЖДСтанция,
		ТекущиеДанные.ТранспортноеСредство, ТекущиеДанные.Поставщик);

	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Ключ", КлючЗаписи);
	ПараметрыФормы.Вставить("ОткрытаИзОбработки", Истина);

	ОткрытьФорму("РегистрСведений.гкс_НастройкиЗапретаВскрытияПоНоменклатуре.ФормаЗаписи",
		ПараметрыФормы, ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено,
		ОписаниеОповещенияОЗакрытииЗапретаВскрытияПоНоменклатуре(),
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыНастройкиНоменклатурыКонтроляУдостоверенияКачества
&НаКлиенте
Процедура НастройкиНоменклатурыКонтроляУдостоверенияКачестваВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ТекущиеДанные = Элементы.НастройкиНоменклатурыКонтроляУдостоверенияКачества.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	КлючЗаписи = ПолучитьКлючЗаписиНоменклатурыКонтроляУдостоверенияКачества(ТекущиеДанные.Номенклатура);

	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Ключ", КлючЗаписи);
	ПараметрыФормы.Вставить("ОткрытаИзОбработки", Истина);

	ОткрытьФорму("РегистрСведений.гкс_НоменклатураКонтроляУдостоверенияКачества.ФормаЗаписи",
		ПараметрыФормы, ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено,
		ОписаниеОповещенияОЗакрытииНоменклатурыКонтроляУдостоверенияКачества(),
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыНастройкиУсиленногоКонтроляКачества
&НаКлиенте
Процедура НастройкиУсиленногоКонтроляКачестваПередУдалением(Элемент, Отказ)
	ОбщегоНазначенияКлиент.СообщитьПользователю(
		НСтр("ru = 'Удаление правила запрещено. Необходимо снять активность.'"), , , , Отказ);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы 

&НаКлиенте
Процедура ОбновитьСписок(Команда)
	УправлениеФормой();
КонецПроцедуры

&НаКлиенте
Процедура СоздатьОтключенияВходногоКонтроля(Команда)
	ПараметрыОткрытия = 
		Новый Структура("ВидПеревозки, ТочкаМаршрута, ОткрытаИзОбработки", 
			ОтборВидПеревозки, Объект.ТочкаМаршрута, Истина);

	ОткрытьФорму("РегистрСведений.гкс_НастройкиОтключенияВходногоКонтроляКачества.ФормаЗаписи", ПараметрыОткрытия,
		ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено,
		ОписаниеОповещенияОЗакрытииОтключенияВходногоКонтроля(), РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСписокОтключенияВходногоКонтроля(Команда)
	УправлениеФормой();
КонецПроцедуры


&НаКлиенте
Процедура ОбновитьСписокНастройкиНоменклатурыКонтроляУдостоверенияКачества(Команда)
	ОбновитьНастройкиНоменклатурыКонтроляУдостоверенияКачества();
КонецПроцедуры

&НаКлиенте
Процедура СоздатьНастройкуНоменклатурыКонтроляУдостоверенияКачества(Команда)
	ПараметрыОткрытия = 
		Новый Структура("ВидПеревозки, ТочкаМаршрута, ОткрытаИзОбработки", 
			ОтборВидПеревозки, Объект.ТочкаМаршрута, Истина);

	ОткрытьФорму("РегистрСведений.гкс_НоменклатураКонтроляУдостоверенияКачества.ФормаЗаписи", ПараметрыОткрытия,
		ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено, 
		ОписаниеОповещенияОЗакрытииНоменклатурыКонтроляУдостоверенияКачества(), 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);	
КонецПроцедуры

&НаКлиенте
Процедура УдалитьНастройкуНоменклатурыКонтроляУдостоверенияКачества(Команда)
	ТекущиеДанные = Элементы.НастройкиНоменклатурыКонтроляУдостоверенияКачества.ТекущиеДанные;
	УдалитьНастройкуНоменклатурыКонтроляУдостоверенияКачестваНаСервере(ТекущиеДанные);
	ОбновитьНастройкиНоменклатурыКонтроляУдостоверенияКачества();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСписокЗапретаВскрытияПоНоменклатуре(Команда)
	ОбновитьНастройкиЗапретаВскрытияПоНоменклатуре();
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЗапретВскрытияПоНоменклатуре(Команда)
	ПараметрыОткрытия = Новый Структура("ВидПеревозки, ОткрытаИзОбработки", ОтборВидПеревозки, Истина);

	ОткрытьФорму("РегистрСведений.гкс_НастройкиЗапретаВскрытияПоНоменклатуре.ФормаЗаписи",
		ПараметрыОткрытия, ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено,
		ОписаниеОповещенияОЗакрытииЗапретаВскрытияПоНоменклатуре(),
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

&НаКлиенте
Процедура УдалитьНастройкуЗапретаВскрытияПоНоменклатуре(Команда)
	ТекущиеДанные = Элементы.НастройкиЗапретаВскрытияПоНоменклатуре.ТекущиеДанные;
	УдалитьНастройкуЗапретаВскрытияПоНоменклатуреНаСервере(ТекущиеДанные);
	ОбновитьНастройкиЗапретаВскрытияПоНоменклатуре();
КонецПроцедуры

#КонецОбласти  

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ИнициализироватьРеквизитыФормы()
	ОтборАктивность = Истина;
	ОтборВидПеревозки = Перечисления.гкс_ТипыТранспортныхСредствДоставки.Автомобиль;
	ИнициализироватьОтборТочкаМаршрута();
КонецПроцедуры

&НаКлиенте
Процедура УправлениеФормой()
	Если ЗначениеЗаполнено(Объект.ТочкаМаршрута) 
		И ЗначениеЗаполнено(ОтборВидПеревозки) Тогда
		УстановитьПараметрыДинамическихСписков();
		Элементы.НастройкиУсиленногоКонтроляКачества.Обновить();
	КонецЕсли;
	
	УправлениеФормойОтключенияВходногоКонтроля();
КонецПроцедуры	

&НаКлиенте
Процедура УправлениеФормойОтключенияВходногоКонтроля()
	Если ЗначениеЗаполнено(Объект.ТочкаМаршрута) 
		И ЗначениеЗаполнено(ОтборВидПеревозки) Тогда
		УстановитьПараметрыДинамическихСписковОтключенияВходногоКонтроля();
		Элементы.НастройкиОтключенияВходногоКонтроляКачества.Обновить();
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ИнициализироватьОтборТочкаМаршрута()
	
	ИспользоватьНесколькоАЛЦ = ПолучитьФункциональнуюОпцию("гкс_ИспользоватьНесколькоАЛЦ");
	
	Элементы.ОтборТочкаМаршрута.Доступность = ИспользоватьНесколькоАЛЦ;
	Элементы.ОтборТочкаМаршрута.РежимВыбораИзСписка = ИспользоватьНесколькоАЛЦ;
	
	АЛЦПользователя = Новый Массив;
	Объект.ТочкаМаршрута = 
		гкс_ПриемкаТранспорта.ПЛКизНастроекБазыПользователя(ИспользоватьНесколькоАЛЦ, АЛЦПользователя);
	Если ИспользоватьНесколькоАЛЦ
		И ЗначениеЗаполнено(АЛЦПользователя) Тогда
		СписокВыбора = Элементы.ОтборТочкаМаршрута.СписокВыбора;
		СписокВыбора.ЗагрузитьЗначения(АЛЦПользователя);
	КонецЕсли;	
  	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПараметрыДинамическихСписков()
	
	Если ЗначениеЗаполнено(ОтборВидПеревозки) Тогда
			
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиУсиленногоКонтроляКачества,
			"ВидПеревозки", ОтборВидПеревозки);
		
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиУсиленногоКонтроляКачества,
			"ТочкаМаршрута", Объект.ТочкаМаршрута);
										
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиУсиленногоКонтроляКачества,
			"Активен", ОтборАктивность);
		
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиУсиленногоКонтроляКачества,
			"ПроизводственныеСутки", ПроизводственныеСуткиПоДате());
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПараметрыДинамическихСписковОтключенияВходногоКонтроля()
	
	Если ЗначениеЗаполнено(ОтборВидПеревозки) Тогда
			
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиОтключенияВходногоКонтроляКачества,
			"ВидПеревозки", ОтборВидПеревозки);
		
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиОтключенияВходногоКонтроляКачества,
			"ТочкаМаршрута", Объект.ТочкаМаршрута);
										
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиОтключенияВходногоКонтроляКачества,
			"Активен", ОтборАктивность);
		
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(НастройкиОтключенияВходногоКонтроляКачества,
			"ПроизводственныеСутки", ПроизводственныеСуткиПоДате());
		
	КонецЕсли;
		
КонецПроцедуры
 
&НаКлиенте
Функция ОписаниеОповещенияОЗакрытииОтключенияВходногоКонтроля()
	Возврат Новый ОписаниеОповещения("ПослеЗакрытияФормыЗаписиОтключенияВходногоКонтроля", ЭтотОбъект);
КонецФункции
 
&НаКлиенте
Процедура ПослеЗакрытияФормыЗаписиОтключенияВходногоКонтроля(Результат, ДопПараметры) Экспорт
	УправлениеФормойОтключенияВходногоКонтроля();
КонецПроцедуры
 

&НаСервереБезКонтекста
Функция ПроизводственныеСуткиПоДате() 
	Возврат
		гкс_ПриемкаТранспорта.ПроизводственныеСуткиПоДата(ТекущаяДатаСеанса());
КонецФункции		

&НаСервере
Процедура УстановитьВидимостьДоступность()
	ЭтоЖД = Перечисления.гкс_ТипыТранспортныхСредствДоставки.ЭтоЖДПеревозка(ОтборВидПеревозки);
	Элементы.НастройкиУсиленногоКонтроляКачестваЖДСтанцияНаименование.Видимость = ЭтоЖД;
	Элементы.НастройкиУсиленногоКонтроляКачестваПунктПогрузки.Видимость = Не ЭтоЖД;
	
	ДоступностьРедактирования = Пользователи.РолиДоступны("гкс_НастройкаУсиленногоКонтроля");
	Элементы.НастройкиУсиленногоКонтроляКачестваСоздать.Доступность = ДоступностьРедактирования;
	Элементы.НастройкиУсиленногоКонтроляКачества.ТолькоПросмотр = Не ДоступностьРедактирования;
	
	ИспользоватьОтключениеВходногоКонтроля 
		= ПолучитьФункциональнуюОпцию("гкс_ИспользоватьОтключениеВходногоКонтроляКачества");
	Элементы.ГруппаОтключенияВходногоКонтроля.Видимость = ИспользоватьОтключениеВходногоКонтроля;
	Элементы.ГруппаУдостоверенияКачества.Видимость = ЭтоЖД;
	Элементы.ГруппаЗапретаВскрытияПоНоменклатуре.Видимость = ЭтоЖД;
	
	Если ИспользоватьОтключениеВходногоКонтроля Тогда
		Элементы.НастройкиОтключенияВходногоКонтроляКачестваЖДСтанция.Видимость = ЭтоЖД;
		Элементы.НастройкиОтключенияВходногоКонтроляКачестваПунктПогрузки.Видимость = Не ЭтоЖД;
		
		Элементы.НастройкиОтключенияВходногоКонтроляКачестваСоздатьОтключенияВходногоКонтроля.Доступность 
			= ДоступностьРедактирования;
		Элементы.НастройкиОтключенияВходногоКонтроляКачества.ТолькоПросмотр = Не ДоступностьРедактирования;
	КонецЕсли;
	
	ИспользоватьАвтоУстановкуЗапретаВскрытия
		= ПолучитьФункциональнуюОпцию("гкс_ИспользоватьАвтоустановкуЗапретаВскрытия");
	Элементы.ГруппаЗапретаВскрытияПоНоменклатуре.Видимость = ИспользоватьАвтоУстановкуЗапретаВскрытия;
	
КонецПроцедуры	

&НаСервере
Процедура ИнициализироватьНастройкиАвтообновления()
	
	// Инициализация соответствий страниц и списков настроек
	// для автоматического обновления настроек активной страницы
	
	// Установка начальных параметров для фильтрации активности
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
		НастройкиУсиленногоКонтроляКачества, "Активен", ОтборАктивность);
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
		НастройкиОтключенияВходногоКонтроляКачества, "Активен", ОтборАктивность);
	
КонецПроцедуры

&НаКлиенте
Процедура Создать(Команда)                                                            
	
	ПараметрыОткрытия = Новый Структура("ВидПеревозки, ТочкаМаршрута, ОткрытаИзОбработки", 
			ОтборВидПеревозки, Объект.ТочкаМаршрута, Истина);

		
	ОткрытьФорму("РегистрСведений.гкс_НастройкиУсиленногоКонтроляКачества.ФормаЗаписи", 
					ПараметрыОткрытия, ЭтотОбъект, УникальныйИдентификатор, Неопределено, Неопределено,
					ОписаниеОповещенияОЗакрытии, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
КонецПроцедуры

#Область СлужебныеПроцедурыИФункцииAвтообновления

// Процедура автообновления настроек активной страницы
&НаКлиенте
Процедура АвтообновлениеНастроек()
	
	АктивнаяСтраница = Элементы.ГруппаНастройки.ТекущаяСтраница;
	
	Если АктивнаяСтраница = Элементы.ГруппаУсиленныйКонтроль Тогда
		Элементы.НастройкиУсиленногоКонтроляКачества.Обновить();
	ИначеЕсли АктивнаяСтраница = Элементы.ГруппаОтключенияВходногоКонтроля Тогда
		Элементы.НастройкиОтключенияВходногоКонтроляКачества.Обновить();
	КонецЕсли;
	
	ПрименитьФильтрАктивностиДляАктивнойСтраницы();
	
КонецПроцедуры

// Процедура применения фильтра активности для активной страницы  
&НаКлиенте  
Процедура ПрименитьФильтрАктивностиДляАктивнойСтраницы()
	
	АктивнаяСтраница = Элементы.ГруппаНастройки.ТекущаяСтраница;
	
	Если АктивнаяСтраница = Элементы.ГруппаУсиленныйКонтроль Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
			НастройкиУсиленногоКонтроляКачества, "Активен", ОтборАктивность);
	ИначеЕсли АктивнаяСтраница = Элементы.ГруппаОтключенияВходногоКонтроля Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
			НастройкиОтключенияВходногоКонтроляКачества, "Активен", ОтборАктивность);
	ИначеЕсли АктивнаяСтраница = Элементы.ГруппаЗапретаВскрытияПоНоменклатуре Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
			НастройкиЗапретаВскрытияПоНоменклатуре, "Активен", ОтборАктивность);
	ИначеЕсли АктивнаяСтраница = Элементы.ГруппаУдостоверенияКачества Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
			НастройкиНоменклатурыКонтроляУдостоверенияКачества, "Активен", ОтборАктивность);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьНастройкуУК(Команда)
	ОткрытьФорму("Справочник.гкс_ПараметрыСозданияНастроекУсиленногоКонтроля.ФормаСписка");
КонецПроцедуры

#КонецОбласти

&НаСервере
Процедура УдалитьНастройкуНоменклатурыКонтроляУдостоверенияКачестваНаСервере(ТекущиеДанные)
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	НаборЗаписей = РегистрыСведений.гкс_НоменклатураКонтроляУдостоверенияКачества.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Номенклатура.Установить(ТекущиеДанные.Номенклатура);
	НаборЗаписей.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура УправлениеФормойЗапретаВскрытияПоНоменклатуре()
    ОбновитьНастройкиЗапретаВскрытияПоНоменклатуре();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьНастройкиЗапретаВскрытияПоНоменклатуре()
	Элементы.НастройкиЗапретаВскрытияПоНоменклатуре.Обновить();	
КонецПроцедуры	

&НаКлиенте
Функция ОписаниеОповещенияОЗакрытииЗапретаВскрытияПоНоменклатуре()
	Возврат Новый ОписаниеОповещения("ПослеЗакрытияФормыЗаписиЗапретаВскрытияПоНоменклатуре", ЭтотОбъект);
КонецФункции

&НаКлиенте
Процедура ПослеЗакрытияФормыЗаписиЗапретаВскрытияПоНоменклатуре(Результат, ДопПараметры) Экспорт
	УправлениеФормойЗапретаВскрытияПоНоменклатуре();
КонецПроцедуры

&НаСервере
Процедура УдалитьНастройкуЗапретаВскрытияПоНоменклатуреНаСервере(ТекущиеДанные)
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	НаборЗаписей = РегистрыСведений.гкс_НастройкиЗапретаВскрытияПоНоменклатуре.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Номенклатура.Установить(ТекущиеДанные.Номенклатура);
	НаборЗаписей.Отбор.ДопускКВскрытию.Установить(ТекущиеДанные.ДопускКВскрытию);
	НаборЗаписей.Отбор.ДатаНачала.Установить(ТекущиеДанные.ДатаНачала);
	НаборЗаписей.Отбор.ВидПеревозки.Установить(ТекущиеДанные.ВидПеревозки);
	НаборЗаписей.Отбор.ЖДСтанция.Установить(ТекущиеДанные.ЖДСтанция);
	НаборЗаписей.Отбор.ТранспортноеСредство.Установить(ТекущиеДанные.ТранспортноеСредство);
	НаборЗаписей.Отбор.Поставщик.Установить(ТекущиеДанные.Поставщик);
	НаборЗаписей.Записать();
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьКлючЗаписиЗапретаВскрытияПоНоменклатуре(Номенклатура, ДопускКВскрытию, ДатаНачала, ВидПеревозки, ЖДСтанция, ТранспортноеСредство, Поставщик)
	СтруктураКлюча = Новый Структура;
	СтруктураКлюча.Вставить("Номенклатура", Номенклатура);
	СтруктураКлюча.Вставить("ДопускКВскрытию", ДопускКВскрытию);
	СтруктураКлюча.Вставить("ДатаНачала", ДатаНачала);
	СтруктураКлюча.Вставить("ВидПеревозки", ВидПеревозки);
	СтруктураКлюча.Вставить("ЖДСтанция", ЖДСтанция);
	СтруктураКлюча.Вставить("ТранспортноеСредство", ТранспортноеСредство);
	СтруктураКлюча.Вставить("Поставщик", Поставщик);
	Возврат РегистрыСведений.гкс_НастройкиЗапретаВскрытияПоНоменклатуре.СоздатьКлючЗаписи(СтруктураКлюча);
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКлючЗаписиНоменклатурыКонтроляУдостоверенияКачества(Номенклатура)
	СтруктураКлюча = Новый Структура;
	СтруктураКлюча.Вставить("Номенклатура", Номенклатура);
	Возврат РегистрыСведений.гкс_НоменклатураКонтроляУдостоверенияКачества.СоздатьКлючЗаписи(СтруктураКлюча);
КонецФункции

&НаКлиенте
Функция ОписаниеОповещенияОЗакрытииНоменклатурыКонтроляУдостоверенияКачества()
	Возврат Новый ОписаниеОповещения("ПослеЗакрытияФормыЗаписиНоменклатурыКонтроляУдостоверенияКачества", ЭтотОбъект);
КонецФункции

&НаКлиенте
Процедура ПослеЗакрытияФормыЗаписиНоменклатурыКонтроляУдостоверенияКачества(Результат, ДопПараметры) Экспорт
	УправлениеФормойНоменклатурыКонтроляУдостоверенияКачества();
КонецПроцедуры

&НаКлиенте
Процедура УправлениеФормойНоменклатурыКонтроляУдостоверенияКачества()
    ОбновитьНастройкиНоменклатурыКонтроляУдостоверенияКачества();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьНастройкиНоменклатурыКонтроляУдостоверенияКачества()
	Элементы.НастройкиНоменклатурыКонтроляУдостоверенияКачества.Обновить();	
КонецПроцедуры	

#КонецОбласти
