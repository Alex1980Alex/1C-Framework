> ## Индекс документации
Полный индекс документации доступен по адресу: https://docs.langchain.com/llms.txt
Используйте этот файл, чтобы просмотреть все доступные страницы, прежде чем продолжить изучение.

# Наблюдаемость ЛангСмита

Трассировка — это последовательность шагов, которые ваше приложение выполняет для перехода от входных данных к выходным. Каждый из этих отдельных шагов представлен запуском. Вы можете использовать [LangSmith](https://smith.langchain.com/) для визуализации этих шагов выполнения. Чтобы использовать его, [включите трассировку для вашего приложения](/langsmith/trace-with-langgraph). Это позволит вам сделать следующее:

* [Отладка локально запущенного приложения](/langsmith/observability-studio#debug-langsmith-traces).
* [Оценка производительности приложения](/oss/python/langchain/evals).
* [Мониторинг приложения](/langsmith/dashboards).

## Предварительные условия

Прежде чем начать, убедитесь, что у вас есть следующее:

* **Учетная запись LangSmith**: Зарегистрируйтесь (бесплатно) или войдите в систему по адресу [smith.langchain.com](https://smith.langchain.com).
* **Ключ API LangSmith**: Следуйте инструкциям по созданию ключа API (/langsmith/create-account-api-key#create-an-api-key).

## Включить трассировку

Чтобы включить трассировку для вашего приложения, установите следующие переменные среды:

```python theme={null}
export LANGSMITH_TRACING=true
export LANGSMITH_API_KEY=<ваш-ключ-API>
```

По умолчанию трассировка будет записываться в проект с именем `default`. Чтобы настроить пользовательское имя проекта, см. [Запись в проект](#log-to-a-project).

Для получения дополнительной информации см. [Trace with LangGraph](/langsmith/trace-with-langgraph).

## Выборочная трассировка

Вы можете отслеживать конкретные вызовы или части вашего приложения, используя менеджер контекста `tracing_context` в LangSmith:

```python theme={null}
import langsmith as ls

# Это будет отслежено
с помощью ls.tracing_context(enabled=True):
    agent.invoke({"messages": [{"role": "user", "content": "Отправить тестовое письмо на адрес alice@example.com"}]})

# Эта трассировка НЕ ​​будет выполняться (если параметр LANGSMITH_TRACING не задан)
agent.invoke({"messages": [{"role": "user", "content": "Отправить еще одно письмо"}]})
```

## Запись в проект

<Accordion title="Statically">
  Вы можете задать пользовательское имя проекта для всего вашего приложения, установив переменную среды `LANGSMITH_PROJECT`:

  ```bash theme={null}
  export LANGSMITH_PROJECT=my-agent-project
  ```
</Аккордеон>

<Заголовок аккордеона="Динамически">
  Для конкретных операций можно программно задать имя проекта:

  ```python theme={null}
  import langsmith as ls

  with ls.tracing_context(project_name="email-agent-test", enabled=True):
      response = agent.invoke({
          "messages": [{"role": "user", "content": "Send welcome email"}]
      })
  ```
</Аккордеон>

## Добавление метаданных к трассировкам

Вы можете добавлять к своим трассировкам пользовательские метаданные и теги:

```python theme={null}
response = agent.invoke(
    {"messages": [{"role": "user", "content": "Отправить приветственное письмо"}]},
    config={
        "теги": ["production", "email-assistant", "v1.0"],
        "метаданные": {
            "user_id": "user_123",
            "session_id": "session_456",
            «окружающая среда»: «производство»
        }
    }
)
```

Функция `tracing_context` также принимает теги и метаданные для более точного управления:

```python theme={null}
с помощью ls.tracing_context(
    project_name="email-agent-test",
    enabled=True,
    tags=["production", "email-assistant", "v1.0"],
    metadata={"user_id": "user_123", "session_id": "session_456", "environment": "production"}):
    response = agent.invoke(
        {"messages": [{"role": "user", "content": "Отправить приветственное письмо"}]}
    )
```

Эти пользовательские метаданные и теги будут прикреплены к трассировке в LangSmith.

<Совет>
  Чтобы узнать больше о том, как использовать трассировку для отладки, оценки и мониторинга ваших агентов, см. [документацию LangSmith](/langsmith/home).
</Совет>

## Используйте анонимайзеры, чтобы предотвратить запись конфиденциальных данных в трассировки

Возможно, вам потребуется скрыть конфиденциальные данные, чтобы предотвратить их запись в LangSmith. Вы можете создать [анонимизаторы](/langsmith/mask-inputs-outputs#rule-based-masking-of-inputs-and-outputs) и применить их к
Создайте свой график, используя конфигурацию. В этом примере будут удалены все данные, соответствующие формату номера социального страхования XXX-XX-XXXX, из трассировок, отправленных в LangSmith.

```python Python theme={null}
from langchain_core.tracers.langchain import LangChainTracer
from langgraph.graph import StateGraph, MessagesState
from langsmith import Client
from langsmith.anonymizer import create_anonymizer

анонимайзер = create_anonymizer([
    # Соответствует номерам социального страхования
    { "pattern": r"\b\d{3}-?\d{2}-?\d{4}\b", "replace": "<ssn>" }
])

tracer_client = Client(anonymizer=anonymizer)
tracer = LangChainTracer(client=tracer_client)
# Определить граф
график = (
    StateGraph(MessagesState)
    ...
    .compile()
    .with_config({'callbacks': [tracer]})
)
```

***

<Callout icon="pen-to-square" iconType="regular">
  [Отредактируйте эту страницу на GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/langgraph/observability.mdx) или [сообщите о проблеме](https://github.com/langchain-ai/docs/issues/new/choose).
</Всплывающее сообщение>

<Tip icon="terminal" iconType="regular">
  [Подключите эти документы](/use-these-docs) к Claude, VSCode и другим сервисам через MCP для получения ответов в режиме реального времени.
</Совет>