# 09.7 Система хуков Claude Code

> Автоматизация рабочего процесса через хуки — обработчики событий,
> срабатывающие на действия Claude Code (ввод пользователя, вызов инструментов, завершение сессии).
>
> **Полная документация системы скиллов:** [11. Система скиллов](../11_СИСТЕМА_СКИЛЛОВ/11.1_Обзор.md) — маршрутизатор, enforcement, каталог, мониторинг.

---

## Обзор

Хуки — это Python-скрипты, которые автоматически запускаются при определённых событиях Claude Code. Они получают JSON на stdin, выполняют логику и влияют на поведение Claude через stdout (systemMessage) или exit code (block/allow).

**Три типа событий:**

| Событие | Когда срабатывает | Пример использования |
|---------|-------------------|---------------------|
| **UserPromptSubmit** | Пользователь отправляет сообщение | Рекомендация скиллов, напоминания |
| **PreToolUse / PostToolUse** | До / после вызова инструмента (Write, Edit, Bash...) | Enforcement, логирование, автокоммит |
| **Stop** | Claude пытается завершить сессию | Блокировка при незавершённых задачах |

---

## Архитектура: три уровня

Хуки образуют трёхуровневую защиту, где каждый уровень работает на своём этапе:

```
┌──────────────────────────────────────────────────────────────┐
│  Уровень 1: ВВОД ПОЛЬЗОВАТЕЛЯ (UserPromptSubmit)            │
│                                                              │
│  skill-router.py          → рекомендация скиллов             │
│  skill-eval-enforcer-shell.py → принуждение к активации      │
│  auto-git-save-prompt.py  → напоминание о коммите            │
│                                                              │
│  Результат: systemMessage (советы Claude перед работой)      │
├──────────────────────────────────────────────────────────────┤
│  Уровень 2: ИНСТРУМЕНТЫ (PreToolUse / PostToolUse)           │
│                                                              │
│  PRE:  code-skill-enforcer.py → BLOCK без скилла             │
│  POST: code-skill-enforcer.py → верификация результата       │
│  POST: auto-git-save.py       → автокоммит                   │
│                                                              │
│  Результат: block (exit 2) или systemMessage                 │
├──────────────────────────────────────────────────────────────┤
│  Уровень 3: ЗАВЕРШЕНИЕ (Stop)                                │
│                                                              │
│  auto-git-save.py         → блокировка при uncommitted       │
│  git-commit-enforcer.py   → проверка git status              │
│  docs-change-enforcer.py  → проверка покрытия документацией  │
│  task-enforcer.py         → проверка pending задач           │
│  ralph_wiggum_stop.py     → контроль Ralph-итераций          │
│                                                              │
│  Результат: block (exit 2) или allow (exit 0)               │
└──────────────────────────────────────────────────────────────┘
```

---

## Полный список хуков

### UserPromptSubmit (при вводе пользователя)

| Хук | Назначение | Timeout |
|-----|-----------|---------|
| `skill-router.py` | Анализирует промпт, рекомендует скиллы из `skill-router-config.json` | 3s |
| `skill-eval-enforcer-shell.py` | Проверяет, активировал ли Claude рекомендованные скиллы | 3s |
| `auto-git-save-prompt.py` | Напоминает о незакоммиченных файлах | 3s |

### PreToolUse (перед вызовом инструмента)

| Хук | Matcher | Назначение | Timeout |
|-----|---------|-----------|---------|
| `code-skill-enforcer.py` | `Write\|Edit\|Bash` | Блокирует код без активации скилла (уровни A-C) | 3s |

### PostToolUse (после вызова инструмента)

| Хук | Matcher | Назначение | Timeout |
|-----|---------|-----------|---------|
| `auto-git-save.py` | `Write\|Edit` | Автокоммит изменённых файлов | 15s |
| `auto-git-save.py` | `Bash` | Детекция ручного `git commit` | 15s |
| `code-skill-enforcer.py` | `Write\|Edit` | Верификация кода против скилла (уровень E-F) | 3s |
| `code-skill-enforcer.py` | `WebSearch\|WebFetch` | Напоминание о кеше (уровень D) | 3s |

### Stop (при завершении сессии)

| Хук | Назначение | Timeout |
|-----|-----------|---------|
| `auto-git-save.py` | Финальная проверка uncommitted файлов | 15s |
| `git-commit-enforcer.py` | Блокировка при dirty git status | 5s |
| `docs-change-enforcer.py` | Блокировка при неоформленных изменениях кода | 5s |
| `task-enforcer.py` | Блокировка при pending mandatory задачах | 10s |
| `ralph_wiggum_stop.py` | Контроль итераций Ralph Wiggum | 5s |

---

## Протокол взаимодействия

### Входные данные (stdin)

Хук получает JSON на stdin с информацией о событии:

```json
{
  "session_id": "abc-123",
  "tool_name": "Write",
  "tool_input": {
    "file_path": "/path/to/file.py",
    "content": "import langchain..."
  }
}
```

### Выходные данные (stdout + exit code)

| Exit code | Значение | Stdout |
|-----------|---------|--------|
| **0** | Разрешить (allow) | Текст → systemMessage для Claude |
| **2** | Заблокировать (block) | Текст → причина блокировки |

**Пример stdout (systemMessage):**
```
SKILL RECOMMENDATION: Активируй скилл 'langgraph-core' через Skill('langgraph-core')
```

**Пример stdout (block):**
```
BLOCKED: Код содержит StateGraph. Сначала активируй скилл 'langgraph-core'.
```

---

## BaseHook — базовый класс

Все хуки наследуют `BaseHook` из `.claude/hooks/base/base.py`:

```python
from base.base import BaseHook, HookInput, HookOutput

class MyHook(BaseHook):
    def process(self, inp: HookInput) -> HookOutput:
        # inp.tool_name — название инструмента
        # inp.tool_input — параметры инструмента
        # inp.detected_event — тип события

        out = HookOutput()
        out.system_message("Совет для Claude")
        return out
```

**Возможности BaseHook:**
- Автоматический парсинг stdin в `HookInput`
- UTF-8 safe flush (без падений на Windows cp1251)
- Таймер `elapsed_ms` для замеров latency
- Автологирование в `data/hook-invocations.jsonl`

**Файл:** `.claude/hooks/base/base.py`

---

## Invocation Logging (JSONL)

Каждый вызов хука записывается в `data/hook-invocations.jsonl`:

```json
{
  "ts": "2026-02-22T14:30:00.123456",
  "hook": "SkillRouter",
  "event": "UserPromptSubmit",
  "tool": null,
  "elapsed_ms": 45,
  "outcome": "message",
  "session": "abc123",
  "agent_id": null,
  "error": null
}
```

**Особенности:**
- Append-only формат (crash-safe)
- Автоматическая ротация при 10MB
- Поддержка `agent_id` для субагентов
- Overhead: 0-3ms на запись

**Файл:** `.claude/hooks/shared/invocation_logger.py`

---

## Защита от бесконечных циклов

### Проблема

Stop-хуки могут создавать бесконечный цикл:
```
ralph_wiggum_stop → изменяет .ralph_wiggum_count →
  git-commit-enforcer → видит изменение → блокирует →
    коммит → ralph снова...
```

### Решение

1. **`.gitignore`** — файлы `.ralph_*` исключены из git
2. **`auto-git-save.py`** — игнорирует `.ralph_active`, `.ralph_criteria.json`, `.ralph_wiggum_count`
3. **`docs-change-enforcer.py`** — пропускает `.ralph_` файлы
4. **`ralph_state.py`** — `stop_lock` предотвращает повторный вход

---

## IDE Filtering

Хук `skill-router.py` фильтрует IDE-события, чтобы не засорять рекомендации:

**Фильтруемые префиксы:**
```
<file_>        — открытие файла в IDE
<selection>    — выделение текста
<cursor>       — позиция курсора
<ide_opened_file> — событие VS Code
```

Также фильтруется любой текст, начинающийся с XML-тегов (эвристика).

---

## Субагенты

Субагенты (Task tool) используют отдельную конфигурацию хуков для предотвращения конфликтов:

**Файл:** `.claude/settings-subagent.json`

Отличия от основных настроек:
- Сокращённый набор хуков (только skill-router, invocation-logger)
- `disabledHooks` для потенциально зацикливающихся хуков
- Поле `agent_id` в логах для идентификации

```python
# Логирование с agent_id
from shared.invocation_logger import log_invocation

log_invocation(
    hook="agent:bsl-debugger",
    event="AgentStart",
    agent_id="sub-abc-789",
)
```

---

## OpenTelemetry Tracing

Для production-мониторинга доступен OTLP exporter:

```bash
# Console export (разработка)
export OTEL_EXPORTER_CONSOLE=true

# OTLP export (production)
export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces
export OTEL_SERVICE_NAME=claude-hooks
```

```python
from shared.otel_exporter import HookTracer

tracer = HookTracer()
with tracer.span("SkillRouter", event="UserPromptSubmit"):
    # ... логика хука ...
    pass
```

**Поддерживаемые backends:** Console, SigNoz, Grafana Tempo, Jaeger, Langfuse.

**Файл:** `.claude/hooks/shared/otel_exporter.py`

---

## Dashboards

### CLI Dashboard

```bash
python scripts/hook-dashboard.py              # Текстовый отчёт
python scripts/hook-dashboard.py --period 7d  # За 7 дней
python scripts/hook-dashboard.py --json       # JSON для CI
```

### Streamlit Dashboard

```bash
pip install streamlit plotly pandas  # зависимости (однократно)
streamlit run src/ui/pages/hook_dashboard.py
```

5 вкладок: Overview, Hook Invocations, Skill Activations, Latency Analysis, Error Log.

### Skill Enforcement Dashboard

```bash
python scripts/skill-enforcement-dashboard.py
```

Метрики code-skill-enforcer: блокировки по уровням, покрытие паттернами, accuracy.

---

## Evaluation Framework

Автоматическое тестирование хуков (40 тестов, 5 suites):

```bash
python scripts/eval-hooks.py --list-suites           # Список suites
python scripts/eval-hooks.py --suite ide-filtering    # Один suite
python scripts/eval-hooks.py --json                   # JSON для CI
```

| Suite | Тестов | Что проверяет |
|-------|--------|--------------|
| `skill-activation` | 20 | Рекомендация и активация 16 реальных скиллов |
| `stop-hook-safety` | 3 | Stop-lock, предотвращение бесконечных циклов |
| `ide-filtering` | 10 | Фильтрация IDE-событий |
| `git-commit-enforcement` | 2 | Блокировка при uncommitted changes |
| `docs-change-enforcement` | 3 | Проверка покрытия документацией |

**Файлы:** `scripts/eval-hooks.py`, `tests/eval/hook_prompts.json`

---

## Регистрация хуков

Все хуки регистрируются в `.claude/settings.json` (или `.claude/settings.local.json` для локальных):

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [{
          "type": "command",
          "command": "python .claude/hooks/skill-router.py",
          "timeout": 3
        }]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [{
          "type": "command",
          "command": "python .claude/hooks/code-skill-enforcer.py",
          "timeout": 3
        }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{
          "type": "command",
          "command": "python .claude/hooks/auto-git-save.py",
          "timeout": 15
        }]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/task-enforcer.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

---

## Файлы системы

| Файл | Описание |
|------|----------|
| `.claude/hooks/base/base.py` | BaseHook, HookInput, HookOutput — базовый класс |
| `.claude/hooks/base/protocol.py` | Абстрактный протокол, auto-logging |
| `.claude/hooks/shared/invocation_logger.py` | JSONL логирование вызовов |
| `.claude/hooks/shared/session_state.py` | Состояние сессии, метрики, pending_learn |
| `.claude/hooks/shared/ralph_state.py` | Управление состоянием Ralph Wiggum |
| `.claude/hooks/shared/otel_exporter.py` | OpenTelemetry OTLP exporter |
| `.claude/hooks/shared/task_master.py` | API управления задачами |
| `.claude/hooks/shared/code-skill-patterns.json` | Конфиг паттернов (7 секций) |
| `.claude/hooks/shared/trust_scorer.py` | Оценка доверия источников |
| `.claude/hooks/skill-router.py` | Рекомендация скиллов на UserPromptSubmit |
| `.claude/hooks/skill-eval-enforcer-shell.py` | Принуждение к активации скиллов |
| `.claude/hooks/code-skill-enforcer.py` | Skill-First Enforcement (6 уровней A-F) |
| `.claude/hooks/auto-git-save.py` | Автокоммит (PostToolUse + Stop) |
| `.claude/hooks/auto-git-save-prompt.py` | Напоминания о коммите (UserPromptSubmit) |
| `.claude/hooks/git-commit-enforcer.py` | Проверка git status (Stop) |
| `.claude/hooks/docs-change-enforcer.py` | Проверка документации (Stop) |
| `.claude/hooks/task-enforcer.py` | Проверка pending задач (Stop) |
| `.claude/hooks/ralph_wiggum_stop.py` | Контроль Ralph-итераций (Stop) |
| `src/pdf_framework/observability/hook_metrics_db.py` | SQLite инжест hook-логов |
| `src/ui/pages/hook_dashboard.py` | Streamlit dashboard |
| `scripts/hook-dashboard.py` | CLI dashboard |
| `scripts/eval-hooks.py` | Evaluation runner |
| `scripts/skill-enforcement-dashboard.py` | Enforcement dashboard |

---

## Навигация

- **Назад:** [09.6 Auto Git Save](09.6_Auto_Git_Save.md)
- **Далее:** [09.8 Skill-First Enforcement](09.8_Skill_First_Enforcement.md)
- **Содержание:** [00 Содержание](../00_СОДЕРЖАНИЕ.md)
