# 11.4 Skill Enforcement

> 6 уровней enforcement (A–F), Task Protocol, паттерны блокировки,
> Trust Scorer, Research Protocol.

---

## Обзор

Enforcement реализован двумя хуками:
- **Code Skill Enforcer** ([code-skill-enforcer.py](../../../.claude/hooks/code-skill-enforcer.py)) — 6 уровней A-F
- **Task Protocol Enforcer** ([task-protocol-enforcer.py](../../../.claude/hooks/task-protocol-enforcer.py)) — gate на Write/Edit

Оба работают через `PreToolUse` / `PostToolUse` события и координируются через `SessionState`.

---

## 6 уровней A–F

```
┌────────────────────────────────────────────────────────────────────┐
│                      PRE MODE (блокирует)                          │
│                                                                    │
│  Level B: Directory Rules                                          │
│  ─────────────────────────                                         │
│  Файл в .claude/hooks/ → требует Skill('create-hook')             │
│  Файл в src/agents/    → требует Skill('langgraph-core')          │
│  Проверяется ПЕРВЫМ (самый специфичный)                            │
│                                                                    │
│  Level A: Content Patterns                                         │
│  ────────────────────────                                          │
│  Код содержит StateGraph → требует Skill('langgraph-core')        │
│  Код содержит QdrantClient → требует Skill('qdrant-operations')   │
│  Regex по содержимому файла                                        │
│                                                                    │
│  Level A.1: Research Protocol Trigger (БЛОКИРУЕТ)                  │
│  ──────────────────────────────────────────────────                 │
│  FastAPI/Redis/Pytest/1C Metadata → BLOCK + Skill('learning-loop')│
│  Паттерны БЕЗ dedicated скиллов → немедленный learning cycle      │
│  Dedup: learn:<label> в activated_skills → не блокирует повторно  │
│                                                                    │
│  Level C: Bash Commands                                            │
│  ──────────────────────                                            │
│  docker compose → требует Skill('deployment')                     │
│  pytest → требует Skill('testing')                                │
│  Regex по команде Bash                                             │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│                     POST MODE (advisory)                           │
│                                                                    │
│  Level D: Research Cache Reminder                                  │
│  ───────────────────────────────                                   │
│  WebSearch/WebFetch → напоминание проверить кеш                   │
│  .claude/skills/tech-research/cache/                               │
│                                                                    │
│  Level E: Post-Verification                                        │
│  ──────────────────────────                                        │
│  must_contain: проверка обязательных паттернов                     │
│  must_not_contain: проверка запрещённых паттернов                  │
│  Только для активированных скиллов                                 │
│                                                                    │
│  Level F: LEARN Phase                                              │
│  ────────────────────                                              │
│  Если pending_learn → создаёт задачи через TaskCreate             │
│  [LEARN] Create skill + Register in config                        │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## Level B: Directory Rules (PRE)

Проверяет путь файла перед записью. Самый приоритетный уровень.

Конфигурация: `code-skill-patterns.json` → `directory_rules.mappings`

| Prefix | Required Skill | Label |
|--------|---------------|-------|
| `.claude/hooks/` | create-hook | Hook Development |
| `.claude/commands/` | create-slash-command | Slash Command Creation |
| `.claude/skills/` | create-skill | Skill Creation |
| `src/agents/` | langgraph-core | LangGraph Agent Development |
| `src/graph_store/` | graph-databases | Graph Database Operations |
| `docker/` | deployment | Docker/Deployment |
| `.github/workflows/` | claude-code-github-actions | CI/CD Pipelines |
| `tests/` | testing | Testing & Test Infrastructure |
| `docs/adr/` | adr | Architecture Decision Records |

**Алгоритм:**
1. Нормализовать путь (replace `\` → `/`, strip project root)
2. Проверить `normalized.startswith(prefix)` для каждого правила
3. Если скилл уже активирован (`SessionState.is_skill_activated`) → пропустить
4. Иначе → `BLOCK` с инструкцией `Skill('X')`

---

## Level A: Content Patterns (PRE)

Проверяет содержимое записываемого кода (regex).

Конфигурация: `code-skill-patterns.json` → `patterns.mappings`

| Pattern (regex) | Required Skill | Domain |
|-----------------|---------------|--------|
| `StateGraph\|CompiledStateGraph\|StateGraphCheckpoint` | langgraph-core | tech |
| `QdrantClient\|QdrantAsync\|AsyncQdrantClient` | qdrant-operations | tech |
| `class\s+\w+Hook\s*\(.+\):\|def\s+run_hook` | create-hook | framework |
| `BaseHook\|HookInput\|HookOutput` | create-hook | framework |
| `Skill\s*\(\|skill:\s*\w+` | create-skill | framework |
| `mcp__\w+__\|@mcp_server\|class\s+\w+MCP` | mcp-development | framework |
| `СправочникМенеджер\.\|ДокументыМенеджер\.` | 1c-object-map | 1c |
| `ВЫБРАТЬ\|СУММА\|КОЛИЧЕСТВО\|МАКСИМУМ\|ЕСТЬNULL` | 1c-query-language | 1c |
| `Новый\s+(Запрос\|ТаблицаЗначений\|Структура)` | 1c-development | 1c |
| `ОбработкаПроведения\|Движения\.\(Выполнить\|Записать\)` | 1c-document-posting | 1c |
| `Формат\(\|СтрШаблон\(\|СтрСоединить\(` | bsl-string-formatting | 1c |
| `КонструкторОписанияТипов\|КвалификаторыЧисла` | bsl-type-system | 1c |
| `HTTPСервис\|ВходящийJSON\|HTTPОтвет` | 1c-http-services | 1c |
| `СКД\|СхемаКомпоновкиДанных\|КомпоновщикНастроек` | 1c-skd | 1c |

**Всего:** 14 паттернов

**Алгоритм:**
1. Извлечь content из `tool_input.content` (Write) или `tool_input.new_string` (Edit)
2. Пропустить если content < 20 символов
3. Проверить regex (compiled, IGNORECASE)
4. Если скилл активирован → пропустить
5. Иначе → `BLOCK`

---

## Level A.1: Research Protocol Trigger (PRE, non-blocking)

Для тем **без** dedicated скиллов (FastAPI, Redis, 1C Metadata и др.) — устанавливает `pending_learn` в `SessionState`, подготавливая данные для Level F.

**Не блокирует** — только побочный эффект (set state).

Конфигурация: `code-skill-patterns.json` → `research_protocol.patterns`

| Pattern (regex) | Label | Domain |
|-----------------|-------|--------|
| `FastAPI\|APIRouter\|from\s+fastapi` | FastAPI Framework | tech |
| `Redis\.\|from\s+redis\|aioredis` | Redis Caching | tech |
| `Справочник\s+\|Документ\s+\|Регистр\s+(накопления\|сведений)` | 1C Metadata Objects | 1c |
| `КомпоновкаДанных\|СхемаКомпоновки` | 1C Data Composition | 1c |
| `Dockerfile\|docker-compose\.yml\|FROM\s+\w+:` | Docker Configuration | devops |
| `@fixture\|conftest\.py` | Test Framework | testing |

**Всего:** 6 паттернов

**Алгоритм:**
1. Проверить `tool_name` in (Write, Edit)
2. Извлечь content; пропустить если < 30 символов
3. Пропустить если Level A уже нашёл совпадение (есть dedicated skill)
4. Пропустить если `SessionState.has_pending_learn()` (не перезаписывать)
5. Проверить regex (compiled, IGNORECASE)
6. При совпадении → `SessionState.set_pending_learn({label, domain, pattern})`

**Связь с Level F:** На следующем `PostToolUse:Write|Edit` Level F обнаружит `pending_learn` и создаст задачи `[LEARN]` для создания нового скилла.

**Тест:** `scripts/test-learn-cycle.py` (8 тестов, 18 assertions)

---

## Level C: Bash Commands (PRE)

Проверяет bash-команды перед выполнением.

| Pattern (regex) | Required Skill | Label |
|-----------------|---------------|-------|
| `docker\s+(compose\|build\|push\|tag)` | deployment | Docker Operations |
| `kubectl\|helm\|kubectx` | kubernetes | Kubernetes Operations |
| `pytest\|coverage\.py\|tox` | testing | Python Testing |
| `npm\s+(publish\|version\|audit)` | npm-publishing | NPM Package Publishing |
| `git\s+(tag\|push\|release)` | git-release | Git Release Management |
| `eslint\|prettier\|black\s+\.\|ruff` | code-quality | Code Quality Tools |
| `mypy\|pyright\|pylint` | type-checking | Type Checking & Linting |

**Всего:** 7 правил

---

## Level D: Research Cache Reminder (POST)

При использовании WebSearch или WebFetch — advisory напоминание:

```
[TIP] Check research cache before using search results:
.claude/skills/tech-research/cache/
```

Не блокирует — только system_message.

---

## Level E: Post-Verification (POST)

Верифицирует записанный код против правил скилла.

Конфигурация: `code-skill-patterns.json` → `post_verification.rules`

**Пример: langgraph-core**
```json
{
  "name": "langgraph-core",
  "file_pattern": "src/agents/.*\\.py",
  "must_contain": [
    "from langgraph",
    "StateGraph",
    "invoke\\s*\\(",
    "checkpointer\\s*="
  ],
  "must_not_contain": [
    "StateGraph\\s*\\(\\s*\\)",
    "node\\s*\\(\\s*\"\"",
    "edge\\s*\\(\\s*\\)"
  ],
  "error_message": "LangGraph agent missing required patterns..."
}
```

**Алгоритм:**
1. Проверить `tool_name` in (Write, Edit)
2. Проверить `file_pattern` regex match
3. Проверить что скилл в `activated_skills`
4. Для каждого `must_contain` — проверить наличие
5. Для каждого `must_not_contain` — проверить отсутствие
6. При нарушении → `system_message` (advisory, не block)

**Текущие правила:** langgraph-core, create-hook, qdrant-operations

---

## Level F: LEARN Phase (POST)

Создаёт задачи для создания нового скилла, если `SessionState.pending_learn` установлен.

**Условия срабатывания:**
1. `tool_name` in (Write, Edit)
2. `SessionState.pending_learn` не None
3. Нет recent completion за последние 10 минут (cooldown)

**Создаваемые задачи:**
```
[LEARN] Create skill for '<label>'
[LEARN] Register '<label>' in skill-router-config.json
```

После создания задач — `SessionState.clear_pending_learn()`.

---

## Task Protocol

Независимый enforcer, работающий параллельно с уровнями A-F.

### Фазы протокола

```
┌──────┐    classify    ┌────────────┐   TaskCreate   ┌────────────┐
│ idle │───────────────>│ classified │──────────────>│ decomposed │
└──────┘                └────────────┘               └──────┬─────┘
                                │                          │
                                │          Skill()         │   Skill()
                                └────────────────┐         │
                                                 ▼         ▼
                                         ┌─────────────────────┐
                                         │   skill_checked     │
                                         │  [ALLOW Write/Edit] │
                                         └─────────────────────┘
```

| Переход | Кто выполняет | Механизм |
|---------|--------------|----------|
| idle → classified | skill-eval-enforcer-shell.py | `SessionState.set_task_classified(complexity)` |
| classified → decomposed | task-protocol-observer.py | `SessionState.record_decomposition()` при TaskCreate |
| * → skill_checked | task-protocol-observer.py | `SessionState.record_skill_checked()` при Skill() |
| * → idle | skill-eval-enforcer-shell.py | `SessionState.reset_task_protocol()` при новом промпте |

### Сообщения блокировки

В зависимости от текущей фазы, enforcer выдаёт контекстно-зависимую подсказку:

| Фаза | Подсказка |
|------|----------|
| `idle` | Classify → Search skills → TaskCreate → Retry |
| `classified` | Skills not checked! → Search skills → Skill() → Retry |
| `decomposed` | Decomposed but skills not checked! → Skill() → Retry |

### Исключения

Task Protocol Enforcer пропускает файлы в:
- `.claude/`, `docs/`, `data/`, `.github/` (каталоги)
- `.json`, `.toml`, `.yml`, `.env`, `.md`, `.txt` и др. (расширения)
- Dotfiles (начинаются с `.`)

---

## Trust Scorer

Модуль [trust_scorer.py](../../../.claude/hooks/shared/trust_scorer.py) оценивает достоверность
информации из внешних источников.

### Источники и рубрики

| Источник | Trust Score | Критерии |
|----------|-----------|----------|
| **Context7** | 1.0 (всегда) | Официальная документация, всегда доверяем |
| **GitHub** | 0.0–1.0 | stars (30%), last_commit_days (25%), has_docs (25%), has_license (20%) |
| **StackOverflow** | 0.0–1.0 | is_accepted (40%), score (30%), recency_days (30%) |
| **Infostart** | 0.0–1.0 | rating (35%), downloads (35%), recency_days (30%) |

### Пороговые значения

**GitHub:**
- stars >= 100 → full credit
- last_commit_days <= 90 → full credit
- has_license in [MIT, Apache, BSD, ISC] → full credit

**StackOverflow:**
- is_accepted = true → full credit
- score >= 3 → full credit
- recency_days <= 365 → full credit

**Infostart:**
- rating >= 4.0 → full credit
- downloads >= 100 → full credit
- recency_days <= 730 → full credit

### Research Protocol (fallback)

Для доменов без dedicated скиллов — порядок источников:

| Домен | Порядок источников |
|-------|-------------------|
| **tech** | Context7 documentation → StackOverflow → GitHub repositories |
| **1c** | Infostart.ru publications → GitHub repositories → its.1c.ru |

---

## Как BLOCK работает

```python
# PreToolUse hook
return HookOutput().block(
    f"SKILL REQUIRED: '{label}' ({skill})\n"
    f"Detected pattern: {pattern}...\n\n"
    f"ACTIVATE THIS SKILL:\n"
    f"  Skill('{skill}')\n\n"
    f"Then retry your action."
)
```

1. `HookOutput.block()` устанавливает `{"continue": false, "decision": "block"}`
2. `emit()` записывает JSON в stdout
3. Hook завершается с exit code 2
4. Claude Code получает сообщение блокировки
5. Claude видит причину и инструкцию → вызывает `Skill('X')` → повторяет действие

---

## Статистика паттернов

Данные из `code-skill-patterns.json` → `metadata.stats`:

| Секция | Количество |
|--------|-----------|
| Content patterns (Level A) | 14 |
| Directory rules (Level B) | 9 |
| Bash rules (Level C) | 7 |
| Research rules (Level D) | 4 |
| Research protocol (fallback) | 6 |
| Post-verification (Level E) | 3 |
| **Всего** | **43** |
