## 16.5 MCP-серверы для 1С

Раздел описывает интеграцию фреймворка с AI-агентами посредством протокола MCP. В экосистему проекта входят четыре специализированных MCP-сервера, обеспечивающих взаимодействие AI с платформой 1С, поиск по исходному коду и получение справочной информации.

### Обзор MCP-интеграции

**MCP (Model Context Protocol)** — открытый стандарт протокола прикладного уровня, предназначенный для обеспечения стандартизированного взаимодействия между языковыми моделями (LLM) и внешними системами. Использование MCP позволяет AI-агентам выполнять запросы к информационным базам 1С, анализировать метаданные и работать с исходным кодом в автоматическом режиме.

В рамках проекта реализованы и поддерживаются следующие 4 MCP-сервера:
*   `1c-mcp-toolkit` — выполнение кода, запросов, работа с метаданными и событиями внутри 1С.
*   `bsl-code-search` — структурный семантический поиск по исходному коду на языке BSL.
*   `bsl-platform-context` — предоставление контекста и справочной информации по API платформы 1С:Предприятие.
*   `METR` (mcp-yaxunit-runner) — автоматическая сборка, синтаксическая проверка и запуск тестов YaXUnit.

---

### 1c-mcp-toolkit

Основной сервер для прямого взаимодействия AI-агента с информационной базой 1С:Предприятие. Доступен в двух вариантах: внешняя обработка (.epf) и расширение конфигурации.

#### Назначение и инструменты

Сервер предоставляет 13 инструментов (MCP tools) для полнофункционального доступа к данным и структуре 1С:

**Работа с данными:**
*   `execute_query` — выполнение произвольных запросов на языке запросов 1С с параметрами и схемой колонок.
*   `execute_code` — выполнение встроенного кода на языке BSL (результат через переменную `Результат`).
*   `validate_query` — проверка синтаксиса запроса без выполнения.

**Метаданные и структура:**
*   `get_metadata` — чтение структуры метаданных конфигурации (сводка / список по типу / детали объекта).
*   `get_metadata_tree` — компактное дерево всех метаданных (18 коллекций) с подсчётом объектов.
*   `get_form_structure` — структура форм объекта метаданных (список форм, реквизиты и команды основной формы).
*   `search_code` — поиск по метаданным конфигурации: имена объектов, синонимы, реквизиты, табличные части.

**Навигация по объектам:**
*   `get_object_by_link` — получение полных данных объекта 1С по навигационной ссылке.
*   `get_link_of_object` — генерация навигационной ссылки из описания объекта.
*   `find_references_to_object` — поиск всех ссылок на объект в документах, справочниках и регистрах.

**Администрирование:**
*   `get_event_log` — чтение журнала регистрации с фильтрацией по дате, уровню, событиям и пользователям.
*   `get_access_rights` — получение прав доступа к объектам метаданных по ролям.
*   `get_bsl_syntax_help` — справочник по встроенному языку BSL (30+ функций с синтаксисом и примерами).

#### Вариант 1: Расширение MCPToolkit (рекомендуется)

Расширение конфигурации с HTTP-сервисом. Автоматически доступно при публикации базы на веб-сервере — не требует ручного запуска, форм или кнопок.

```
Claude Code ──stdio──> proxy.py ──HTTP POST──> IIS/Apache ──> HTTP-сервис расширения ──> База данных
```

**Преимущества перед .epf:**
*   Автоматический запуск — сервис доступен сразу после публикации базы.
*   Не требует открытия формы обработки и ручного нажатия «Запустить».
*   Работает в серверном режиме без интерактивного сеанса пользователя.
*   Не зависит от NativeAPI компоненты.

**Структура расширения:**
*   `mcp_MCPToolkitСервер` — серверный общий модуль (~1680 строк): маршрутизация MCP-методов, реализация 13 инструментов.
*   `mcp_MCPToolkitСериализация` — серверный общий модуль: безопасная сериализация значений 1С в JSON (ссылки, массивы, структуры, таблицы значений, перечисления).
*   `mcp_MCP` — HTTP-сервис с шаблоном `/endpoint` (метод POST), обработчик JSON-RPC 2.0.

**Установка расширения:**

1.  Добавьте расширение `MCPToolkit` в конфигураторе: Конфигурация → Расширения конфигурации → Добавить.
2.  Загрузите из XML: Конфигурация → Загрузить конфигурацию из файлов → путь к `src/`.
3.  Обновите конфигурацию БД (F7).
4.  Опубликуйте базу с HTTP-сервисами расширений: Администрирование → Публикация на веб-сервере → вкладка «HTTP сервисы» → галочка «Публиковать HTTP сервисы расширений по умолчанию» → Опубликовать.

**Конфигурация Claude Code:**

В файле `.claude/mcp-servers.json` добавьте stdio-прокси:

```json
{
  "mcpServers": {
    "1c-mcp-toolkit": {
      "command": "python",
      "args": ["scripts/claude-backend/mcp-1c-proxy/proxy.py"],
      "cwd": "D:\\1C-Enterprise_Framework"
    }
  }
}
```

Прокси `proxy.py` преобразует stdio MCP-протокол в HTTP POST запросы к эндпоинту `http://localhost/<база>/hs/mcptoolkit/endpoint` с Basic-авторизацией.

**URL HTTP-сервиса:** `http://<сервер>/<публикация>/hs/mcptoolkit/endpoint`

#### Вариант 2: Внешняя обработка (.epf) v1.5.0

Компонент реализован в виде внешней обработки `.epf`, содержащей встроенный NativeAPI HTTP-сервер. Архитектура не требует публикации на веб-сервере. По умолчанию сервер принимает входящие соединения на порту `6003`.

```
Claude Code ──MCP (Streamable HTTP)──> .epf в 1С (NativeAPI :6003) ──> База данных
```

Обработка содержит 8 из 13 инструментов расширения (без `validate_query`, `get_form_structure`, `get_bsl_syntax_help`, `search_code`, `get_metadata_tree`). Дополнительно предоставляет:
*   `submit_for_deanonymization` — отправка данных на деанонимизацию (779 правил).
*   Формат ответов **TOON** — экономия 30-60% токенов по сравнению с JSON.
*   Механизм анонимизации (`[ORG-00001]`, `[INN-00001]`, `[PER-00001]`).
*   Интерактивную форму с настройками порта, режима, журналом событий.

**Установка и запуск:**

1.  Откройте файл `tools/1c-mcp-toolkit/MCP_Toolkit_v1.5.0.epf` в 1С:Предприятие.
2.  В форме обработки выберите режим работы: «Встроенный сервер».
3.  Укажите порт: `6003`.
4.  Нажмите кнопку «Запустить».

**Конфигурация:**

```json
{
  "mcpServers": {
    "1c-mcp-toolkit": {
      "url": "http://localhost:6003/mcp",
      "transport": "streamable-http"
    }
  }
}
```

#### Сравнение вариантов

| Характеристика | Расширение MCPToolkit | Обработка .epf v1.5.0 |
| :--- | :--- | :--- |
| Автозапуск | Да (через веб-сервер) | Нет (ручной запуск формы) |
| Требует веб-сервер | Да (IIS/Apache) | Нет (встроенный NativeAPI) |
| Интерактивный сеанс | Не нужен | Нужен (форма открыта) |
| Инструментов | **13** | 9 (+деанонимизация) |
| Уникальные инструменты | `validate_query`, `get_form_structure`, `get_bsl_syntax_help`, `search_code`, `get_metadata_tree` | `submit_for_deanonymization` |
| Анонимизация | Нет | Да (779 правил) |
| Формат TOON | Нет | Да (экономия 30-60%) |
| Изменение конфигурации | Расширение (AddOn) | Не требуется |

---

### bsl-code-search

Сервер для статического анализа и поиска по исходному коду модулей 1С без необходимости подключения к информационной базе.

#### Назначение и инструменты

Обеспечивает структурный поиск по коду с использованием синтаксического дерева. Включает 3 инструмента:
*   `search_symbols` — поиск символов (функций, процедур, переменных) по кодовой базе.
*   `find_callers` — поиск мест вызова конкретной функции или процедуры.
*   `get_module_ast` — получение абстрактного синтаксического дерева (AST) указанного модуля.

#### Технические детали

Парсинг кода выполняется с использованием библиотеки `tree-sitter-bsl` версии 0.1.6. Скорость построения AST составляет от 1 до 23 мс на файл, что позволяет обрабатывать крупные проекты в режиме реального времени.

#### Расположение

Исходный код сервера находится в директории: `tools/bsl-code-search/`.

---

### bsl-platform-context

Сервер контекстной справки, предоставляющий AI-агентам актуальную документацию по методам платформы 1С:Предприятие.

#### Назначение и инструменты

Содержит актуальную документацию по API платформы версии 1С:8.3.27. Позволяет AI избегать галлюцинаций при генерации кода за счет точного знания сигнатур методов и их поведения.

Доступные инструменты:
*   `search` — полнотекстовый поиск по справке.
*   `getMembers` — получение списка членов объекта (методов, свойств).
*   `getMember` — получение детального описания конкретного метода или свойства.
*   `getConstructors` — получение информации о конструкторах объекта.
*   `info` — получение общей информации о системе справки.

---

### Сводная таблица MCP-серверов для 1С

| Название | Версия | Назначение | Инструменты (MCP Tools) | Требования |
| :--- | :--- | :--- | :--- | :--- |
| **MCPToolkit (расширение)** | 1.0.0 | Прямое взаимодействие с базой 1С (автозапуск, 13 tools) | `execute_query`, `execute_code`, `validate_query`, `get_metadata`, `get_metadata_tree`, `get_form_structure`, `search_code`, `get_object_by_link`, `get_link_of_object`, `find_references_to_object`, `get_event_log`, `get_access_rights`, `get_bsl_syntax_help` | Расширение конфигурации, IIS/Apache, proxy.py |
| **1c-mcp-toolkit (.epf)** | v1.5.0 | Прямое взаимодействие с базой 1С (ручной запуск) | 8 основных + `submit_for_deanonymization`, TOON, анонимизация | Обработка `.epf`, NativeAPI, порт 6003 |
| **bsl-code-search** | 1.0.0 | Структурный поиск BSL-кода | `search_symbols`, `find_callers`, `get_module_ast` | `tree-sitter-bsl@0.1.6`, Node.js |
| **bsl-platform-context** | 0.3.1 | Справка по API платформы 1С:8.3.27 | `search`, `getMembers`, `getMember`, `getConstructors`, `info` | Java jar |
| **METR** | 0.5.1 | Сборка, синтаксис-проверка, YaXUnit тесты | `build_project`, `check_syntax_designer_config`, `check_syntax_designer_modules`, `check_syntax_edt`, `run_all_tests`, `run_module_tests`, `dump_config`, `launch_app` | Java 17, `application-testdb.yml` |
