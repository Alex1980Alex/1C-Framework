# 25.5 Практический пример

## Сценарий: «Add Redis caching to API»

### Шаг 1: Запрос пользователя

```
Пользователь: "Добавь кеширование в API через Redis"
```

### Шаг 2: Блокировка

Claude пытается выполнить `Write("src/api/cache.py", ...)`.

`code-skill-enforcer` Level A.1 обнаруживает паттерн `redis` в `research_protocol` — нет dedicated skill.

```
BLOCK ❌
LEARNING REQUIRED: 'Redis' — no dedicated skill exists.
ACTIVATE: Skill('learning-loop')
```

### Шаг 3: Запуск субагента

```python
Agent(
    subagent_type="learning-loop",
    prompt="Задача: добавить кеширование API через Redis. "
           "Домен: tech-python. Библиотека: redis/aioredis. "
           "Проект: Python 3.11+, async-first, FastAPI."
)
```

### Шаг 4: Выполнение пайплайна (внутри субагента)

```
SEARCH:
  grep -ri "redis" .claude/skills/*/SKILL.md
  → 0 результатов

FETCH (домен: tech-python):
  1. WebSearch("site:stackoverflow.com aioredis async mistakes")
     → accepted answer: "всегда используй ConnectionPool"
  2. WebSearch("site:github.com redis async python stars:>100")
     → redis-py (12K stars, MIT)
  3. WebFetch("https://redis-py.readthedocs.io")
  → KB: ConnectionPool, decode_responses, expire, asyncio
  → маркеры: [ConnectionPool, decode_responses, expire, asyncio.Lock]

EXECUTE:
  Write src/api/cache.py
    # Source: [redis-py docs - ConnectionPool]
    async def get_redis_pool():
        return ConnectionPool.from_url(REDIS_URL, decode_responses=True)
    # Source: [SO #45231 - accepted answer]
    async def cached_search(query: str, ttl: int = 300):
        ...

VERIFY (inline):
  grep "ConnectionPool" cache.py     → ✅ найден
  grep "decode_responses" cache.py   → ✅ найден
  grep "expire" cache.py             → ✅ найден
  Self-review: async ✅, no blocking ✅, Pydantic ✅
  → [CODE-VERIFY-PASS]

CREATE:
  Write .claude/skills/redis-cache/SKILL.md (120 строк)
  Edit skill-router-config.json → bundle "caching"
```

### Шаг 5: Возврат

Субагент возвращает summary:
```
## Learning Loop Complete
- Skill: redis-cache
- Sources: 3 (redis-py docs, SO #45231, GitHub redis-py)
- Markers verified: 3/3
- Files created: .claude/skills/redis-cache/SKILL.md, src/api/cache.py
- Verdict: [CODE-VERIFY-PASS]
```

### Шаг 6: Самоусиление

При следующем запросе с "Redis":
```
SEARCH → grep "redis" .claude/skills/*/SKILL.md
  → НАЙДЕН: redis-cache/SKILL.md
  → Skill используется напрямую, без FETCH/EXECUTE
```
