# 27.3 Memory-First Hook — автоконтекст памяти

## Проблема

MCP-серверы памяти (memory-ai, vector-memory, skill-learning) — это инструменты, которые Claude вызывает **по своему решению**. Нет гарантии, что перед ответом он проверит память проекта. Результат: повторные объяснения, потеря контекста, игнорирование ранее принятых решений.

## Решение

Фаза **P0.5** добавляет `UserPromptSubmit` hook, который перехватывает каждый промпт пользователя и автоматически ищет релевантные записи в памяти **до** начала ответа. Найденный контекст инжектируется как `systemMessage`, и Claude получает его до генерации.

**Файл:** [`.claude/hooks/memory-first-hook.py`](.claude/hooks/memory-first-hook.py)
**Событие:** `UserPromptSubmit`
**Тесты:** 41 PASS (`scripts/claude-backend/tests/test_memory_first_hook.py`)

---

## Архитектура

```
User prompt (stdin JSON)
        |
        v
+-------+------------------+
| Skip checks              |
|  - prompt length < 20    |
|  - starts with '/'       |
|  - single-word query     |
|  - cooldown active (30s) |
+-------+------------------+
        | (pass)
        v
+-------+------------------+
| load_all_memories()      |
|  - read MEMORY.md index  |
|  - parse YAML frontmatter|
|  - fallback: glob *.md   |
+-------+------------------+
        |
        v
+-------+------------------+
| Tokenize + Russian stem  |
|  - query tokens          |
|  - memory tokens         |
+-------+------------------+
        |
        v
+-------+------------------+
| Weighted scoring         |
|  name x3 / desc x2 / body x1
+-------+------------------+
        |
        v
  score = 0.7 × coverage + 0.3 × density
  threshold: score >= 0.3
        |
        v
+-------+------------------+
| Format systemMessage     |
|  top-3 memories          |
+-------+------------------+
        |
        v
Claude receives prompt + [MEMORY CONTEXT]
```

---

## Алгоритм поиска

### Весовая модель токенов

| Поле `.md` | Вес | Обоснование |
|---|---|---|
| `name` (заголовок / YAML frontmatter `name`) | ×3 | Самый сильный сигнал |
| `description` (YAML frontmatter `description`) | ×2 | Краткая характеристика |
| body (содержимое файла) | ×1 | Дополнительный контекст |

### Формула scoring

```
score = 0.7 × query_coverage + 0.3 × memory_density

query_coverage = matched_unique_query_tokens / total_unique_query_tokens
memory_density = matched_memory_tokens / total_memory_tokens
threshold      = 0.3  (ниже — не включать)
```

### Логика

1. Токенизация промпта (lowercase, split по не-буквенным символам)
2. Стемминг каждого токена кириллицы (см. Russian Stemming ниже)
3. Для каждой памяти: токенизация + стемминг полей name/description/body
4. Подсчёт пересечения стеммов с весами по полям
5. Вычисление coverage и density, комбинация в `score`
6. Отсечение по threshold, сортировка по убыванию score
7. Топ-3 инжектируются как systemMessage

---

## Russian Stemming (v1.1)

Лёгкий суффиксный стриппер без морфологии (не pymorphy3 — для скорости). Работает только с кириллицей, минимальная длина стема — 3 символа.

### Таблица правил

29 суффиксов обрабатываются в порядке убывания длины (3-char → 2-char → 1-char):

| Длина | Суффиксы |
|---|---|
| 3-char | ами, ями, ова, ева, его, ого, ему, ому, ыми, ими, ыми |
| 2-char | ам, ям, ом, ем, ей, ий, ый, ой, ая, яя, ое, ие, ые, ую, юю, ов |
| 1-char | а, я, о, е, у, ю, ы, и, ь |

### Примеры

| Словоформа | Стем | Удалённый суффикс |
|---|---|---|
| агенты | агент | -ы |
| агентов | агент | -ов |
| агентам | агент | -ам |
| агентами | агент | -ами |
| документации | документаци | -и |
| документацию | документаци | -ю |
| конфигурации | конфигураци | -и |
| хуков | хук | -ов |
| настройка | настройк | -а |

### Ограничения

- Английские токены не стеммируются (работают хорошо и так)
- Минимальная длина стема — 3 символа (защита от избыточного обрезания)
- Обрабатывается только кириллица
- Без морфологии: "конфигурация" → "конфигураци" (не идеально, но достаточно)
- Транслитерация cyr↔lat не выполняется

---

## Оптимизации

| Параметр | Значение | Назначение |
|---|---|---|
| Min prompt length | 20 символов | Пропуск приветствий и коротких команд |
| Cooldown | 30 секунд | Файл `cache/memory-first-cooldown.json` |
| Timeout | 2 секунды | Threading-based (Windows-compatible) |
| Score threshold | 0.3 | Отсечение низкорелевантного шума |
| Skip patterns | `/command`, однословные | Служебные запросы |

### Graceful degradation

При любой ошибке hook возвращает `{"continue": true}` и логирует в `cache/hooks-error.log`. Пользователь не видит сбоев, Claude продолжает работу без контекста памяти.

---

## Формат systemMessage

При успешном поиске инжектируется блок:

```
[MEMORY CONTEXT] Found 3 relevant memories for your query:
1. [feedback] Git Commit Workflow — Когда git-commit-enforcer требует коммит → просто сделай коммит, не исследуй... (confidence: 0.82)
2. [project] Hook infrastructure — BaseHook + TaskMaster + HookLock + core_paths в .claude/hooks/base/... (confidence: 0.61)
3. [user] Task Protocol — обязательный алгоритм: Classify → Skill Check → Decompose → Execute → Verify... (confidence: 0.45)

Use this context to inform your response. If memory conflicts with current code, trust current code.
```

---

## Отличие от исходного плана

В плане P0.5 предполагался вызов `unified_search` через MCP (HTTP/subprocess). Реализация пошла проще:

| Аспект | План (MCP federated) | Реализация (локальный .md) |
|---|---|---|
| Зависимости | MCP-сервер memory-orchestrator | Только файловая система |
| Latency | 200–500ms | <50ms |
| Доступность | Только при запущенном MCP | Всегда |
| Надёжность | Падения MCP → тишина | Graceful degradation из коробки |

Путь к памяти: `C:\Users\AlexT\.claude\projects\D--1--Framework\memory\` (user-level auto-memory directory).

Чтение: `MEMORY.md` (индекс) + парсинг YAML frontmatter из каждого файла `.md`.

---

## Известные ограничения

| Ограничение | Влияние | Решение |
|---|---|---|
| Только локальные `.md` | Не ищет в SQLite/Qdrant | Upgrade: P5.2 federated recall |
| Стемминг без морфологии | "конфигурация" → "конфигураци" | pymorphy3 или snowball-stemmer |
| Кириллица/латиница не транслитерируются | "GLM" (lat) ≠ "ГЛМ" (cyr) | Добавить transliteration mapping |
| Нет semantic similarity | Только token overlap, без синонимов | Upgrade: embedding через Qdrant (P5.2) |
| Без кэша результатов | Каждый поиск читает файлы заново | Компенсируется cooldown 30s |

---

## Upgrade path → P5

Фаза **P5.2** (Federated Recall) расширяет hook на 3-layer поиск:

- **Layer 1:** SQLite FTS (структурированные факты, <50ms)
- **Layer 2:** Qdrant semantic (глубокое сходство, <200ms, опционально)
- **Layer 3:** `.md` files (текущая реализация, всегда работает)

Результаты объединяются через RRF с source weights 0.4 / 0.35 / 0.25. Подробности — [27.8 Session Memory Bridge](27.8_Session_Memory_Bridge.md).
