# 05.6 Sandbox исполнения agent-generated кода

> **Статус (2026-05-15):** ✅ **Hermes Ф5 COMPLETE** — все 3 backend готовы (DryRun + LangSmith + E2B), 38/38 tests pass, `select_backend()` env-driven helper, skill `sandbox-execution` обновлён.

## Зачем

Когда research-агенты (`architecture-research`, `tech-research`) генерируют
прототип кода, его НЕЛЬЗЯ запускать в основном процессе — это `eval()` в
production. Sandbox изолирует:

- Файловая система (write только в `/tmp` песочницы)
- Зависимости (отдельный pip env)
- Сеть (запрещена; explicit allowlist для paid backends)
- CPU/memory (timeout 30s, soft RAM limit)

## Архитектура

```
SandboxBackend (ABC)
  ├─ DryRunBackend          ✅ zero-dep CI/no-key fallback (50-call quota)
  ├─ LangSmithBackend        ✅ Firecracker microVM (GA May 2026, langsmith[sandbox])
  └─ E2BBackend              ✅ Stateful Jupyter (e2b-code-interpreter, ~$0.10/h vCPU)
```

**Auto-selector** (`src.pdf_framework.sandbox.select_backend`):
1. `prefer="auto"` (default) → LangSmith (если `LANGSMITH_API_KEY`) → E2B (если `E2B_API_KEY`) → DryRun fallback
2. Явный выбор: `select_backend("langsmith")` / `"e2b"` / `"dryrun"`

**Уникальные фичи backends:**
- **LangSmith:** snapshots + copy-on-write forks (10 parallel branches ≈ стоимость одного), Auth Proxy (секреты не доходят до runtime), blueprints (cold-start reduction)
- **E2B:** stateful Jupyter Kernel — переменные/импорты persist между `execute` вызовами в рамках одного sandbox; pause-resume preserves state до 30 дней при $0 compute

## Файлы

| Путь | Назначение |
|---|---|
| [`src/pdf_framework/sandbox/base.py`](../../../../src/pdf_framework/sandbox/base.py) | `SandboxBackend` ABC + `SandboxResult` + `SandboxQuotaExceeded` |
| [`src/pdf_framework/sandbox/dry_run_backend.py`](../../../../src/pdf_framework/sandbox/dry_run_backend.py) | Zero-dep fallback, 50-call per-instance quota |
| [`src/pdf_framework/sandbox/langsmith_backend.py`](../../../../src/pdf_framework/sandbox/langsmith_backend.py) | LangSmith microVM (F1 2026-05-15, ~155 LoC) |
| [`src/pdf_framework/sandbox/e2b_backend.py`](../../../../src/pdf_framework/sandbox/e2b_backend.py) | E2B Code Interpreter (F2 2026-05-15, ~140 LoC) |
| [`src/pdf_framework/sandbox/__init__.py`](../../../../src/pdf_framework/sandbox/__init__.py) | Public API + `select_backend()` helper |
| [`tests/unit/pdf_framework/sandbox/test_dry_run_backend.py`](../../../../tests/unit/pdf_framework/sandbox/test_dry_run_backend.py) | 13 contract tests |
| [`tests/unit/pdf_framework/sandbox/test_langsmith_backend.py`](../../../../tests/unit/pdf_framework/sandbox/test_langsmith_backend.py) | 13 unit tests (mock-based) |
| [`tests/unit/pdf_framework/sandbox/test_e2b_backend.py`](../../../../tests/unit/pdf_framework/sandbox/test_e2b_backend.py) | 12 unit tests + stateful Jupyter persistence + selector |

## Когда использовать

| Use case | Backend |
|---|---|
| CI / no API key | DryRun (zero cost) |
| Local development | DryRun (zero cost) |
| Production research agent | LangSmith (когда landed) или E2B |
| Mock в unit-тестах высокоуровневого кода | DryRun |

## Quick start

```python
from src.pdf_framework.sandbox import DryRunBackend

backend = DryRunBackend()
result = await backend.execute("print('hello')")
# SandboxResult(stdout="[dry-run] code not executed", exit_code=0, ...)

# Records all calls for introspection
backend.calls  # [{"method": "execute", "args": {...}}]
```

## Полная документация

Skill: [`sandbox-execution`](../../../../.claude/skills/sandbox-execution/SKILL.md) — детальный API reference, примеры, integration patterns.

## Связано

- Spec: [hermes-llm-wiki Phase 5](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/tasks.md)
- Commits: `9b392c465` (skeleton), `12f7a0198` (skill + log)
- Pattern: `agent-orchestration` (где integration произойдёт)

## Наследие Hermes / доп. детали (перенесено из 37.6, R2 260705)

### Контракт ABC

[`src/pdf_framework/sandbox/base.py`](../../../../src/pdf_framework/sandbox/base.py):

```python
from abc import ABC, abstractmethod
from dataclasses import dataclass
from pathlib import Path


@dataclass
class SandboxResult:
    stdout: str
    stderr: str
    exit_code: int
    duration_seconds: float
    truncated: bool = False  # True if output > ~100 KB


class SandboxQuotaExceeded(Exception):
    """Raised on 51st execute() call (daily quota)."""


class SandboxBackend(ABC):
    @abstractmethod
    async def execute(self, code: str, language: str = "python", timeout: float = 30.0) -> SandboxResult: ...

    @abstractmethod
    async def install(self, packages: list[str]) -> None: ...

    @abstractmethod
    async def upload(self, local_path: Path, remote_path: str) -> None: ...

    @abstractmethod
    async def download(self, remote_path: str, local_path: Path) -> None: ...

    @abstractmethod
    async def close(self) -> None: ...
```

### DryRunBackend — квота и introspection

```python
backend = DryRunBackend()
backend.calls  # [{"method": "execute", "args": {"code": ..., "language": ..., ...}}]

# Quota: 50 execute() ok, 51st raises
for _ in range(50):
    await backend.execute("...")
try:
    await backend.execute("...")  # 51st
except SandboxQuotaExceeded:
    pass
```

### LangSmithBackend — установка и internal command building

```toml
# pyproject.toml [project.optional-dependencies]
sandbox-langsmith = ["langsmith[sandbox]>=0.3.0"]
```

```bash
pip install "langsmith[sandbox]>=0.3.0"
export LANGSMITH_API_KEY=ls_...
```

```python
# langsmith_backend.py:_build_cmd
@staticmethod
def _build_cmd(code: str, language: str) -> str:
    """Build shell command. Assumes POSIX shell (sh/bash) receiver."""
    if language == "python":
        # POSIX safe escape: ' → '\''
        escaped = code.replace("'", "'\\''")
        return f"python -c '{escaped}'"
    if language == "bash":
        return code
    raise ValueError(f"Unsupported language: {language}")
```

**Security note:** Escape pattern `'\''` — textbook POSIX safe quote-break-out. Assumes sh/bash receiver (default для `python-sandbox` template). Если LangSmith когда-то ship'нет cmd.exe template — escape breaks silently, update before switching.

Дополнительные фичи LangSmith (сверх Snapshots/Auth Proxy/Blueprints выше): **Service URLs** (network access к специфичным сервисам per allowlist), **Sandbox CLI** (local replay/debug). Pricing: free tier (LangChain ecosystem), точная цена за sandbox-секунду на платных планах — TBD.

### E2BBackend — установка и internal error mapping

```toml
sandbox-e2b = ["e2b-code-interpreter>=1.2.0"]
```

```bash
pip install e2b-code-interpreter>=1.2.0
export E2B_API_KEY=e2b_...
```

```python
# e2b_backend.py:execute
execution = await asyncio.to_thread(self._sandbox.run_code, code, timeout=int(timeout))

stdout = "\n".join(execution.logs.stdout)
stderr = "\n".join(execution.logs.stderr)
exit_code = 0

err = getattr(execution, "error", None)
if err:
    name = getattr(err, "name", "Error")           # "NameError"
    value = getattr(err, "value", "")              # "x is not defined"
    tb = getattr(err, "traceback", "")
    stderr = f"{name}: {value}\n{tb}"
    exit_code = 1
```

Pricing: $0.000014/s per vCPU = $0.000028/s (2 vCPU default) ≈ **$0.10/час**; pause = $0 compute preserves state; free tier для тестов (limited hours).

### Сравнение backends

| Критерий | DryRun | LangSmith | E2B |
|---|---|---|---|
| Isolation | None (no exec) | Firecracker microVM | Firecracker microVM |
| Stateful | N/A | Snapshots/forks | Jupyter Kernel |
| Pricing | $0 | Free tier (LangChain) | $0.10/час vCPU |
| Auth model | N/A | Auth Proxy | Direct API key |
| GA date | 2026-04 | 2026-05 | 2024+ |
| Lock-in | None | LangChain ecosystem | Vendor-neutral |
| Streaming | N/A | websockets | WebSocket |
| Cold-start | <10ms | 500ms-2s (reduced by blueprint) | ~500ms-1s |
| Когда использовать | CI / no API key | Production research agent | Stateful workflows / scientific compute |

### Selector helper (полный код)

[`__init__.py:select_backend()`](../../../../src/pdf_framework/sandbox/__init__.py):

```python
def select_backend(prefer: str = "auto") -> SandboxBackend:
    """Select sandbox backend by env keys + preference.

    Order: prefer → LANGSMITH_API_KEY → E2B_API_KEY → DryRun fallback.
    """
    if prefer == "dryrun":
        return DryRunBackend()

    if prefer == "langsmith" or (prefer == "auto" and os.getenv("LANGSMITH_API_KEY")):
        from .langsmith_backend import LangSmithBackend
        return LangSmithBackend()

    if prefer == "e2b" or (prefer == "auto" and os.getenv("E2B_API_KEY")):
        from .e2b_backend import E2BBackend
        return E2BBackend()

    return DryRunBackend()
```

### Тесты — детализация

| File | Tests | Coverage |
|---|---|---|
| `test_dry_run_backend.py` | 13 | Contract: execute/install/upload/download/close + quota + truncation |
| `test_langsmith_backend.py` | 13 | Mock-based: init, lazy sandbox creation, escape, timeout=124, quota, truncation, install, upload/download, close, idempotent |
| `test_e2b_backend.py` | 12 + selector | Mock-based: init, execute, quota, non-python lang, timeout, error mapping, stateful persistence, truncation, install, upload/download |

**38/38 passing.** Live tests gated behind API keys (`@pytest.mark.langsmith_live`, `@pytest.mark.e2b_live`) — not yet added.

### Security architecture

**Что sandbox защищает:**

1. **Filesystem** — write только в `/tmp` песочницы, нет доступа к host filesystem (кроме `upload`/`download` API)
2. **Network** — outbound по default запрещён (E2B), explicit allowlist для paid backends (LangSmith via Auth Proxy)
3. **CPU/Memory** — soft RAM limit, timeout 30s default, daily quota 50 execute() calls
4. **Secrets** — LangSmith Auth Proxy keeps credentials off the sandbox; E2B requires explicit env var injection

**Threat model:**

| Угроза | Mitigation |
|---|---|
| Agent generates `rm -rf /` | Sandbox filesystem isolation — кроме `/tmp` ничего не достижимо |
| Agent generates `import os; os.system(...)` | Same — bare metal недоступен |
| Agent leaks API keys через `print(os.environ)` | LangSmith Auth Proxy — keys not in env; E2B — каждый user manages own keys |
| Resource exhaustion (mining cryptocurrency) | Daily quota 50 + per-execute timeout 30s + sandbox lifetime 24h max |
| Stateful kernel pollution between sessions | Каждый `SandboxBackend` instance = own sandbox; closing releases |
| Network exfil to attacker-controlled host | E2B default deny outbound; LangSmith Auth Proxy gate-keeps |

**Что НЕ защищается:** кодовая логика agent-generated кода — sandbox не понимает intent. Если agent просит execute "calculate x", но malicious input — sandbox executes obediently. Защита от такого уровня — guardrails / input validation на уровне agent prompt, не sandbox.

### Интеграция (TODO/deferred)

```python
# В будущем — research-skills:
from src.pdf_framework.sandbox import select_backend, SandboxResult

async def execute_prototype(code: str) -> SandboxResult:
    backend = select_backend("auto")  # env-driven
    try:
        return await backend.execute(code, timeout=30.0)
    finally:
        await backend.close()
```

**Status:** integration в `architecture-research` / `tech-research` отложено до first real usage (deferred polish).

Триггеры skill `sandbox-execution`: «sandbox», «code-interpreter», «firecracker», «agent code execution», «langsmith sandbox», «E2B».
