# 36.1 Autonomous Debug Control — Обзор

> **JOB-based execution (2026-05-12, ✅ RC2 gap закрыт):** `гкс_ОтладкаВыполненияКода` CommonModule задеплоен в ИБTransportManagementDevelop. `ФоновыеЗадания.Выполнить("гкс_ОтладкаВыполненияКода.ВыполнитьКод", ...)` спаунит новый JOB rphost → `DBGUIExtCmdInfoStarted` → auto-attach → BPs fire без деструктивного force_recycle. **Шаблон 6** добавлен в [SKILL.md](../../../../.claude/skills/1c-debug-hmr/SKILL.md). §10.1 в [§36.8](36.8_Advanced_Debug_Features.md).
>
> **Roadmap 260511 P0.A–G batch (2026-05-11, later same day):** добавлены 7 advanced features (Conditional + Hit-count BPs, Logpoints, Source mapping, Cascade auto-Continue, BP-propagation drain, Warm-pool arming visible+silent). 3 new MCP tools (`debug_set_logpoint`, `debug_arm_warm_rphosts`, `debug_arm_next_rphost`). 4 features live-verified на кластере. Подробности: [§36.8 Advanced Debug Features](36.8_Advanced_Debug_Features.md). Roadmap: [`260511_DEEP_ANALYSIS_DEBUG_HMR_BEST_PRACTICES.md`](../../roadmap/260511_DEEP_ANALYSIS_DEBUG_HMR_BEST_PRACTICES.md).
>
> **Roadmap 260511 update (2026-05-11):** P0-P0.5 batch добавил **5 fixes** к wrapper'у, закрывающих RC1+RC2+RC3 из GKSTCPLK-2468 incident:
> - **§P0.1** — `_validate_infobase_alias` через `rac infobase list` ПЕРЕД attach (closes RC1: silent registered=true для несуществующего alias)
> - **§P0.2** — `recycle_strategy` parameter в `debug_connect`: 5 вариантов (`auto`/`none`/`pre_existing`/`all_rphosts_of_ib`/`all_rphosts_of_cluster`), backward-compat для `force_recycle_rphost`
> - **§P0.4** — post-spawn auto-attach polling в `_ping_loop` (каждые ~6с `get_targets()` diff против `_known_attached_targets`)
> - **§P0.5** (CRITICAL) — расширенный `setAutoAttachSettings` filter включая `HTTPService`/`WEBService`/`JOB`/`JobFileMode`/`COMConnector`/`OData` (yukon39 `debugBaseData.xsd:105-123` confirmed full enum); previous ROLLBACK 2026-05-10 ошибочно интерпретировал HTTP 400 как XSD invalidation. **Closes RC2: BPs fire on HTTP-service / JOB rphost (1c-mcp-crud:execute_code)**.
> - **§P0.5** + **`_reapply_bp_workspace`** helper — re-apply BPs на каждое `targetStarted` event для коротко-живущих JOB targets (<100ms execute→quit cycle).
>
> **Новые MCP tools:** `debug_wait_for_target` (§P1.4), `debug_launch_thin_client` (§P1.5). **`debug_ping`** теперь surface'ит `no_fire_diagnostics` (§P0.6) после 3 consecutive empty pings.
>
> **E2E** (2026-05-11, ИБTransportManagementDevelop): 7 targets stopped at `StopOnNextLine` после composite re-post через `1c-mcp-crud:execute_code`, stack trace 10 frames captured, runtime confirmation `ТипЗнч(ДокументРегистрации) = Тип("ДокументСсылка.гкс_ФормированиеНомераПробы")`. Tests: 199 → 222 passed (+23).
>
> **Roadmap:** [`260511_ROADMAP_1C_DEBUG_HMR_DEFICIENCIES_FROM_GKSTCPLK_2468.md`](../../roadmap/260511_ROADMAP_1C_DEBUG_HMR_DEFICIENCIES_FROM_GKSTCPLK_2468.md).

## Назначение

Четырёхуровневая (L1–L4) система автоматизации отладки 1С через wrapper [`tools/bsl-debug-server/mcp_debug_server.py`](../../../tools/bsl-debug-server/mcp_debug_server.py). Цель — **полный контроль результатов работы без ручной синхронизации среды**: Claude Code или CI запускает ОДНУ команду и получает готовое заключение «ready / not ready», «BPs fired / failed», «session metrics» в structured формате.

Система реализована 2026-05-10 как §12 roadmap'а [`260508_ROADMAP_BSL_DEBUG_WRAPPER_POST_BP_HANDSHAKE.md`](../../roadmap/260508_ROADMAP_BSL_DEBUG_WRAPPER_POST_BP_HANDSHAKE.md) после серии live-validations выявивших 5+ implementation gap'ов в RDBG-протоколе на 8.3.27.1936.

> **Связано:**
> - [16.7 Autonomous Debug Workflow](../16_ПОДКЛЮЧЕНИЕ_1С/16.7_Autonomous_Debug_Workflow.md) — базовый Scenario B (manual debug через Claude как Debug UI)
> - [17.5 Команды 1С Pipeline](../17_ТЕСТИРОВАНИЕ_1С/17.5_КОМАНДЫ_ПАЙПЛАЙНА.md) — VA BDD pipeline (тестирование UI слоя)
> - [`scripts/autonomous_debug_test.py`](../../../scripts/autonomous_debug_test.py) — Level 2 runner
> - [`scripts/scenarios/post_lab_doc.json`](../../../scripts/scenarios/post_lab_doc.json) — пример scenario JSON

---

## 1. Четыре уровня контроля

| Level | Цель | API |
|---|---|---|
| **L1** Preflight health-check | «Готова ли среда к debug?» | MCP-tool `debug_health_check(mode, actions?)` |
| **L2** Autonomous E2E runner | «Прогнать сценарий end-to-end без user input» | CLI `python scripts/autonomous_debug_test.py <scenario>` |
| **L3** Post-mortem session summary | «Что произошло в сессии? Регрессии?» | MCP-tool `debug_session_summary(format)` + `debug_session_diff(prev_id)` |
| **L4** Coverage & Replay | «Какие строки BSL покрыты? Что случилось на конкретном останове?» | `debug_coverage_register` + `debug_coverage_export` → `genericCoverage.xml` (P1.A); `debug_session_record` + `debug_replay_list` + `debug_replay_seek` (P2.A) |

Все 4 уровня **взаимозависимы**: L2 внутри использует L1 (auto-prepare) и L3 (вывод markdown в конце прогона). L4 ортогонален — coverage и replay работают поверх любого L1/L2/L3 прогона. При этом каждый уровень полезен и **standalone** — L1 для ad-hoc проверки, L3 для аудит-trail в PR-описаниях.

---

## 2. Архитектура

```
┌──────────────────────────────────────────────────────────────────┐
│  Claude Code / CI / Developer                                    │
└─────────┬──────────────────────────────┬─────────────────────────┘
          │ MCP stdio                    │ subprocess (CLI)
          ▼                              ▼
┌─────────────────────────┐    ┌──────────────────────────────────┐
│  L1: debug_health_check │    │  L2: autonomous_debug_test.py    │
│  L3: debug_session_*    │    │  ─ scenario JSON validation      │
│  (MCP tools, real-time) │    │  ─ uses L1 health + auto-prepare │
└─────────────┬───────────┘    │  ─ trigger BSL via IIS POST      │
              │                │  ─ poll for stop events          │
              │                │  ─ inspect + Continue per BP     │
              │                │  ─ uses L3 summary at end        │
              │                └──────────────┬───────────────────┘
              │                               │
              ▼                               ▼
        ┌──────────────────────────────────────────┐
        │  mcp_debug_server.py (RDBGClient)        │
        │  ─ dbgs.exe HTTP RDBG (port 1550)        │
        │  ─ rac.exe (cluster admin via 1545)      │
        │  ─ counters в RDBGClient (in-process)    │
        │  ─ data/debug_sessions/<id>.json mirror  │
        └──────────────────────────────────────────┘
```

---

## 3. Что было до vs после

| Аспект | До (manual workflow) | После (autonomous control) |
|---|---|---|
| Проверка готовности среды | 5-7 ручных вопросов user'у (dbgs up? `-debug`? rphost'ы?) | Одна команда `debug_health_check`, ответ за <1с |
| Подготовка среды | User вручную: kill rphost / restart service / set env | `mode=prepare` + whitelist actions, безопасный auto-fix |
| Запуск тестового сценария | Manual: connect → set BP → ask user "Провести" → wait | `python autonomous_debug_test.py <scenario>` — exit code = вердикт |
| Аудит результатов | Свободный пересказ в чате | Structured JSON + готовый Markdown в PR |
| Regression detection | Сравнивать вручную, помнить baseline | `debug_session_diff(prev_id)` — `verdict: REGRESSION/NO_REGRESSION` |
| Воспроизводимость | Требует точных шагов user'a | Scenario JSON в репе → бесконечно повторяется |

---

## 4. Структура раздела 36

| § | Файл | О чём |
|---|---|---|
| 36.1 | [Обзор](36.1_Обзор.md) | Этот файл — концепция, архитектура |
| 36.2 | [Level 1 — Health Check](36.2_Level1_Health_Check.md) | 8 probes, K8s readiness JSON, auto-prepare actions |
| 36.3 | [Level 2 — Autonomous Runner](36.3_Level2_Autonomous_Runner.md) | CLI script, scenario JSON schema, exit codes |
| 36.4 | [Level 3 — Session Summary + Diff](36.4_Level3_Session_Summary.md) | Counters, markdown render, cross-session diff |
| 36.5 | [Workflows](36.5_Workflows.md) | Bug fix / feature dev / regression hunt / perf |
| 36.6 | [Диагностика](36.6_Диагностика.md) | Коды ошибок, известные ограничения, troubleshooting |
| 36.7 | [HMR Subprocess Wrapper](36.7_HMR_Subprocess_Wrapper.md) | `mcp_hmr_proc.py` + persistent session + unified ping dispatch |
| 36.8 | [Advanced Debug Features](36.8_Advanced_Debug_Features.md) | P0.A–G batch (2026-05-11): Conditional/Hit-count BPs, Logpoints, Source mapping, Cascade auto-Continue, BP-propagation drain, Warm-pool arming. **§10.1 (2026-05-12):** JOB-based execution закрывает RC2 gap |

---

## 5. Что валидировано (live tests 2026-05-10 – 2026-05-12)

| Сценарий | Дата | Результат |
|---|---|---|
| Single-BP thin client triggered Провести | 2026-05-10 | ✅ BP fires, eval работает (composite types Структура/ДокументСсылка) |
| Multi-BP across 2 modules (3 BPs total) | 2026-05-10 | ✅ После Fix #5 BP aggregation все 3 fire последовательно |
| L1 standalone probe | 2026-05-10 | ✅ 7 probes за 627ms, recommended_workflow корректен |
| L2 autonomous runner | 2026-05-10 | ✅ Прогоняет 4 фазы без user; auto-detected Fix #1 regression |
| L3 markdown summary | 2026-05-10 | ✅ Готовый блок для PR description с метриками |
| `force_recycle_rphost` через rac | 2026-05-10 | ✅ Kills rphost, ragent spawn'ит fresh worker |
| IIS-trigger через 1c-mcp-crud | 2026-05-11 | ✅ 7 targets stopped after composite re-post; RC2 закрыт через `setAutoAttachSettings` full enum |
| JOB-based execution (`гкс_ОтладкаВыполненияКода`) | 2026-05-12 | ✅ RC2 warm-pool gap полностью закрыт; новый JOB rphost → auto-attach → BP fires без force_recycle |
| Conditional BP (`condition` param) | 2026-05-11 | ✅ BSL-фильтр выполняется RDBG-native; неподходящие итерации не останавливают |
| Hit-count BP (`hit_condition` `>N`/`%N`) | 2026-05-11 | ✅ Wrapper-level counter; fire только при совпадении паттерна |
| Logpoint (`debug_set_logpoint`) | 2026-05-11 | ✅ JSONL log без halt; `{expr}` placeholders резолвятся через eval |
| Source mapping (`resolved_source` in stack) | 2026-05-11 | ✅ UUID→FQN через `uuid_index`; file_path в каждом frame |
| Warm-pool arming (`debug_arm_warm_rphosts`) | 2026-05-11 | ✅ Attach visible HTTPService/JOB targets; BP workspace re-applied |
| P1.A Code coverage (`debug_coverage_export`) | 2026-05-11 | ✅ SonarQube `genericCoverage.xml` с hit-count per line |
| P1.B Session artifacts ZIP (`format="artifacts"`) | 2026-05-11 | ✅ Bundle: summary.json + summary.md + stop_events + stack_snapshots |
| P2.A Snapshot replay (`debug_replay_seek`) | 2026-05-11 | ✅ Post-mortem инспекция любого stop без live session |
| P3.B Exception BPs (`debug_set_exception_bp`) | 2026-05-11 | ✅ Filter по message/module pattern; halt только на matching exceptions |
| Tests count | 2026-05-12 | ✅ 222 pass (+23 от baseline 199) |

---

## 6. Тестовое покрытие

- **`tests/test_mcp_debug_server.py`** — **182 unit-tests** (95 baseline + 87 для L1/L2/L3 + helpers)
- **`scripts/test_autonomous_debug_test.py`** — **13 schema validator tests**
- Mocked subprocess + FastMCP in-memory client pattern (best practice из microsoft/debugpy + jlowin/fastmcp)

Запуск: `pytest tools/bsl-debug-server/tests/ scripts/test_autonomous_debug_test.py` — должно быть **195+ pass**.
