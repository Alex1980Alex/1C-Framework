# 40.8 User Action Items — Maximum CI tier

Status: 3 ручных действия пользователя до 100% покрытия.

See [40.6](40.6_SMTP_setup_TODO.md), [40.7](40.7_Gmail_filter_setup.md).

## Action #1 — Rotate ANTHROPIC_API_KEY (SECURITY URGENT)

Ключ был передан в чат → попал в session transcripts. Rotate:
1. https://console.anthropic.com/settings/keys → Revoke `sk-ant-api03-ECXsIW1vt8pSLX7bMHqZQgN...`
2. Create new key
3. `gh secret set ANTHROPIC_API_KEY --repo Alex1980Alex/1C-Framework` → paste new

## Action #2 — Gmail app-password (SMTP)

Создать app-password https://myaccount.google.com/apppasswords (требует 2FA включена), label "claude-code-pr-automation". Добавить в `.claude/settings.local.json` env:
```json
"AUTO_PR_SMTP_PASS": "abcd efgh ijkl mnop"
```

## Action #3 — Gmail UI filter

Создать filter `from:notifications@github.com + has:words "Alex1980Alex/1C-Framework"` → apply label `claude-watching` + опционально skip inbox. Подробности: [40.7](40.7_Gmail_filter_setup.md).

## Action #4 (опционально) — Apply `auto-pr` label на PR

После Action #1-3, чтобы post-task-push-pr.py заработал autopilot:
1. На каждом PR где хочешь auto-push + auto-PR creation → apply label `auto-pr` (GitHub UI)
2. Хук создаёт label если не существует
3. `AUTO_PR_AUTO_MERGE=0` по-прежнему — никогда auto-merge без явного согласия

## Action #5 — CODECOV_TOKEN (опционально)

Tokenless OIDC работает для public repo, но имеет rate limit 250 uploads/неделю. Если упрёшься:
1. https://codecov.io/login/github → авторизоваться через GitHub
2. Выбрать Alex1980Alex/1C-Framework → Settings → Repository Upload Token
3. Copy token
4. `gh secret set CODECOV_TOKEN --repo Alex1980Alex/1C-Framework --body "<TOKEN>"`

## Action #6 — Enable Dependabot security updates (опционально)

GitHub Settings → Security → Code security and analysis → "Dependabot security updates" → Enable. Активирует auto-PR на CVE deps (помимо обычных version bumps от dependabot.yml).
