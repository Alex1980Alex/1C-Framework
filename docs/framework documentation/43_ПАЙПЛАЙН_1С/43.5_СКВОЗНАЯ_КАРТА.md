# 43.5 Сквозная карта: парадигма → команды → инструменты и системы (1С-задача)

> Сквозной top-down разрез решения 1С-задачи: **4-этапная парадигма** (ADR-017/018) → **специализированные
> команды Пайплайн 1С** → **инструменты и системы** (+ их обязательные enforcer) с **оценкой покрытия и
> пробелов**. Сводит воедино [43.1](43.1_ОБЗОР_И_ПАРАДИГМА.md) (парадигма), [43.2](43.2_КОМАНДЫ_И_РЕЖИМЫ.md)
> (команды), [43.3](43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md) + [43.4](43.4_СПРАВОЧНИК_ИНСТРУМЕНТОВ.md)
> (инструменты/системы). Состояние на 2026-06-15.

## Сквозная диаграмма (одна картинка)

```
ПАРАДИГМА (этап)        КОМАНДА 1С                 ОСНОВНЫЕ ИНСТРУМЕНТЫ                ENFORCER (Stop/гейт)
──────────────────────────────────────────────────────────────────────────────────────────────────────
1. Планирование ──┐
   архитектуры     ├─ /analyze-1c-task ──────► 1c-mcp-crud · edt-mcp ·            pipeline-protocol-stop
2. Дизайн          │     (Фазы 1-5)            bsl-semantic-search ·              (пайплайн заведён)
   реализации     ─┘                          bsl-platform-context ·             + approve-гейт G4
                        ▼ [approve-гейт G4]     pdf-vector-graph · 1c-debug-hmr    (pipeline-gate)
                                                (Runtime Trace 2.5)
3. Кодирование ──── /implement-1c-task ──────► edt-mcp(write) · 1c-mcp-crud ·     code-skill-enforcer
                        (Этапы 0-8)            bsl-debugger · bsl_lint.py ·       (Skill() до Write/Edit)
                                                1c-debug-hmr (Live BP-verify 5.x)
4. Тестирование ─── /write-1c-tests ─────────► 1c-mcp-crud (Stage 4a) ·          (зелёный .run-state →
                    /run-1c-tests              run-bdd.ps1 · mcp-onec-test-runner  этап 4 done)
──────────────────────────────────────────────────────────────────────────────────────────────────────
   AUTO: /run-1c-task = analyze → авто-approve → implement → test за один проход (без паузы)
──────────────────────────────────────────────────────────────────────────────────────────────────────
СКВОЗНЫЕ СИСТЕМЫ (поперёк ВСЕХ этапов) → ЕДИНЫЙ task-completion gate (один чеклист, без каскада):
   • Память (гл.27)       recall → apply → capture        ─┐
   • Внешний анализ        WebSearch (Infostart+GitHub)    ─┼→ onec-task-completion-stop
   • Система скиллов (гл.11)  Skill() на каждом этапе          │   (+ code-skill-enforcer: skill на Write)
   • Инструментирование (W)  TOOL-PLAN → лог → оценка        ─┘   (+ tool_usage_report — отчёт)
```

## Главная матрица (этап → команда → инструменты/системы → покрытие)

| Этап парадигмы | Команда 1С | Методика (skill) | Основные инструменты | Сквозные системы | Артефакт | Покрытие |
|---|---|---|---|---|---|---|
| **1. Планирование архитектуры** | `/analyze-1c-task` (Фазы 1-2) | `analyze-1c-task-v2` | 1c-mcp-crud (метаданные/запросы), edt-mcp (код), bsl-semantic-search (call graph), pdf-vector-graph (docs RAG) | память (recall), скиллы, внешний анализ (docs 8.3.27 + Infostart + GitHub) | `ANALYSIS-REPORT.md` | ✓ полно |
| **2. Дизайн реализации** | `/analyze-1c-task` (Фазы 3-5) | `analyze-1c-task-v2` | + bsl-platform-context (API 8.3.27), 1c-debug-hmr (Runtime Trace 2.5), routing_matrix (REFACTOR) | память (apply), внешний анализ | `ANALYSIS-REPORT.md` + **approve (G4)** | ✓ полно |
| **3. Кодирование** | `/implement-1c-task` | `implement-1c-task` (+ bsl-refactoring/symbol-editing) | edt-mcp (write/update_database/get_project_errors), 1c-mcp-crud (execute_*), bsl-debugger, bsl_lint.py, 1c-debug-hmr (Live BP-verify) | скиллы, память (apply) | `IMPLEMENTATION-PROGRESS.md` | ✓ полно |
| **4. Тестирование** | `/write-1c-tests` + `/run-1c-tests` | `va-bdd-testing` | 1c-mcp-crud (Stage 4a pre-check), run-bdd.ps1, mcp-onec-test-runner, edt-mcp (Form.Module) | память (capture после PASS), скиллы (code-verify) | `.run-state.json` | ✓ полно |
| **AUTO (все 4)** | `/run-1c-task` | `run-1c-task` (оркестратор) | делегирует методикам выше | все 4 петли | все артефакты | ✓ (авто-approve вместо G4) |

## Сквозные системы (поперёк всех этапов)

Четыре системы работают на каждом этапе и **принудительны** через свой Stop-enforcer (все с opt-out + graceful):

| Система | Что делает на задаче | Логирование | Оценка эффективности | Enforcer (opt-out) |
|---|---|---|---|---|
| **Память (гл. 27)** | recall (`unified_search`/`search_patterns`) → apply (`apply_pattern`) → capture (`capture_pattern`/`route_and_save`/`.md`) | `hook-invocations` / транскрипт; `learned_patterns`, `skill-learning` pending | `tool-effectiveness` + confidence Beta(7,3) | `memory-protocol-stop` (`MEMORY_PROTOCOL_DISABLE=1`) |
| **Система скиллов (гл. 11)** | каждый этап через профильный `Skill()` (analyze/implement/va-bdd/code-verify) | `skill-accuracy.jsonl` (posttooluse-skill-metrics + db_writer), `session-skills.json`, `skill-router.log` | `skill_system_acceptance.py`, `eval-skill-router.py`, `skill-quality-monitor`, `skill-health-report.md` | `code-skill-enforcer` (Level A-F) |
| **Внешний анализ** | docs 8.3.27 (первоисточник) + **Infostart** (доверенный) + **GitHub best-practices** через `WebSearch`/`WebFetch` | `knowledge-cache` (`architecture-research/cache/`) после WebSearch | reuse кеша + атрибуция (1c-doc-research) | `research-protocol-stop` (`RESEARCH_PROTOCOL_DISABLE=1`) |
| **Инструментирование (W)** | TOOL-PLAN (план) → лог (`hook-invocations`) → оценка (`tool_usage_report.py`) → `TOOL-USAGE-REPORT.md` | `data/hook-invocations.jsonl` (`correlationid=run_id`) | `tool_usage_report.py` (err%/avg_ms ✓/⚠/✗) + `tool-effectiveness.jsonl` (`--rollup`) | — (отчёт; в AUTO — шаг оркестрации) |

Плюс **пайплайн-парадигма** сама принудительна: `pipeline-protocol-stop` (ADR-018) блокирует завершение,
если была правка без проводки через `pipeline/<slug>/.pipeline-state.json`.

## Оценка покрытия и пробелы (честно)

**Хорошо покрыто (✓):**
- **Анализ/Дизайн** — тройка live-данные (1c-mcp-crud) + код (edt-mcp) + семантика (bsl-semantic-search) + первоисточник-RAG (pdf-vector-graph) + опц. Runtime Trace (1c-debug-hmr); ревью закрыт approve-гейтом **G4**.
- **Кодирование** — write-path edt-mcp с обязательными циклами (`validate_query`→`execute_query`→write; write→`get_project_errors`), статанализ (bsl_lint/bsl_analyze), **Live BP-verification** каждой точки.
- **Тестирование** — VA BDD + **Stage 4a** pre-check данных (экономит ~95% на «нет данных») + retry/auto-fix + YAxUnit.
- **4 обязательные петли enforced** (пайплайн / память / внешний анализ / скиллы) — забыть любую нельзя без явного opt-out.

**Исправлено (2026-06-15, по адверсариальному анализу N1–N12, `pipeline/impl-thin-spots-hardening/`):**
- **N4 (high)** — единый предикат `is_1c_task_title()` в `pipeline_1c_bridge.py` вместо 6 копий литерала
  `startswith("1С-задача…")` (был рассинхрон: bridge без скобки vs хуки со скобкой). Убрана точка молчаливого
  false-allow + дубль. Регресс-тест.
- **N3 (med, false-block)** — `TRANSCRIPT_TAIL_BYTES=8 МБ` в memory/research-хуках: ранний recall/research в
  длинной сессии больше не обрезается tail-окном (лог остаётся 2 МБ).
- **N6 (latent false-allow)** — capture по `.md` сужен до курируемой памяти (`/.claude/.../memory/*.md`):
  `src/memory/*.md` / `docs/*/memory/*.md` больше не засчитываются ложно. Регресс-тест.
- **N5 (audit)** — AUTO `/run-1c-task` ставит `approve --by auto` → метка `approved_by=auto` (audit-след «дизайн
  не ревьюился человеком»).
- **N1 + N11 (UX/каскад)** — превью в block-сообщении `pipeline-protocol-stop` («после Stop проверятся также
  память + внешний анализ») + явные under-steps в `run-1c-task` SKILL (recall+WebSearch в начале, capture после
  PASS) → закрывает тройной Stop-каскад за один проход.
- **N10 (audit)** — opt-out пишет `outcome="allow-optout"` → blanket-bypass виден в `tool_usage_report`.

**Осталось by-design (фикс = false-block / суть режима — обоснованно):**
- **N7 research-relevance** — засчитывается любой `WebSearch`/`WebFetch`; проверка релевантности Infostart/GitHub
  неизбежно даёт false-block (легитимный 1С-запрос находит Infostart без слова «infostart»). Симметрично recall.
- **N9 AUTO авто-approve (G4)** — это смысл AUTO-режима; компенсируется хард-правилом «AUTO ≠ игнор блокеров» +
  N5-меткой. Enforce ревью в AUTO противоречит режиму.
- **apply_pattern** — нельзя enforce «применил ли паттерн» (нет tool-сигнала); recall+capture обязательны, apply — по релевантности.
- **N12 advance по имени файла** (ADR-019 F-1.5 best-effort), **N2 session_start=None / N8 git-edge** — graceful, корень N2 снижен N3.

**Закрыто (2026-06-15):**
- ✅ **Единый task-completion gate** — `onec-task-completion-stop` (заменил `memory-protocol-stop` +
  `research-protocol-stop`): один проход по транскрипту → консолидированный чеклист всех петель
  (✓/✗ ПАЙПЛАЙН / RECALL / CAPTURE / RESEARCH / SKILL), блок ОДИН раз со всеми ✗ — каскада нет.

**Пробелы (кандидаты на доработку):**
- **W cross-task rollup** (`tool_usage_report.py --rollup`) и корректировка плана — вручную.
- **Граф-store** пишется на commit-reindex, не привязан к task-циклу (read OK).
- **Сквозной прогон `/run-1c-task` на живой 1С-задаче ещё не делался** — приёмка предложена (roadmap 260614).

## Связанные разделы

- [43.1 Обзор и парадигма](43.1_ОБЗОР_И_ПАРАДИГМА.md) · [43.2 Команды и режимы](43.2_КОМАНДЫ_И_РЕЖИМЫ.md) ·
  [43.3 Маршрутизация, хуки, инструментирование](43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md) ·
  [43.4 Справочник инструментов](43.4_СПРАВОЧНИК_ИНСТРУМЕНТОВ.md)
- [Глава 17 «Тестирование 1С»](../17_ТЕСТИРОВАНИЕ_1С/17.5_КОМАНДЫ_ПАЙПЛАЙНА.md) — этап 4 ·
  [Глава 11 «Система скиллов»](../11_СИСТЕМА_СКИЛЛОВ/11.1_Обзор.md) · [Глава 27 «Unified Memory»](../27_UNIFIED_MEMORY/27.1_Обзор.md)
