# 43.7 Анализ всей конфигурации — инструментарий массового аудита

> **Глава об инструментах анализа на уровне ВСЕЙ конфигурации 1С** (не одной задачи): вес объектов,
> качество кода, граф зависимостей, инвентарь и валидация метаданных, аудит форм/СКД/макетов.
> Питает **этап 1 (Планирование)** пайплайна ([43.1](43.1_ОБЗОР_И_ПАРАДИГМА.md)) — impact-анализ,
> поиск похожего, оценка масштаба — и операционные аудиты вне задач. Состояние на **2026-06-21**
> (ADR-030/031/032, гл. [28_1 Auto-Reports](../28_1_AUTO_REPORTS/), гл. [27 Память](../27_UNIFIED_MEMORY/27.1_Обзор.md)).

## Зачем отдельный слой

[43.4 Справочник инструментов](43.4_СПРАВОЧНИК_ИНСТРУМЕНТОВ.md) перечисляет инструменты **на задачу**.
Здесь — **массовый срез по всей конфигурации**: «какие объекты самые тяжёлые», «где код-смелы», «кто кого
вызывает», «что не валидно», «аудит всех форм/СКД». Это поддерживает Планирование (оценка масштаба,
impact) и регулярные операционные аудиты. Потребность **полностью закрыта существующими инструментами** —
отдельный Java-проект на `mdclasses` не нужен (см. §«Эффективность»).

## 5 слоёв инструментария (обзор)

| Слой | Инструмент | Готов | Что даёт на уровне всей конфигурации |
|---|---|:--:|---|
| 1. Качество кода (CI) | SonarQube BSL + BSL LS | ✅ | code smells / сложность / диагностики по всем `.bsl` |
| 2. Движок разбора метаданных | BSL Language Server (= `mdclasses` внутри) | ✅ | типизированный разбор MDO вкл. формы — **без Java-build** |
| 3. Граф кода | `build_call_graph.py` + `bsl-semantic-search` | ✅ | вызовы / импакт / dead-code / статистика по всем модулям |
| 4. Инвентарь + валидация метаданных | EDT-MCP | ✅ | состав объектов + маркеры валидации по всей конфигурации |
| 5. Поэлементный → массовый (offline) | `cc-1c-skills` (`*-info`) | ✅ | батч-аудит форм/СКД/метаданных по файлам, скриптуемо |

## Полный инвентарь инструментов анализа по этапам (с реальными прогонами)

Этапы пайплайна: **1** Планирование · **2** Дизайн · **3** Кодирование · **4** Тестирование
([43.1](43.1_ОБЗОР_И_ПАРАДИГМА.md)). Колонка «Прогон» — **реальный пример, выполненный 2026-06-21** на боевой
`Конфигурация`/ИБTransportManagementDevelop (✅ работает · ⚠ работает с оговоркой · ❌ не работает).

| Инструмент (тул) | Сервер/скрипт | Этап | Что анализирует | Реальный прогон 2026-06-21 → результат |
|---|---|:--:|---|---|
| `get_metadata_tree` | 1c-mcp-crud | 1 | инвентарь всех объектов | ✅ 96 справ. / 32 док. / 202 РС / 566 ОМ / 216 ролей / 35 отч. / 18 подсист. |
| `scan_metadata_index` | codepilot1c / edt | 1 | индекс объектов + фильтры | ✅ docs `*Направление*`→1, reports→35 |
| `cf-info` | cc-1c-skills | 1 | обзор конфигурации (счётчики/состав) | ✅ smoke OK (дублирует `get_metadata_tree` offline) |
| `bsl_stats` | bsl-semantic-search | 1 | размер индекса кода | ✅ `bsl_code_v4_late`, 4096d, **37 694** точки, green |
| `bsl_search` (hybrid) | bsl-semantic-search | 1,2 | семантический поиск кода | ✅ «разгрузка заблок. ТС» → `СформироватьНаправленияНаРазгрузкуДляРазблокированныхТС` (rel 1.123) |
| `bsl_impact_analysis` | bsl-semantic-search | 1,2 | транзитивный импакт (кто сломается) | ✅ `ПолучитьПоРегистрации` → **24** затронутых символа (d0/1/2) |
| `bsl_dead_code` | bsl-semantic-search | 4 | мёртвый экспорт по всей конфе | ✅ **3596** кандидатов (с путями/строками) |
| `bsl_object_info` | bsl-semantic-search | 1 | объект + граф-статы | ⚠ резолв по имени папки капризен (`not found` на `гкс_*`) |
| `edt_metadata_details` / `get_metadata_details` | codepilot1c / edt | 1,2 | свойства + дети объекта | ✅ `гкс_НаправлениеНаРазгрузку`: 16 реквиз. / 2 формы / 1 ТЧ |
| `bsl_list_methods` | codepilot1c | 2,3 | методы модуля + сигнатуры + доки | ✅ ManagerModule → **14** методов (export/строки/документация) |
| `bsl_analyze_method` | codepilot1c | 2,3,4 | сложность / unused / риски | ✅ `СоздатьДокументНаОсновании`: loc 24, cyclo 2, try 1, unused-параметр `ДанныеЗаполнения` |
| `inspect_form_layout` / `form-info` | codepilot1c / cc-1c-skills | 1,2 | дерево формы | ✅ `ФормаДокумента`: 25 элементов + команда |
| `dcs_manage` / `skd-info` | codepilot1c / cc-1c-skills | 1,2 | структура СКД | ⚠ codepilot=0 (read сломан); ✅ cc-1c-skills (наборы/поля/параметры) |
| `inspect_template` / `mxl-info` | codepilot1c / cc-1c-skills | 1,2 | макет (табличный документ) | ⚠ codepilot не читает `.mxlx`; ✅ cc-1c-skills (области/ячейки/merge) |
| `validate_query` | edt / codepilot1c / crud | 3 | синтаксис+семантика запроса | ✅ нет-поля → `line:col`; `dcsMode` → «нельзя указывать итоги» |
| `execute_query` | 1c-mcp-crud | 1,4 | запрос к живой ИБ | ✅ `КОЛИЧЕСТВО гкс_НаправлениеНаРазгрузку` = **29 873** |
| `execute_code` | 1c-mcp-crud | 4 | runtime BSL / рендер | ✅ макет → PDF 41 553 б (см. ADR-031 R-1) |
| `get_project_errors` / `get_problem_summary` | edt-mcp | 4 | маркеры валидации | ✅ `гкс_ПечатьАктРасхожденияВеса` → 2 BSL-варнинга |
| `update_database` | edt-mcp | 3,4 | деплой конфигурации в ИБ | ✅ INCREMENTAL → UPDATED (ADR-031) |
| `build_call_graph.py` → `bsl_call_graph.db` | скрипт | 1 | граф вызовов всей конфы | ✅ ~33k символов / 80k вызовов / 2k модулей |
| `analyze_run.py --mode graph` | скрипт | 4 | авто-отчёт по графу (dangling/orphan) | ✅ 33 880 символов / 80 246 вызовов / **5 353** user-dangling (typos/удалённые) → `data/reports/graph/` |
| SonarQube BSL | sonar-scanner | 4 | smells / сложность всей конфы | ✅ полный скан 2026-06-21 (см. ниже): 639 621 ncloc, **57 268 BSL-issues**, Quality Gate=ERROR |
| `bsl-platform-context` | MCP | 2,3 | справка API платформы 8.3.27 | ✅ `ПолучитьМакет` → `ПолучитьМакетОформления` (возвр. ТабличныйДокумент) |
| `1c-debug-hmr` / `bsl-debugger` | MCP | 2,4 | live BP-trace / runtime | ◑ per-task, не whole-config |
| `search_symbols` / `find_callers` / `get_module_ast` | **bsl-code-search** | 2,3 | символы/вызовы по имени | ✅ **ИСПРАВЛЕНО 2026-06-21** (было ❌ пусто): фикс `BSL_SOURCE_DIR` + кириллица в regex → 1954 файла / 32901 символ; `search_symbols ПолучитьПоРегистрации`→3, `find_callers`→3. ⚠ нужен `/mcp reconnect` |

### Находки реальных прогонов (важно)
- **`bsl-code-search` — ИСПРАВЛЕНО 2026-06-21** (было: пусто на реальном символе с 24 caller'ами). Два бага:
  (1) `.mcp.json` `BSL_SOURCE_DIR=configuration` (1 .bsl — JIRA-папки) вместо `…/Конфигурация/src` (2104 .bsl);
  (2) JS-regex `\w` в `index.js` не матчит кириллицу → имена методов/вызовов 1С пропускались (после фикса
  пути индексировалось лишь 11 файлов). Фикс: путь + оба `(\w+)`→`([\wА-Яа-яЁё]+)`. Проверено end-to-end:
  1954 файла / 32901 символ / 25379 call-targets, `search_symbols`/`find_callers` работают. **Нужен
  `/mcp reconnect`**, чтобы live-сессия подхватила (запущенный сервер держит старый код). Урок: в JS-парсерах
  1С `\w`/`\b` ASCII-only → всегда `[\wА-Яа-яЁё]` / `\p{L}` с флагом `u`.
- **`bsl_object_info`** — резолвит объект по имени папки капризно (`not found` на `гкс_*`); для свойств
  объекта надёжнее `edt_metadata_details` / `get_metadata_details`.
- **Чтение СКД/макетов:** у codepilot1c read контента сломан (`dcs_manage`=0, `inspect_template` не видит
  `.mxlx`) — структуру брать из `cc-1c-skills` (`skd-info`/`mxl-info`) или прямого XML (ADR-031/032).
- **Два среза «инвентаря» расходятся by design:** `get_metadata_tree` (566 ОМ / 216 ролей — все
  зарегистрированные объекты, вкл. без кода) vs FS-обход по `src/` (438 ОМ-каталогов с содержимым) — разные
  линзы (метамодель vs файлы), обе валидны.

### SonarQube — полный скан всей конфигурации (2026-06-21, реальные числа)
Прогон `scripts/run-sonar-analysis.ps1` (scanner 6.2.1 → сервер v26.6.0; токен через admin:admin).
⚠ **Операционка:** wrapper вернул `exit 255` в пост-шаге, но **анализ успешно загружен** (`report-task.txt` +
ceTaskId, measures на сервере) — статус скана проверять **по API** (`/api/project_analyses`, `/api/measures`),
НЕ по exit-коду. BSL-сенсор (= BSL LS) отработал 2104 файла за 29 с.
- **Объём:** ncloc **639 621**, сложность 141 164, дубли 6.7%, тех-долг (SQALE) ~287 583 мин (~599 чел-дней).
- **Issues всего:** bugs **1 904** · vulnerabilities **314** · code_smells **57 753**.
- **BSL-issues: 57 268** — по severity: BLOCKER **25** · CRITICAL **4 429** · MAJOR 12 473 · MINOR 23 440 · INFO 16 901; по типу: CODE_SMELL 56 263 · BUG 691 · VULNERABILITY 314.
- **Top-8 BSL-правил:** `LineLength` 10 163 · `MissingSpace` 6 260 · `MagicNumber` 5 211 · `MissingParameterDescription` 4 147 · `Typo` 4 141 · `UsingServiceTag` 3 598 · `DuplicateStringLiteral` 2 541 · `IfElseIfEndsWithElse` 2 405.
- **Quality Gate: ERROR** (условия new-code): `new_violations` 28 (порог 0) · `new_security_hotspots_reviewed` 0% (порог 100) · `new_reliability_rating` 3 (порог 1) · `new_duplicated_lines_density` 3.16% (порог 3). Dashboard: `http://localhost:9000/dashboard?id=upravlenie-transportom-plk`.

### Профиль правил BSL — максимальная проверка («библиотека ошибок»)
Плагин `communitybsl` **v1.18.1** (актуальный) — **180 правил загружено**. Встроенный профиль
«BSL Language Server rules» (read-only) активирует только **167** (default-on в BSL LS); 13 ценных
диагностик помечены `activatedByDefault=false`. Создан кастомный профиль **«1C BSL Way profile»**
(наследник дефолтного) скриптом [`scripts/sonar_setup_quality_profile.py`](../../../scripts/sonar_setup_quality_profile.py)
(идемпотентно: create → change_parent → activate_rule → set_default) + активированы **все 13** default-off →
**180/180 active**, назначен default — режим **жёсткой/максимальной проверки**. Включённые ценные 1С-правила:
`FieldsFromJoinsWithoutIsNull` (ЕСТЬNULL), `MissingTempStorageDeletion`, `DenyIncompleteValues` (незаполненные
измерения регистров), `UsingLikeInQuery` (ПОДОБНО), `CodeAfterAsyncCall`, `UseSystemInformation` +
безопасность (`FileSystemAccess`/`InternetAccess`) + стилевые (`BadWords`/`TernaryOperatorUsage`/`TooManyReturns`/`FunctionNameStartsWithGet`).
**Ре-скан 2026-06-21 под 180 правилами:** BSL-issues 57 268 → **64 286** (+7 018), vulnerabilities 314 → **1 311** (+997, в осн. `FileSystemAccess` 922 / `InternetAccess` 75), bugs 1 904 → 2 258, тех-долг +~71 чел-дней. Новые содержательные: `MissingTempStorageDeletion` 399 (утечки врем. хранилища), `DenyIncompleteValues` 381, **`FieldsFromJoinsWithoutIsNull` 300 (ЕСТЬNULL)**, `UsingLikeInQuery` 54; основной шум — стилевой `TernaryOperatorUsage` 2 703 (кандидат на off, если мешает).

### Этап исправления ошибок по итогам SonarQube ([ADR-033](../../../.claude/skills/architecture-research/adr/033-sonar-remediation-stage.md))
Триггерный remediation-этап поверх Sonar-отчёта (раньше его не было — Sonar только репортил, QG=ERROR даже не блокировал CI). 4 шага:
1. **Pull + приоритизация:** [`scripts/sonar_issues_pull.py`](../../../scripts/sonar_issues_pull.py) (zero-dep) — `/api/issues/search` → группировка severity/правило/файл → worklist в `data/reports/sonar/`. Флаги: `--severities`, `--types BUG,VULNERABILITY`, `--path-contains` (фокус кастома), `--max`. Проверено live: **4454/4454** BLOCKER+CRITICAL.
2. **Scope-гейт (ОБЯЗАТЕЛЬНО):** править **только кастомный код** (`гкс_*`, `configuration/<JIRA>`) + **реальные дефекты** (BUG / VULNERABILITY / `CreateQueryInCycle` / `ExecuteExternalCode`). **НЕ трогать вендорную БСП** — ~90% issues именно там (топ-файлы: `УправлениеДоступомСлужебный` 367, `УниверсальныйОбменДаннымиXML` 196), но правки сорвут обновление типовой.
3. **Fix через 1С-конвейер:** батч = один файл/модуль → содержательные через `/implement-1c-task` (BSL пишет Opus), тривиальные (`MissingSpace`/`LineLength`/`Typo`) — пакетно EDT-MCP.
4. **Re-scan + verify:** повторный `run-sonar-analysis.ps1` → дельта по затронутым файлам (`sonar_issues_pull.py` → 0) → замкнуть на QG «new code».

**Обязательный паттерн** (2026-06-21, PATTERN 2.14): обработка Sonar-багов идёт **только** через команду/скилл [`/fix-sonar-task`](../../../.claude/skills/fix-sonar-task/SKILL.md) — оркестратор `pull → триаж → (per-cluster) /analyze-1c-task → /implement-1c-task → /run-1c-tests → re-scan`. **Sonar-issue никогда не фиксится ad-hoc** — каждый реальный баг проходит пайплайн с ANALYSIS-REPORT (intended behavior зафиксирован); FP/extension/БСП/cosmetic → не код-правка. Запуск: `/fix-sonar-task --types BUG,VULNERABILITY` (или `python scripts/sonar_issues_pull.py …` для одного pull-шага). Жёсткий QG-блокер CI (fail на `new_violations`) — **отложен** (ADR-033): сперва baseline + scope на кастом, иначе БСП-наследие завалит любой PR.

### Триаж кастомных BLOCKER (живой прогон 2026-06-21)
Из 25 BLOCKER — **15 кастомных** (`гкс_`), 10 БСП (skip по scope-гейту). Триаж кастомных через `scan_metadata_index`/`bsl_list_methods`:
- **`ServerSideExportFormMethod` ×7** — экспортный серверный метод в модуле формы (конвенция БСП). Безопасный механический фикс (убрать `Экспорт`), но низкоценный.
- **`MissingCommonModuleMethod` ×4 — РЕАЛЬНЫЕ битые вызовы** (подтверждено): `гкс_ПодтверждениеДоставкиСообщений.СоздатьДокумент` (метода нет), `…ОтразитьТранспортныйДокумент` → в модуле есть `ОтразитьДвижения` (незакрытый рефакторинг-переименование). Рантайм-ошибка при исполнении ветки. **Фикс не механический** → 1С-пайплайн (нужен intended target).
- **`QueryToMissingMetadata` ×4 — смешанные:** `ПланОбмена.ИмяПланаОбмена` = плейсхолдер шаблона (**ложное**); `Документ.ЦБТ_ДвижениеЗапасов` ×3 — объект отсутствует в базе (префикс `ЦБТ_`: расширение/удалён) → проверить домен.
**Вывод:** Sonar-BLOCKER нельзя авто-фиксить вслепую — реальные баги (битые вызовы/запросы) требуют доменного анализа (1С-пайплайн), часть = ложные (плейсхолдеры/extension), часть = косметика. Это подтверждает scope-гейт + «не блокировать CI слепо» из [ADR-033](../../../.claude/skills/architecture-research/adr/033-sonar-remediation-stage.md).

### Маппинг этапов (кратко)
- **1 Планирование:** инвентарь (`get_metadata_tree`/`scan_metadata_index`/`cf-info`) + impact/поиск
  похожего (`bsl_search`/`bsl_impact_analysis`) + понимание объектов (`edt_metadata_details`/`form-info`/`skd-info`) + граф (`bsl_call_graph.db`).
- **2 Дизайн:** структура форм/СКД/макетов, сигнатуры методов (`bsl_list_methods`), API платформы
  (`bsl-platform-context`), runtime-trace гипотез (`1c-debug-hmr`).
- **3 Кодирование:** `validate_query` перед вставкой запроса, offline-правка артефактов (`cc-1c-skills`),
  `bsl_analyze_method` (ревью сложности/unused), `update_database` (деплой).
- **4 Тестирование:** `get_project_errors`/SonarQube/`bsl_dead_code` (аудит), `execute_code`/`execute_query`
  (runtime-verify), `bsl-debugger`/`1c-debug-hmr` (BP-verify), `analyze_run.py` (авто-отчёт).

---

## 1. CI-аудит качества кода — SonarQube BSL

[`sonar-project.properties`](../../../sonar-project.properties) → плагин `sonar-bsl-plugin-community 1.18.1`,
сканирует всю `ИБTransportManagementDevelop/Конфигурация`. Источники добавляются **динамически** через
[`scripts/sonar_sources.py`](../../../scripts/sonar_sources.py) (ADR-021 G19 — новые `configuration/<JIRA>`
авто-подхватываются без правки конфига). Плагин гоняет **bsl-language-server встроенно** (Sensor BSL Core,
verified 2026-06-15) → массовый прогон диагностик/код-смелов по всей конфигурации. Рецепт локального прогона:
память `reference_bsl_sonar_scan_recipe`. Coverage отложен (ADR-020 BLOCKED).

## 2. BSL Language Server — это и есть наш «mdclasses» (без Java-build)

Рабочие JAR'ы в репо: [`tools/bsl-ls/bsl-language-server.jar`](../../../tools/bsl-ls/bsl-language-server.jar),
`tools/multilspy-fork/.../0.22.0/bsl-language-server.jar`, + интеграция Serena.
**Ключевой факт:** BSL LS внутри использует `io.github.1c-syntax:mdclasses` для разбора метаданных
(включая формы). То есть движок `mdclasses` **уже упакован и запускаем** через SonarQube/LSP — отдельный
Java-проект на `mdclasses` писать не нужно (см. §«Эффективность» и [ADR-032](../../../.claude/skills/architecture-research/adr/032-1c-ui-report-tooling-selection.md)).

## 3. Граф кода всей конфигурации

- [`scripts/build_call_graph.py`](../../../scripts/build_call_graph.py) → [`cache/bsl_call_graph.db`](../../../cache/bsl_call_graph.db)
  — **~33k символов / 80k вызовов / 2k модулей** + детектор «висячих» (dangling) вызовов.
- MCP `bsl-semantic-search`: `bsl_call_graph`, `bsl_dead_code`, `bsl_impact_analysis`, `bsl_stats`,
  `bsl_similar`; `bsl-code-search`: `find_callers` / `search_symbols` / `get_module_ast`.
- [`scripts/analyze_run.py --mode graph`](../../../scripts/analyze_run.py) (`--source sqlite|neo4j|qdrant_graph`)
  — авто-отчёт по графу (dangling/orphan), гл. [28_1 Auto-Reports](../28_1_AUTO_REPORTS/). Плюс Neo4j +
  Qdrant `graph_embeddings`.

## 4. Инвентаризация и валидация метаданных (EDT-MCP)

EDT-MCP (см. [skill `edt-mcp`](../../../.claude/skills/edt-mcp/SKILL.md)): `get_problem_summary` /
`get_markers` / `get_project_errors` (маркеры валидации по всей конфигурации), `scan_metadata_index`,
`get_metadata_objects`, `get_objects_by_tags` / `get_tags`. Даёт массовый срез «что есть + что валидно».
Целостность ссылок — `resync_to_disk` (детектор dangling-ссылок в `Configuration.mdo`).

## 5. Поэлементный анализ, который батчится в массовый (cc-1c-skills, offline)

Вендоренный [`external/cc-1c-skills`](../../../external/cc-1c-skills/VENDOR.md) (ADR-031): `form-info`,
`skd-info`, `meta-info`, `role-info`, `cf-info`, `subsystem-info` — per-object, но **скриптуемы**
(Python/PowerShell). `glob` всех `Form.form` → `form-info` в цикле = массовый аудит форм **без Java**.
⚠ паттерн «один процесс на объект» дорог на масштабе (см. §«Эффективность»).

---

## Виды «веса» объекта и чем считать

«Вес» неоднозначен — разные метрики закрываются разными слоями:

| Метрика «веса» | Чем считать |
|---|---|
| по коду (строки/байты BSL на объект) | прямой обход EDT-src (single-pass скрипт) |
| по сложности кода (cyclomatic, smells) | SonarQube BSL (слой 1) + BSL LS |
| по связности (вызовы / импакт / dead-code) | `bsl_call_graph.db` + `bsl-semantic-search` (слой 3) |
| по структуре метаданных (#реквизитов / ТЧ / форм / движений) | EDT-MCP `get_metadata_details` (батч) / `cc-1c-skills meta-info` / `mdclasses` (типиз. модель) |
| по проблемам (маркеры валидации на объект) | EDT-MCP `get_problem_summary` / `get_markers` (слой 4) |

## Пример: вес по коду — живой срез 2026-06-21

Single-pass обход `Конфигурация/src` (без Java, ~секунды). **TOP-10 объектов по строкам BSL:**

| # | BSL-строк | объект |
|--:|--:|---|
| 1 | 50 939 | DataProcessor.ИнтеграцияС1СДокументооборот (78 форм) |
| 2 | 43 765 | CommonModule.УправлениеДоступомСлужебный |
| 3 | 18 127 | DataProcessor.КонвертацияОбъектовИнформационныхБаз |
| 4 | 17 090 | DataProcessor.УниверсальныйОбменДаннымиXML |
| 5 | 14 363 | CommonModule.ОбменДаннымиСервер |
| … | … | (БСП-модули; первый кастомный `гкс_` — #20 `гкс_ПриемкаТранспорта`, 8 409) |

**Сводка по видам:** CommonModules 438 объектов / 457 801 строк · DataProcessors 80 / 237 880 ·
Catalogs 96 / 98 321 · InformationRegisters 202 / 59 801 · Documents 32 / 28 314 · Reports 35 / 19 565.
**Итого: 2496 top-level объектов · 974 477 строк BSL · 206 МБ.** (Срез на 2026-06-21; обновляется повторным
прогоном.) Вес сосредоточен в **БСП**; кастомный код (`гкс_`) — малая доля.

## Эффективность: single-pass vs per-object (когда нужен mdclasses-напрямую)

Главная ось эффективности — **один проход с загрузкой модели в память** vs **обработка по объекту**
(subprocess/MCP на каждый), а НЕ «Java vs Python». Оценка на нашем конфиге (~2496 объектов):

| Подход | Модель | Время (оценка) |
|---|---|---|
| FS-скрипт (вес по коду) | 1 процесс, без парса XML | ~5–15 с (замерено) |
| single-pass Python по `.mdo` (lxml) | 1 процесс | ~20–60 с |
| `mdclasses` (Java) | 1 JVM-загрузка типиз. модели | ~10–60 с + build |
| SonarQube / BSL LS | 1 проход (внутри = `mdclasses`) | минуты, готовые метрики |
| `cc-1c-skills *-info` батч | **subprocess на каждый объект** | ~6–12 мин |
| EDT-MCP `get_metadata_details` батч | **MCP round-trip на каждый** + EDT | ~10–30 мин |

**Вывод (ADR-032):** `mdclasses` выигрывает в ~10–20× против per-object батчей, но **на нашем масштабе
ничьей с single-pass Python по `.mdo`** — выигрывает паттерн «один проход», не язык. Для метрик кода
SonarQube/BSL LS уже делают полный проход и **внутри = `mdclasses`**, поэтому свой Java-проект дублировал бы
имеющееся. `mdclasses`-напрямую оправдан **только** при регулярном CI-аудите **структуры метаданных** с
**множеством** запросов, где важны корректность типизации и масштаб (ERP 10k+ объектов).

## Когда что применять

| Задача | Инструмент |
|---|---|
| Вес/метрики кода по всей конфигурации | FS single-pass скрипт (мгновенно) + SonarQube (smells) |
| Качество/смелы/сложность в CI | SonarQube BSL (слой 1) |
| Impact/«кто вызывает»/dead-code | `bsl-semantic-search` + `bsl_call_graph.db` (слой 3) |
| Инвентарь + валидность объектов | EDT-MCP `scan_metadata_index` + `get_problem_summary` (слой 4) |
| Массовый аудит форм/СКД (структура) | `cc-1c-skills *-info` батч (слой 5) / EDT-MCP батч |
| Кастомные Java/CI-метрики структуры (масштаб ERP) | `mdclasses` (или его движок в BSL LS) — низкий приоритет |

## Связанные материалы

- ADR: [030 стратегия](../../../.claude/skills/architecture-research/adr/030-1c-ui-report-artifact-editing-strategy.md) ·
  [031 adopt cc-1c-skills](../../../.claude/skills/architecture-research/adr/031-cc-1c-skills-adopt-offline-mxl-dcs-editor.md) ·
  [032 выбор инструментария](../../../.claude/skills/architecture-research/adr/032-1c-ui-report-tooling-selection.md)
- Кеш фактов: [`1c-form-skd-spreadsheet-tooling-2026.md`](../../../.claude/skills/architecture-research/cache/1c-form-skd-spreadsheet-tooling-2026.md) (рейтинг ≥12 инструментов)
- [43.4 Справочник инструментов](43.4_СПРАВОЧНИК_ИНСТРУМЕНТОВ.md) (на задачу) · [28_1 Auto-Reports](../28_1_AUTO_REPORTS/) · [27 Память](../27_UNIFIED_MEMORY/27.1_Обзор.md)
