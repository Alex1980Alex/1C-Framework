# 43.7 Аудит консистентности слоёв пайплайна

> Честный снимок согласованности между тремя слоями пайплайна 1С (команда → скилл → хук, см.
> [43.6](43.6_АНАТОМИЯ_КОМАНД.md)). Источник — сквозной разбор всех 5 команд, 4 скиллов и 8 хуков +
> `settings.json` (2026-06-16). Каждый пункт — с `file:line`. Классификация: ✅ исправлено (первый заход) ·
> 🔵 by-design · ✅ закрыто (второй заход). Открытых пунктов (⚠) не осталось.

## Методика

Три параллельных чтения исходников (не по памяти): (1) все `.claude/commands/*1c*.md`; (2) скиллы
`analyze-1c-task-v2`, `implement-1c-task`, `run-1c-task`, `va-bdd-testing`; (3) хуки пайплайна + регистрация
в `settings.json`. Сверялись: `allowed-tools` команды vs инструменты, упомянутые в скилле; версии frontmatter
vs тело; имена MCP-серверов; наличие frontmatter/`commands:`.

**Сводка:** найдено ~22 расхождения. **Исправлено 10** (первый заход) + **4** (второй заход, после сверки
с `.mcp.json`), **by-design 4** (разъяснено), **осталось 0**.

---

## ✅ Исправлено (2026-06-16)

| # | Что было | Где (`file:line`) | Исправление |
|---|---|---|---|
| 1 | В блоке «ВАЖНО» выпали имена инструментов («Для BSL используй ⟨пусто⟩…») | `commands/analyze-1c-task.md:78-82` | Восстановлены: `bsl-semantic-search`/`bsl-code-search`, `bsl-platform-context`, `bsl_similar`, `pdf-vector-graph`, `memory-orchestrator__route_and_save` |
| 2 | Тело называло допущенные MCP «Serena, ast-grep…» — противоречие `:78` («не Serena») и frontmatter | `commands/analyze-1c-task.md:53` | Приведено к фактическому набору: `bsl-semantic-search, bsl-platform-context, 1c-mcp-crud, pdf-vector-graph, 1c-debug-hmr` |
| 3 | «Запуск — через `/run-1c-tests` (будет создан отдельно)» — команда уже существует | `commands/write-1c-tests.md:535-536` | Устаревший плейсхолдер удалён |
| 4 | Старое имя сервера `1c-mcp-server` в 8 местах: `allowed-tools`, примеры кода Stage 4/4a, ссылка на соседний скилл | `skills/va-bdd-testing/SKILL.md:27-30`, `:996`, `:1014`, `:1030`, `:1480` | Все → `1c-mcp-crud` (везде используется crud, см. память [[project_1c_mcp_replacement]]); каталога `skills/1c-mcp-server/` не существует — ссылка была битой |
| 5 | Нет frontmatter вовсе (ни `description`) | `commands/write-1c-tests.md:1` | Добавлен `description` (без рестриктивного `allowed-tools` — наследует от скилла) |
| 6 | Нет frontmatter вовсе | `commands/run-1c-tests.md:1` | Добавлен `description` |
| 7 | `version: 4.2.0`, но тело описывает v4.3 (`--auto-propose`) | `skills/analyze-1c-task-v2/SKILL.md:7,21` | Синхронизировано: `version: 4.3.0`, `updated: 2026-05-20`, заголовок → v4.3 |
| 8 | `version: 2.8.0`, но тело описывает v2.8.1; нет `commands:` | `skills/implement-1c-task/SKILL.md:4-12` | → `version: 2.8.1`, `updated: 2026-06-15`, добавлен `commands: /implement-1c-task` |
| 9 | Нет `version`/`commands:` в frontmatter | `skills/run-1c-task/SKILL.md:1-4` | Добавлены `version: 1.0.0`, `updated: 2026-06-15`, `commands: /run-1c-task` |
| 10 | Нет `commands:` (связь с двумя командами только по обратным ссылкам) | `skills/va-bdd-testing/SKILL.md:36-39` | Добавлен `commands: /write-1c-tests, /run-1c-tests` |

После правок: поле `commands:` есть у всех четырёх скиллов; версии frontmatter совпадают с телом; имена
MCP-серверов в `va-bdd-testing` согласованы с остальным пайплайном; у всех 5 команд есть `description`.

---

## 🔵 By-design (разъяснено — не дефект)

| Наблюдение | Где | Почему так задумано |
|---|---|---|
| Имя команды ≠ имя скилла (`/analyze-1c-task` → `analyze-1c-task-v2`) | `commands/analyze-1c-task.md:2` | Методика версионируется независимо; одна методика может обслуживать несколько команд |
| `/run-1c-task`, `/write-1c-tests`, `/run-1c-tests` без `allowed-tools` | `commands/run-1c-task.md`, `write-1c-tests.md`, `run-1c-tests.md` | Оркестратор и тест-команды **наследуют** инструменты делегируемого скилла (`va-bdd-testing` несёт свой `allowed-tools`) |
| Хардкод путей `D:\va-test\...` | `commands/run-1c-tests.md:249`, `write-1c-tests.md:503` | Реальное расположение VA-харнесса (память [[reference_va_debug_attach]]) — не баг среды |
| «4 основных MCP-сервера», хотя инструменты от 8 серверов | `commands/implement-1c-task.md` (раздел «4 основных MCP-сервера») | «4 основных» (EDT-MCP, 1c-mcp-crud, bsl-debugger, 1c-debug-hmr) + вспомогательные — терминологическое сокращение |

---

## ✅ Закрыто (второй заход, 2026-06-16)

Все 4 пункта прежнего раздела ⚠ разрешены после сверки с `.mcp.json` (реальные имена серверов):

| # | Наблюдение | Резолюция |
|---|---|---|
| 1 | Проза `bsl-debug-server` vs неймспейс `mcp__bsl-debugger__*` | `.mcp.json`: статанализатор `bsl_analyze`/`bsl_execute` — это MCP-сервер **`bsl-debugger`** (`tools/bsl-debugger/`, OneScript). `bsl-debug-server` — лишь **каталог** `tools/bsl-debug-server/`, где живут RDBG-серверы `1c-debug`/`1c-debug-hmr`. Прозаическое `bsl-debug-server` → `bsl-debugger` в команде `implement-1c-task.md` (4 места) и скилле `implement-1c-task/SKILL.md` (~13 мест) |
| 2 | Plain `mcp__1c-debug__*` не в `allowed-tools` | Сервер `1c-debug` реален (`.mcp.json`). В `allowed-tools` команды `implement-1c-task.md` добавлены 13 plain-инструментов `mcp__1c-debug__*` — флаг `IMPLEMENT_1C_USE_PLAIN_DEBUG=true` теперь покрыт whitelist'ом |
| 3 | `ast-grep-mcp` + `mcp-reasoner` | **`ast-grep-mcp`** — реальный сервер (`.mcp/full.json`, `bsl.json`, `lazy-mcp-config.json`) → оставлен. **`mcp-reasoner`** — фантом (нет ни в одном `.mcp*.json`): удалены оба упоминания в `write-1c-tests.md` (строка-таблица + проза) |
| 4 | Рассинхрон параметров `run-1c-tests` | Каноном принята таблица «Параметры команды» (9 флагов). Раздел «Входные данные» дополнен `--retries`/`--no-retry`/`--fix-mode`/`--verbose` + указатель на полную таблицу |

Открытых пунктов нет.

---

## Инвариант поддержки

Перечень — **снимок на 2026-06-16**. Чтобы аудит не устаревал: при правке команды/скилла сверять три
слоя (`allowed-tools` команды ↔ инструменты скилла ↔ хуки состояния), держать `version`/`commands:` в
frontmatter скилла актуальными, а имена MCP-серверов — едиными (`1c-mcp-crud`, не `1c-mcp-server`).
Раздел «✅ Закрыто (второй заход)» снял все ранее открытые пункты (сверка с `.mcp.json`); открытых нет.

---

## Связанные разделы

- [43.6 Анатомия команд пайплайна (3 слоя)](43.6_АНАТОМИЯ_КОМАНД.md) — описание слоёв, по которым проведён аудит.
- [43.5 Сквозная карта](43.5_СКВОЗНАЯ_КАРТА.md) — оценка покрытия и пробелов на уровне процесса (H1–H7, N1–N12).
- [43.4 Справочник инструментов](43.4_СПРАВОЧНИК_ИНСТРУМЕНТОВ.md) — эталонный перечень инструментов по этапам.
