# 27.13 Memory Subsystem Changelog (§§22–27, verbatim из CLAUDE.md)

> Исторические детальные заметки эволюции подсистемы памяти, вынесенные из корневого
> `CLAUDE.md` (раздувал файл до 41.5k > лимита 40k). Содержит commit-хэши, env-флаги, пути
> файлов и счётчики тестов на момент landing'а. Канонические главы: 27.9–27.12. Перенесено 2026-06-05.

---

## Полный текст буллета `**Memory**` (verbatim, одной строкой как был в CLAUDE.md)

- **Memory**: `src/memory/` (ai_memory, vector_memory, skill_learning, orchestrator, infrastructure). P0-P4 DONE, P5 in progress. Hook: `session-memory-save.py` (Stop, auto-saves session to SQLite). Skill: `memory-unified`. **L5 drafts pipeline (2026-05-14)**: `session-memory-save.py:try_promote_patterns()` после save вызывает `python -m scripts.export_graph_to_wiki promote-patterns` (subprocess timeout 4s, swallow errors, opt-out `SESSION_MEMORY_NO_PROMOTE=1`); закрывает L2→L5 promotion gap, документированный в [roadmap 260514](docs/roadmap/260514_ROADMAP_WIKI_PROMOTION_GAP.md). Field-name drift `usage_count`→`application_count` исправлен в `src/memory/librarian/wiki_promoter.py`; canonical field пишется `src/memory/vector_memory/server.py:391 handle_apply_pattern` (bayesian update `confidence += 0.02 if success else -0.01`, clamp [0,1]; `application_count += 1`). Pipeline-activation подтверждена smoke-тестом end-to-end (synthetic bump → CLI создал draft + log entry → state revert). **Confidence Lifecycle (§22, 2026-05-31, P0+P1 DONE)**: наивный `+0.02/−0.01` заменён на **decayed Beta(7,3) posterior** — `src/memory/vector_memory/confidence.py` (pure: `derive_confidence`/`apply_to_payload`/`decay_counts`/`seed_counts_from_legacy`) + `reinforce.py` (fail-soft Qdrant wrapper); `confidence=(7+succ)/(10+succ+fail)`, старт 0.70, lazy count-decay (дрейф к prior, не к 0), legacy-миграция `succ=conf·n`. **P1 reinforcement loop** (`.claude/hooks/shared/pattern_reinforce.py`): `memory-first-hook` логирует surfaced learned_patterns (score≥0.3) → `session-memory-save` на Stop зовёт `reinforce_session(detect_session_success(ctx))` (idempotent per-session sentinel + per-pattern cooldown 6h + cap 50 + opt-out `P1_REINFORCE_DISABLE=1`, fail-soft). Стратегия + research (~20 repos): [roadmap §22](docs/roadmap/260523_ROADMAP_FULL_DEV_LIFECYCLE_ANALYSIS.md). **P0-P4 DONE** (P2 lazy decay-on-read `payload_effective_confidence`; P3 forgetting/revive `expired_at` invalidate-not-delete + `should_archive`; P4 FSRS-lite `stability_adjusted_rate` — established затухают медленнее), independent review исправил critical surfacing-баг (#1), live-verify на реальном Qdrant PASS. **Lifecycle logging (2026-05-31, «логирование всех процессов»):** confidence-мутации трассируются в `.claude/cache/confidence-lifecycle.log` (JSONL) через общий модуль [`src/memory/vector_memory/lifecycle_log.py`](src/memory/vector_memory/lifecycle_log.py) (`log_event()`, fail-soft, atomic-rotation 2MB, opt-out `CONFIDENCE_LOG_DISABLE=1`). Пишут одним файлом обе стороны: `reinforce.py:reinforce_pattern` (`reinforce` с old→new confidence + succ/fail + application_count, `reinforce_miss`, `reinforce_error`) и Stop-hook `shared/pattern_reinforce.py:reinforce_session` (`session` с applied/skipped/errors/candidates, `session_error`) — раньше rich status-dict отбрасывался. Метаданные-only (pattern_id/counts/floats/truncated-errors, без тел паттернов). **Scope (review follow-up):** reinforce + Stop-hook + **MCP-side `server.py`** — `apply`/`decay_sweep`/`save`/`delete` (зеркалят §24 epoch-bump точки) + `server_start` (pid stamp → `/mcp reconnect` наблюдаем; MCP-side требует reconnect). **Append:** buffered `open(...,"a")` — cross-process race принят low-risk для trace-лога; `os.open(O_APPEND)`-вариант откатан (хуже на win32: concurrent-open sharing violations теряли записи). code-verify PASS, 130/130 тестов. Дополняет §24 surfacing-trace. **⚠ Важно ([[feedback-mcp-stale-code-reconnect]]):** правки кода MCP-сервера (`src/memory/vector_memory/*`) требуют `/mcp reconnect` — persistent stdio-процесс держит старый код; хуки спавнятся свежими. **§23 Auto-Cache Pipeline (ADR-D5)**: Stop-хук [`lifecycle-cache.py`](.claude/hooks/lifecycle-cache.py) авто-кеширует dev-lifecycle (research→...→lessons) детерминированно из transcript+git в `data/lifecycle/*.md` (`shared/lifecycle_extract.py`, substantive-gate, idempotent, opt-out `LIFECYCLE_CACHE_DISABLE=1`). Ограничение: WebSearch в субагентах не в главном transcript. **§24 Memory Surfacing (ADR-D6, P0 `6906210c9`)**: `memory-first-hook` всплывает learned_patterns **семантически** (в `SEMANTIC_COLLECTIONS`) + **confidence-gating** (`_pattern_score_gate`: hard floor eff<0.15→drop + floored-multiply `×max(0.3,eff)`; raw multiply — анти-паттерн per research) + **archived hard-exclude** (`expired_at`; opt `MEMORY_INCLUDE_ARCHIVED=1`) + token-overlap TEI-down fallback. Замыкает §22 confidence на surfacing. **P1 DONE (`74a4aa761`):** true hybrid — arms `{skill,experience,conversation,pattern_dense,pattern_lexical}` → client-side **RRF k=60** (`rrf_merge`, lexical 0.7/dense 0.3 для BSL), token-overlap ALWAYS-ON, TEI-down→lexical-only. Side-fix: §22 P3 `handle_search_patterns` archived ×0.5→hard-exclude. **P2 DONE (off-by-default):** `_rerank_results()` post-fusion LLM reorder top-N (Ollama `qwen2.5-coder:7b`, зеркалит BSL `_llm_rerank`) — gated `MEMORY_RERANK=1`, т.к. эмпирически ~2.5s warm/~6.5s cold > hot-path budget (enable → поднять `settings.json` timeout ≥10s + warm Ollama); skippable → fused-order; **§24 полностью закрыт (P0+P1+P2)**. **Execution cache (2026-05-31):** `memory-first-hook` hash-memoize'ит весь pipeline по `hash(query_tokens)` на диске (`.claude/cache/memory-first-surfacing-cache.json`, TTL `MEMORY_SURFACE_CACHE_TTL=300s`, FIFO 200, opt-out `MEMORY_SURFACE_CACHE_DISABLE=1`); hit пропускает TEI+Qdrant+RRF+rerank (~2s), reinforcement реплеится. **Epoch-keyed invalidation (2026-05-31):** `cache_key` теперь включает `confidence-epoch` ([`src/memory/vector_memory/epoch.py`](src/memory/vector_memory/epoch.py) — `bump()` атомарно пишет `time.time()` в `.claude/cache/confidence-epoch.txt`, `read()` — дешёвый локальный read **без Qdrant-roundtrip**, fail-soft→0.0); любая мутация confidence/archive (`reinforce.py:reinforce_pattern` + server.py `handle_apply_pattern`/`decay_confidence`/`save_pattern`) бампает эпоху → мгновенный cache-miss, закрывает 5-мин stale-window между §22 confidence и §24 surfacing. Server.py-бампы — persistent MCP-процесс → требуют `/mcp reconnect` ([[feedback-mcp-stale-code-reconnect]]); reinforce.py + hook-reader спавнятся свежими (effective сразу). **Pipeline trace logging (2026-05-31, §24.4 «логирование всех процессов»):** `memory-first-hook` пишет structured JSONL на каждый вызов в `.claude/cache/memory-first-surfacing.log` (`_surface_log()` + module-global `_TRACE`; `_trace_reset/_trace_set/_trace_gate`): per-stage `cache` (hit/miss), `tei` (ok/down/no_embedding + `tei_error`), `arms` (hit-counts skill/experience/conversation/pattern_dense/pattern_lexical), `gate` (archived/below_floor/passed), `layers`, `rrf_fused`, `rerank` (off/too_few/no_budget/error/no_ranking/parse_error/applied), `reinforced_pids`, `duration_ms`, terminal `outcome` (injected/injected-cached/no-results/cached-empty). Закрывает «слепоту» fail-soft pipeline (`except: pass`). Fail-soft, atomic size-rotation (mkstemp+`os.replace`, cap 2MB tail-keep), opt-out `MEMORY_SURFACE_LOG_DISABLE=1`, без prompt-контента/тел паттернов (counts+outcomes+truncated-160 errors). code-verify PASS, 18/18 тестов, E2E поймал `tei=down`. Анализ: `tail` + `json.loads` построчно. **cp1251 stdout hotfix:** surfacing-вывод через `_emit_stdout()` (UTF-8 bytes via `sys.stdout.buffer`, как `HookOutput.emit()`) — `print()` с кириллицей на cp1251-pipe ронял `UnicodeEncodeError` и молча терял всю инъекцию ([[feedback-windows-hook-stdout-cp1251]]). Research ~11 repos: [[memory-surfacing-ranking-2026]]. **Feedback draft-capture (2026-06-01):** курируемая память (`memory/*.md` + `MEMORY.md`) пишется только вручную; для авто-захвата код-ревью правил добавлены хуки [`userpromptsubmit-feedback-detector.py`](.claude/hooks/userpromptsubmit-feedback-detector.py) (UserPromptSubmit — детект «используй X вместо Y» RU+EN regex + обязательный code-token → ЧЕРНОВИК в `data/memory_drafts/` через [`shared/feedback_draft.py`](.claude/hooks/shared/feedback_draft.py), dedupe по нормализованному тексту, near-ready frontmatter, fail-soft) + [`stop-feedback-draft-aggregator.py`](.claude/hooks/stop-feedback-draft-aggregator.py) (Stop — батч-напоминание о pending-черновиках, cap 5, не блокирует). **Draft-mode:** в `memory/` ничего не пишется без подтверждения (перенос черновика вручную). Opt-out `FEEDBACK_DRAFT_DISABLE=1`. Smoke PASS. Закрывает пробел «session-memory-save пишет только сводку, курируемый слой — ручной». **§25 Surfacing Self-Tuning (2026-06-01, Part A+B DONE):** замкнута петля «измеряю→анализирую→тюню» поверх §24-логов. **A** — analyzer [`scripts/analyze_memory_effectiveness.py`](scripts/analyze_memory_effectiveness.py) + Stop-хук `memory-effectiveness-analyzer.py` (агрегаты hit/no-results/tei-down/gate-drop/confidence-drift + rule-based recs, reports в `data/reports/memory/`; опц. `--auto-rollback` (§25 B2): при config-attributable регрессии (gate_drop/no_results; TEI-down исключён) + наличии promotion-снапшота → авто `tune_memory_surfacing.py rollback`, gated `MEMORY_AUTOTUNE_ROLLBACK_APPLY=1`, dry-run by default). **B** — golden-set [`data/memory/golden/memory_queries.jsonl`](data/memory/golden/memory_queries.jsonl) (43 query, relevance по content-hash = rrf-dedup-ключ) + harness [`scripts/memory_golden_harness.py`](scripts/memory_golden_harness.py) (precompute-then-sweep: live `capture` раз → pure `evaluate` replays gating+RRF для любого config, Qdrant-independent; метрики hit/precision/recall/NDCG/MRR) + tuner [`scripts/tune_memory_surfacing.py`](scripts/tune_memory_surfacing.py) (`sweep` train → `promote` gated на held-out + `rollback`). **Hook-wiring:** `memory-first-hook.py::_load_surfacing_tuning()` оверлеит [`data/memory/surfacing_tuning.json`](data/memory/surfacing_tuning.json) на `SURFACE_RRF_WEIGHTS`/`MIN_SURFACE_CONF`/`CONF_FLOOR`/`SURFACE_RRF_K`/`SURFACE_CACHE_TTL` при импорте, clamped к `clamp_ranges`, fail-soft→defaults, reversible (удалить файл), opt-out `MEMORY_SURFACE_TUNING_DISABLE=1`. Promotion gated: best выбирается на train, **валидируется на held-out**, пишется только при улучшении target (NDCG+hit) без регрессии; dry-run by default (`--apply`/`MEMORY_AUTOTUNE_APPLY=1`), snapshot `surfacing_tuning.prev.json`, audit→confidence-lifecycle.log. Live baseline hit@5=0.79/NDCG@5=0.66; sweep+gate проверены end-to-end (train-winner `skill=0.9` overfit → held-out gate **корректно отказал** в промоушене). 16 smoke-тестов (10 harness + 6 tuner), code-verify PASS, 29/29 hook-тестов без регрессии. B3 (online-MAB) — FUTURE. Дизайн: [roadmap 260601](docs/roadmap/260601_ROADMAP_MEMORY_EFFECTIVENESS.md), chapter [27.10](docs/framework%20documentation/5_ПАМЯТЬ/5.1_UNIFIED_MEMORY/27.10_Memory_Surfacing_Quality.md). **§26 Ingestion/Sync (2026-06-03)**: P0 DONE (`content_hash` foundation, [260602 roadmap](docs/roadmap/260602_ROADMAP_MEMORY_INGESTION_SYNC.md)). **Q1 ADR**: `experience_embeddings`/`conversation_memory` → **DEPRECATED+dropped** (0-writer, роль покрыта episodic+`learned_patterns`; surfacing-arms убраны — [260603_ADR_Q1](docs/roadmap/260603_ADR_Q1_EXPERIENCE_CONVERSATION_COLLECTIONS.md)). **P1 D1.1 patterns-harvester** (Stop-хук [`patterns-harvester.py`](.claude/hooks/patterns-harvester.py) + [`shared/pattern_harvest.py`](.claude/hooks/shared/pattern_harvest.py)): майнит **confirmed** feedback-drafts (`data/memory_drafts/`, `metadata.status: confirmed`/`approved`, placeholder-free) + **session-lessons** (`data/lifecycle/*.md` "Lessons", noise-filter на auto-task-titles) → payload зеркалит `_pattern_to_payload` (+`content_hash`, conf seed 0.70) → прямой upsert в `learned_patterns` (т.к. `save_pattern` MCP-only). Passage-side TEI embed (БЕЗ query-instruction). Gating: dedup через deterministic `UUID5(content_hash)` point-id (re-harvest = 0 new) + per-run cap `PATTERNS_HARVEST_CAP=5` + content-quality floor; fail-soft (Qdrant/TEI down → exit 0); opt-out `PATTERNS_HARVEST_DISABLE=1`. 8 unit + live E2E (real TEI+Qdrant throwaway-collection) PASS. **P1 D1.2 skills-harvester** (Stop-хук [`skills-harvester.py`](.claude/hooks/skills-harvester.py) + [`shared/skills_harvest.py`](.claude/hooks/shared/skills_harvest.py)): инкрементально индексирует изменённые/новые `SKILL.md` в `skill_library` (зеркалит `index-skills-to-qdrant.py`: passage embed, point-id `uuid5(NAMESPACE_URL, skill_name)`, payload), idempotency по content-hash в `.claude/cache/skills-harvest-state.json`. **Cold-start seed**: первый запуск пишет хеши БЕЗ эмбеддинга (skill_library уже наполнен batch-индексером) → нет 80-embed шторма в Stop. Stale-cleanup: удалённый скилл → delete point. Per-run cap `SKILLS_HARVEST_CAP=10`, fail-soft, opt-out `SKILLS_HARVEST_DISABLE=1`. 7 unit + cold-start smoke (85 skills seeded) PASS. **P1 DONE** (D1.1+D1.2+D1.3). **P2 DONE (2026-06-03)** — episodic→semantic консолидация: **D2.1 reflection** ([`shared/reflection.py`](.claude/hooks/shared/reflection.py) + CLI [`scripts/reflect_memory.py`](scripts/reflect_memory.py)) кластеризует повторяющиеся episodic-факты `memory_ai.db` (Jaccard token-overlap union-find, `session_summary` исключён) → 1 semantic pattern на тему; триггер `size≥min_cluster` ИЛИ `Σimportance≥theta` (parameterised); **dry-run by default**, content count-free (стабильный content_hash при росте кластера). **D2.2 skill-learning bridge** — confirmed `data/skill_learning/patterns.jsonl` стал третьим источником `patterns-harvester` (AUTO, closes silo). Рефактор: общий `ingest_items()` (dedup→cap→embed→upsert + `on_created`-callback) из `harvest()`. **D2.3 DERIVES_FROM** links (pattern→source episodes) через `on_created` (только на реальный upsert, idempotent). 25 unit + live dry-run (25 кластеров→3 консолидации). P3 (cross-store sync/dedup) + P4 (scheduling/ForgetGate) — next. **§27 Full Observability (2026-06-05, P0-P4 DONE — наблюдаемость памяти 100%)**: «логирование всех процессов» доведено до полного покрытия 8 sink'ов × ~25 процессов ([260605 roadmap](docs/roadmap/260605_ROADMAP_MEMORY_FULL_OBSERVABILITY.md)). **P0** оживил мёртвые sink'и: `memory-ingestion.log` (harvester `_emit_ingest_stats` per-action + cadence `record_store_size`), ForgetGate script-path `run_forget`→`log_event("forget")`+`epoch.bump`, audit write-path `_audit` (CREATE/LINK/PROPAGATE/ROLLBACK + flush-on-stop, re-entrant-lock deadlock fix). **P1** новые per-process event-логи через shared [`src/memory/infrastructure/trace_log.py`](src/memory/infrastructure/trace_log.py) (`write_trace`, fail-soft/rotation/opt-out + global `MEMORY_TRACE_DISABLE`): routing (`route_and_save`→`memory-routing.log`), federated-read (`UnifiedSearchEngine.search`→`memory-read.log`, arm_hits/latency, query-текст НЕ логируется), propagation (`memory-propagation.log`, reach/depth), circuit-breaker (`_transition_to`→`memory-circuit.log`). **P2** persist: metrics-snapshot на orchestrator `stop()`→`memory-metrics.jsonl` (graceful-only) + cadence append-only `memory-maintenance-runs.jsonl`. **P3** единый envelope + сквозная корреляция: [`src/memory/infrastructure/event_envelope.py`](src/memory/infrastructure/event_envelope.py) (`normalize`/`make_envelope`/`known_sinks`, stable `CORE_KEYS` → DuckDB-колонки всегда есть; reader-адаптер, НЕ миграция on-disk per Q3) + DuckDB cross-log query [`scripts/memory_observability_query.py`](scripts/memory_observability_query.py) (mirror `audit_query.py`; views recent/by-source/error-rate/latency/freshness + **fact-trace** по `content_hash` ИЛИ `pattern_id`=`UUID5(content_hash)` — тредит факт ingestion→reinforce→forget одним запросом; CAST-fix DuckDB UUID-inference). Harvester теперь эмитит `content_hash`+`pattern_id` в ingestion-события (D3.2-ключ, backward-compat count-fallback). **P4** анализ: [`scripts/memory_observability_report.py`](scripts/memory_observability_report.py) — cross-process метрики (ingest_rate/dup_rate/forget_volume+rate/routing-dist/read-hit-rate+per-source/propagation-reach/circuit-trips/audit-coverage/store_growth) + **stale-sink regression detection** (present-but-stale=regression; `missing`/`empty` cold/MCP-side=НЕ regression) → `data/reports/memory/observability-*.md` + `_observability_latest.md`. Guardrails как §22/§24: fail-soft, metadata-only (без тел prompt'ов/паттернов), atomic-rotation 2MB, opt-out на каждый лог. 14 unit (P3+P4) + 5 (P1 trace) + 6 (P2) + 16 (P0), code-verify PASS, live-verified. ⚠ P1/P2 MCP-side логи появляются после orchestrator-run + `/mcp reconnect`. **Surfacing TEI-import fix (2026-06-05, найден сквозной проверкой §27):** `memory-first-hook` логировал `tei=down` со `ModuleNotFoundError: shared.semantic_search` на 100% вызовов — dense-плечи всегда пусты. Причина — **namespace-collision**: `src/shared/` (реальный пакет) затенял собственный `.claude/hooks/shared/` хука при `sys.path.insert(0, src_path)` (ради `memory.*`) → первый `import shared` привязывался к `src/shared` (без `semantic_search.py`/`pattern_reinforce.py`) на весь процесс. Фикс — `append` вместо `insert(0)` на 2 точках (hooks-dir остаётся на `sys.path[0]`); после — `tei=ok`, плечи `{skill,pattern_dense,pattern_lexical}` populated, hybrid восстановлен. Правило: в хуках, импортирующих и `shared.*` (hooks-local) и `memory.*` (src), добавлять `src` через **`append`**, НЕ `insert(0)` ([[feedback-hook-src-shared-collision]]). Сверка схемы 27.11 §9 с живыми логами подтвердила §22-математику (apply 0.70→0.7273=8/11; reinforce 11/15) + исправила stale «5 arms»→«3 arms» (experience/conversation удалены §26 Q1). **Cascade-on-apply wired (2026-06-05):** `handle_apply_pattern` (`vector_memory/server.py`) теперь зовёт `_cascade_confidence` — depth-1 same-store распространение confidence-дельты на соседей-паттернов по `SUPPORTS/EXTENDS/BASED_ON/DERIVES_FROM` (нудж Beta succ/fail, декей `link_strength×0.5×time`, bounded ≤10, fail-soft, gated `MEMORY_APPLY_CASCADE_DISABLE=1`, пишет `memory-propagation.log`, bump epoch). Закрывает §22 P3 deferred «neighbour-aware link_registry gate» + оживляет propagation-sink. ⚠ No-op пока `link_registry` граф — только тест-фикстуры (реальные pattern↔pattern связи появляются через DERIVES_FROM/cross-link/MIRRORS/PROMOTED_TO); MCP-side → `/mcp reconnect`. 5 unit + real-smoke + code-verify PASS. **P2:** 2 «light» паттерна оказались `session_summary`-миграциями — нормализатор корректно их пропускает (не pattern_type), `--apply` = no-op.

---

## 2026-06-11 — Chain-testing 260610: 5 production-фиксов по результатам прогона 26/29 цепочек

Тест-карта [260610_ROADMAP_MEMORY_CHAIN_TESTING](../../../roadmap/260610_ROADMAP_MEMORY_CHAIN_TESTING.md) (§18 — полные вердикты). Изменения в коде:

- **F2** — ai-плечо `unified_search` падало (`'<' not supported between float and str`) на legacy TEXT-`importance`: общий `_coerce_importance()` (label-map high/medium/… + clamp [0,1]) в [`memory_orchestrator.py`](../../../../src/memory/orchestrator/memory_orchestrator.py) (adapter-read / `_save_to_target`-write / propagation-handler) и [`ai_memory/server.py`](../../../../src/memory/ai_memory/server.py) (`save_important_message`); data-repair 3 строк. ⚠ MCP-side → `/mcp reconnect`.
- **F4** — `_cascade_confidence` ([`vector_memory/server.py`](../../../../src/memory/vector_memory/server.py)) не видел рёбра в unified-формате (`semantic:vector-memory:<pid>`), которые пишет orchestrator `create_link`: двойной lookup (bare+unified) + нормализация `target_id` (strip префикса; cross-store → `cascades_prevented`). ⚠ MCP-side → `/mcp reconnect`.
- **F6** — один malformed point ронял весь `search_patterns`: tolerant `EvidenceSource.from_dict` ([`models.py`](../../../../src/memory/vector_memory/models.py)) + **`pattern_harvest.py` пишет полную схему EvidenceSource** (`source_type/reference/weight/timestamp/metadata`) вместо `[{"source": …}]` ([`.claude/hooks/shared/pattern_harvest.py`](../../../../.claude/hooks/shared/pattern_harvest.py)); data-repair точки `d4b763da…`.
- **F7** — `PROMOTED_TO`-ребро молча терялось: [`export_graph_to_wiki.py`](../../../../scripts/export_graph_to_wiki.py) `cmd_promote_patterns` теперь передаёт `link_registry=LinkRegistry()` в WikiPromoter (opt-in параметр; этим путём ходит §26 P4 cadence и `try_promote_patterns`).
- **F11** — **`memory-first-hook.py`**: lexical-плечо получило собственные часы бюджета (`time.monotonic()` вместо общего `start`) — раньше TEI-попытка съедала `QDRANT_TIMEOUT`, и при TEI-down lexical давал 0 хитов ровно в сценарии «sole survivor», который обещал docstring. Хук спавнится свежим — действует сразу.

Открытые находки без фикса (детали в §18 роадмапа): F1 мульти-target роутинг недостижим (explicit-словари перекрывают категорийные); F8 versioning без писателей; F9 TTL bookkeeping-only (cleanup не трогает сами store'ы); F10 circuit breaker непроводим (`_apply_update` глотает исключения handler'ов, реестр оркестратора пуст); F12 `sources_failed:[]` при TEI-down (silent degradation vector-плеча).

---

## 2026-06-11 — Governance & Honest-Failure Wiring (260611): хвост находок 260610 закрыт

Роадмап [260611_ROADMAP_MEMORY_GOVERNANCE_WIRING](../../../roadmap/260611_ROADMAP_MEMORY_GOVERNANCE_WIRING.md) (§18 — детали). Принцип: ошибка доезжает до ответа/лога, а не глотается.

- **F10** — `PropagationResult.failed_entities{entity→reason}` + tri-state `_apply_update` (`applied`/`skipped_*`/`failed:<ExcType>`) в [`propagation_engine.py`](../../../../src/memory/orchestrator/propagation_engine.py); named breakers `propagation:<source>` вокруг handler-вызовов (реестр оркестратора → `memory_circuit_status`/`reset` управляют реальными breakers, OPEN → `failed:circuit_open` fail-fast). Sink `memory-circuit.log` ожил — observability 10/10 fresh.
- **F12** — blanket-except снят с `VectorMemorySearchAdapter.search` ([`memory_orchestrator.py`](../../../../src/memory/orchestrator/memory_orchestrator.py)) — TEI/Qdrant-outage доезжает до `unified_search.sources_failed[]`.
- **F8 (ADR-V wire-minimal)** — versioning получил писателей на orchestrator-mediated мутациях: `route_and_save` (CREATE), propagation-handlers (UPDATE-снапшот), rollback (+ **store-writeback** `_apply_version_to_store`: memory-ai content/importance, vector payload-поля `vector_reembedded:false`). Прямые писатели MCP-серверов вне версионирования (JSONL не concurrent-safe).
- **F9** — `memory_ttl_cleanup` исполняет до store'ов: vector → archive (`expired_at` + epoch bump), memory-ai → DELETE; ответ `{removed_ledger, store_actions, failed}`.
- **F5** — `_pattern_from_payload` маппит `expired_at` ([`vector_memory/server.py`](../../../../src/memory/vector_memory/server.py)).
- **F13** — WikiPromoter идемпотентен: `_mark_promoted` пишет `promoted_to` на source-точку, pre-filter кандидата, substring-dedup в `_append_log` ([`wiki_promoter.py`](../../../../src/memory/librarian/wiki_promoter.py)).
- **F14** — **`shared/pattern_reinforce.py`** + **`pattern-reinforce-stop.py`**: `not_found`-miss → счётчик `missing` (return + lifecycle-событие `session` + баннер `[REINFORCE] … missing=N`), errors больше не включают легитимно удалённые паттерны. Хуки спавнятся свежими — действует сразу.

Тесты: +22 (`test_propagation_honest` 10, `test_governance_wiring` 7, `test_unified_search_honest` 2, `test_quick_fixes_260611` 3, `test_pattern_reinforce` +2); гейт `-m unit` 1415 passed; code-verify PASS. ⚠ Orchestrator/vector-memory — MCP-side → `/mcp reconnect`; live MCP re-run D2–D5 после reconnect. Вне scope: F1 (мульти-target — продуктовое решение), F3 (perf hot-path), F15-кандидат (BFS double-apply при двух путях к узлу).

---

## 2026-06-11 — Live MCP re-run D2–D5 + F16/cold-start фиксы (закрытие 260611)

После `/mcp reconnect` все 4 governance-цепочки прогнаны живыми MCP-tools — **PASS** ([260611 §18](../../../roadmap/260611_ROADMAP_MEMORY_GOVERNANCE_WIRING.md)): D4 (реальный breaker `propagation:memory-ai` в `memory_circuit_status`), D3 (history CREATE+UPDATE → compare → rollback со store-writeback), D2 (`memory_ttl_cleanup` → `store_actions{archived/deleted}`, F5 подтверждён live), D5 (TEI-down → `sources_failed:["vector-memory"]`, recovery после старта). Два follow-up фикса по находкам прогона:

- **F16** — `_apply_version_to_store` (MEMORY_AI-ветка, [`memory_orchestrator.py`](../../../../src/memory/orchestrator/memory_orchestrator.py)) разворачивает `metadata.importance` из CREATE-снапшотов `route_and_save`: rollback к v1 восстанавливает importance, не только content (top-level приоритетен — UPDATE-снапшоты propagation не задеты). Открытый N2-гэп: vector-ветка rollback-to-CREATE восстанавливает только `content` — ответ честный (`fields`), чинить при появлении live-пути.
- **Cold-start warmup** — первый tool-вызов vector-memory MCP после reconnect сгорал на 60s client-timeout (ленивый импорт `qdrant_client` под contention одновременного старта серверов): daemon-поток `_warmup_qdrant` при старте ([`vector_memory/server.py`](../../../../src/memory/vector_memory/server.py)) пре-оплачивает импорт+init; fail-soft, opt-out `MEMORY_VECTOR_NO_WARMUP=1`; `_get_qdrant` под double-checked lock, publish-after-ensure (упавший init оставляет глобал None — следующий вызов ретраит весь lazy-путь), `_ensure_collection(client)` принимает клиент параметром.

Тесты: +3 (`test_governance_wiring` 8, `test_quick_fixes_260611` 5); code-verify PASS. ⚠ MCP-side → активно после следующего `/mcp reconnect`.

---

## 2026-06-12 — Skill-learning revival (260611): pending/saved/rejected как рабочий контур

Роадмап [260611_ROADMAP_SKILL_LEARNING_REVIVAL](../../../roadmap/260611_ROADMAP_SKILL_LEARNING_REVIVAL.md) (§18 — детали). Целевая модель: pending/saved/rejected = карантинный конвейер, в который Claude пишет по ходу работы и состояние которого видит на старте сессии и в surfacing.

- **P0 гигиена** ([`skill_learning/server.py`](../../../../src/memory/skill_learning/server.py)): `_write_jsonl` атомарен (tmp + `os.replace`, kill-safe); rejected включён в `_existing_hashes` как негативный dedup — re-capture отклонённого → `action=dup_rejected` + `record_ingest("dup", reason="rejected")`; `get_learning_stats` derive-on-read из файлов (`learning_stats.json` — write-through кэш, дрейф 2→1 устранён). [`route_and_save`](../../../../src/memory/orchestrator/memory_orchestrator.py) target=skill-learning пишет в **pending** (`metadata.routed:true`) с dedup по всем 3 silo; opt-out — явный `auto_confirm` в metadata.
- **P1 вход**: SKILL.md `task-protocol` (шаг 6 CAPTURE) + `code-verify` (после PASS) — протокольный `capture_pattern` после verify PASS нетривиальной задачи; негативный вход (P1.3) — `capture(confidence=0.3)` + немедленный `reject_pattern` при коррекции пользователем. **P1.2 авто-capture**: [`pattern_harvest.py`](../../../../.claude/hooks/shared/pattern_harvest.py) — `HarvestItem.confidence` (confirmed drafts/skill-learning = 0.85, auto-lessons = 0.7), `harvest()` сплитит по `PATTERNS_QUARANTINE_THRESHOLD=0.8`: ниже → `quarantine_items()` (pending, схема capture_pattern, dedup vs 3 silo **и** vs `learned_patterns` по детерминированному point_id — уроки, собранные старым маршрутом, не дублируются), выше → прямой Qdrant как раньше. Opt-out `PATTERNS_QUARANTINE_DISABLE=1`; баннер `[PATTERNS-HARVEST] … +N → pending (карантин)`.
- **P2 выход**: SessionStart-хук [`skill-learning-pending-on-start.py`](../../../../.claude/hooks/skill-learning-pending-on-start.py) — `[SKILL-LEARNING] pending=N (oldest Xd): top-3` + напоминание о модерации (opt-out `SKILL_LEARNING_PENDING_DISABLE=1`); [`memory-first-hook`](../../../../.claude/hooks/memory-first-hook.py) — новое плечо `skill_learning` (weight 0.4) по patterns.jsonl + pending_patterns.jsonl, pending damped ×0.7 + помечен `[pending]`/`sl/pending` (opt-out `MEMORY_SURFACE_SKILL_LEARNING_DISABLE=1`); `SkillLearningSearchAdapter` — casefold + RU-стемминг (`_sl_tokenize`) вместо точного word-overlap, кириллические морфоформы матчатся.
- **P3 модерация**: job `review_pending` в [`memory_maintenance.py`](../../../../scripts/memory_maintenance.py) — pending старше `SKILL_PENDING_TTL_DAYS=30` → auto-reject (`reject_reason=ttl_expired`, dry-run default, `--apply` исполняет; записи без `created_at` — честный skip); `handle_confirm` детачит немедленный harvest подтверждённого паттерна в `learned_patterns` (daemon-поток, `ingest_items` → идемпотентный `content_hash.point_id` + epoch.bump → surfacing видит сразу; importlib file-load обходит коллизию `src/shared`↔hooks/shared, регистрация в `sys.modules` ДО `exec_module` — иначе `@dataclass` падает).
- **P4 наблюдаемость**: capture→confirm→harvest→reject тредятся `fact-trace` по общему `content_hash` (events: `skill_learning/{dup,skipped,rejected}` + `learned_patterns/saved` harvester=`confirm_pattern`/`patterns-quarantine`/`review_pending`).

Тесты: +16 [`test_skill_learning_revival.py`](../../../../tests/unit/test_skill_learning_revival.py) (P0/P1.2/P2.2/P2.3/P3.1 + пины code-verify B1/B2, все silo в tmp). ⚠ `skill_learning/server.py` + `memory_orchestrator.py` — MCP-side → `/mcp reconnect`; хуки спавнятся свежими — действуют сразу. **Acceptance-окно 2 недели автоматизировано**: [`scripts/skill_learning_acceptance.py`](../../../../scripts/skill_learning_acceptance.py) (критерии: ≥10 capture, median pending age <7d, ≥1 reject, arm=skill_learning fired в surfacing-логе, `dup` ≠ 0) + SessionStart-хук [`skill-learning-acceptance-on-start.py`](../../../../.claude/hooks/skill-learning-acceptance-on-start.py) — дневной прогресс-баннер, после 2026-06-26 финальный вердикт до фиксации «Acceptance вердикт» в §18 роадмапа (self-terminating).

---

## 2026-06-12 — ADR-011: модель синхронизации курируемой памяти + R1 curation-конвейер

Исследование GitHub-практик (кеш `curated-memory-event-store-sync-2026.md`): репликация курируемого слоя и Event Store с авто-store'ами — **non-goal** (read-time merge + manual HITL-promotion + односторонняя event-эмиссия; конфликт → канон). Реализован R1 — входящий конвейер store→канон: SessionStart-хук [`memory-curation-candidates-on-start.py`](../../../../.claude/hooks/memory-curation-candidates-on-start.py) — баннер `[CURATION]` для зрелых паттернов (effective_confidence≥0.85, application_count≥5, не archived), не покрытых `memory/*.md` (покрытие: pattern_id/content_hash в корпусе ИЛИ prefix-Jaccard имени со слагом ≥0.5); cooldown 20h, opt-out `MEMORY_CURATION_BANNER_DISABLE=1`, fail-soft. Live: 32 паттерна, mature=0 (топ 0.79/apps 4) — баннер молчит честно, включится по мере §22 reinforce. R2: memory-записи несут citations (CLAUDE.md). Тесты: `tests/unit/test_curation_candidates.py` (6). ADR: [011](../../../../.claude/skills/architecture-research/adr/011-curated-memory-sync-model.md).

---

## 2026-06-12 — LinkRegistry full verification (260612): P0–P3 + ADR-L1 одним батчем

Роадмап [260612_ROADMAP_LINK_REGISTRY_FULL_VERIFICATION](../../../roadmap/260612_ROADMAP_LINK_REGISTRY_FULL_VERIFICATION.md) (§18 — детали). Третий блок семейства full-verification: граф связей очищен (миграция raw-UUID рёбер по живым store'ам → 31 ребро / 0 сырых), вход только unified-ID (ValueError на сырой UUID), удаления журналируются + `link_stats` пересчитываем (`rebuild_stats` + maintenance-job). **ADR-L1**: типология 10→8 (`based_on`/`graph_node` ретированы; авто-писатели mirrors/derives_from/session_context/promoted_to + ручной словарь supports/contradicts/extends/superseded_by); W4-фикс — `route_and_save` линковал сырые store-id (корень легаси-рёбер), теперь unified. Наблюдаемость: sink `memory-links.log` (`link_create`/`link_delete`/`cascade_empty` — голодание каскада видно). Acceptance [`link_registry_acceptance.py`](../../../../scripts/link_registry_acceptance.py): день 1/14 — 6/6 PASS. Тесты +6 (`test_link_registry_p0.py`) + 5 файлов под ADR-L1. ⚠ orchestrator/vector_memory — MCP-side → `/mcp reconnect`. Skill `memory-unified` (секция Link Types) и карта 27.12 синхронизированы.

---

## 2026-06-12 — memory-ai full verification (260612): P0–P4 одним батчем

Роадмап [260612_ROADMAP_MEMORY_AI_FULL_VERIFICATION](../../../roadmap/260612_ROADMAP_MEMORY_AI_FULL_VERIFICATION.md) (§18 — детали). Эпизодический store закрыт исполняемыми цепочками вход→выход, данные несут `content_hash`, выход нормализован, retention управляем.

- **P0 обезвреживание + гигиена**: G1-контур `deferred_qdrant_indexing.py` ретирован в [`scripts/attic/`](../../../../scripts/attic/deferred_qdrant_indexing.py) (E5 1024d → 4096d-коллекция = dimension-mismatch; эпизодика ≠ семантика, §26 Q1 ADR — sanctioned-путь reflection). **Уточнение инвентаризации**: из 33 точек `indexed_in_qdrant=1` 18 ЖИВЫ (re-embedded 4096d при Qwen3-миграции) — сброшены только 15 мёртвых (verify по uuid5 ДО сброса, как требовал §6). [`scripts/memory_ai_backfill.py`](../../../../scripts/memory_ai_backfill.py) (dry-run default): content_hash 12/107 → **98/98**, 5 dup-групп смержены (старейший выжил, 16 рёбер link_registry перенесены, 107 → 98 строк). Новый [`ai_memory/db.py`](../../../../src/memory/ai_memory/db.py) — единая точка path-резолюции (`MEMORY_AI_DB_PATH`) и подключения (**WAL + busy_timeout 5s**); env-override теперь уважают ВСЕ 9 мест оркестратора + 3 хука + 4 скрипта (был только server.py + 3 места).
- **P1 цепочки A1–A8**: [`tests/unit/test_memory_ai_chains.py`](../../../../tests/unit/test_memory_ai_chains.py) — 16 тестов на tmp-БД (A1 dedup+hash, A2 coercion ×5, A3-lite W2, A4 route-таргет, A7 delete+G5, A8 конкурентные писатели 4×25 без потерь, B4/G6/P3.2/C2). A5/A6 уже покрыты 260609/260611 — не дублированы (§5 re-inventory).
- **P2 выход**: G2 — [`text_norm.py`](../../../../src/memory/text_norm.py) (стеммер вынесен из оркестратора, single-source) → `search_messages` и `AiMemorySearchAdapter` матчат кириллические морфоформы (SQLite `LOWER` ASCII-only — голый LIKE был слеп); G6 — R1 [`memory-first-hook`](../../../../.claude/hooks/memory-first-hook.py) окно 200→500 (`MEMORY_SQLITE_SCAN_LIMIT`) + recency-ordering (замер: 8.5ms cold при бюджете 200ms); G5 — delete (MCP `delete_message` + TTL-cleanup) каскадно чистит рёбра link_registry (`links_removed` в ответе, `delete_links_for_entity` у оркестратора); C2 — `_safe_json` в читателях (битая строка скипается, не роняет хендлер/плечо).
- **P3 retention**: [`ai_memory/retention.py`](../../../../src/memory/ai_memory/retention.py) — P3.1 lazy importance-decay на чтении (half-life 90d, ТОЛЬКО `session_summary`/`general`; курируемые категории инвариантны) в R1+R2; P3.2 job `archive_episodic` в [`memory_maintenance.py`](../../../../scripts/memory_maintenance.py) (>90d & importance<0.8 → `metadata.archived_at`, invalidate-not-delete, читатели фильтруют); P3.3 — job стоит ПОСЛЕ reflect в каденсе (кластер консолидируется до архива).
- **P4 наблюдаемость + acceptance**: W2 (`session-memory-save`) теперь штампует `content_hash` + `record_ingest("saved")` (G3 не зарастает); W6 эмитит `record_ingest("deleted")` — все 6 писателей видны fact-trace. Acceptance-окно 2026-06-12 → 2026-06-26: [`scripts/memory_ai_acceptance.py`](../../../../scripts/memory_ai_acceptance.py) (6 критериев: ≥5 эпизодов вне session_summary, `layers.sqlite>0`, dup≠0, 0 строк без hash, ≥1 DERIVES_FROM, ≥1 archive-run) + SessionStart-хук [`memory-ai-acceptance-on-start.py`](../../../../.claude/hooks/memory-ai-acceptance-on-start.py); **каркас переиспользован, не скопирован** — общие [`scripts/acceptance_common.py`](../../../../scripts/acceptance_common.py) + [`hooks/shared/acceptance_watch.py`](../../../../.claude/hooks/shared/acceptance_watch.py), skill-learning-пара отрефакторена на них же.

Тесты: +16 chains; гейт `-m unit` **1452 passed**. ⚠ `ai_memory/server.py` + `memory_orchestrator.py` — MCP-side → `/mcp reconnect`; хуки и скрипты действуют сразу. Live-слой (A3 реальный Stop, B1/B3/B6/B7 живыми MCP-tools) — после reconnect, вердикты в §18 роадмапа.

**Post-review remediation (та же сессия, code-verify PARTIAL → fixed):** (1) trace-маппинг jobs→rc различает исходы `archive_episodic` — `"skipped"` строкой / `rc:0` success / `rc:-1` error (раньше `.get("rc")`→`None` на всех трёх ⇒ acceptance-критерий `archive_ran` был вакуумно-истинным); `memory_ai_acceptance` считает строго `rc==0`. (2) `memory_ai_backfill` дедупит рёбра survivor'а после re-pointing (коллизия с идентичным существующим ребром; 16 продакшн-дублей вычищено). (3) Зафиксировано осознанное расхождение: на токен-пустых запросах (emoji/CJK) `search_messages` падает на legacy-LIKE, `AiMemorySearchAdapter` возвращает `[]` (плечо federated search — пустой результат честнее шумного).

---

## 2026-06-12 — Skill System Full Verification (260612): skill_library под write-contract, surfacing-floor, петля метрик

Роадмап [260612_ROADMAP_SKILL_SYSTEM_FULL_VERIFICATION](../../../roadmap/260612_ROADMAP_SKILL_SYSTEM_FULL_VERIFICATION.md) (§18 — полные вердикты и числа). Memory-стык:

- **S1/S2 (призрак + drift)** — `mirror_skill_library()` в [`shared/skills_harvest.py`](../../../../.claude/hooks/shared/skills_harvest.py) + CLI [`scripts/reindex_skill_library.py`](../../../../scripts/reindex_skill_library.py): reconcile против КОЛЛЕКЦИИ (инкрементальный harvester слеп к точкам старше своего state-файла — так выжил призрак `1c-mcp-toolkit`), prune со снапшотом, upsert по `content_hash`-mismatch. Apply: 85 upserted / 1 pruned / drift 0. Каденс-job `reindex_skill_library` (apply-only) в `memory_maintenance.py`.
- **S3 (вне write-contract)** — оба писателя skill_library (инкрементальный harvester + mirror) теперь штампуют `content_hash` (= `sha256(SKILL.md)[:16]`) в payload + `record_ingest` saved/pruned/error → `memory-ingestion.log`; жизнь скилла тредится `fact-trace` по content_hash.
- **S4 (surfacing-шум)** — `memory-first-hook`: абсолютный cosine-floor `SKILL_SURFACE_MIN_SCORE=0.55` для плеча `skill` (env `MEMORY_SURFACE_SKILL_MIN_SCORE`, 0=откат, trace `gate.skill_below_floor`). Замер: нерелевантный промпт 5/5 → 0/5 шумовых skill-слотов; релевантный — без изменений.
- **S6 (петля метрик)** — `skill-health-analyzer.py` (`--exit-zero`, секция NO TRAFFIC) зарегистрирован каденс-job'ом `skill_review` → `data/reports/skills/skill-health-report.md`.
- **B2 phantom-guard** — `code-skill-enforcer._skill_exists()`: маппинг на несуществующий скилл → честный CONFIG ERROR без блока (см. 11.4).
- 27.12 дополнен: skill-система как вход в память (subgraph + строки в store-карте и write-contract таблице).
