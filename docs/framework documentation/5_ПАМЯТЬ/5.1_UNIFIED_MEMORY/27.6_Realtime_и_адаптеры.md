# 27.6 Realtime и адаптеры

Фаза **P3** добавила событийную шину для внутрипроцессной координации, адаптер RAG-поиска по документации и 4 extended-инструмента.

Тесты: 61 PASS (28 core + 8 subscription + 6 docs_rag + 19 extended tools), суммарно 203/203 в P0–P3.

---

## EventBus / EventStore / SubscriptionManager / ConflictResolver

Каноническое описание EventBus/EventStore/SubscriptionManager — см. [27.12.5 Блок Отдельно](27.12.5_Блок_Отдельно.md), часть 2 «Event Store + EventBus». ConflictResolver (4 стратегии: LWW/SOURCE_PRIORITY/MERGE_FIELDS/MANUAL) — см. [27.4 Инфраструктура](27.4_Инфраструктура.md), раздел ConflictResolver. Ниже — только то, что специфично для этого раздела: MCP realtime-инструменты, поисковые адаптеры и extended tools.

---

## 8 MCP Realtime Endpoints

| Endpoint | Назначение |
|---|---|
| `memory_subscribe` | Создать подписку по паттерну |
| `memory_unsubscribe` | Удалить подписку |
| `memory_publish` | Опубликовать событие в шину |
| `memory_get_events` | Забрать накопленные события для подписки |
| `memory_replay` | Replay исторических событий |
| `memory_event_history` | История событий с фильтрами |
| `memory_event_stats` | Статистика: counts, types, bus/store/subscription health |
| `memory_subscription_health` | Heartbeat/list активных подписок |

---

## DocsRag Adapter — РЕТИРОВАН (2026-06-12)

`DocsRagSearchAdapter` (SQLite FTS5 + опциональный Qdrant) ретирован в
[`scripts/attic/docs_rag.py`](../../../../scripts/attic/docs_rag.py) — roadmap
[260612 pdf-docs P0.1 / D1](../../../roadmap/260612_ROADMAP_PDF_DOCS_FULL_VERIFICATION.md).
Контур был мёртв с рождения (default `chunks_db_path=""`, несуществующая коллекция
`docs_chunks`, заимствованный 4096d-эмбеддер) и жил только в integration-тестах.
Документация в памяти = production-коллекции `pdf_documents`/`wiki_pages_v1` через
`pdf-vector-graph` MCP — см. [27.12.3 §4](27.12.3_Блок_Хранилища.md).

---

## Адаптеры (interfaces)

Два независимых семейства адаптеров с общим паттерном «backend за унифицированным интерфейсом»:

| Адаптер | Абстракция | Роль |
|---|---|---|
| [`BaseMemoryAdapter`](../../../../src/memory/ai_memory/adapters/base.py) | Storage-side (`save`/`SearchResult`/`BackendStats`) | Базовый класс для ai_memory/vector_memory/skill_learning бэкендов записи |
| [`GraphSearchAdapter`](../../../../src/memory/orchestrator/adapters/graph_adapter.py) | `BaseSearchAdapter` (search-side) | Stub — источник `SourceServer.LIGHTRAG`, ждёт Phase 4 (entity embeddings) |
| [`WikiSearchAdapter`](../../../../src/memory/orchestrator/adapters/wiki_adapter.py) | `BaseSearchAdapter` (search-side) | Stub — источник `SourceServer.OBSIDIAN_VAULT`, ждёт Phase 1 (obsidian-mcp) |

---

## Extended Tools

Четыре дополнительных инструмента ([`orchestrator/tools/`](../../../../src/memory/orchestrator/tools/)).

| Tool | Класс | Возможности |
|---|---|---|
| Research | `ResearchTool` | relationships, anomalies, clusters, timeline, summary |
| ID Management | `IDManagementTool` | UUIDv7 generate, resolve, conflict detection, batch ops |
| Surprise | `SurpriseTool` | Novelty scoring, token overlap, batch-обработка |
| Warmup | `WarmupTool` | Стратегии прогрева кэша: frequent, recent, important, pattern |

Все зарегистрированы в MCP-оркестраторе (`memory_research`, `memory_id_management`, `memory_surprise`, `memory_warmup`).

Тесты: 19/19 (Research 4, ID 6, Surprise 5, Warmup 4).

---

## Интеграция с MemoryOrchestrator

EventBus и EventStore инициализируются в `MemoryOrchestrator.start()` и завершаются в `stop()`.

Внутренний helper `_emit_event()` выполняет fire-and-forget публикацию в bus и запись в store. Вызывается из `route_and_save()` и `create_link()`:

```python
class MemoryOrchestrator:
    async def start(self):
        self.event_bus = get_event_bus()
        await self.event_bus.start()
        self.event_store = EventStore(...)
        await self.event_store.initialize()

    def _emit_event(self, event_type: str, data: dict) -> None:
        # fire-and-forget: не блокирует write-операции
        asyncio.create_task(self.event_bus.publish(event_type, data))
        asyncio.create_task(self.event_store.append(event_type, data))

    async def route_and_save(self, content, ...):
        record_id = await self._save(content)
        self._emit_event("memory.saved", {"id": record_id, "type": ...})
        return record_id
```

Такой подход гарантирует минимальную задержку операций записи — публикация событий не блокирует основной путь.

---

## Связанные разделы

- [27.12.5 Блок Отдельно](27.12.5_Блок_Отдельно.md) — канон EventBus/EventStore/SubscriptionManager
- [27.4 Инфраструктура](27.4_Инфраструктура.md) — ConflictResolver, Resilience (Retry/Timeout/CircuitBreaker), Cache/Metrics
