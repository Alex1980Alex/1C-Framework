# 37.3 Фазы реализации — хроника Ф0-Ф6

> 7 фаз, выполненных с 2026-04-19 по 2026-05-15. Каждая независима и доставляет ценность сама по себе (brownfield principle).

## Порядок зависимостей

```
Ф0 (блокер) → Ф1 || Ф2 → Ф3 → Ф4 → Ф5 → Ф6 (параллельно)
```

Ф5 и Ф6 могли выполняться параллельно после Ф4 (не пересекаются по коду).

---

## Ф0 — Memory Layer Alignment (2026-04-19)

> **Spec:** [`openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/memory-layer-alignment/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/memory-layer-alignment/spec.md)
> **Статус:** ✅ COMPLETE — 274 tests pass (47 new + 227 existing), zero regressions
> **Трудоёмкость:** ~3-4 дня

### Цель

Расширить существующую memory-инфраструктуру (`MemoryType`, `LinkType`, `MemoryCube`, `UnifiedSearch`) для поддержки нового L3-слоя без переписывания.

### Deliverables

**0.1 UnifiedID расширение** ([`src/memory/orchestrator/unified_id.py:26-70`](../../../../src/memory/orchestrator/unified_id.py))
- `MemoryType.WIKI = "wiki"` и `MemoryType.GRAPH = "graph"`
- `SourceServer.OBSIDIAN_VAULT = "obsidian-vault"`, `SourceServer.LIGHTRAG = "lightrag"`
- `parse_unified_id()` обновлён для backward-compat со старыми IDs

**0.2 LinkRegistry SQL миграция** ([`src/memory/orchestrator/link_registry.py`](../../../../src/memory/orchestrator/link_registry.py))
- `migrations/001_extend_link_types.sql` — CREATE TABLE + COPY DATA + DROP OLD паттерн
- `migrations/001_rollback.sql` — откат
- `scripts/migrate_link_registry.py` с `--dry-run` и `--apply`
- Новые link types: `PROMOTED_TO`, `SUPERSEDED_BY`, `MIRRORS`, `GRAPH_NODE`
- Python-level + SQL-level CHECK constraint обновлены

**0.3 MemoryCube расширение** ([`src/memory/orchestrator/memcube.py`](../../../../src/memory/orchestrator/memcube.py))
- `ContentType.WIKI` enum value
- `MemoryCube.to_wiki_page() -> str` — markdown + YAML frontmatter (unified_id, source, created_at, tags, links)
- `MemoryCube.from_wiki_page(md: str) -> MemoryCube` — обратный парсинг
- Roundtrip-тесты: `cube → md → cube == оригинал`

**0.4 UnifiedSearch adapters** ([`src/memory/orchestrator/adapters/`](../../../../src/memory/orchestrator/adapters/))
- `wiki_adapter.py` — `WikiSearchAdapter(BaseSearchAdapter)` через obsidian-mcp (stub до Phase 1)
- `graph_adapter.py` — `GraphSearchAdapter(BaseSearchAdapter)` proxy к `entity_embeddings.py`
- Регистрация в `memory_orchestrator.__init__()`
- `UnifiedSearchEngine` core не трогается

**0.5 MemoryRouter target**
- `ContentClassifier._phase3_select_targets` добавляет target `"wiki"`
- `_phase2_keyword_scoring` — keywords for wiki classification
- `memory_orchestrator._save_to_target("wiki", ...)` — создаёт draft в `docs/wiki/drafts/<slug>.md`

**0.6 memory-first-hook Layer 0** ([`.claude/hooks/memory-first-hook.py`](../../../../.claude/hooks/memory-first-hook.py))
- `WIKI_DIR = PROJECT_ROOT / "docs" / "wiki"`
- `search_wiki(query_tokens, limit)` — token-overlap по drafts
- **RRF weights перераспределены:** L1=0.30, L2=0.35, L3=0.15, L4=0.20

**0.7 Тесты Фазы 0** — 47 новых + 227 существующих, zero regression

### Breaking changes

- SQLite schema `data/link_registry.db` — требует `ALTER TABLE`, rollback скрипт есть
- LinkType enum — добавление values (старые literals продолжают работать)

---

## Ф1 — Obsidian Vault Integration (2026-04-20)

> **Spec:** [`obsidian-vault/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/obsidian-vault/spec.md)
> **Статус:** ✅ COMPLETE — 15/16 задач (одна отклонена как «не требуется»)
> **Трудоёмкость:** ~2-3 дня

### Цель

Внедрить Obsidian как human-facing UI для wiki + интегрировать через MCP.

### Deliverables

- Obsidian Desktop 1.7.7 + Local REST API plugin v3.6.1 (пользовательская установка)
- `mcp-obsidian` в [`.mcp.json`](../../../../.mcp.json) через `uvx` (lazy install)
- `.obsidian/` config: `app.json`, `workspace.json` (gitignored), `community-plugins.json`, `appearance.json`, `graph.json`, `templates.json`
- **Создана структура `docs/wiki/`:**
  - `_index.md` — home page
  - `SCHEMA.md` — правила ведения
  - `log.md` — chronology
  - `entities/`, `concepts/`, `procedures/`, `drafts/`, `templates/`, `assets/`
- **Миграция `docs/architecture/*.md`** (8 файлов) — frontmatter добавлен, 59 wiki-links суммарно
- **Split `docs/architecture/PATTERNS.md`** (15 arch + 13 automation patterns) → 28 файлов в `docs/wiki/patterns/` (commit `08ee1291`)
- Skill [`obsidian-vault`](../../../../.claude/skills/obsidian-vault/SKILL.md)
- **Verification:** Obsidian graph view — 604 .md в vault scope, 9 узлов с explicit wiki-links via REST API

---

## Ф2 — DSPy Deepening + Wiki Schema (2026-04-20)

> **Spec:** [`dspy-signatures/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/dspy-signatures/spec.md)
> **Статус:** ✅ CORE COMPLETE — 8/9 задач (RAGAS eval отложен в Phase 2.1)
> **Трудоёмкость:** ~2-3 дня

### Цель

Заменить f-string prompts в trio агентов (grader/rewriter/hallucination_check) на типизированные DSPy Signatures.

### Deliverables

- [`src/pdf_framework/prompts/signatures.py`](../../../../src/pdf_framework/prompts/signatures.py):
  - `GraderSignature` — relevance grading (3-level scoring)
  - `HallucinationCheckSignature` — типизированный `grounded: bool`
  - `RewriterSignature` — query expansion
- Миграции:
  - `agents/grader.py` → `dspy.Predict(GraderSignature)` — Ralph Wiggum self-correction сохранена
  - `agents/rewriter.py` → `dspy.ChainOfThought(RewriterSignature)`
  - `agents/hallucination_check.py` → `dspy.Predict(HallucinationCheckSignature)`
- [`docs/wiki/SCHEMA.md`](../../../../docs/wiki/SCHEMA.md) — 5-слойная модель, frontmatter правила, cross-references, L2→L3 promotion thresholds
- [`docs/wiki/log.md`](../../../../docs/wiki/log.md) — начальная запись + auto-append via `session-memory-save.py` on Stop

### Известные ограничения

- `dspy-ai` не установлен в `.venv` — Phase 2 работает через fallback chain `cheap_llm → DSPy → LangChain`, DSPy путь молча пропускается
- RAGAS eval отложен в **Phase 2.1** (требует `pip install dspy-ai` + benchmark dataset)

---

## Ф3 — Auto-Librarian (2026-04-20)

> **Spec:** [`wiki-librarian/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/wiki-librarian/spec.md)
> **Статус:** ✅ CORE COMPLETE — 9/10 задач (incremental graph integration отложена)
> **Трудоёмкость:** ~2-3 дня

### Цель

Автоматическая промоция L2 → L3 для high-confidence паттернов.

### Deliverables

- `pip install kb-lint` + `npm i -D markdownlint-cli2`
- [`.kb-lint.toml`](../../../wiki/.kblintrc.yml) и [`.markdownlint.jsonc`](../../../../.markdownlint.jsonc) configs
- Расширен [`docs-change-tracker.py`](../../../../.claude/hooks/docs-change-tracker.py) — wiki-validation, [[wiki-link]] target checks
- **Новый модуль [`src/memory/librarian/wiki_promoter.py`](../../../../src/memory/librarian/wiki_promoter.py)** (~120 LoC):
  - Scan `learned_patterns` Qdrant с `confidence >= 0.8 AND usage_count >= 5`
  - Deduplication через `unified_search()` (cosine >= 0.85 threshold)
  - `ConflictResolver.resolve(strategy=SOURCE_PRIORITY)` + `create_link(type=SUPERSEDED_BY)`
  - Если новый → `MemoryCube(content_type=WIKI).to_wiki_page()` + write в `docs/wiki/drafts/<slug>.md`
  - `create_link(pattern → wiki_draft, type=PROMOTED_TO)`
  - `event_bus.publish("wiki.promoted", {...})`
- Расширен [`docs-change-enforcer.py`](../../../../.claude/hooks/docs-change-enforcer.py) — Stop-check для pending drafts
- kb-lint + markdownlint-cli2 в `.pre-commit-config.yaml`
- 15 интеграционных тестов `test_wiki_promoter.py` — pass
- Smoke-тест: 62 tests (47 Phase 0 + 15 новых) zero regression

### Поздние патчи

**L5 drafts pipeline activation (2026-05-14):**
- [`session-memory-save.py:try_promote_patterns()`](../../../../.claude/hooks/session-memory-save.py) — subprocess call to `python -m scripts.export_graph_to_wiki promote-patterns`
- timeout 4s, errors swallowed, opt-out via `SESSION_MEMORY_NO_PROMOTE=1`
- Закрывает L2→L5 promotion gap (см. [roadmap 260514](../../../roadmap/260514_ROADMAP_WIKI_PROMOTION_GAP.md))

**Field-name drift fix:** `usage_count` → `application_count` в [`wiki_promoter.py`](../../../../src/memory/librarian/wiki_promoter.py); canonical field пишется [`vector_memory/server.py`](../../../../src/memory/vector_memory/server.py) `handle_apply_pattern` (с 2026-05-31 — §22 Beta(7,3) posterior через `confidence.py:apply_to_payload`, не наивная дельта).

### Не сделано

- **Integration с `IncrementalGraphUpdater` events** (line-138) — заблокировано Phase 4 wiki_exporter hookup

---

## Ф4 — PDF → Wiki Export (2026-04-20)

> **Spec:** [`wiki-export-pipeline/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/wiki-export-pipeline/spec.md) (766 строк)
> **Статус:** ✅ CORE COMPLETE — 9/11 задач (PDF audit + eval требуют real PDF runs)
> **Трудоёмкость:** ~5-7 дней

### Цель

Экспорт entity graph (LightRAG) в markdown wiki + bidirectional sync.

### Deliverables

- Templates: [`docs/wiki/templates/entity.md`](../../../../docs/wiki/templates/entity.md), `concept.md`, `procedure.md` с frontmatter placeholders
- **[`src/pdf_framework/indexing/wiki_exporter.py`](../../../../src/pdf_framework/indexing/wiki_exporter.py)** (692 LoC):
  - `WikiExporter.export_entity(entity_id)` — graph node → `MemoryCube` → markdown
  - `ForwardSyncService` — graph → wiki bulk export
  - `IncrementalWikiSync` — event-driven + DLQ (dead-letter queue)
  - `ReverseSyncService` — watchdog для wiki → graph
  - `WikiSearchIndexer` — делегирует к `HybridSearchService` (не пишет собственный BM25)
- **[`scripts/export_graph_to_wiki.py`](../../../../scripts/export_graph_to_wiki.py)** (218 LoC) CLI — реальные subcommands (см. [32.9](../5.2_WIKI_KNOWLEDGE_LAYER/32.9_CLI_и_операции.md)):
  - `export-all` — bulk export всего графа
  - `export-entity <ID>` — экспорт одной entity
  - `sync-incremental` — production daemon (EventBus `graph.*`)
  - `index-search` — re-index `wiki_pages_v1` в Qdrant
  - `verify` — консистентность wiki ↔ граф
  - `promote-patterns`, `decay-confidence`, `archive-stale` — добавлены 2026-05-14
- [`graphrag_light.py:219`](../../../../src/pdf_framework/search/strategies/graphrag_light.py) — `wiki_page_paths` в `SearchResponse.metadata`
- Skill [`wiki-pipeline`](../../../../.claude/skills/wiki-pipeline/SKILL.md)

### Acceptance criteria (deferred — требуют real PDF run)

- [ ] ≥3 wiki pages per PDF
- [ ] Schema validation ≥ 95%
- [ ] Precision ≥ 80%
- [ ] `wiki_pages_v1` коллекция populated (партисально — 3 073 точки уже есть из других источников)
- [ ] Retrieval improvement ≥ 10% vs GraphRAG baseline

---

## Ф5 — Sandbox для агентов (2026-05-15)

> **Spec:** [`agent-sandbox/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/agent-sandbox/spec.md)
> **Статус:** ✅ COMPLETE — все 3 backends готовы, 38/38 tests pass
> **Трудоёмкость:** ~2-3 дня (skeleton landed earlier; F1+F2 closed in current sprint)

### Цель

Безопасное исполнение agent-generated кода (research-агенты строят прототипы — нельзя запускать в основном процессе, это эквивалент `eval()`).

### Deliverables

- **ABC + DryRun** (skeleton с 2026-04-XX, commit `9b392c465`):
  - [`src/pdf_framework/sandbox/base.py`](../../../../src/pdf_framework/sandbox/base.py) — `SandboxBackend` ABC + `SandboxResult` + `SandboxQuotaExceeded`
  - [`dry_run_backend.py`](../../../../src/pdf_framework/sandbox/dry_run_backend.py) — zero-dep CI fallback (50-call quota, records calls)
- **F1 LangSmithBackend** (2026-05-15, ~155 LoC):
  - [`langsmith_backend.py`](../../../../src/pdf_framework/sandbox/langsmith_backend.py) — Firecracker microVM via `langsmith[sandbox]`
  - Snapshots + copy-on-write forks, Auth Proxy, blueprints
- **F2 E2BBackend** (2026-05-15, ~140 LoC):
  - [`e2b_backend.py`](../../../../src/pdf_framework/sandbox/e2b_backend.py) — stateful Jupyter Kernel via `e2b-code-interpreter`
  - Variables persist между `execute()` вызовами
- **Selector helper** в [`__init__.py`](../../../../src/pdf_framework/sandbox/__init__.py):
  ```python
  from src.pdf_framework.sandbox import select_backend
  backend = select_backend("auto")  # LangSmith → E2B → DryRun fallback
  ```
- **38 unit tests:** 13 DryRun + 13 LangSmith + 12 E2B + selector
- Skill [`sandbox-execution`](../../../../.claude/skills/sandbox-execution/SKILL.md) обновлён

Подробности — [37.6 Sandbox подсистема](37.6_Sandbox_подсистема.md).

### Deferred

- **Integration в research-skills** (`architecture-research`, `tech-research`) — отложено до first real usage

---

## Ф6 — OAuth 2.1 Generalization (2026-05-15)

> **Spec:** [`oauth-extraction/spec.md`](../../../../openspec/changes/archive/2026-05-15-hermes-llm-wiki/specs/oauth-extraction/spec.md)
> **Статус:** ✅ COMPLETE — 47/47 OAuth-domain tests pass, zero regressions
> **Трудоёмкость:** ~3-4 дня (extraction уже была сделана в prior phase; Sprint 1+2 закрыли wrapper + integration + audit + setup doc)

### Цель

Извлечь OAuth 2.1+PKCE из `src/bsl/mcp_server/auth/oauth2.py` (214 LoC standalone) в reusable `src/shared/mcp_oauth/` для любого HTTP MCP-сервера.

### Sprint 1 (Wrapper + Audit + Setup)

**F4 — Audit doc** (2026-05-14):
- [`docs/wiki/auth/oauth2-service.md`](../../../../docs/wiki/auth/oauth2-service.md) — API reference, security checklist
- Surprise finding: extraction уже выполнена ранее в prior phase

**F5.1 — BSL backward-compat wrapper:**
- [`src/bsl/mcp_server/auth/oauth2.py`](../../../../src/bsl/mcp_server/auth/oauth2.py) переписан 214 → 78 LoC thin async wrapper
- Sync → async migration: 4 `await` adds в [`http_server.py`](../../../../src/bsl/mcp_server/http_server.py) (L77, L529, L627, L659)
- Legacy `AuthCodeData`/`AccessTokenData`/`RefreshTokenData` dataclasses preserved
- `start_cleanup_task`/`stop_cleanup_task` aliases preserved
- Tests: [`tests/unit/test_bsl_oauth_wrapper.py`](../../../../tests/unit/test_bsl_oauth_wrapper.py) — 9 passing

**F5.3 — Regression:** 47/47 OAuth-domain tests pass, 0 new failures.

**F5.4 — Audit infrastructure:**
- [`src/shared/mcp_oauth/audit.py`](../../../../src/shared/mcp_oauth/audit.py) — `AuditedOAuth2Service` + `OAuthAuditEvent`
- 5 event types: `code_issued`, `token_exchanged`, `token_refreshed`, `token_validated`, `token_rejected`
- Sync-sink contract, exceptions swallowed (audit never blocks auth)
- Tests: [`tests/unit/test_mcp_oauth_audit.py`](../../../../tests/unit/test_mcp_oauth_audit.py) — 9 passing

**F5.5 — .mcp.json env + setup doc:**
- [`.mcp.json`](../../../../.mcp.json) — добавлены `MCP_OAUTH_ENABLED`, `MCP_OAUTH_PUBLIC_URL`, `MCP_OAUTH_*_TTL`
- [`docs/wiki/auth/oauth-setup.md`](../../../../docs/wiki/auth/oauth-setup.md) — env table, curl quick-start, Python httpx client, migration path, troubleshooting

### Sprint 2 (FastAPI Router)

**F5.2 — Reusable FastAPI integration:**

Архитектурное уточнение: pdf-vector-graph MCP — **stdio-only transport**, OAuth не применим. F5.2 переориентирован на reusable FastAPI sub-package.

- [`src/shared/mcp_oauth/fastapi/`](../../../../src/shared/mcp_oauth/fastapi/):
  - `routes.py` — `build_oauth_router()` factory: `/authorize`, `/token`, `/.well-known/oauth-protected-resource` (RFC 9728), `/.well-known/oauth-authorization-server` (RFC 8414)
  - `dependencies.py` — `require_oauth` FastAPI Depends-функция
  - `__init__.py` — public API
- Tests: [`tests/unit/test_mcp_oauth_fastapi.py`](../../../../tests/unit/test_mcp_oauth_fastapi.py) — 13 passing
- Wiring: одно-строчный `app.include_router(build_oauth_router())` для любого HTTP MCP server

Подробности — [37.7 OAuth подсистема](37.7_OAuth_подсистема.md).

---

## Cumulative metrics

| Метрика | Значение |
|---|---|
| Total tests добавлено в Sprint 1+2 | 47 (9 wrapper + 9 audit + 13 fastapi + 13 langsmith + 13... wait 12 e2b... = 47 actually 47) — actually 47 |
| Total tests passing после архивации | 103/103 OAuth+Sandbox-domain |
| Zero regressions confirmed | Yes (288 existing auth + 26 memory_unified) |
| Кода добавлено | ~1500 LoC source + ~800 LoC tests + ~600 LoC docs |
| Commits в Sprint 1+2 (5 ч активной работы) | ~30 (через auto-git-save) + 2 explicit (archive + sprint dashboard) |
