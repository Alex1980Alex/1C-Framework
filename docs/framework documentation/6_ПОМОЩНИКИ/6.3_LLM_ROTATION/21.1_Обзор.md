# 21.1 LLM Rotation Service — обзор

> Мульти-провайдерный сервис для LLM-запросов с автоматической ротацией провайдеров, circuit breaker и бюджетным трекингом.

---

## Назначение

Сервис решает три задачи:

1. **Отказоустойчивость** — автоматический fallback при недоступности провайдера
2. **Экономия бюджета** — приоритизация дешёвых провайдеров, трекинг расходов
3. **Соблюдение лимитов** — rate limiting (RPM) и адаптивный скоринг

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     LLMRotationService                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  complete(prompt, ...)                                      │
│      │                                                      │
│      ▼                                                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Phase 1: Force-Primary Retry                         │  │
│  │  ├── Попытка: primary provider (zai-glm5)             │  │
│  │  ├── BackoffStrategy (exponential + jitter)           │  │
│  │  └── Повтор: primary_max_retries раз                  │  │
│  └───────────────────────────────────────────────────────┘  │
│      │ (все попытки неудачны)                               │
│      ▼                                                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Phase 2: Fallback Rotation                           │  │
│  │  ├── Выбор: по приоритету + AdaptiveScorer            │  │
│  │  ├── Проверка: CircuitBreaker + ProviderRateLimiter   │  │
│  │  ├── Model-level failover (альтернативная модель)     │  │
│  │  └── Учёт: BudgetTracker                              │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  CircuitBreaker | BackoffStrategy | AdaptiveScorer          │
│  BudgetTracker  | ProviderRateLimiter                       │
└─────────────────────────────────────────────────────────────┘
```

## Ключевые компоненты

| Модуль | Файл | Назначение |
|--------|------|------------|
| Сервис | `service.py` | LLMRotationService, ProviderConfig, ProviderState |
| Circuit Breaker | `circuit_breaker.py` | Паттерн с 3 состояниями (closed/open/half_open) |
| Backoff | `backoff.py` | Экспоненциальный backoff с jitter |
| Adaptive | `adaptive.py` | AdaptiveScorer + BudgetTracker, цены токенов |
| Rate Limiter | `rate_limiter.py` | Token Bucket алгоритм |
| Config | `config.py` | LLMRotationSettings (pydantic-settings, env: `LLM_ROTATION_`) |
| MCP Server | `mcp.py` | 5 инструментов для мониторинга и управления |
| Adapter | `adapter.py` | CheapLLM: cheap_llm_call(), evaluate_response() |
| Z.AI Proxy | `zai_proxy.py` | ZAIProxy (OpenAI -> Anthropic translator) |

## Провайдеры по умолчанию

| # | Имя | Модель | Формат API | RPM | Особенность |
|---|-----|--------|-----------|-----|-------------|
| 0 | zai-glm5 | GLM-5 | anthropic | 30 | Основной (русский + код) |
| 1 | zhipu | glm-4-flash | openai | 60 | Китайская LLM |
| 2 | gemini | gemini-2.5-flash | openai | 10 (daily: 250) | Быстрый |
| 3 | openrouter | gemma-3-27b-it:free | openai | 20 (daily: 200) | Бесплатный |
| 4 | mistral | mistral-small-latest | openai | 60 | Европейский |
| 5 | ollama-local | qwen2.5:7b | ollama | -- | Локальный offline |
| 6 | ollama-cloud | qwen2.5:7b | ollama | -- | Облачный Ollama |

## Двухфазный алгоритм complete()

### Phase 1: Force-Primary Retry

- Попытки выполнить запрос на основном провайдере (`primary_provider`)
- Количество повторов: `primary_max_retries=2`
- Задержка между попытками: экспоненциальный backoff с jitter
- При успехе — возврат результата, при неудаче — переход к Phase 2

### Phase 2: Fallback Rotation

- Перебор провайдеров по приоритету (от низкого индекса к высокому)
- Проверка состояния CircuitBreaker и лимитов ProviderRateLimiter
- **Model-level failover**: при transient error (timeout, 429, 5xx) попытка альтернативной модели того же провайдера
- Выбор провайдера с учётом AdaptiveScorer (история успехов/неудач)

### Auto-timeout

Таймаут выбирается автоматически по `max_tokens`:

| max_tokens | Таймаут |
|------------|---------|
| <= 1024 | 60s |
| <= 3000 | 90s |
| > 3000 | 180s |

## Структура файлов

```
src/shared/llm_rotation/
  __init__.py          # Публичный API, версия 2.0.0
  service.py           # LLMRotationService, ProviderConfig, ProviderState
  circuit_breaker.py   # CircuitBreaker, CircuitState
  backoff.py           # BackoffStrategy, RateLimitError
  adaptive.py          # AdaptiveScorer, BudgetTracker, PRICE_PER_1K_TOKENS
  rate_limiter.py      # TokenBucket, ProviderRateLimiter
  config.py            # LLMRotationSettings (env prefix: LLM_ROTATION_)
  mcp.py               # MCP сервер (5 tools)
  adapter.py           # CheapLLM: cheap_llm_call(), evaluate_response()
  zai_proxy.py         # ZAIProxy (OpenAI -> Anthropic translator)
```
