# 09.4 Мониторинг

Центральная точка для всех observability-инструментов фреймворка. Конкретные subsystems вынесены в соседние файлы — здесь только overview, cross-cutting секции (JSON tracing, Hook observability, Coverage, Retry policy) и навигация.

## Observability Stack

| Компонент | Назначение | Тип | Документация |
|---|---|---|---|
| **JSON Tracing** | Логирование всех операций | Файловый | [Ниже](#json-tracing) |
| **Langfuse** | LLM observability (traces, costs) | SaaS/Self-hosted | [09.4.1 Langfuse](09.4.1_Langfuse.md) |
| **Prometheus + Grafana** | Метрики (latency, throughput) | Metrics | [09.4.2 Prometheus + Dashboards](09.4.2_Prometheus.md) |
| **HTML Dashboard** | Локальный визуальный дашборд | Web | [09.4.2 → HTML Dashboard](09.4.2_Prometheus.md#html-dashboard) |
| **Hook Metrics DB** | SQLite инжест hook invocations | SQLite | [Ниже](#hook-observability) |
| **Hook Dashboard** | Streamlit UI для хуков и скиллов | Web | [Ниже → Streamlit](#streamlit-dashboard) |
| **OTLP Exporter** | OpenTelemetry для хуков | OTLP | [Ниже → OpenTelemetry](#opentelemetry-tracing-для-хуков) |

**Когда что использовать:**

| Вопрос | Инструмент |
|---|---|
| «Сколько токенов сожрала эта query?» | Langfuse (per-trace breakdown) |
| «Какой rate ошибок прямо сейчас?» | Prometheus + Grafana (realtime triage) |
| «Какие хуки тормозят сессию?» | Hook Metrics DB + Streamlit dashboard |
| «Покажи последний trace локально без cloud-доступа» | JSON Tracing (data/traces) |
| «Какие spans привязаны к этому slash-run?» | Hook Metrics DB `get_run_trace(run_id=...)` |

---

## JSON Tracing

Локальный файловый sink — работает без cloud-зависимостей, удобен для unit-тестов и offline-debugging.

```env
OBSERVABILITY__TRACER=jsonfile
OBSERVABILITY__TRACE_DIR=data/traces
```

```bash
# Поиск с verbose
python -m src.cli.main search "конфигуратор" --verbose
# → Trace сохраняется в data/traces/
```

Подходит для:
- Локальной разработки без сетевого доступа к Langfuse Cloud.
- Smoke-тестов в CI (артефакт остаётся в `data/traces/`, можно сравнить со снапшотом).
- Отладки конкретного запроса без поиска в Langfuse UI.

**Не подходит для:**
- Production cost tracking (см. [09.4.1 Cost Baseline](09.4.1_Langfuse.md#cost-baseline)).
- Cross-restart агрегации (теряется при ротации `data/`).

---

## Hook Observability

Мониторинг хуков Claude Code: invocations, skill activations, latency, cross-hook correlation.

### Hook Metrics DB

SQLite pipeline для инжеста JSONL-логов хуков в базу данных:

```python
from src.pdf_framework.observability.hook_metrics_db import HookMetricsDB

db = HookMetricsDB()
db.ingest_from_logs()

recent = db.get_recent_invocations(limit=100)
metrics = db.get_hook_metrics(hours=24)
skills = db.get_skill_metrics(hours=24)
# skills содержит: total_recommended, total_activated, activation_rate,
# by_source: {"prompt-detection": N, "post-tool-use": N},
# per_skill: {name: {recommended, activated, rate, sources: {source: count}}}
```

Таблицы `skill_activations` и `skill_accuracy` содержат колонку `source TEXT` для различения источников активаций (`prompt-detection` vs `post-tool-use`). Auto-migration добавляет колонку при обновлении.

**Файл:** [`src/pdf_framework/observability/hook_metrics_db.py`](../../../../src/pdf_framework/observability/hook_metrics_db.py)

#### Cross-hook correlation (roadmap §3.3, 2026-05-09)

`invocations` schema расширена тремя полями: `run_id` (UUID slash-command run), `category` (hook / mcp_call / slash_run), `agent_id` (subagent monitoring). Поля приходят из JSONL уже с Phase 8 — auto-migration добавляет соответствующие колонки в существующих БД и индекс `idx_invocations_run_id` для быстрых cross-hook trace queries.

```python
# Полная цепочка хуков для одного slash-command run
trace = db.get_run_trace(run_id="abc123-...")
# → [{ts, hook, event, elapsed_ms, category, ...}, ...] в chronological order

# Топ дорогих/застрявших runs за последние 24 часа
expensive = db.get_run_summary(hours=24, min_invocations=2)
# → [{run_id, hook_count, total_ms, first_ts, last_ts}, ...] sorted by total_ms desc
```

**Откуда берётся run_id:** [`slash-command-tracker.py`](../../../../.claude/hooks/slash-command-tracker.py) (UPS hook) генерирует UUID при запуске `/cmd`, складывает в [`data/.current-runs.json`](../../../../data/.current-runs.json) через [`shared/run_context.py`](../../../../.claude/hooks/shared/run_context.py). Все последующие hook-invocations в той же session подхватывают `run_id` через `shared.invocation_logger.log_invocation(run_id=...)`. MCP-логгер ([`mcp-invocation-logger.py`](../../../../.claude/hooks/mcp-invocation-logger.py)) автоматически прицепляет тот же `run_id` к каждой записи MCP-вызова — даёт полный цепочный трейс slash-run → MCP calls → Stop.

### OpenTelemetry Tracing для хуков

```python
from src.pdf_framework.observability.tracer import HookTracer

tracer = HookTracer()
with tracer.span("hook_execution", hook="SkillRouter"):
    # hook logic
    pass
```

Поддерживаемые backends: JsonFile, LangSmith, Langfuse, OpenTelemetry.

**Файл:** [`src/pdf_framework/observability/tracer.py`](../../../../src/pdf_framework/observability/tracer.py)

### CLI Dashboard

```bash
python scripts/hook-dashboard.py              # Текстовый отчёт
python scripts/hook-dashboard.py --period 7d  # За 7 дней
python scripts/hook-dashboard.py --json       # JSON для CI
python scripts/hook-dashboard.py --section skills  # Только скиллы с Source колонкой
```

Секция SKILL ACTIVATION показывает колонку Source (prompt-detection:N, post-tool-use:N) и summary с разбивкой `via <source>: N`.

### Streamlit Dashboard

```bash
streamlit run src/ui/pages/hook_dashboard.py
```

5 вкладок: metrics, timeline, skill activation, latency, errors. Tab "Skill Activations" показывает карточки по источнику (Prompt Detection / PostToolUse) и колонку Source в таблице скиллов. Подробнее: [06.1 Web UI](../../3_ИНСТРУМЕНТЫ/3.1_ИНТЕРФЕЙСЫ/06.1_Web_UI.md#hook--skill-dashboard-streamlit).

---

## Тестирование хуков

### E2E тесты

```bash
python scripts/test-hooks-e2e.py            # Все тесты
python scripts/test-hooks-e2e.py --verbose   # Подробный вывод
python scripts/test-hooks-e2e.py --filter encoding  # Только encoding-тесты
```

Покрывает: PostToolUse, UserPromptSubmit, Stop хуки + encoding stress-test.

**Файл:** [`scripts/test-hooks-e2e.py`](../../../../scripts/test-hooks-e2e.py)

### Evaluation framework

```bash
python scripts/eval-hooks.py --list-suites   # Доступные suites
python scripts/eval-hooks.py --suite ide-filtering  # Один suite
python scripts/eval-hooks.py --json           # JSON для CI
```

5 suites: skill-activation (20 тестов), stop-hook-safety (3), ide-filtering (10), git-commit-enforcement (2), docs-change-enforcement (3).

**Файлы:** [`scripts/eval-hooks.py`](../../../../scripts/eval-hooks.py), [`tests/eval/hook_prompts.json`](../../../../tests/eval/hook_prompts.json)

### Subagent мониторинг

Субагенты используют отдельный набор хуков для предотвращения бесконечных циклов.

**Файлы:** [`.claude/settings-subagent.json`](../../../../.claude/settings-subagent.json), [`.claude/agents/monitor.md`](../../../../.claude/agents/monitor.md)

---

## Roadmap status snapshot (2026-05-09)

Closure findings из inventory audits во время 2-сессионной проработки `260509_ROADMAP_CONSOLIDATED_BACKLOG.md`:

| Roadmap item | Real state | Closure |
|---|---|---|
| §3.2 Contextual Retrieval | ✅ Phase 50 already DONE — `processing/context_generator.py` + `ContextualRetrievalSettings` + CLI `--contextual` + docs в 02.2/03.2/06.3/01.2 | NO-OP (audit устарел, only benchmark лимитирован v2.0 golden_v1) |
| §4.5 Delegation Iter 4-5 | 🟡 PARTIAL — Iter 3 LinUCB реализован в [`src/shared/delegation_bandit.py`](../../../../src/shared/delegation_bandit.py) (4 arms: Soft/Medium/Hard/Never, warm-up rule-based<20). Iter 4 trained router (vector similarity over outcome embeddings) и Iter 5 SAFLA (composite reward, quality degradation) НЕ реализованы — нет `router/trained.py`, нет SAFLA markers | OPEN — truly blocked by Langfuse production trace для outcome embeddings |

---

## Coverage tracking (§2.5.4 enabled 2026-05-09)

Coverage генерится pytest'ом (`--cov=src --cov-report=xml`) и публикуется через **two channels**:

1. **Codecov SaaS** — `codecov/codecov-action@v4` с `CODECOV_TOKEN` secret. Codecov применяет [`codecov.yml`](../../../../codecov.yml): patch coverage 70% gate на changed lines + project tracking informational. Dashboard: `https://app.codecov.io/gh/Alex1980Alex/1C-Framework`.

2. **GitHub Actions artifact** (belt-and-suspenders) — `actions/upload-artifact@v4` сохраняет coverage.xml на 30 days. Доступ через GitHub Actions UI → run → Artifacts.

Локальный просмотр:

```bash
# Через Codecov dashboard (preferred, has historical trend)
open https://app.codecov.io/gh/Alex1980Alex/1C-Framework

# Или скачать artifact, преобразовать в HTML локально
.venv/Scripts/python.exe -m coverage html
# открыть htmlcov/index.html
```

**Setup status (2026-05-09):**
- ✅ Repo `Alex1980Alex/1C-Framework` сделан **public** → Codecov tokenless OIDC mode (CODECOV_TOKEN secret НЕ требуется для public repos)
- ✅ `codecov/codecov-action@v4` в обоих jobs (test-unit, test-integration) с `flags: unit/integration` — token kwarg остаётся в spec но fallback to OIDC при missing secret
- ✅ `fail_ci_if_error: false` — даже при Codecov outage CI не падает
- ✅ Free tier для public repos: **unlimited uploads** (нет лимита 250/мес как у private)
- ✅ Codecov dashboard: `https://app.codecov.io/gh/Alex1980Alex/1C-Framework`

---

## Retry Policy (resilience)

> Roadmap reference: [260509_ROADMAP_CONSOLIDATED_BACKLOG.md §3.7](../../../../docs/roadmap/260509_ROADMAP_CONSOLIDATED_BACKLOG.md). Closed 2026-05-09.

Транзиентные I/O сбои (LLM rate limits, network blips, Qdrant temporary unavailability) обрабатываются через единый wrapper [`src/pdf_framework/utils/retry.py`](../../../../src/pdf_framework/utils/retry.py) на базе `tenacity`. Этот же модуль предоставляет 4 pre-configured декоратора:

| Декоратор | Сценарий | Параметры |
|---|---|---|
| `@retry_llm_call` | LLM API (Anthropic/OpenAI) | 5 attempts / 120s, exp jitter, retries on Timeout/Connection/OS |
| `@retry_embedding` | Embedding service (TEI/OpenAI) | 5 attempts / 90s, exp jitter, +RuntimeError (CUDA/model load) |
| `@retry_db_operation` | Qdrant/SQLite write | 3 attempts, fixed 0.5s |
| `@retry_network` | HTTP запросы | 4 attempts / 60s, exp jitter |
| `create_retry(...)` | Кастомные параметры | factory с `max_attempts`, `max_delay`, `initial_wait`, `jitter`, `retry_on` |

**Ralph Wiggum loops vs transient retry**: в codebase есть `for attempt in range(...)` циклы которые **НЕ** являются transient retry — это semantic correction loops (LLM feedback injection, length/language/refusal validation per attempt). Они intentional и не должны мигрироваться на tenacity. Документация о таких exceptions находится в docstring [`utils/retry.py`](../../../../src/pdf_framework/utils/retry.py).

**Тесты**: 16 unit-тестов в [`tests/test_retry.py`](../../../../tests/test_retry.py) покрывают sync/async × LLM/DB/embedding/network/custom configs.

---

## Навигация

- **Подразделы:**
  - [09.4.1 Langfuse](09.4.1_Langfuse.md) — LLM observability, cost baseline, scoring API
  - [09.4.2 Prometheus + Dashboards](09.4.2_Prometheus.md) — Prometheus metrics, HTML dashboard, Grafana setup
- **Назад:** [09.3 Rate Limiting](09.3_Rate_Limiting.md)
- **Далее:** [09.5 Docker](09.5_Docker.md)
- **Содержание:** [00 Содержание](../../00_СОДЕРЖАНИЕ.md)
