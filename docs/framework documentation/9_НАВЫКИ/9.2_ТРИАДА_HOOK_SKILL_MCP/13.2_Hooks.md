# 13.2 Hooks — событийная автоматизация (КОГДА)

> Хуки — лёгкие Python-скрипты, которые реагируют на события Claude Code:
> ввод пользователя, вызов инструмента, остановку сессии.
> Хуки не выполняют поиск, не вызывают LLM, не модифицируют файлы.

---

## Протокол

Все хуки работают по одному протоколу: stdin JSON → логика → stdout JSON.

```
Claude Code Event  →  stdin (JSON)  →  Hook (.py)  →  stdout (JSON)
                                                        │
                                          ┌─────────────┤
                                          │             │
                                    allow/pass     block (exit 2)
```

**Вход (stdin):**
```json
{
  "session_id": "abc123",
  "prompt": "текст промпта",
  "tool_name": "Read",
  "tool_input": {"file_path": "..."},
  "tool_result": "..."
}
```

**Выход (stdout) — два варианта:**

Подсказка (не блокирует):
```json
{"continue": true, "systemMessage": "Информация для Claude"}
```

Блокировка:
```json
{"continue": false, "decision": "block", "reason": "причина блокировки"}
```

**Timeout:** 3-5 секунд. Хуки должны быть лёгкими — keyword matching, file read,
проверка условий. Тяжёлые вычисления — в MCP.

## BaseHook — абстрактная база

Все хуки наследуют `BaseHook` из [`.claude/hooks/base/protocol.py`](../../../../.claude/hooks/base/protocol.py).

```
BaseHook (ABC)
├── __init__()        — запускает таймер
├── run()             — entry point: read stdin → execute() → emit → log
├── execute(inp)      — абстрактный метод (реализуется в подклассах)
└── elapsed_ms        — миллисекунды с начала
```

Три класса протокола:

| Класс | Назначение | Ключевые методы |
|-------|-----------|-----------------|
| `HookInput` | Парсинг stdin JSON | `.prompt`, `.tool_name`, `.tool_input`, `.tool_result`, `.detected_event` |
| `HookOutput` | Построение stdout JSON | `.allow()`, `.block(reason)`, `.system_message(msg)`, `.emit()` |
| `BaseHook` | Абстрактная база | `.run()`, `.execute(inp)`, `.elapsed_ms` |

**Graceful degradation:** при любом исключении в `execute()` хук молча завершается
с `exit(0)` — никогда не блокирует workflow из-за внутренней ошибки. Ошибки
логируются в `.claude/cache/hook-errors.log`.

## 4 основных события

| Событие | Когда | Может блокировать | stdin содержит |
|---------|-------|:-----------------:|---------------|
| **UserPromptSubmit** | Пользователь отправил сообщение | Да | `prompt` |
| **PreToolUse** | До вызова инструмента | Да | `tool_name`, `tool_input` |
| **PostToolUse** | После вызова инструмента | Нет | `tool_name`, `tool_input`, `tool_result` |
| **Stop** | Claude завершает работу | Да (exit 2) | `transcript`, `reason` |

Дополнительные события (редко используемые):

| Событие | Назначение |
|---------|-----------|
| `Notification` | Уведомление |
| `SessionStart` / `SessionEnd` | Начало/конец сессии. В проекте используется `ensure-docker-qdrant.py` (fire-and-forget авто-старт Docker Desktop + контейнера `pdf-rag-qdrant`, graceful degradation без блокировки сессии) |
| `PreCompact` | Перед компактификацией контекста |
| `SubagentStop` | Субагент завершился |
| `PermissionRequest` | Запрос разрешений |

## Каталог хуков

> Каталог авто-генерируется из `settings.json` — [`scripts/gen_hooks_catalog.py`](../../../../scripts/gen_hooks_catalog.py); regenerate при изменении хуков (`PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe scripts/gen_hooks_catalog.py`). `research-task-detector.py` присутствует в `.claude/hooks/` файлом, но **не зарегистрирован** в `settings.json` (не входит в таблицы ниже).

<!-- АВТО-ГЕНЕРАЦИЯ: python scripts/gen_hooks_catalog.py — НЕ править руками -->

**Всего регистраций: 99** (UserPromptSubmit 19 / UserPromptExpansion 1 / PreToolUse 21 / PostToolUse 17 / Stop 27 / SessionStart 14).

### UserPromptSubmit (19)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `onec-task-input.py` | `(all)` | 5s |
| `memory-first-hook.py` | `(all)` | 10s |
| `pipeline-gate.py` | `(all)` | 4s |
| `pipeline-protocol.py` | `(all)` | 3s |
| `userpromptsubmit-feedback-detector.py` | `(all)` | 3s |
| `prework_dispatcher.py` | `(all)` | 10s |
| `session-context-enforcer.py` | `(all)` | 2s |
| `skill-router.py` | `(all)` | 5s |
| `skill-eval-enforcer-shell.py` | `(all)` | 3s |
| `ralph_activator.py` | `(all)` | 5s |
| `decision-to-triad.py` | `(all)` | 5s |
| `document-persistence.py` | `(all)` | 3s |
| `todo-sync.py` | `(all)` | 3s |
| `auto-git-save-prompt.py` | `(all)` | 15s |
| `z-ai-delegation-enforcer.py` | `(all)` | 3s |
| `skill-quality-monitor.py` | `(all)` | 2s |
| `slash-command-tracker.py` | `(all)` | 3s |
| `implement-1c-task-preflight.py` | `(all)` | 30s |
| `analyze-1c-task-preflight.py` | `(all)` | 30s |

### UserPromptExpansion (1)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `slash-command-tracker.py` | `(all)` | 3s |

### PreToolUse (21)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `github-search-via-ecosystem-scan.py` | `WebSearch` | 3s |
| `bsl-tool-router.py` | `Read\|Grep\|Glob` | 3s |
| `task-protocol-enforcer.py` | `Write\|Edit` | 3s |
| `code-review-enforcer.py` | `Write\|Edit` | 3s |
| `code-verify-reminder.py` | `Write\|Edit` | 3s |
| `docs-change-tracker.py` | `Write\|Edit` | 5s |
| `code-skill-enforcer.py` | `Write\|Edit\|Bash` | 3s |
| `root-clutter-guard.py` | `Write\|Edit` | 3s |
| `z-ai-write-guard.py` | `Write\|Edit` | 3s |
| `factory-enforcer.py` | `Write\|Edit` | 5s |
| `delegation-outcome-tracker.py` | `Write\|Edit` | 3s |
| `search-optimizer.py` | `Bash` | 3s |
| `bulk-action-guard.py` | `Bash` | 3s |
| `pre-merge-review-check.py` | `Bash` | 6s |
| `process-guard.py` | `Bash\|PowerShell` | 3s |
| `approval-gate.py` | `Skill` | 3s |
| `task-protocol-observer.py` | `Skill` | 3s |
| `skill-usage-metrics.py` | `Skill` | 3s |
| `task-protocol-observer.py` | `TaskCreate` | 3s |
| `task-protocol-observer.py` | `mcp__llm-rotation__llm_complete` | 3s |
| `mcp-invocation-logger.py` | `mcp__.*` | 3s |

### PostToolUse (17)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `mcp-invocation-logger.py` | `mcp__.*` | 3s |
| `pipeline-1c-advance.py` | `Write\|Edit` | 5s |
| `post-task-push-pr.py` | `TaskUpdate` | 1320s |
| `posttooluse-skill-metrics.py` | `Skill` | 3s |
| `code-verify-reminder.py` | `Skill` | 3s |
| `code-verify-reminder.py` | `Task` | 3s |
| `posttooluse-web-cache.py` | `WebSearch\|WebFetch` | 3s |
| `knowledge-cache-reminder.py` | `WebSearch\|WebFetch` | 5s |
| `posttooluse-docs-tracker.py` | `Write\|Edit` | 3s |
| `posttooluse-quality-feedback.py` | `Write\|Edit` | 5s |
| `posttooluse-auto-git-save.py` | `Write\|Edit` | 10s |
| `code-verify-reminder.py` | `Write\|Edit` | 3s |
| `openspec-change-coverage.py` | `Write\|Edit` | 5s |
| `posttooluse-delegation-tracker.py` | `mcp__llm-rotation__llm_complete` | 3s |
| `posttooluse-bash-errors.py` | `Bash` | 3s |
| `posttooluse-stackoverflow-on-error.py` | `Bash` | 3s |
| `post-task-push-pr.py` | `TaskUpdate` | 1320s |

### Stop (27)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `ralph_wiggum_stop.py` | `(all)` | 5s |
| `stop-feedback-draft-aggregator.py` | `(all)` | 3s |
| `slash-command-tracker.py` | `(all)` | 3s |
| `implement-1c-task-smoke-stop-alert.py` | `(all)` | 5s |
| `post-indexing-analyzer.py` | `(all)` | 30s |
| `memory-maintenance-cadence.py` | `(all)` | 10s |
| `opsx-apply-postvalidate.py` | `(all)` | 5s |
| `openspec-task-progress.py` | `(all)` | 8s |
| `git-commit-enforcer.py` | `(all)` | 5s |
| `docs-change-enforcer.py` | `(all)` | 10s |
| `roadmap-progress-enforcer.py` | `(all)` | 5s |
| `pipeline-protocol-stop.py` | `(all)` | 8s |
| `onec-task-completion-stop.py` | `(all)` | 8s |
| `gate-orchestrator-stop.py` | `(all)` | 10s |
| `code-verify-reminder.py` | `(all)` | 5s |
| `task-enforcer.py` | `(all)` | 10s |
| `memory-sync.py` | `(all)` | 5s |
| `session-memory-save.py` | `(all)` | 15s |
| `pattern-reinforce-stop.py` | `(all)` | 15s |
| `patterns-harvester.py` | `(all)` | 8s |
| `skills-harvester.py` | `(all)` | 8s |
| `auto-git-save.py` | `(all)` | 30s |
| `delegation-outcome-stop.py` | `(all)` | 3s |
| `post-merge-revert-stop.py` | `(all)` | 5s |
| `ci-catchup-on-stop.py` | `(all)` | 5s |
| `lifecycle-cache.py` | `(all)` | 8s |
| `memory-effectiveness-analyzer.py` | `(all)` | 15s |

### SessionStart (14)

| Хук | Matcher | Timeout |
|-----|---------|---------|
| `ensure-docker-qdrant.py` | `(all)` | 10s |
| `logging-status-banner.py` | `(all)` | 3s |
| `submodule-status-check.py` | `(all)` | 10s |
| `session-mypy-banner.py` | `(all)` | 5s |
| `audit-coverage-check.py` | `(all)` | 5s |
| `ci-catchup-on-start.py` | `(all)` | 5s |
| `tei-warmup-on-start.py` | `(all)` | 5s |
| `memory-curation-candidates-on-start.py` | `(all)` | 8s |
| `gh-notif-intake-on-start.py` | `(all)` | 8s |
| `skill-learning-pending-on-start.py` | `(all)` | 3s |
| `skill-learning-acceptance-on-start.py` | `(all)` | 5s |
| `memory-ai-acceptance-on-start.py` | `(all)` | 5s |
| `skill-system-acceptance-on-start.py` | `(all)` | 10s |
| `onec-toolgate-validation-on-start.py` | `(all)` | 8s |

<!-- /АВТО-ГЕНЕРАЦИЯ -->

## Shared-модули

Общая инфраструктура в [`.claude/hooks/shared/`](../../../../.claude/hooks/shared/):

| Модуль | Назначение |
|--------|-----------|
| `session_state.py` | `SessionState`: activated/recommended skills, prompt_id, pending_learn, task_protocol |
| `task_master.py` | Задачи: `add_task()`, `complete_task()`, `get_pending_tasks()`, cooldown |
| `invocation_logger.py` | JSONL логирование всех вызовов → `data/hook-invocations.jsonl` |
| `trust_scorer.py` | Trust scoring для источников (Context7/GitHub/SO/Infostart) |
| `tfidf_scorer.py` | TF-IDF scoring: pure numpy, Layer C матчинга |
| `hook_lock.py` | Межхуковая синхронизация (file lock) |
| `code-skill-patterns.json` | Pattern → Skill маппинг (7 секций, 38 правил: 17 content + 8 directory + 2 bash + 4 research_rules + 4 research_protocol + 3 post-verification) |

## Регистрация в settings.json

Хуки регистрируются в [`.claude/settings.json`](../../../../.claude/settings.json)
в секции `hooks`:

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "C:/1С-Framework/.venv/Scripts/python.exe C:/1С-Framework/.claude/hooks/skill-router.py",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "C:/.../python.exe C:/.../code-skill-enforcer.py",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
```

**Matcher паттерны:**

| Паттерн | Описание |
|---------|----------|
| `"Write"` | Конкретный инструмент |
| `"Read\|Write\|Glob"` | Несколько через `\|` |
| `"mcp__serena__.*"` | Regex для MCP |
| `""` или отсутствие | Все инструменты |

**Абсолютные пути обязательны** — bash на Windows съедает обратные слеши
в относительных путях (подробнее: skill `windows-hooks-paths`).

## Graceful degradation

Принцип: **хуки никогда не ломают workflow**.

```
execute() → исключение → catch в BaseHook.run()
                              │
                              ├── log в hook-errors.log
                              └── sys.exit(0) → allow (pass through)
```

Если хук упал — Claude продолжает работу, как будто хука нет. Ошибка
записывается в `.claude/cache/hook-errors.log` для отладки.

## Подробнее

- Архитектура хуков, 3 уровня — [09.7 Система хуков](../../7_ПРОВЕРКА/7.2_АДМИНИСТРИРОВАНИЕ/09.7_Система_хуков.md)
- Skill-First Enforcement, уровни A-F — [09.8 Skill-First Enforcement](../../7_ПРОВЕРКА/7.2_АДМИНИСТРИРОВАНИЕ/09.8_Skill_First_Enforcement.md)
- Task Protocol, phase machine — [12.1 Обзор декомпозиции](../../1_ИНСТРУКЦИЯ/1.1_ДЕКОМПОЗИЦИЯ_ЗАДАЧ/12.1_Обзор.md)
- Создание хука: skill `create-hook`
- Отладка хуков: skill `hook-debugging`
