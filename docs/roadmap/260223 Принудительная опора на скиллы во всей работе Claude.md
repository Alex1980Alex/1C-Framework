# Дорожная карта: Skill-First Enforcement

> Принудительная опора на скиллы во всей работе Claude

**Дата:** 2026-02-23
**Источник:** Plan `skill-first enforcement`
**Статус:** Planned

---

## Принцип

> Прежде чем Claude что-то делает -- проверь скилл-кеш.
> После того как сделал -- проверь результат против скилла.
> Если скилла не было -- создай его из полученного опыта.

Три фазы замкнутого цикла: **PRE** (проверь до) -> **POST** (проверь после) -> **LEARN** (сохрани знания).

---

## Milestone 1: Инфраструктура (фундамент)

### 1.1 Конфиг паттернов

| # | Задача | Файл | Описание |
|---|--------|------|----------|
| 1.1.1 | Создать файл конфига | `.claude/hooks/shared/code-skill-patterns.json` | Структура JSON с 7 секциями |
| 1.1.2 | Заполнить секцию `patterns` | -- | 14 regex -> skill маппингов (StateGraph, QdrantClient, BaseHook, ...) |
| 1.1.3 | Заполнить секцию `directory_rules` | -- | 9 директорий -> skill маппингов (.claude/hooks/ -> create-hook, ...) |
| 1.1.4 | Заполнить секцию `bash_rules` | -- | 7 команд -> skill маппингов (docker compose -> deployment, ...) |
| 1.1.5 | Заполнить секцию `research_rules` | -- | 4 темы поиска -> skill-кеш маппингов (qdrant -> tech-research, ...) |
| 1.1.6 | Заполнить секцию `research_protocol` | -- | 6 паттернов без скиллов (FastAPI, Redis, 1С объекты, ...) |
| 1.1.7 | Заполнить секцию `post_verification` | -- | 3 правила проверки (langgraph-core, create-hook, qdrant-operations) |
| 1.1.8 | Добавить метаданные | -- | `version`, `description` поля |

**Критерий готовности:** Файл валидный JSON, все 7 секций заполнены.

### 1.2 Trust Scale (шкала доверия источников)

| # | Задача | Файл | Описание |
|---|--------|------|----------|
| 1.2.1 | Создать модуль trust_scorer | `.claude/hooks/shared/trust_scorer.py` | Файл с rubric-данными |
| 1.2.2 | Определить rubric для Context7 | -- | trust = 1.0 (всегда, официальные доки) |
| 1.2.3 | Определить rubric для GitHub | -- | 4 критерия: stars(0.30), last_commit(0.25), docs(0.25), license(0.20) |
| 1.2.4 | Определить rubric для StackOverflow | -- | 3 критерия: accepted(0.40), votes(0.30), recency(0.30) |
| 1.2.5 | Определить rubric для Infostart.ru | -- | 3 критерия: rating(0.35), downloads(0.35), recency(0.30) |
| 1.2.6 | Реализовать `format_trust_rubric()` | -- | Форматирует rubric как текст для systemMessage |
| 1.2.7 | Реализовать `compute_score()` | -- | Вычисляет weighted trust score 0.0-1.0 |
| 1.2.8 | Реализовать `_matches_threshold()` | -- | Внутренний matcher для пороговых выражений (>=100, yes/no, MIT/Apache) |

**Критерий готовности:** Модуль импортируется, `compute_score("github", {"stars": 500, "last_commit_days": 30})` возвращает float.

### 1.3 Расширение session_state

| # | Задача | Файл | Описание |
|---|--------|------|----------|
| 1.3.1 | Добавить `set_pending_learn()` | `.claude/hooks/shared/session_state.py` | Записывает pending_learn в state |
| 1.3.2 | Добавить `get_pending_learn()` | -- | Читает pending_learn, возвращает dict или None |
| 1.3.3 | Добавить `clear_pending_learn()` | -- | Очищает pending_learn после создания tasks |
| 1.3.4 | Обновить `_empty_state()` | -- | Добавить `"pending_learn": null` в начальное состояние |

**Критерий готовности:** Три новые функции работают, данные сохраняются в session-skills.json.

---

## Milestone 2: Хук code-skill-enforcer (ядро системы)

### 2.1 Каркас хука

| # | Задача | Файл | Описание |
|---|--------|------|----------|
| 2.1.1 | Создать файл хука | `.claude/hooks/code-skill-enforcer.py` | Каркас с imports, class, main |
| 2.1.2 | Path resolution | -- | sys.path для base/, shared/ (паттерн из skill-router.py) |
| 2.1.3 | Наследовать BaseHook | -- | `class CodeSkillEnforcer(BaseHook)` |
| 2.1.4 | Определение режима | -- | `inp.detected_event` -> PRE / POST-WRITE / POST-SEARCH |
| 2.1.5 | Lazy-load конфига | -- | `_get_patterns()` с кешированием, graceful degradation |
| 2.1.6 | Компиляция regex | -- | Pre-compile все паттерны при первом вызове |

**Критерий готовности:** Хук запускается без ошибок, возвращает `exit 0` на любом input.

### 2.2 Режим PRE: Уровень A -- контент-паттерны

| # | Задача | Описание |
|---|--------|----------|
| 2.2.1 | `_extract_content()` | Извлечь content (Write) или new_string (Edit) из tool_input |
| 2.2.2 | Минимальная длина | Пропускать content < 20 символов |
| 2.2.3 | Матчинг patterns | `re.search(compiled_pattern, content)` для каждого паттерна |
| 2.2.4 | Проверка activated | `session_state.get_already_activated()` -- скилл активирован? |
| 2.2.5 | BLOCK response | `HookOutput().block(reason)` + `exit 2` для неактивированных |
| 2.2.6 | Сообщение блока | Текст: какие скиллы активировать, через Skill() tool |

**Критерий готовности:** `echo '{"tool_name":"Write","tool_input":{"content":"StateGraph"}}' | python hook.py` -> exit 2.

### 2.3 Режим PRE: Уровень B -- директория-паттерны

| # | Задача | Описание |
|---|--------|----------|
| 2.3.1 | Извлечь file_path | `tool_input.get("file_path", "")` |
| 2.3.2 | Нормализация пути | Привести к forward slashes, убрать абсолютный путь проекта |
| 2.3.3 | Матчинг directory_rules | `if normalized_path.startswith(dir_prefix)` |
| 2.3.4 | Приоритет | directory_rules проверяются ПЕРЕД content patterns |
| 2.3.5 | BLOCK если не активирован | Аналогично уровню A |

**Критерий готовности:** Write в `.claude/hooks/` без `create-hook` -> exit 2.

### 2.4 Режим PRE: Уровень C -- Bash-команды

| # | Задача | Описание |
|---|--------|----------|
| 2.4.1 | Извлечь command | `tool_input.get("command", "")` для tool_name=="Bash" |
| 2.4.2 | Матчинг bash_rules | `re.search(compiled_pattern, command)` |
| 2.4.3 | BLOCK если не активирован | Аналогично уровню A |
| 2.4.4 | Сообщение блока | Указать какой скилл нужен для этой команды |

**Критерий готовности:** `Bash("docker compose up")` без `deployment` -> exit 2.

### 2.5 Режим PRE: Research Protocol (fallback)

| # | Задача | Описание |
|---|--------|----------|
| 2.5.1 | Матчинг research_protocol | Если ни patterns, ни directory_rules не сработали |
| 2.5.2 | Определение домена | `domain: "tech"` или `"1c"` из конфига |
| 2.5.3 | Tech research message | Context7 -> StackOverflow -> GitHub + Trust Scale |
| 2.5.4 | 1С research message | infostart.ru -> GitHub -> its.1c.ru + Trust Scale |
| 2.5.5 | Включение trust rubric | Вызвать `format_trust_rubric()` и добавить в message |
| 2.5.6 | Установка pending_learn | `set_pending_learn({label, domain, pattern})` |
| 2.5.7 | ADVISE response | `HookOutput().system_message(protocol_text)` + exit 0 |

**Критерий готовности:** Write с FastAPI -> systemMessage с research protocol + pending_learn в state.

### 2.6 Режим POST-WRITE: Уровень E -- post-verification

| # | Задача | Описание |
|---|--------|----------|
| 2.6.1 | Определить PostToolUse | `inp.detected_event == "PostToolUse"` и `tool_name in ("Write", "Edit")` |
| 2.6.2 | Извлечь file_path + content | Из tool_input (PostToolUse получает те же поля) |
| 2.6.3 | Найти activated skills | `session_state.get_already_activated()` |
| 2.6.4 | Матчинг post_verification | file_path matches `file_pattern` + skill activated |
| 2.6.5 | Проверка must_contain | Все паттерны из `must_contain` должны быть в content |
| 2.6.6 | Проверка must_not_contain | Ни один паттерн из `must_not_contain` не должен быть в content |
| 2.6.7 | Advisory systemMessage | Сообщение о несоответствии с указанием что исправить |
| 2.6.8 | Создание mandatory task | `add_task()` через task_master при несоответствии |

**Критерий готовности:** PostToolUse Write в agents/ с AgentExecutor -> systemMessage + task.

### 2.7 Режим POST-WRITE: Уровень F -- LEARN

| # | Задача | Описание |
|---|--------|----------|
| 2.7.1 | Проверить pending_learn | `get_pending_learn()` -- есть ли ожидающее обучение? |
| 2.7.2 | Task 1: создать скилл | `add_task("LEARN: Создать skill для [label]")` |
| 2.7.3 | Task 2: зарегистрировать | `add_task("LEARN: Зарегистрировать [label] в skill-router-config.json")` |
| 2.7.4 | Task 3: мигрировать паттерн | `add_task("LEARN: Перенести [label] из research_protocol в patterns")` |
| 2.7.5 | Очистить pending_learn | `clear_pending_learn()` после создания tasks |
| 2.7.6 | Anti-spam cooldown | `has_recent_completion("code-skill-enforcer", cooldown_minutes=10)` |

**Критерий готовности:** После research protocol + Write -> 3 mandatory tasks в hook-todos.json.

### 2.8 Режим POST-SEARCH: Уровень D -- research enforcement

| # | Задача | Описание |
|---|--------|----------|
| 2.8.1 | Определить PostToolUse search | `tool_name in ("WebSearch", "WebFetch")` |
| 2.8.2 | Извлечь query | `tool_input.get("query", "")` или `tool_input.get("url", "")` |
| 2.8.3 | Матчинг research_rules | Regex match query против правил |
| 2.8.4 | Определить кеш-путь | `tech-research` -> `.claude/skills/tech-research/cache/` |
| 2.8.5 | Advisory systemMessage | "Проверь кеш [path] перед использованием результатов" |

**Критерий готовности:** WebSearch "qdrant collection" -> systemMessage с путём к кешу.

---

## Milestone 3: Context7 MCP интеграция

| # | Задача | Файл | Описание |
|---|--------|------|----------|
| 3.1 | Проверить Node.js >= 18 | -- | `node --version` |
| 3.2 | Получить API key | -- | Регистрация на context7.com/dashboard |
| 3.3 | Добавить в .mcp.json | `.mcp.json` | Секция `context7` с command `npx -y @upstash/context7-mcp` |
| 3.4 | Настроить API key | -- | Переменная `CONTEXT7_API_KEY` в env секции |
| 3.5 | Тест resolve-library-id | -- | Вызвать `resolve-library-id("FastAPI")` -- получить ID |
| 3.6 | Тест get-library-docs | -- | Вызвать `get-library-docs(id, "routing")` -- получить доки |
| 3.7 | Интегрировать в research protocol | `code-skill-enforcer.py` | Context7 -- первый шаг в tech research message |

**Критерий готовности:** `resolve-library-id("LangGraph")` возвращает ID, `get-library-docs` возвращает документацию.

---

## Milestone 4: Регистрация и подключение

### 4.1 settings.json -- PreToolUse

| # | Задача | Описание |
|---|--------|----------|
| 4.1.1 | Добавить PreToolUse entry | matcher: `"Write\|Edit\|Bash"`, command: `code-skill-enforcer.py` |
| 4.1.2 | Поставить ПЕРВЫМ | До root-clutter-guard.py и search-optimizer.py |
| 4.1.3 | Timeout: 3s | Достаточно для regex matching + session state read |

### 4.2 settings.json -- PostToolUse Write|Edit

| # | Задача | Описание |
|---|--------|----------|
| 4.2.1 | Добавить PostToolUse entry | Добавить `code-skill-enforcer.py` в существующий `Write\|Edit` matcher |
| 4.2.2 | Порядок | ПОСЛЕ auto-git-save и docs-change-tracker |
| 4.2.3 | Timeout: 3s | Для verification + task creation |

### 4.3 settings.json -- PostToolUse WebSearch|WebFetch

| # | Задача | Описание |
|---|--------|----------|
| 4.3.1 | Добавить PostToolUse entry | Добавить `code-skill-enforcer.py` в `WebSearch\|WebFetch` matcher |
| 4.3.2 | Порядок | ПОСЛЕ knowledge-cache-reminder.py |
| 4.3.3 | Timeout: 3s | |

### 4.4 task-enforcer совместимость

| # | Задача | Описание |
|---|--------|----------|
| 4.4.1 | Зарегистрировать created_by | Добавить `"code-skill-enforcer"` в MANDATORY_HOOKS task-enforcer.py |
| 4.4.2 | Проверить формат tasks | Убедиться что add_task() создаёт tasks в формате hook-todos.json |

**Критерий готовности:** Все 3 события зарегистрированы, хук вызывается при Write/Edit/Bash/WebSearch.

---

## Milestone 5: Тестирование

### 5.1 Unit tests (stdin pipe)

| # | Задача | Input | Expected |
|---|--------|-------|----------|
| 5.1.1 | Уровень A: BLOCK | Write + "StateGraph" | exit 2, block |
| 5.1.2 | Уровень A: ALLOW | Write + "print(hello)" | exit 0 |
| 5.1.3 | Уровень A: ADVISE | Write + "from fastapi import APIRouter" | exit 0, systemMessage |
| 5.1.4 | Уровень B: BLOCK | Write в `.claude/hooks/` | exit 2, block с "create-hook" |
| 5.1.5 | Уровень B: ALLOW | Write в `src/utils/` | exit 0 |
| 5.1.6 | Уровень C: BLOCK | Bash "docker compose up" | exit 2, block с "deployment" |
| 5.1.7 | Уровень C: ALLOW | Bash "ls -la" | exit 0 |
| 5.1.8 | Уровень D: ADVISE | PostToolUse WebSearch "qdrant" | exit 0, systemMessage |
| 5.1.9 | Уровень E: PASS | PostToolUse Write agents/ + StateGraph | exit 0 |
| 5.1.10 | Уровень E: FAIL | PostToolUse Write agents/ + AgentExecutor | exit 0, systemMessage + task |
| 5.1.11 | Уровень F: LEARN | PostToolUse Write + pending_learn | exit 0, 3 tasks |
| 5.1.12 | Edge: wrong tool | Read tool | exit 0 |
| 5.1.13 | Edge: no config | Удалить конфиг | exit 0 (graceful) |
| 5.1.14 | Edge: empty content | Write + "" | exit 0 |
| 5.1.15 | Edge: malformed JSON | Невалидный stdin | exit 0 |

### 5.2 Integration tests

| # | Задача | Сценарий |
|---|--------|----------|
| 5.2.1 | PRE-A flow | Skill не активирован -> BLOCK -> Skill('X') -> retry -> ALLOW |
| 5.2.2 | PRE-B flow | Write в hooks/ -> BLOCK -> Skill('create-hook') -> retry -> ALLOW |
| 5.2.3 | PRE-C flow | Bash docker -> BLOCK -> Skill('deployment') -> retry -> ALLOW |
| 5.2.4 | Research flow | FastAPI -> ADVISE -> Claude researches -> writes code -> OK |
| 5.2.5 | POST-E flow | Activated skill + bad pattern -> advisory + task -> Stop block |
| 5.2.6 | LEARN flow | Research -> Write -> 3 tasks created -> complete tasks -> Stop OK |

### 5.3 E2E сценарии

| # | Задача | Сценарий |
|---|--------|----------|
| 5.3.1 | Полный цикл PRE | Промпт "сделай агента" -> Read -> Write StateGraph -> BLOCK -> Skill -> retry -> OK |
| 5.3.2 | Полный цикл LEARN | Новая библиотека -> research -> apply -> LEARN tasks -> create skill -> verify |
| 5.3.3 | Между сессиями | Сессия 1: LEARN создал skill. Сессия 2: тот же паттерн -> BLOCK -> быстрый путь |

**Критерий готовности:** 15 unit tests pass, 6 integration tests pass, 3 E2E pass.

---

## Milestone 6: Оптимизация и мониторинг

### 6.1 Производительность

| # | Задача | Описание |
|---|--------|----------|
| 6.1.1 | Замерить latency | `elapsed_ms` для каждого вызова через invocation_logger |
| 6.1.2 | Оптимизировать regex | Pre-compile, кешировать, lazy-load |
| 6.1.3 | Проверить timeout budget | Все вызовы < 3s на Windows |
| 6.1.4 | Исключить ложные срабатывания | Паттерны типа `@tool` слишком широкие -- сузить если нужно |

### 6.2 Логирование

| # | Задача | Описание |
|---|--------|----------|
| 6.2.1 | Добавить enforcement log | `data/code-skill-enforcer.log` -- уровень, паттерн, действие |
| 6.2.2 | Метрики в invocation_logger | hook=CodeSkillEnforcer, outcome=block/allow/advise/learn |
| 6.2.3 | LEARN tracking | Логировать сколько скиллов создано через LEARN |
| 6.2.4 | Accuracy tracking | Сколько BLOCK'ов привели к активации скилла vs отмене |

### 6.3 Dashboard

| # | Задача | Описание |
|---|--------|----------|
| 6.3.1 | Расширить hook-dashboard | Секция "Skill Enforcement" в scripts/hook-dashboard.py |
| 6.3.2 | Метрики | blocks/session, learns/session, patterns coverage |
| 6.3.3 | Рост базы скиллов | График: patterns count vs research_protocol count over time |

---

## Milestone 7: Эволюция конфига

### 7.1 Наращивание patterns

| # | Задача | Описание |
|---|--------|----------|
| 7.1.1 | Мониторинг research_protocol | Какие паттерны чаще всего попадают в research? |
| 7.1.2 | Автоматическая миграция | Скрипт: если скилл создан через LEARN -- предложить перенос |
| 7.1.3 | Покрытие | Цель: 90% Write/Edit попадают в patterns, <10% в research |

### 7.2 Наращивание post_verification

| # | Задача | Описание |
|---|--------|----------|
| 7.2.1 | Добавить правила | Для каждого нового скилла -- добавить post_verification |
| 7.2.2 | must_contain из скилла | Автоматически извлекать обязательные паттерны из SKILL.md |
| 7.2.3 | must_not_contain из скилла | Автоматически извлекать антипаттерны из SKILL.md |

### 7.3 Новые домены

| # | Задача | Описание |
|---|--------|----------|
| 7.3.1 | Docker/DevOps | Паттерны для Dockerfile, docker-compose.yml, nginx.conf |
| 7.3.2 | Тестирование | Паттерны для pytest fixtures, conftest, markers |
| 7.3.3 | CI/CD | Паттерны для GitHub Actions, workflows |

---

## Сводная таблица: все задачи

| Milestone | Задач | Файлы | Зависимости |
|-----------|-------|-------|-------------|
| **1. Инфраструктура** | 20 | 3 файла (config, trust_scorer, session_state) | -- |
| **2. Хук (ядро)** | 35 | 1 файл (code-skill-enforcer.py) | M1 |
| **3. Context7 MCP** | 7 | 1 файл (.mcp.json) | Node.js, API key |
| **4. Регистрация** | 9 | 2 файла (settings.json, task-enforcer.py) | M2 |
| **5. Тестирование** | 24 | -- | M1-M4 |
| **6. Оптимизация** | 10 | -- | M5 |
| **7. Эволюция** | 8 | -- | M6 |
| **ИТОГО** | **113** | **7 файлов** | |

---

## Граф зависимостей

```
M1 Инфраструктура ─────┐
  1.1 Конфиг             │
  1.2 Trust Scale        ├─→ M2 Хук ────────→ M4 Регистрация ──→ M5 Тесты ──→ M6 Оптимизация ──→ M7 Эволюция
  1.3 Session State      │
                         │
M3 Context7 MCP ─────────┘
  (параллельно с M1)
```

```
Порядок выполнения:

[M1.1] ──→ [M1.2] ──→ [M1.3] ──→ [M2.1] ──→ [M2.2] ──→ [M2.3] ──→ [M2.4] ──→ [M2.5]
  │                                                                                  │
  └── [M3] (параллельно) ──────────────────────────────────────────────────→ [M2.5] ←┘
                                                                                  │
                                                                    [M2.6] ──→ [M2.7] ──→ [M2.8]
                                                                                              │
                                                                    [M4.1] ──→ [M4.2] ──→ [M4.3] ──→ [M4.4]
                                                                                                        │
                                                                    [M5.1] ──→ [M5.2] ──→ [M5.3]
                                                                                              │
                                                                    [M6.1] ──→ [M6.2] ──→ [M6.3]
                                                                                              │
                                                                    [M7.1] ──→ [M7.2] ──→ [M7.3]
```

---

## Полная карта enforcement (результат)

```
                    ┌─────────────────────────────────────────────┐
                    |         ПРИНЦИП SKILL-FIRST                 |
                    |  Проверь ДО. Проверь ПОСЛЕ. Сохрани знания. |
                    └────────────────┬────────────────────────────┘
                                     |
Этап работы Claude          Фаза   Уровень  Действие             Принуд.
────────────────────────────────────────────────────────────────────────────
Пользователь пишет промпт → PRE    router   Рекомендация          Советует
Claude пишет файл в dir/   → PRE    B        BLOCK                 100%
Claude пишет код с pattern → PRE    A        BLOCK                 100%
Claude пишет код без skill → PRE    fallback Research + Context7   Советует
Claude запускает Bash      → PRE    C        BLOCK                 100%
Claude ищет в интернете    → PRE    D        "Проверь кеш"         Советует
                                     |
                              ═══════╪═══════ действие выполнено
                                     |
Claude записал код         → POST   E        Verify vs skill       Task→Stop
Claude исследовал          → POST   F        LEARN: создать скилл  Task→Stop
Claude ищет повторно       → POST   -        Кеш-напоминание       Советует
Claude останавливается     → STOP   enforcer BLOCK если tasks      100%

                              ═══════╪═══════ следующая сессия
                                     |
Тот же паттерн             → PRE    A        BLOCK (быстрый путь)  100%
Тот же промпт              → PRE    router   Рекомендация (новый   Советует
                                              bundle в config)
```

---

## Замкнутый цикл обучения

```
  Паттерн найден
       |
       ├─ Скилл ЕСТЬ → [PRE] BLOCK → активируй → [POST] проверь → OK
       |
       └─ Скилла НЕТ → [PRE] research protocol
                              |
                              ├─ Context7 / SO / GitHub / infostart
                              ├─ Применил знания (написал код)
                              ├─ [POST] проверил результат
                              |
                              └─ [LEARN]
                                    |
                                    ├─ 1. doc-to-skill → SKILL.md
                                    ├─ 2. Register в skill-router-config.json
                                    ├─ 3. Migrate: research_protocol → patterns
                                    |
                                    └─ Следующая сессия:
                                       Паттерн → скилл ЕСТЬ → быстрый путь
```
