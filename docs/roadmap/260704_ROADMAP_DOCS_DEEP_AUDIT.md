# 260704 — Дорожная карта: глубокий аудит документации (9 слоёв, 315 файлов)

> **Метод:** 6 параллельных read-агентов, каждый прочитал все файлы своей зоны и
> spot-check'нул факты против кода (`.mcp.json`, `settings.json`, `service.py`,
> `registry.yaml`, `src/memory/*`, `ci.yml`, хуки). Рубрика: ОШИБКА (факт против кода /
> межглавное противоречие) · УСТАРЕЛО (обогнано реальностью) · СЛАБО (структура/дубли) ·
> УЛУЧШЕНИЕ. Вердикт главы: **A** — ок, **B** — точечные правки, **C** — переработка.
>
> Контекст: этому аудиту предшествовали 4 волны фиксов той же сессии (92e39496):
> P0 `119a70012`+`8e7e9f93d` (enforcement-контур), P1 `e46038985` (фантомы GLM/таблиц),
> P2 `16af6203c` (26 ссылок + привязка), P3 `de688e295` (покрытие 5 групп).
> Здесь — то, что осталось ПОСЛЕ них.

---

## Сводная карта вердиктов

| Слой | A (ок) | B (точечно) | C (переработка) |
|---|---|---|---|
| 0 ВВЕДЕНИЕ | 0.3 Harness Map, 00_СОДЕРЖАНИЕ | 0.1 Обзор, 0.2 Быстрый старт | — |
| 1 ИНСТРУКЦИЯ | 1.1 Декомпозиция | 1.2 SDD | — |
| 2 КОНТЕКСТ | 2.10 Ecosystem | 2.1, 2.2, 2.3, 2.4, 2.6, 2.7, 2.8, 2.9 | **2.5 Индексация проектов** |
| 3 ИНСТРУМЕНТЫ | 3.1, 3.5, 3.6, 3.7 | 3.2 Подключение 1С | **3.3 Lazy-MCP**, **3.4 Extensions** |
| 4 МЫШЛЕНИЕ | 4.4 Пайплайн 1С, 4.5, 4.6 | 4.2 AutoResearch v2 | **4.1 AutoResearch v1**, **4.3 Analyze-1C-Research** |
| 5 ПАМЯТЬ | 27.4, 27.5, 27.6, 27.9, 32.2–32.5, 32.8–32.10, 37.6, 37.7 | 27.1–27.3, 27.7, 27.10, 27.12.x, 27.13, 32.1, 32.7, 37.1, 37.8, 37.9 | **27.8**, **27.11**, **32.6**, **37.2**, **37.3**, **37.4**, **37.5** |
| 6 ПОМОЩНИКИ | 6.1 Ralph | 6.2 Token Economy, 6.4 GLM (банер есть) | **6.3 LLM Rotation** |
| 7 ПРОВЕРКА | 7.1, 7.10 | 7.3, 7.4, 7.5, 7.6, 7.7, 7.9 | **7.2 Администрирование**, **7.8 PR Automation** |
| 9 НАВЫКИ | 9.3 Learning Loop, 13.4–13.6 | 9.1 (11.1/11.2/11.5/11.7), 9.2 | **11.3**, **11.6**, **11.8**, **9.4 XSkill (вся)** |

**Интегрально:** каркас документации здоров (недавно синхронизированные главы — 43.x,
44.x, 42.x, 12.x, 49.1 — образцовые), но 4 системных болезни: (1) главы-фантомы
(XSkill, Extensions, analyze-1c-research-команды); (2) legacy-стек E5/Ollama/D:\-пути,
поданный как текущий; (3) дрейф счётчиков (бандлы/скиллы/коллекции/tools/провайдеры);
(4) память 37.x описывает код, которого никогда не было (async/unified_search).

---

## P0 — Ошибки, активно вводящие в заблуждение (≤1 день)

| # | Глава/файлы | Проблема | Действие |
|---|---|---|---|
| ✅ P0.1 | 7.8 / 40.8_user_action_items | **DONE (verify 2026-07-04).** Основное дерево чистое: фрагмент заменён на «см. журнал ротации», заголовок = 6 пунктов (#1–#3 обязательные, #4–#6 опциональные). Grep `sk-ant` по репо — только плейсхолдеры-примеры. Дополнительно redacted stale-копия в worktree `.claude/worktrees/agent-a4e6ce0279d63bbb6/…/40.8` (последний физический след фрагмента на диске) | ~~Удалить фрагмент…~~ |
| ✅ P0.2 | 9.4 / 29.1–29.7 (вся глава) | **DONE (verify 2026-07-04).** Банер «SUPERSEDED → гл. 5.1 Unified Memory» стоит на всех 7 файлах (формат GLM 6.4); фантомность подтверждена независимой проверкой (0 `.py`, нет `/xskill-*`, нет теста). Дополнительно удалён последний физический след — stale `scripts/hooks/learning/__pycache__/experience-embedder.cpython-313.pyc` | ~~Банер на 7 файлов~~ |
| ✅ P0.3 | 2.8 / 31.2↔31.3↔31.5↔31.6 | **DONE (verify 2026-07-04).** Доки реконцилированы (SUPERSEDED-баннеры + std-pool путь во всех 4 файлах); код-факт: `reindex_bsl_qwen3.py --pooling-mode default="standard"`, late-chunking opt-in. **Найдено и исправлено живое противоречие В КОДЕ:** `scripts/reindex_bsl_fast.ps1` хардкодил `--pooling-mode late-chunking` + рекламировал его как quality-путь (`:5-7`,`:19-22`,`:92`) → переведён на `standard` + SUPERSEDED-нота; тот же хвост в skill `bsl-reindex` («Full → qwen3-st (Late Chunking)») исправлен. Parse-check PASS | ~~Реконциляция 31.3/31.5/31.6~~ |
| ✅ P0.4 | 6.3 / 21.1, 21.8, 21.4 (+15.1, 15.4) | **DONE 2026-07-04.** Доки уже были синхронны на 4 провайдера + 90/120/240 (аудит опирался на устаревший снапшот). Дополнительно по решению пользователя сменён default primary `claude-cli-haiku`→`claude-cli-sonnet` (sonnet-first): правки `config.py` + `service.py` (DEFAULT_PROVIDERS reorder) + таблицы 21.1/21.8/15.1/15.4 + SKILL.md. Фантомная нота про `.mcp.json`-override `LLM_ROTATION_PRIMARY_PROVIDER=zai-glm5` **удалена** — такого ключа в `.mcp.json` нет (там только `ZAI_API_KEY` + `LLM_ROTATION_TIMEOUT=60`); runtime-primary = дефолт кода. **Требует `/mcp reconnect`** для подхвата | ~~Перегенерировать таблицы; зафиксировать ноту про `.mcp.json`-override `zai-glm5`~~ |
| ✅ P0.5 | 3.2 / 16.5 | **DONE (verify 2026-07-04).** Все 5 пунктов уже соответствуют `.mcp.json`: `1c-mcp-crud`+4 варианта (16.5:12), 19 инструментов (16.5:27, сверено с живым MCP-реестром), `MCP_ONEC_*` (16.5:102-119), `mcp_1c_stdio_launcher.py` (16.5:66), `cwd C:\…\external\1c_mcp` (16.5:138). Минорный не-P0 хвост: внутреннее расхождение путей исходников 16.5:73 (`src/bsl/mcp_integration/`) vs :124/:162 (`src/external/1c_mcp/src/1c_ext/`) | ~~Переписать секцию~~ |
| ✅ P0.6 | 4.3 / 22.1, 22.4 | **DONE (verify 2026-07-04).** 22.1:3-9 и 22.4:7-10 уже несут предупреждение «команда не существует» + статус «экспериментальный/в простое» + ссылку на 43.x; реальная точка входа = скрипт `analyze-1c-research.ps1`. Дополнительно исправлен пропущенный аудитом хвост **22.5:30** — команда упоминалась как нормальный путь миграции без пометки-фантома | ~~Убрать команду; пометить главу~~ |
| ✅ P0.7 | 3.4 / 35.1 | **DONE (verify 2026-07-04).** 35.1:3-6 несёт банер «концептуальная глава-задел, каталога нет»; :17-22 явно: submodule отменён, MCPToolkit deprecated → заменён `1c-mcp-crud` (`src/bsl/mcp_integration/`, ссылка на 16.5). Код-факты подтверждены (`src/extensions/` нет, `.gitmodules` чист). Глава оставлена в TOC как задел — соответствует варианту «переписать» | ~~Переписать/удалить из TOC~~ |
| ✅ P0.8 | 5.3 / 37.2, 37.4 (+32.1, 32.7) | **DONE (verify 2026-07-04).** Код-истина: `LAYER_WEIGHTS = {sqlite:0.30, qdrant:0.35, md:0.15, wiki:0.20}` (`memory-first-hook.py:102`), sync/последовательно, stdout-inject, MAX_RESULTS=5, timeout 10s, `search_wiki()` = stub `return []`. 37.2/37.4 уже полностью соответствуют (веса, sync-оговорки, stub процитирован, L4=Qdrant-тир помечен «хук его не вызывает» 37.2:134); 32.1 таблица верна. Дозачищены хвосты: **32.7:233** «parallel async» → sync/sequential, **32.7:177** и **32.1:116** «systemMessage» → stdout-инъекция | ~~Таблица L1–L5; 37.4 по sync-коду~~ |

## P1 — Устаревшее и дрейф счётчиков (1–2 дня)

> **✅ DONE 2026-07-04** (commit `d6f902f2d` + Langfuse `bf8462c50`). Все 5 доменов отработаны 5 параллельными агентами, код/конфиг/Qdrant = истина (~71 файл). **Единственная реальная code-правка** — Langfuse v2→v4 миграция (не «разъехавшийся пин», а несовместимость API: код звал `client.generation()/.span()`, которых нет в установленном v4.6.1 → трейсинг молча мёртв; по решению пользователя мигрирован callback на нативный `langfuse.langchain.CallbackHandler` + tracer на `start_observation/create_score`, пин `>=4.0,<5.0`; 8/8 тестов, reviewer PASS). **Выверенные каноны:** скиллы 97, бандлы 52/v9/8доменов, хуки 97ф/99рег; паттерны 17/8/2/4/3; lazy-mcp 24серв/10катег (роадмап ошибочно 11); .mcp.json 28 серверов; 1c-debug-hmr 30 tools; debug-тесты 273; ci.yml 10 job (без schedule), ci-1c.yml 5 job; auto-git-save threshold=1/timeout=30; платформа 1С 1936 (1859 не установлен); Qdrant `bsl_code_v4_late` 37 694, `bsl_code_erp_ref` 819 903, `learned_patterns` 51; confidence = Beta(7,3) posterior; autoresearch dormant; SetFit ВКЛ. Скрипт-генератор каталога (11.6) — оставлен на P3.1. Детали правок — в теле коммита.

### 9 НАВЫКИ — счётчики и каталог
- **11.6 Каталог скиллов — регенерация**: «57 скиллов» при реальных ~97; перечислены несуществующие (`1c-object-map`, `1c-query-language`, `bsl-string-formatting`, `1c-skd`, `create-skill`); нет ~40 актуальных. Генерировать из `ls .claude/skills` (желательно скриптом, см. P3.1).
- **Сквозной счётчик бандлов**: реально **52 бандла / config v9 / 8 доменов** — правится в 01.2 («47»), 11.2 («25/6»), 11.3 («25», внутр. противоречие v8/v9), 11.8 («25/57/v6/6» + Phase 16 «scale до 50-100» уже пройдена), 13.1 («25 хуков»), 13.2 («28/6», «43 правила»), 13.3 («28» vs сумма 25; «thefuzz» → rapidfuzz+pymorphy3).
- 11.4: статистика паттернов 14/9/7/6/3 → реальные 17/8/2/4/3 (`code-skill-patterns.json`); Level C «3 правила» → 2; phantom-скиллы уровней A/A.1.
- 01.2: «HOOKS (94) / SKILLS (91)» + разбивка по событиям → реальные (89 регистраций/97 файлов; UPS2·Pre11·Post10·Stop1 в разрезе settings).

### 2 КОНТЕКСТ — legacy-стек и счётчики
- **2.5 (вся глава 19.x) → ARCHIVE-банер**: Ollama nomic 768d / MiniLM 384d / bsl_code_v3 / metadata_1c — мёртвый стек; один банер-редирект на 31.x вместо per-file нот.
- Точечный legacy E5-1024d как «текущее»: 03.1 (диаграмма), 03.3 (entity emb), 04.2 (:7,:37), 07.2, 02.3:42, 10.3 (таблица моделей без Qwen3!), 30.2 (ASCII «768d nomic»).
- 28.4 §12 — supersede-нотка по образцу 31.7 («RRF hurt / BM25 deferred» обогнаны hybrid-DBSF 2026-06-20).
- Счётчики: 28.1/28.3 «54 752 pts + Late Chunking» → 37 694 std/hybrid; 28.1 «6 инструментов» → 7; 31.1/31.2 `bsl_code_erp_ref` 187 391 vs 31.6 819 903 — выверить по Qdrant; 31.3 «9 активных» → 10; 27.7 «22 паттерна» — снять хрупкую конкретику.
- 04.9: счётчик v4_late 24 455 vs 54 800 внутри файла.

### 5 ПАМЯТЬ — устаревшие модели и константы
- **Confidence-модель**: `+= 0.02/−0.01` как текущая в 32.6, 37.2, 37.5 → Beta(7,3) posterior (§22); promotion-математика 32.6 пересчитать; развести WikiDecayService vs §22 count-decay (две «decay» с разной семантикой).
- **27.11**: archived «×0.5» → hard-exclude (3 места: §3.1/§5/§9); «3 arms» → 4 (+skill_learning); «systemMessage» → stdout-inject.
- 27.12.4: «3 плеча»→4, scroll 100→50, TTL 300→900. 27.10: TTL 300→900, cache-key → top-8 salient.
- 27.3: budget 4.0s→3.0s, суффиксы 29→37, путь `D--1--Framework`→`C--…`, 3-layer→4-layer (+wiki).
- **27.8 (вердикт C)**: git-команда, auto-tag-таблица, таймауты, веса — всё против кода; переработать по `session-memory-save.py`.
- 27.2: формулы propagation (`exp(−λt)` и пр.) → реальные из кода (27.12.2 уже помечает ⚠ — взять оттуда); «6 типов связей» → 8 (после ADR-L1).
- 37.3/37.8: имена CLI-субкоманд → реальные `export-all/export-entity/sync-incremental/index-search/verify/…` (32.9 верна — взять оттуда); 37.1/37.9 «все 7 фаз закрыты» → честный CORE+deferred; 37.5 `MemoryCube.from_pattern` — вычистить.
- 27.1: «47 файлов» → ~70; статус P5 согласовать с 27.13.

### 7 ПРОВЕРКА
- 09.2: фантомный weekly pip-audit cron (в `ci.yml` нет `schedule:`) + неверный якорь строк.
- 40.1/40.2/40.5 ↔ 09.6: порог auto-git-save «3» → **1** (`CLAUDE_COMMIT_THRESHOLD`); 09.6 timeout 15→30.
- 40.2/40.6: snippets `AUTO_PR_*`/`AUTO_PR_SMTP_*` «уже настроено» — в реальном settings.local.json их НЕТ; `dev-master` → master.
- Langfuse: pyproject `>=2.0` vs доки/код v4 — поднять пин ИЛИ исправить 39.1/39.3 (выбрать по факту работы кода).
- **D:\-пути → C:\1С-Framework**: 17.1, 17.4, 41.2, 41.3 (+ из зоны 3/6: 16.5, 15.3, 21.5, 26.1, 26.5, 23.2, 29.7).
- 41.1: `ci-1c.yml` не «legacy» (развивается: Sonar QG, Coverage41C, Allure); «три job'а» → 4; 42.7 «5 джобов» — выверить список.
- 17.4 vs 17.6: версия платформы 1859 vs 1936 — унифицировать.

### 3 ИНСТРУМЕНТЫ / 4 МЫШЛЕНИЕ
- **26.x Lazy-MCP регенерация**: 26.3 «27 серверов/10 категорий» → 24/11 по `registry.yaml` (+`code-quality/sonarqube`, `context7`; −фантомные `conversation-memory`, `ripgrep`); 26.7 «20 active» → 28 по `.mcp.json` (нет codepilot1c/whisper/scene-detect/1c-debug-hmr/crud-вариантов); межглавное 10 vs 11 категорий.
- 16.6: добавить `codepilot1c` (:8766) в карту среды.
- 36.8: «25 MCP tools» → 30 (синхронно со skill 1c-debug-hmr); 36.1 «222 pass» → 273.
- **AutoResearch 18.x/20.x**: статус-штамп «инфраструктура готова, активных прогонов нет с 2026-03/04» (v1 — dormant, `data/autoresearch.jsonl` пуст); 22.5 — метрики датировать; решить судьбу: архив или реанимация.
- SetFit: 43.3/43.5.6 добавить оговорку «в этом репо ВКЛ через settings.local.json» (как в 43.5.4).

## P2 — Структура: дубли, нумерация, простыни (по мере прохода)

> **✅ DONE 2026-07-04** (commit `2191440a5`, code-first). 6 параллельных агентов, file-ownership
> без пересечений, каждый code-verify PASS; net −911 строк. **Code-развилка P2.6 «Opus 4.6»:**
> аудит счёл её doc-only дефектом, но `claude-opus-4-6` — фактический дефолт кода (config/routing/
> cost/tests) → по решению пользователя **bump кода → `claude-opus-4-8`** + цены $15/75→$5/25
> (реальный тариф 4.8), тесты 20/20; доки синхронизированы следом. **Заметки исполнителей:**
> пара P2.4 09.9↔42.x оказалась НЕ дублем (MCP-health vs CI-Monitor) — оставлена; 37.x нёс СТАЛЫЙ
> код (`MemoryCube.from_pattern`) → канон 32.x. Backlog'и вынесены в `260704_ROADMAP_{AUTO_REPORTS,PR_AUTOMATION}_BACKLOG.md`.

| # | Что | Действие |
|---|---|---|
| P2.1 | Заголовки-мисматчи: 04.1 = «# 19.1», все 6 файлов 2.7 = «# 38.x» | Перенумеровать заголовки под фактические ID файлов |
| P2.2 | 04.1: «14 стратегий» vs 19 строк таблицы (№15–19 — авто-мусор `Graph R A G Auto`) + блок `/search/ask` разрывает таблицу | Вычистить авто-строки, восстановить таблицу, счётчик 14 |
| P2.3 | 43.5 §0 (~260 строк детектора) полностью дублирует 43.5.1–43.5.6 (детектор описан ~7×) | Свести §0 к диаграмме-указателю на 43.5.x |
| P2.4 | Дубль-пары: 09.4.1↔39.x (Langfuse), 18.x↔20.x (~60% AutoResearch), 32.x↔37.x (wiki/promotion), 09.9↔42.x (мониторинг) | В каждой паре выбрать канон, второй сжать до указателя |
| P2.5 | Простыни: 27.13 (verbatim-строка ~10k симв.), 40.4 (session-log rounds 1-6), 28_1.6 (roadmap T3-T7 в reference-доке), 04.9 (300 строк бенчей) | 27.13 — переформатировать; 40.4/28_1.6 — вынести backlog в docs/roadmap/; 04.9 — исторический бенч в архив-секцию |
| P2.6 | Мелочь: 32.8 typo «taablesonon»; 44.2:68 продублированная строка; 14.x «9-шаговый» vs нумерация 1–10; 13.6 `.vscode/mcp.json` → `.mcp.json`; 05.1 «Claude Opus 4.6» — несуществующая модель; 02.2 `SELF_RAG__GRADER_MODEL` пример vs дефолт кода | Точечные однострочники |

## P3 — Улучшения (опционально, снижают повторный дрейф)

> **✅ DONE 2026-07-05** (commit `cdf25cbd8` + верификационные хвосты). 5 параллельных агентов + адверсариальный ревьюер PASS.
> **P3.1** → `scripts/docs_counters.py` (живые счётчики: 97 скиллов/52 бандла/v9/28 MCP/24+10 lazy/97 хуков + Qdrant-probe; drift-lint `--check` — 128 кандидатов, advisory; `--strict` для будущего CI; 11 unit).
> **P3.3/P3.5 — главный улов:** доки 33.1/34.1 ВРАЛИ против кода — redact-режим не применяет `redacted_text`, output-фильтр не вызывается из `dispatch()`, выдуманные env `KB__*_DB` и методы `create_collection`/`register_document` — переписано по факту кода с file:line.
> **P3.6a — аудит ошибался:** `/opsx:approve` — это skill, slash-команды на диске нет → в КОМАНДЫ не добавлен.
> P3.2 (штампы 46.1–46.8+45.1), P3.4 (GLM «оценки, не измерялись»), P3.6b (cc_docs → 31.1/31.2), P3.7 (05.5 секции по коду plan_execute/research_v2), P3.8 (24.3 openspec-имена; 24.4 LoC=96 сверено).

1. **Генерация счётчиков вместо ручных**: скрипт (расширение `audit_docs_skills.py`) для авто-подстановки живых чисел (бандлы/скиллы/серверы/коллекции/tools) — корень болезни №3; кандидаты-якоря: `skill-router-config.json`, `registry.yaml`, `.mcp.json`, Qdrant introspection.
2. **Статус-штампы** для методологических глав (46.x, 45.1): дата + датированные ссылки вместо «в этой сессии».
3. **33.1 Guardrails** — расширить до полноценной главы (детали реализации + `src/api/middleware/guardrails.py` + troubleshooting): единственный файл на всю подсистему.
4. 23.1/23.6 (GLM): сфабрикованные бенчмарки («91/100», «34/34 tests») свернуть в «проектные оценки, не измерялись» — банер есть, но тела глав в настоящем времени.
5. 34.1 Knowledge Base — добавить глубину (примеры, схема DocumentRegistry).
6. КОМАНДЫ_CLAUDE_CODE.md: добавить `/opsx:approve`; 44.8: `cc_docs`-коллекцию в реестр 31.1/31.2.
7. 05.5: заменить авто-заглушки «Research V2»/«Plan Execute» на содержательные секции.
8. 24.3: имена скиллов `opsx-*` → реальные каталоги `openspec-*`; 24.4 «146 LoC» → 96.

---

## Порядок исполнения (рекомендация)

1. **Волна P0** (8 пунктов) — один проход, каждый пункт независим; P0.1 (ключ) — немедленно.
2. **Волна P1-скрипты**: сначала P3.1 (генератор счётчиков), потом механические счётчики P1 им же — иначе к моменту правки цифры снова уйдут.
3. **Волна P1-контент** (память 5.x, LLM Rotation, D:\-пути) — по подглавам, с re-verify против кода.
4. **P2** — при следующем проходе по соответствующим главам (не отдельной кампанией).
5. После каждой волны — прогон `scripts/audit_docs_skills.py --fix` + link-чекер сессии 92e39496.

> ⚠ Память проекта: roadmap-оценки скоупа исторически завышены 1.5–3× — этот roadmap
> опирается на инвентарный аудит (315/315 файлов прочитано), но объёмы волн P1/P2
> при исполнении сверять с фактом.

---

## §18 Прогресс

> Append-only, новые записи сверху.

### 2026-07-05 — P3 исполнен, commit `cdf25cbd8` — роадмап ЗАКРЫТ (P0–P3 DONE)
- **P3 DONE**: 5 агентов, 26 файлов (+795/−197) + верификационные хвосты (регекс-precision, 4 теста scan_drift → 11 unit).
- Новый инструмент против дрейфа счётчиков: [`scripts/docs_counters.py`](../../scripts/docs_counters.py) (`--json`/`--check`/`--strict`); wiring в CI — кандидат на отдельное решение.
- Главы 33.1/34.1 переписаны по факту кода (найдены и задокументированы расхождения дизайн↔код в guardrails middleware).
- Коррекция аудита: `/opsx:approve` — skill, не команда (P3.6a не выполнялся сознательно).
- Адверсариальный ревьюер: PASS (сигнатуры/LoC/константы/штампы сверены с кодом дословно).
- **Весь роадмап 260704 закрыт: P0 ✅ P1 ✅ P2 ✅ P3 ✅.**

### 2026-07-04 — P2 исполнен (code-first), commit `2191440a5`
- Волна **P2 DONE**: 6 параллельных агентов (код + P2.1–P2.6), 43 файла, net −911 строк.
- Code-first: `claude-opus-4-6` → `claude-opus-4-8` во всём коде + цены $5/$25, тесты 20/20 (решение пользователя по развилке P2.6).
- P2.1 (04.1/2.7/05.1 заголовки), P2.2 (04.1 таблица 14 стратегий), P2.3 (43.5 §0 696→484), P2.4 (дубль-пары→канон+указатели; 09.9↔42.x признан НЕ дублем), P2.5 (27.13/40.4/04.9 + 2 backlog-файла в docs/roadmap/), P2.6 (SELF_RAG env, model-sync, 13.6/32.8/44.2/14.x).
- Остаётся: **P3** (улучшения, опционально) — pending.

### 2026-07-04 — Аудит выполнен, дорожная карта создана
- 6 read-агентов прочитали все 315 файлов 9 слоёв; сводные вердикты: A=24 глав/подглав-групп, B=…, C=13 (см. карту).
- Сформированы волны P0 (8 критических) / P1 (устаревшее+счётчики) / P2 (структура) / P3 (улучшения).
- Предшествующие волны фиксов той же сессии: `119a70012`, `8e7e9f93d`, `e46038985`, `16af6203c`, `de688e295`.
- Статус: P0–P3 **pending** (не начаты).
