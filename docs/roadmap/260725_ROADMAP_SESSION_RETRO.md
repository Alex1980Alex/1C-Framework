# 260725 - Ретро сессии 2026-07-25: ошибки, слабые места, карта исправлений

## §1 Контекст

Сессия = аудит SessionStart-сигналов -> 6 одобренных этапов (push 131 коммита сабмодулей + форк `external/1c_mcp`; снятие gitlink `.claude/worktrees`; крос-чек OTel читает ротированные архивы с обеих сторон; отказ от мьюта edt-mcp мутаций по ADR-055; пороги tool-health - блоки гардов и shell-провалы; event-driven memory-синки) + LFS-миграция 260304 без force-push + снятие опровергнутой known-issue записи `update_database`. Коммиты сессии: `66b10f22e`, `66f483838`, `1c6597dae`, `5dc72cf99`, `c8f64a2bd`, `f2fa66fb1`, `6216924c1`, `603976d84`. Артефакт сессии: `pipeline/audit-session-start-signals/pipeline.md`. Ретро - по мандату пользователя; факты перепроверены прогонами, не по памяти.

## §2 Реестр ошибок сессии

| № | Что случилось | Класс | Как поймано | Статус |
|---|---|---|---|---|
| E1 | Pre-flight Stop-гейта гонялся на `pipeline-protocol-stop.py`, который при `GATE_ORCHESTRATOR_ENABLE=1` early-return'ит -> ложный exit 0 -> реальный блок на Stop | процесс | блоком хука | нота в `pipeline.md` + `capture_pattern` в ретро |
| E2 | Glob `hook-invocations.jsonl*` не матчит архивы `.N.jsonl` -> ложное «лог содержит один день» | методика | самокоррекция следующим шагом | исправлено в том же разборе |
| E3 | Заявлено «ни одной записи с реальным error_type» - фактически 3 из 14 673 | неточность | собственный verify-прогон | исправлено в артефакте |
| E4 | Допущение «cherry-pick прогонит LFS clean-фильтр» - неверно, переносит готовые объекты, блоб остался сырым 131.5МБ | незнание инструмента | верификация ДО push (`git lfs ls-files` пуст) | коммит пересобран через рабочее дерево (`cherry-pick -n -> reset -> add -A -> commit -C`) |
| E5 | Клон 260304 в длинный scratchpad-путь -> MAX_PATH, 41 файл не выгружен (предвидимо: кириллические пути репо известны) | предсказуемая | ошибками checkout | переезд в `%TEMP%\lm` + `core.longpaths=true` |
| E6 | Ad-hoc вызов `_apply_known_issues(health)` вместо `(health["tools"])` -> «known-issue: 0», чуть не отрапортован ложный слом подавления qa_run/qa_generate | сигнатуру не читал | перепроверка перед выводом | перепроверено, вывод верный |
| E7 | Фильтр native-событий по `tool_name == "mcp__edt-mcp__update_database"` -> 0 событий (native обобщает MCP до «mcp_tool»; матчить надо по `tool_use_id` - факт уже был известен из собственного крос-чека) | потеряна итерация | самопроверка | исправлено |
| E8 | 4 push'а в master без проверки CI - в день, когда master только что вылечили; mypy локально не гонялся | процесс | проверено в ретро: все прогоны зелёные (Python CI + CodeQL) - повезло, а не процесс | правило в карту |
| E9 | `CLAUDE.md` строка ~79 несёт опровергнутый инвариант «enforcer exit 2 преемптит canonical-логгер, Write unpaired=0 при живом блоке» - docstring `tool_effectiveness` исправлен, `CLAUDE.md` нет | доки vs код | ручной разбор в ретро | P0 |
| E10 | После смены логики вердиктов отчёты на диске не перегенерированы - завтрашний баннер сюрфейсил бы опровергнутое (Write degraded, 3 stale sinks, `update_database` known-issue) | процесс | ручной разбор в ретро | закрыто в ретро - `_latest.md` = broken 4 / degraded 9 / known-issue 2, alerts 13; `_observability_latest.md` = 0 stale / 3 quiet |
| E11 | Делегированный `llm_complete` (провайдер claude-cli) исполнил промпт «сгенерируй файл для docs/roadmap» АГЕНТНО: клиент отвалился по таймауту 60с, а субпроцесс дожил и сам записал этот файл в репо (mtime 13:03:23) - side-effect вместо текста | контракт делегата | обнаружено Write-конфликтом при попытке записать свой вариант | файл принят (контент = мой спек), риск -> W11/P1 |
| E12 | Авто-коммит `4226dd71c` унёс `docs/roadmap/*.md` (исключение покрывает auto-save пути, но не sync-commit) и назвал в сабже сабмодули, которых в диффе нет (subject строится из списка файлов сессии, не из диффа) | инфраструктура хуков | ручной разбор в ретро | поглощён amend'ом; -> P2 |

## §3 Неточности отчётности (гигиена, не баги)

- N1: числа живого лога дрейфуют между прогонами (native 5333 -> 5341 -> 5350 в одном повествовании) - снимок не пинился.
- N2: 36/49 (замер: native-absent + блок ±5с) vs 44/46 (продакшн: consumable 1:1 по session+времени) - оба верны, но соседствуют в docstring/коммитах без примирения методик.
- N3: «алертов 17 -> 16» (этап 5, considered без known-issue overlay) несравнимо с финальным «4/10/3 -> 4/9/2» (с overlay) - before/after мерились разными пайплайнами.
- N4: промежуточная таблица «явные ошибки в Post» (Bash 4870 и т.п.) показана до валидации предиката - оказалась мусором (empty-string trap, открытый позже в той же сессии).

## §4 Слабые стороны / остаточные риски

- W1: `CALLER_AUTHORED_FAILURES` гасит error-rate degraded у Bash/PowerShell целиком - слепая полоса 10-50% error-rate (реальный слом shell-инфры в ней невидим до broken-порога 50%).
- W2: `agent_rollup` не вычитает блоки гардов (искажение <1% на (main), вердиктов per-agent нет).
- W3: ~0.5% битых строк hook-лога (377 из 75 649, конкурентные append ~106 хуков) + гонка ротации (архив `.3` из 2 строк при соседях по 13k).
- W4: phase-machine session-state глобальна между сессиями -> 2 ложных «TASK PROTOCOL» блока за сессию (`activated_skills` целы, phase сброшена извне).
- W5: мёртвый двойник `data/session-skills.json` (03.07, старая схема) рядом с живым `.claude/data/session-skills.json`.
- W6: асимметрия окон крос-чека: native cap = 5 архивов x 20МБ (`otel-collector.yaml` `max_backups: 5`) vs hook 12 архивов -> `native_only`/`hook_only` смещены по построению; окна в отчёте не показаны.
- W7: LFS-квота: 7 untracked `.webm` ~555МБ в 260304; правило `*.webm` -> LFS подхватит их при первом `add` и съест free-квоту 1ГБ; `.gitignore` на `*.webm` нет.
- W8: 17 унаследованных битых ссылок в 27.13 (root-относительные пути без `../../../../`; мои 136 ссылок целы).
- W9: незакрытые диагностики:
  - (а) codepilot1c form-семейство - `apply_form_recipe` 40%, `inspect_form_layout` 50%, `mutate_form_model` 75% success, сервер 65% - худший;
  - (б) edt-mcp мутации `delete_project`/`create_project`/`modify_metadata` - корень не установлен (два кластера: 60с-потолок `TelemetrySafeError` + мгновенный `McpToolCallError`);
  - (в) `update_database` - следующий шаг журнал EDT (`tail_edt_logs` / Eclipse `.log`), гипотеза модального диалога реструктуризации (`stateAfter!=UPDATED -> fullUpdate=true`);
  - 2 pending tool-health-banner задачи ждут.
- W10: паттерны сессии не капчурились по ходу (task-protocol шаг 6) - выполнено постфактум в ретро (2 захвата: cherry-pick/LFS-trap, pre-flight оркестратора).
- W11: провайдер claude-cli в llm-rotation НЕ text-only - спавнит агент с Write/Bash-доступом к репо; side-effect переживает client-timeout (E11). Делегирование «сгенерируй файл X» может молча мутировать репо.

## §5 Дорожная карта

### P0 (сразу)

- [x] CI на 4 push'ах - зелёные (проверено в ретро)
- [x] перегенерация `_latest.md` + `_observability_latest.md` (сделано в ретро)
- [x] `CLAUDE.md:79` - опровергнутый инвариант снят (`451b8e4f9`): прежняя формулировка оставлена цитатой с явной пометкой «ОПРОВЕРГНУТО 2026-07-25» + факт (блоки ПОПАДАЮТ в непарные Pre, 36/49, вычет `guard_blocks_by_tool`). Цитата сохранена намеренно - CLAUDE.md грузится каждую сессию, узнаваемая формулировка с отрицанием надёжнее молчаливого удаления
- [x] 7 untracked `.webm` - `*.webm` в `.gitignore` сабмодуля 260304 (`23581dac`, гитлинк `451b8e4f9`); LFS-правило сохранено для осознанного `add -f`. Проверено: `add -A --dry-run` = 108 файлов и 0 из 8 `.webm`, отслеживаемая запись не затронута, `check-attr filter` на неотслеживаемой по-прежнему `lfs`

### P1 (неделя)

- [ ] W9а form-семейство codepilot1c (через `edt_validate_request` + логи сервера)
- [ ] W9б edt-mcp мутации через журнал EDT
- [ ] W9в `update_database` - проверить гипотезу `fullUpdate=true`, при подтверждении корня - либо методика, либо честный known-issue
- [x] W1 - раздел «Остаточный риск подавлений: слепые полосы» в 50.3: таблица 4 классификаторов с ценой каждого; главная полоса 10-50% error-rate у shell. Мягкий порог `success<70%` зафиксирован как способ лечения ПРИ удорожании полосы, а не введён сейчас (нет наблюдений)
- [x] W2 - вычет блоков в `agent_rollup`. ⚠ Оценка «<1%» в §4 неверна: по calls ~1пп (95.87%→96.94%), но по **error-count 76 из 283 = 27%**. Также опровергнут план «ключевать по `agent_id`» - block-записи его НЕ несут (198 блоков, поле пустое). Первая реализация списывала блок дважды (на `(main)` и субагента) - поймано тестом, переделано через общий `blocked_ids`; сверка per-agent = per-tool = 76
- [x] W8 - **47** ссылок (не 17: дефект был не только в 27.13, а во всей главе 5.1 - 27.5 -12, 27.4 -10, 27.8 -3, 27.3 -2, 27.2/27.6/27.7 -1). Итог: 448 ссылок, 0 битых. Фиксер трогал только битые-и-резолвимые-от-корня, глубину префикса считал от расположения файла
- [x] N2 - примирение цифр в docstring `completion_stats`: 36/49 (разведочный замер: native-absence + block ±5с) vs 44/46 (фактический вычет, расходуемое 1:1) - разные знаменатели и критерии; сошлись независимо на остатке 2
- [x] W11 - делегат стал text-only: `disallowed_tools` в `ClaudeAgentOptions` (Write/Edit/NotebookEdit/Bash/PowerShell/Task/Agent/AskUserQuestion) + раздел в skill `z-ai-delegation`. Уязвимость подтверждена живым probe (файл реально создавался), фикс проверен свежим процессом (`NO_TOOLS`, файла нет). ⚠ Держится на denylist: `allowed_tools=[]` в SDK 0.2.82 = «allowlist не задан»

### P2 (по случаю)

- [ ] W3 - расследовать torn-lines (лок/буферизация append) + метрика доли битых строк в отчёте
- [x] W4 - ⚠ **диагноз в §4 был неверен** («глобальна между сессиями», «нужна per-session изоляция»). Настоящий корень: `record_decomposition` ставила `decomposed` БЕЗУСЛОВНО, откатывая уже достигнутый `skill_checked` (фаза монотонна) → порядок «скилл → TaskCreate → Write» ронял запись в ложный блок; за сессию сработало **5 раз**. Обсервер к тому же на PreToolUse, а не PostToolUse (skill `task-protocol` описывает устаревше) - значит и «ненадёжный PostToolUse» мимо. Фикс: не понижать фазу
- [x] W5 - удалён мёртвый `data/session-skills.json` (untracked + gitignored ⇒ чистка локальная); живой - `.claude/data/session-skills.json`
- [x] W6 - окна обеих сторон печатаются в секции крос-чека; живьём native с 18.07, hook с 16.07 (разница 1.5 суток объясняет перекос `native_only`/`hook_only`)
- [ ] снести `backup/pre-lfs-260725` в 260304 после недели стабильности (держит старый 138МБ блоб на диске)
- [x] E12 - roadmap-исключение добавлено в третий хук трио (`auto-git-save-prompt.py`, UserPromptSubmit, префикс `chore: auto-commit`): в двух других оно было с самого начала, этот пропустили. Регресс пинит инвариант для всех трёх + обратную сторону. **Subject из фактического диффа НЕ делал** - это переделка общего `format_commit_message` для трёх хуков, отдельная задача с большим blast radius, чем польза; после roadmap-исключения основной вред снят

### P3 (методика, в 50.3)

- [x] Все четыре внесены в 50.3 разделом «Гигиена самого разбора (уроки ретро 260725)»: before/после одним пайплайном (N3); снимок пинится до повествования (N1); промежуточные таблицы - после валидации предиката (N4); после push смотреть CI (E8). Плюс пятое общее: вердикт метрики - гипотеза до сверки с независимым источником по `tool_use_id`

## §6 Acceptance

`CLAUDE.md` без опровергнутых инвариантов; баннер следующей сессии = 4/9/2 без ложных вердиктов; каждая W9-диагностика имеет исход (корень установлен / known-issue с корнем / self-clean на свежем окне); 27.13 - 0 битых ссылок; полный unit-suite зелёный.

## §18 Progress Log

### 2026-07-25 - создан по ретро сессии

В ходе ретро закрыты: проверка CI (4 push'а зелёные), регенерация обоих отчётов (tool-health 4/9/2 alerts 13; memory 0 stale / 3 quiet), 2 `capture_pattern`. Само ретро добавило два экспоната: E11 (таймаутнувший делегат claude-cli сам записал этот файл в репо - side-effect пережил клиентский таймаут) и E12 (auto-commit унёс roadmap-файл и назвал в сабже сабмодули, которых нет в диффе).
