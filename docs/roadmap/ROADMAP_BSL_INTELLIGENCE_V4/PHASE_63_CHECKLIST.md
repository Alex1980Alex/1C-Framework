# Чеклист готовности: Фаза 63 — Contextual BSL Search

**Приоритет:** MEDIUM | **Срок:** 2-3 дня | **Зависимости:** Фазы 59, 60, 62

## Предусловия
- [ ] Фаза 59 завершена: AST парсер извлекает метаданные символов
- [ ] Фаза 60 завершена: code-optimized эмбеддинги в Qdrant
- [ ] Фаза 62 завершена: knowledge graph с объектами 1С
- [ ] Определена схема метаданных: `subsystem`, `object_type`, `dependencies`
- [ ] Eval dataset (Фаза 58) доступен для замера прироста recall

## Артефакты (файлы/код)
- [ ] `src/bsl/indexing/context_enricher.py` — обогащение чанков контекстом (подсистема, тип, зависимости)
- [ ] `src/bsl/search/contextual_query.py` — построение запросов с фильтрами по контексту
- [ ] Обновлённый pipeline индексации с шагом context enrichment
- [ ] Qdrant payload обновлён: `subsystem`, `object_type`, `depends_on`

## Метрики приёмки
- [ ] Recall@10 >= baseline + 15% (замер на eval dataset)
- [ ] Precision не упала более чем на 2%
- [ ] Latency поиска с фильтрами <= 150% от базового поиска
- [ ] Coverage: 100% чанков содержат поля контекста

## Интеграционные проверки
- [ ] Изоляция: поиск по подсистеме не возвращает результаты из других подсистем
- [ ] Зависимости: поиск метода показывает его использование в корректных модулях
- [ ] Обратная совместимость: API работает без контекстных фильтров (старый режим)
- [ ] Переиндексация с контекстом завершается без утечек памяти

## Блокеры для следующих фаз
- [ ] Без контекста блокируется Фаза 64 (MCP: contextual_search tool)
- [ ] Некорректные зависимости блокируют Фазу 66 (Coding Assistant: контекст модуля)
