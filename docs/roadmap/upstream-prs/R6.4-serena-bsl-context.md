# R6.4: Upstream PR — Serena BSL Context

**Target:** https://github.com/oraios/serena
**Type:** yml-only, opt-in via `--context bsl`
**Impact:** No changes to core logic, no default behavior change

## Discovery Issue

**Title:** Proposal: add BSL (1C Enterprise) context

**Body:**

### Motivation

1C Enterprise uses BSL (Built-in Scripting Language) — a Cyrillic-native language used by millions of developers in Russia/CIS. The Serena codebase already includes experimental BSL Language Server support (`bsl_language_server.py`, 551 lines), but there's no dedicated context configuration for BSL developers.

### Proposal

Add `src/serena/resources/config/contexts/bsl.yml` with:
- BSL-specific prompt guidance (Cyrillic identifiers, EDT project structure, export visibility)
- Tool allowlist optimized for BSL (symbol navigation, pattern search)
- Shell command excluded (BSL has no REPL equivalent)

### Why opt-in

BSL is niche compared to Python/TS/Go/Rust. Making it opt-in (`--context bsl`) ensures zero impact on default Serena behavior while providing value to BSL users.

### Artifact

The proposed yml file is attached. Only 1 new file needed — no changes to any existing code.

## Files

- `src/serena/resources/config/contexts/bsl.yml` (new, ~20 lines)
- Optional: `docs/contexts/bsl.md` (documentation page)

## Steps

1. Open discovery issue on `oraios/serena`
2. Fork → branch `feat/bsl-context`
3. PR with yml file only
4. If core-team asks for tests: add `test_bsl_context_loads` in context test suite

## Fallback

If declined, the yml is stored at `tools/serena-fork/contexts/bsl.yml` for internal use (maintenance cost ≈ 0).
