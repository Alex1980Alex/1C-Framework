# Design: GKSTCPLK-2507

> Companion to [proposal.md](proposal.md) и [tasks.md](tasks.md). Source ANALYSIS-REPORT см. в proposal.md.

## Problem

Бизнес-проблема: оператор АРМ Приёмки По Качеству вручную переключает запреты вскрытия (Фт/Фм/Прет/Вет) для каждого вагона/автомобиля при оформлении регистрации на ПЛК. Для рискованных номенклатур (где запреты обязательны) это — типовая операция, которая (a) занимает время оператора, (b) подвержена человеческой ошибке (забыл переключить → ТС пропустят без блокировки), (c) повторяется единообразно для одних и тех же номенклатур.

Техническая проблема: текущая функция `ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию` ([ManagerModule.bsl:615-679](../../../ИБTransportManagementDevelop/Конфигурация/src/Documents/гкс_РегистрацияНаПЛК/ManagerModule.bsl#L615)) формирует начальные движения по `гкс_СтатусыДопускаВагоновКВскрытию` с жёстко зашитой логикой: `ВскрытиеЗапрещено = Истина` только для Претензионного. Расширить через хук-процедуру невозможно — нет точки расширения; нужно либо изменить SQL запроса проведения, либо добавить хук-процедуру в общий модуль проведения. Выбран SQL-overlay (см. Solution).

## Solution

Решение строится по паттерну **«регистр-настройка → overlay в SQL движений»**, уже применённому в конфигурации для «зелёного коридора» (`гкс_НастройкиОтключенияВходногоКонтроляКачества`) и усиленного контроля (`гкс_НастройкиУсиленногоКонтроляКачества`). Три компонента:

1. **Регистр-настройка** `гкс_НастройкиЗапретаВскрытияПоНоменклатуре` — декларативные правила автоустановки запретов. Управляются бизнес-аналитиком через АРМ.
2. **SQL-overlay** в `Документы.гкс_РегистрацияНаПЛК.МодульМенеджера.ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию` — `ЛЕВОЕ СОЕДИНЕНИЕ` с регистром-настройкой во время формирования движений по `гкс_СтатусыДопускаВагоновКВскрытию`. Дефолтная ветка (Прет=Истина, остальные=Ложь) сохраняется при отсутствии правила; при наличии правила — `ВскрытиеЗапрещено=Истина` всегда.
3. **UI** — третья страница в `Группа Настройки` формы АРМ Приёмки По Качеству; CRUD над регистром-настройкой по образцу двух других вкладок.

Транзакционная семантика не меняется — движения формируются в тот же момент проведения, в том же регистре. Перепроведение применяет актуальные правила (overlay по текущему состоянию РС).

## Impact

- **Затронутые модули** (изменяются):
  - [`Документы/гкс_РегистрацияНаПЛК/ManagerModule.bsl`](../../../ИБTransportManagementDevelop/Конфигурация/src/Documents/гкс_РегистрацияНаПЛК/ManagerModule.bsl) — стр. 51 (условие вызова), стр. 615-679 (тело SQL).
  - [`DataProcessors/гкс_АРМПриемкаПоКачеству/Forms/Форма/Form.form`](../../../ИБTransportManagementDevelop/Конфигурация/src/DataProcessors/гкс_АРМПриемкаПоКачеству/Forms/Форма/Form.form) — третья страница ГруппаНастройки.
  - [`DataProcessors/гкс_АРМПриемкаПоКачеству/Forms/Форма/Module.bsl`](../../../ИБTransportManagementDevelop/Конфигурация/src/DataProcessors/гкс_АРМПриемкаПоКачеству/Forms/Форма/Module.bsl) — handler-процедуры.
- **Новые метаданные**: РегистрСведений + ФункциональнаяОпция + Константа + Роль (4 объекта).
- **DB migration**: создание физической таблицы нового РС (через `update_database`).
- **Operational**: единая точка управления правилами автоустановки запретов в АРМ Приёмки По Качеству.
- **Compatibility**: при пустом РС поведение проведения полностью совпадает с текущим (regression-safe).
- **Affected users**: операторы АРМ Приёмки По Качеству (новая вкладка); операторы АРМ Приёмки И Отгрузки на ПЛК (видят авто-заполненные запреты после проведения).

## Архитектура решения

Решение строится по паттерну **«регистр-настройка → overlay в SQL движений»**, уже применённому в конфигурации для «зелёного коридора» (`гкс_НастройкиОтключенияВходногоКонтроляКачества`) и усиленного контроля (`гкс_НастройкиУсиленногоКонтроляКачества`).

Три компонента:

1. **Регистр-настройка** `гкс_НастройкиЗапретаВскрытияПоНоменклатуре` — декларативные правила автоустановки запретов. Управляется бизнес-аналитиком через АРМ.
2. **SQL-overlay** в `Документы.гкс_РегистрацияНаПЛК.МодульМенеджера.ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию` — `ЛЕВОЕ СОЕДИНЕНИЕ` с регистром-настройкой во время формирования движений по `гкс_СтатусыДопускаВагоновКВскрытию`. Дефолтная ветка (Прет=Истина, остальные=Ложь) сохраняется при отсутствии правила; при наличии правила — `ВскрытиеЗапрещено=Истина` всегда.
3. **UI** — третья страница в `Группа Настройки` формы АРМ Приёмки По Качеству; CRUD над регистром-настройкой по образцу двух других вкладок.

Транзакционная семантика не меняется — движения формируются в тот же момент проведения, в том же регистре. Перепроведение применяет актуальные правила (overlay по текущему состоянию РС).
## Структура данных

### Новый РС `гкс_НастройкиЗапретаВскрытияПоНоменклатуре`

```
Измерения (mainFilter=true):
    Номенклатура       CatalogRef.Номенклатура              denyIncompleteValues
    ДопускКВскрытию    EnumRef.гкс_ДопускиКВскрытиюВагонов  denyIncompleteValues
    ДатаНачала         Date (Date-only)
Ресурс:
    Активен            Boolean
Реквизиты:
    ДатаОкончания      Date (Date-only)
    Комментарий        String(150)
    ДатаВремяИзменения Date
    Пользователь       CatalogRef.Пользователи
Формы: ФормаЗаписи, ФормаСписка
RLS: РазрешитьЧтениеИзменение ГДЕ ЗначениеРазрешено(Номенклатура)
```

### Связь с `гкс_СтатусыДопускаВагоновКВскрытию` (целевой регистр движений)

Существующая структура регистра-приёмника НЕ меняется. Изменение касается **только текста запроса**, который формирует движения при проведении документа. Ключ JOIN: `(Настройки.Номенклатура, Настройки.ДопускКВскрытию) = (НачальныеДопуски.Номенклатура, НачальныеДопуски.ДопускКВскрытию)` + `Настройки.Активен`.

### Новая ФО + Константа

`Constants.гкс_ИспользоватьАвтоустановкуЗапретаВскрытия (Boolean)` ↔ `FunctionalOption.гкс_ИспользоватьАвтоустановкуЗапретаВскрытия` — один-к-одному, по образцу `гкс_ИспользоватьОтключениеВходногоКонтроляКачества`. Используется для (a) видимости вкладки в АРМ, (b) опциональной guard'ы overlay в SQL.
## Поток управления при проведении

```
ОбработкаПроведения(Документ.гкс_РегистрацияНаПЛК)
  └── гкс_ПроведениеДокументов.ОбработкаПроведенияДокумента(...)
      └── ПолучениеДанныхДляПроведения вызывает МодульМенеджера.ПолучениеДанныхДляПроведения
          └── ТекстыЗапроса.Добавить(ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию(...))   ← MODIFIED
              ╞═ если ЭтоПриемкаЖДТранспорта(Документ) Или ЭтоПриемкаАвтоТранспорта(...)        ← MODIFIED (2bis)
              │   └── текст 2-го пакета содержит ЛЕВОЕ СОЕДИНЕНИЕ с новым РС                   ← MODIFIED (2)
              ╞═ запрос выполняется → ТаблицаДвижений
              └── движения по гкс_СтатусыДопускаВагоновКВскрытию записываются с overlay
```

**Семантика `ВскрытиеЗапрещено` после изменения:**

| Настройки.Номенклатура | ДопускКВскрытию = Претензионный | ВскрытиеЗапрещено | Комментарий |
|---|---|---|---|
| NULL (нет правила) | Истина | **Истина** | «Автоматически при создании регистрации на ПЛК» |
| NULL | Ложь | **Ложь** | «Автоматически при создании регистрации на ПЛК» |
| not NULL (есть правило) | Истина | **Истина** | «Автоматически по настройкам запрета вскрытия по номенклатуре» |
| not NULL | Ложь | **Истина** | «Автоматически по настройкам запрета вскрытия по номенклатуре» |

Существующее поведение полностью сохраняется при отсутствии правил (Row 1+2 = дефолт). Новая логика добавляет ветви Row 3+4.
## UI-интеграция в АРМ

`Обработка.гкс_АРМПриемкаПоКачеству.Форма.Форма` уже имеет pages-контейнер `ГруппаНастройки` с двумя страницами (Усиленный контроль + Отключение входного контроля) и общим handler'ом `ГруппаНастройкиПриСменеСтраницы`. Новая страница `ГруппаЗапретаВскрытияПоНоменклатуре` добавляется аддитивно:

```
ГруппаНастройки (Pages)
  ├── ГруппаУсиленныйКонтроль                  ← существует
  ├── ГруппаОтключенияВходногоКонтроля         ← существует (зелёный коридор)
  └── ГруппаЗапретаВскрытияПоНоменклатуре      ← NEW
       └── НастройкиЗапретаВскрытияПоНоменклатуре  (DynamicList реквизит)
            ├── Table с FormField'ами реквизитов и измерений нового РС
            └── <commandBar><СоздатьЗапретВскрытияПоНоменклатуре>
```

Обработчики Module.bsl (по образцу зелёного коридора) — см. секцию 6 в [tasks.md](tasks.md). Ключевая особенность: видимость новой страницы управляется ФО `гкс_ИспользоватьАвтоустановкуЗапретаВскрытия` через `Элементы.ГруппаЗапретаВскрытияПоНоменклатуре.Видимость = ПолучитьФункциональнуюОпцию(...);` в `УстановитьВидимостьДоступность`.

Право редактирования (CRUD команды) ограничивается ролью `гкс_НастройкаЗапретаВскрытияПоНоменклатуре` через `Пользователи.РолиДоступны(...)`. Пользователи без роли видят список read-only.
## Открытые архитектурные вопросы

### 1. Историчность настроек

JOIN не содержит условия по периоду (`Настройки.ДатаНачала ≤ Дата документа ≤ ДатаОкончания`). Поведение по умолчанию — **retroactive**: при перепроведении старой регистрации применяются **текущие** правила, а не правила на момент исходного проведения.

**Альтернатива (если требуется срез на дату документа):** расширить JOIN условием:

```sql
И Настройки.ДатаНачала <= Регистрация.Дата
И (Настройки.ДатаОкончания = ДАТАВРЕМЯ(1, 1, 1)
   ИЛИ Настройки.ДатаОкончания >= Регистрация.Дата)
```

`ДАТАВРЕМЯ(1,1,1)` = пустая дата = бессрочно (паттерн зелёного коридора).

**Рекомендация на approve**: подтвердить retroactive (минимальный SQL) или потребовать срез. На обороте — потенциальный риск регрессии при перепроведении старых регистраций после изменения правил, если оператор ожидал «как было».

### 2. Семантика «Вагон» при расширении на Авто

Регистр-приёмник `гкс_СтатусыДопускаВагоновКВскрытию` имеет синоним «Статусы допуска **вагонов** к вскрытию». Расширение на Авто-приёмку (Точка 2bis) формально нарушает терминологию — в регистр пойдут движения по автомобилям.

**Опции:**
- **(a) Принять terminology как универсальную** (вагон = ТС, в коде давно `Регистрация.ТранспортноеСредство КАК Вагон`). Минимум работы. Документировать в `comment`-поле регистра.
- **(b) Переименовать синоним** в «Статусы допуска транспортных средств к вскрытию» (имя `name`/`fullName` не менять — это сломает все JOIN'ы). Косметика для UI/отчётов.
- **(c) Создать отдельный регистр для Авто** (`гкс_СтатусыДопускаАвтоКВскрытию`). Перебор, отбросили в Alternatives Considered proposal.

**Рекомендация**: (b) — `synonym` правится без поломки кода, UX становится корректным.

### 3. Условие на стр. 51 ManagerModule.bsl (Точка 2bis)

Текущее: `Если ЭтоПриемкаЖДТранспорта(Документ) Тогда ...`. Целевое — расширить на Авто. Нужно проверить grep:
- Существует ли `ЭтоПриемкаАвтоТранспорта` (симметричная функция)? Если да — добавить `Или ЭтоПриемкаАвтоТранспорта(Документ)`.
- Если нет — реализация может (a) добавить такую функцию, (b) убрать условие совсем (если функция всегда применима к ТС-приёмке), (c) добавить inline-условие на `Документ.ВидПеревозки`.

**Выбор фиксируется на стадии apply** (после grep'а в репозитории). Дизайн-решение: предпочтительная стратегия — (a) добавить симметричную функцию (наиболее чистая семантика).
