# Tasks for GKSTCPLK-2507 — Автоматическая установка вида допуска в РМ Приёмка/отгрузка по номенклатуре

## 1. Preparation

- [ ] **1.1** Подтвердить актуальность ANALYSIS-REPORT.md (verify ✓ get_metadata, ✓ validate_query)
- [ ] **1.2** Создать ветку фичи в submodule `ИБTransportManagementDevelop/Конфигурация`
- [ ] **1.3** Открыть проект в EDT, убедиться `state=ready` через `mcp__edt-mcp__list_projects`
- [ ] **1.4** Snapshot `гкс_СтатусыДопускаВагоновКВскрытию` для regression baseline (`execute_query`)

## 2. Создание нового регистра сведений (Точка 1)

- [ ] **2.1** Создать `.mdo` `гкс_НастройкиЗапретаВскрытияПоНоменклатуре` (новые UUID, `comment=GKSTCPLK-2507`)
- [ ] **2.2** Измерения: `Номенклатура` (`CatalogRef.Номенклатура`, mainFilter+denyIncompleteValues), `ДопускКВскрытию` (`EnumRef.гкс_ДопускиКВскрытиюВагонов`, mainFilter+denyIncompleteValues), `ДатаНачала` (`Date`, mainFilter)
- [ ] **2.3** Ресурс `Активен` (`Boolean`)
- [ ] **2.4** Реквизиты `ДатаОкончания`, `Комментарий(150)`, `ДатаВремяИзменения`, `Пользователь`
- [ ] **2.5** `ManagerModule.bsl` с RLS: `РазрешитьЧтениеИзменение ГДЕ ЗначениеРазрешено(Номенклатура)`

- [ ] **2.6** `Forms/ФормаЗаписи/Form.form` + `Module.bsl` (`ПередЗаписью` пишет `ДатаВремяИзменения = ТекущаяДата()` и `Пользователь`)
- [ ] **2.7** `Forms/ФормаСписка/Form.form` по образцу зелёного коридора
- [ ] **2.8** Регистрация РС в `Subsystems/гкс_ФункциональныеСодружество/Subsystems/ВходнойКонтрольКачества/ВходнойКонтрольКачества.mdo`
- [ ] **2.9** Проверить регистрацию в `Configuration.mdo`
- [ ] **2.10** `mcp__edt-mcp__get_project_errors(severity=ERRORS)` = 0

## 3. Расширение SQL движений (Точка 2)

- [ ] **3.1** `read_method_source` `ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию` (актуальные строки)
- [ ] **3.2** В `ВТ_НачальныеДопуски` SELECT добавить `Регистрация.Номенклатура КАК Номенклатура`
- [ ] **3.3** В финальный SELECT добавить `ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.гкс_НастройкиЗапретаВскрытияПоНоменклатуре КАК Настройки ПО Настройки.Номенклатура = ВТ_НачальныеДопуски.Номенклатура И Настройки.ДопускКВскрытию = ВТ_НачальныеДопуски.ДопускКВскрытию И Настройки.Активен`
- [ ] **3.4** `ВЫБОР` для `ВскрытиеЗапрещено`: `КОГДА Настройки.Номенклатура ЕСТЬ NULL ТОГДА <старая логика> ИНАЧЕ ИСТИНА`
- [ ] **3.5** `ВЫБОР` для литерала `Комментарий` (NULL → старый, совпадение → «Автоматически по настройкам запрета вскрытия по номенклатуре»)
- [ ] **3.6** `mcp__edt-mcp__validate_query` → `valid: true`
- [ ] **3.7** `write_module_source` с обёрткой `// GKSTCPLK-2507 Начало` / `// GKSTCPLK-2507 Конец`
- [ ] **3.8** `get_project_errors` = 0

## 3. Integration

- [ ] **3.1** Integration testing
- [ ] **3.2** Documentation
- [ ] **3.3** Code review
