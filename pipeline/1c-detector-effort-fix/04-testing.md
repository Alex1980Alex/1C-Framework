# 04 — Тестирование

## Юнит-тесты
`pytest tests/unit/test_pipeline_1c_bridge.py -p no:randomly` (гейт OFF = CI-конфиг) → **67 passed**
(63 исходных + 4 новых регресс-теста Находки 1). Под гейтом ON 3 теста `semantic_source` ожидаемо
требуют `tfidf` (среда-зависимы, не относятся к фиксу — см. ниже).

## Линт / компиляция
- `ruff check scripts/eval_1c_detector.py .claude/hooks/shared/pipeline_1c_bridge.py` → All checks passed.
- `py_compile` обоих файлов → OK. GT-JSON валиден (248 строк, 35 confident с complexity).

## Eval до/после (`scripts/eval_1c_detector.py --split all`)
| Метрика | Baseline (230 строк, гейт ON) | После (248 строк, гейт ON `--setfit`) |
|---|---|---|
| is_1c F1 | 0.974 | 0.916 |
| is_1c Recall | 0.978 | **0.978 (не упал)** |
| is_1c Precision | 0.970 | 0.862 (честная просадка от 18 hard-негативов) |
| complexity-band acc | — (не измерялась) | **0.943** |
| AUTO-мисроуты | невидимы | **2 (документированы)** |

- **is_1c на ИСХОДНЫХ 230 строках = 0.974 без изменений** → код не регрессировал детект; F1-просадка
  целиком от добавленных реалистичных негативов (цель Находки 4 — честный замер precision).
- **Находка 1 закрыта:** все develop-задачи (печатные формы, «разработать механизм», «настроить обмен
  с базой») → `medium/ask_flow`, НЕ `auto`. Ноль auto-мисроутов в develop-классе.
- **2 остаточных auto-мисроута** («разблокировать регистрацию усиленного контроля», «Отображение УК…
  к обмену») — namespace без лексического сигнала работы; помечены medium по доменному суждению.
  Это семантический gap (класс Находки 8 recall-edge), задокументирован, НЕ лечится лексикой без риска FP.

## Остаточные / отложенные (честно)
- Находка 2 (≈80% FP на русскоязычной разработке) — теперь ИЗМЕРЯЕТСЯ (precision 0.862). Ужесточение
  `_1C_SIGNAL` regex / переобучение SetFit на расширенном GT — отдельный roadmap-пункт (риск recall).
- 3 теста `semantic_source` среда-зависимы (assert `tfidf`, проходят при гейте OFF = CI). Не входят в scope.
