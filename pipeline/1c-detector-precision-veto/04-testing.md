# 04 — Тестирование

## Юнит
`pytest tests/unit/test_pipeline_1c_bridge.py -p no:randomly` (gate OFF=CI) → **72 passed**
(67 + 5 новых veto-тестов). Veto-тесты проходят и при gate ON (детерминичны: is_1c через regex). ruff/compile OK.

## Eval до/после (полный GT 248 строк)

| Метрика | До veto (gate ON) | После veto (gate ON `--setfit`) |
|---|---|---|
| is_1c Precision | 0.862 | **0.970** |
| is_1c Recall | 0.978 | **0.978 (не упал)** |
| is_1c F1 | 0.916 | **0.974** |
| FP | 21 | **4** |
| FN | 3 | **3 (не добавлено)** |

- **17 из 18 framework-dev FP подавлены**; recall полностью сохранён (3 FN — те же пред-существующие
  no-verb 1С-задачи, veto их НЕ трогал).
- Holdout (свежие, НЕ в GT): 0/8 остаточных FP на framework-dev; 0 потерь recall от veto на 6 свежих 1С.

## Остаточные FP (4, вне scope Находки 2)
- 3 `near_domain` («проведение планёрки», «отгрузка посылки постаматом», «опрос сотрудников») — не
  тех-контекст, FP от пересечения 1С-домен-слов с бытовым русским. Класс Находки 2 (framework-dev) НЕ
  затрагивает; лечится семантикой/доменным сужением — отдельный срез.
- 1 `framework-dev-neg` («форму обратной связи на сайте») — единственный маркер «сайт» намеренно НЕ в
  denylist (риск ветировать 1С↔web-интеграцию). Уходит в ask_1c (низкий вред).

## code-verify
quality-review + behavior-preservation: PASS (см. транскрипт).
