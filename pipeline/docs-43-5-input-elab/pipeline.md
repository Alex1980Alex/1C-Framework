# Пайплайн: docs-43-5-input-elab

Задача (medium, 1 файл, документация): подробнее расписать в
[`43.5_СКВОЗНАЯ_КАРТА.md`](../../docs/framework%20documentation/43_ПАЙПЛАЙН_1С/43.5_СКВОЗНАЯ_КАРТА.md)
вход «§0 ВХОД» — как из **свободного текста в чате** (НЕ слэш-команда и НЕ путь к папке ТЗ) система
понимает, что это 1С-задача.

## 1. Планирование
Пользователь указал на верхний блок диаграммы (стр. 18): формулировка `(либо /слэш-команда, либо путь к
папке ТЗ)` маскирует **третью, основную и самую сложную** форму входа — свободный текст, который нужно
распознать по содержимому. Прочитал код-первоисточник: `classify_1c_task` / `estimate_effort` /
`route_1c_task` в [`pipeline_1c_bridge.py`](../../.claude/hooks/shared/pipeline_1c_bridge.py) +
хук [`onec-task-input.py`](../../.claude/hooks/onec-task-input.py). Заземлил каждое утверждение на `file:line`.

## 2. Дизайн
Не переписывать §0 целиком (риск рассинхрона ссылок), а **прицельно дополнить**:
- верхний блок диаграммы → 2 строки, форграунд свободного текста (ширина 94, выровнена с рамкой);
- §0.1 → лид-абзац + нота про асимметрию обработки (формы 2–3 коротко замыкают, цепочку проходит только текст);
- §0.3 → интро «сырая строка, чистый regex, почему не LLM» + дерево решения + колонка `file:line`/роль в таблице
  групп + расширенные «тонкости» false-positive; формульный блок НЕ трогать (точен);
- §0.3.1 (новое) → пошаговые трассы A/B/C: строка → предикаты → `is_1c`/`ttype`/`confident`/`flow`.

## 3. Кодирование
4 правки через Edit (диаграмма-бокс, §0.1, §0.3 двумя кусками, §0.3.1). Бокс выровнен по измеренной ширине
(венв-python, UTF-8). Edit §0.3-таблицы упал на первой попытке (пропущена ячейка «Примеры» строки `_1C_SIGNAL`)
→ перечитал, поправил, прошло.

## 4. Тестирование
Перечитал §0 (бокс + §0.1 + §0.3 + §0.3.1 + стык с §0.4) — markdown/таблицы/код-фенсы целы, рамка выровнена.
Сверил все 3 трассы с кодом (глаголы/сигналы/confident/points-банды) и каждую `#L`-ссылку — совпадает.
