# Дизайн

В `43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md` заменить краткую секцию «Инструментирование» на
**полный жизненный цикл (W)**:
- ASCII-диаграмма 4 шагов: TOOL-PLAN → лог (авто) → оценка (авто) → запись в артефакт.
- По шагу: Шаг 1 TOOL-PLAN.md (план, оператор); Шаг 2 hook-invocations.jsonl (авто, correlationid=run_id);
  Шаг 3 tool_usage_report.py (агрег по tool + вердикт ✓/⚠/✗ авто из err%); Шаг 4 TOOL-USAGE-REPORT.md +
  tool-effectiveness.jsonl (+ --rollup cross-task).
- Таблица «что авто / что вручную» (честно: TOOL-PLAN+заметка — ручные; лог+метрики+базовый вердикт — авто).
- Точные механики из кода: фильтр `correlationid==run_id`, пороги вердикта (≥30→✗, >0→⚠, 0→✓).

Глава 43 (3 файла) уже создана; 17.5 сведена к тестированию. Этот срез — только углубление секции W +
оформление docs-работы через пайплайн (Stop-блок ADR-018).

**Статус: approved (оператор).**
