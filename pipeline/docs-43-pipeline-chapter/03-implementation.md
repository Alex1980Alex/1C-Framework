# Кодирование (документация)

В `43.3_МАРШРУТИЗАЦИЯ_ХУКИ_ИНСТРУМЕНТИРОВАНИЕ.md` секция «Инструментирование» переписана в **полный
жизненный цикл (W)**: ASCII-диаграмма 4 шагов + по-шаговое описание (TOOL-PLAN → лог авто → оценка авто →
запись в артефакт) + таблица «что авто / что вручную». Точные механики взяты из кода
`scripts/tool_usage_report.py` (фильтр `correlationid==run_id`; вердикт ✓/⚠/✗ из порогов err% ≥30/>0/0;
append в `tool-effectiveness.jsonl`; `--rollup`).

Контекст: глава 43 (43.1/43.2/43.3) и сведение 17.5 к тестированию — предыдущий срез (коммит `8284472be`).
