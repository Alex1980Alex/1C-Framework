# Тестирование (документация)

| Проверка | Результат |
|---|---|
| Точность описания vs код | Сверено с `scripts/tool_usage_report.py`: 4 шага, фильтр `correlationid==run_id`, вердикт ✓/⚠/✗ из err% (≥30→✗, >0→⚠, 0→✓), append `tool-effectiveness.jsonl`, `--rollup` — всё совпадает |
| Честность | Таблица auto/manual: TOOL-PLAN + `_заметка_` — ручные; лог + метрики + базовый вердикт — авто |
| Ссылки | `scripts/tool_usage_report.py`, `hook-invocations.jsonl` — существуют (проверено прошлым срезом) |

**Вердикт: DONE.** Цикл инструментирования (W) в гл. 43.3 описан максимально и точно сверен с реализацией.
Закрыт Stop-блок ADR-018 (docs-работа оформлена через пайплайн).
