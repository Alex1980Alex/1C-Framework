# Пайплайн (trivial): Глава 47 Scene-Detect MCP в документацию

> Тип: trivial (документация) · ADR-018 компактный пайплайн (4 секции в одном файле).
> Дата: 2026-06-28

## 1. Планирование

Запрос: «добавь по scene-detect-mcp главу в документацию `docs/framework documentation`».
Объём: новая глава о MCP-сервере `scene-detect` (раскадровка видео по сменам сцен). Источники истины:
скилл `scene-detect-mcp`, исходник `tools/scene-detect-mcp/scene_detect_mcp.py`, `README.md`,
`requirements.txt`, регистрация в `.mcp.json`.

## 2. Дизайн

- Новая глава **47** (следующий свободный номер после 46), одна директория `47_SCENE_DETECT_MCP/`
  с одним обзорным файлом `47.1_Обзор.md` — паттерн single-file глав (33/34/45). Сервер компактный,
  дробить = over-engineering.
- Стиль = существующие главы (шапка версия/дата/статус + блок ссылок + футер «Навигация»).
- Регистрация в `00_СОДЕРЖАНИЕ.md`: запись раздела 47 + строка в «Быстрых ссылках».
- Все ссылки относительные, проверяются на резолв.

## 3. Реализация

- Создан `docs/framework documentation/47_SCENE_DETECT_MCP/47.1_Обзор.md` (~10 КБ): Назначение,
  Когда использовать, Инструмент `extract_scene_frames` (параметры + схема возврата), Под капотом
  (PySceneDetect v0.7, version-robust импорт, нормализация вывода), Установка/регистрация,
  параметр `threshold`, Примеры, Антипаттерны, Навигация.
- `00_СОДЕРЖАНИЕ.md`: добавлен раздел 47 (стр. 446–448) + быстрая ссылка (стр. 508).

## 4. Тестирование

- Все cross-ref'ы резолвятся: внутренние доки (46.1, 13.4, 26.1, Содержание) + repo-relative цели
  (`scene_detect_mcp.py`, `requirements.txt`, SKILL.md, `.mcp.json`) — проверено `[ -f ]`, MISS нет.
- Контент сверен с фактическим исходником / README / requirements / `.mcp.json` (стр. 310–316).
- Документация-only (markdown) — code-verify не применим; верификация = резолв ссылок + сверка фактов.
