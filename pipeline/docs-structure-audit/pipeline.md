# docs-structure-audit — компактный пайплайн (trivial, analysis-only)

**Задача:** структурный аудит соответствия `docs/framework documentation` ↔ кодовая база
(какие системы не описаны, где ошибки описания).

**Тип:** анализ без правок продуктового кода. Артефакт — отчёт в чате сессии 92e39496.

## Ход

1. **План:** прогнать `scripts/audit_docs_skills.py`, снять структуру доков и кода,
   детерминированная валидация ссылок/путей (скрипт в scratchpad), fan-out 3 Explore-агентов
   (интерфейсы / память / хуки).
2. **Выполнено:**
   - авто-аудит: фиктивные 0% — скрипт целится в дореформенные пути глав;
   - валидация: 2726 ссылок (28 битых), 409 inline-путей (80 битых), 23 подсистемы без упоминаний,
     18/97 хуков не упомянуты;
   - агенты: REST/CLI/MCP-расхождения; память 27.x (5-arms vs 3-arms противоречие);
     хуки (65/89 зарегистрированных не описаны, ошибки событий/таймаутов).
3. **Ключевая системная находка:** перенумерация глав в 9 слоёв (be7ddd589) сломала 5 кодовых
   потребителей старых путей: `scripts/audit_docs_skills.py`, `scripts/pdf_docs_acceptance.py`,
   `.claude/hooks/docs-change-enforcer.py`, `.claude/hooks/docs-change-tracker.py`,
   `.claude/hooks/slash-command-tracker.py`.
4. **Правок кода нет** → этапы 3-4 не применимы; сгенерированный прогоном
   `docs/analysis/AUDIT_DOCS_SKILLS.md` (с фиктивными 0%) откатан `git checkout`.
