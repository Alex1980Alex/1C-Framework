# 03 — Реализация: что изменено

## Новый файл
- **`.claude/skills/yaxunit-unit-testing/references/api-reference.md`** — полный сверенный с исходником
  справочник (13 разделов): регистрация, матчеры (полный каталог), БД-утверждения, изоляция данных,
  Мокито (полный поток + `&Вместо`-обвязка + can't-mock + DI), data-driven, lifecycle-события,
  дискавери/деплой, безопасный режим, запуск/config.json, `ЮТДымовыеТесты` vs рек.3, CI/раннеры/покрытие,
  связь с рек.3. Все факты — с провенансом (модуль-источник / URL офиц. доков).

## Изменённые файлы

### `.claude/skills/yaxunit-unit-testing/SKILL.md` (переписан, 519→ frontmatter+367 body)
- Восстановлен frontmatter `name`+`description` (был утерян при переписывании → регресс NODESC, исправлен).
- Прогрессивное раскрытие: полный справочник вынесен в `references/`, body под BODY500.
- Исправлен галлюцинированный API: Мокито (`Создать`/`Обучить`/`Параметры`→`Обучение`/`Когда`/`Прогон`/
  `Проверить.КоличествоВызовов`), `ОжидаетВыброс`→`.ВыбрасываетИсключение()`/Попытка, `ДобавитьСерверныйТест`
  2-арг, удалён `ДобавитьСерверныйТестСКонтекстом`, удалены `ЮТУтверждения.ПроверитьТип/ПроверитьКолонку`,
  `.НеПуста`. Фаза 4: «авто-дискавери» → «регистрация в подсистеме `Модульные` ОБЯЗАТЕЛЬНА».
- Добавлено: рек.3-нота, `ЮТест.Пропустить`, безопасный режим, DI>Мокито, лента anti-patterns (over-mocking,
  order-dependence, flaky-random), «❌ не существуют» список галлюцинаций, API-шпаргалка (сверенная).

### `docs/…/17.6_НАПИСАНИЕ_YAXUNIT_ТЕСТОВ.md`
- §3 БД: `.СодержитЗаписьСПолями`→`.СодержитЗаписи(ЮТест.Предикат(...))` (+ СКодом/СНаименованием).
- §4 Мокито: `Мокито.Обучить(...)`→`Мокито.Обучение(...).Когда(...)`+`Мокито.Прогон()`;
  `Проверить(...).БылВызван().ОдинРаз()/.СПараметрами(...)`→`Проверить(...).КоличествоВызовов(...).Равно(N)`.
- §4 регистрация: убраны несуществующие `.СПараметромОкружения(...)`/`.ВыполнитьНоваяПопытка()`.
- §4 фикстуры: `.СоздатьЭлемент("Стр").УстановитьЗначениеПоля(...).Сохранить()`/`.Проверить()` →
  реальные `СоздатьЭлемент(Менеджер, Наим, Структура)` / `СоздатьДокумент(Менеджер, Структура)`.
- §5 дискавери: убран ложный «имя должно начинаться с ЮТТ» (ЮТТ = префикс подсистемы, не модулей).
- +§9: рек.3-нота, безопасный режим ломает и Мокито, `ЮТДымовыеТесты`≠рек.3-смоук, CI/раннеры/покрытие
  (METR/vrunner/Coverage41C/onec-docker) с URL.

### `.claude/commands/write-1c-unit-tests.md`
- Те же API-фиксы: Мокито (`Создать`/`Обучить`/`Параметры`→реальные+`Прогон`), `ОжидаетВыброс`(×3)→
  `.ВыбрасываетИсключение()`, `ДобавитьСерверныйТест` 2-арг, Фаза 4 «авто-дискавери»/`ЮТТесты.ДобавитьМодуль`
  (фантом)→регистрация в подсистеме `Модульные`.

## Ядро рек.3 (код гейта) — НЕ ТРОНУТО (корректно). Изменены только методика/справочник, что агент читает при написании смоука.