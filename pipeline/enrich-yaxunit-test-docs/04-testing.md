# 04 — Верификация

## В1. Отсутствие галлюцинированного API (grep 3 файла)
`Мокито.Создать|Обучить|Параметры|ОжидаетВыброс|ДобавитьСерверныйТестСКонтекстом|СодержитЗаписьСПолями|
ПроверитьКолонку|ВызванМетод|ВыполнитьНоваяПопыт|СПараметромОкружения|.НеПуста|ДобавитьМодуль|БылВызван`
→ остаточные совпадения ТОЛЬКО в «❌ не существует»-нотах (SKILL.md ×4, write-cmd ×2); 17.6 — 0. **PASS.**
+ `СоздатьЭлемент("...")` строкой в доках → 0 (все на форме `Менеджер`). **PASS.**

## В2. Каждый исправленный/обогащённый вызов сверен с исходником `src/bsl/exts/YAXUNIT/`
| Область | Проверенные символы | Источник |
|---|---|---|
| Регистрация | `ДобавитьСерверныйТест(Имя,Предст,Теги)` `ДобавитьТест(...Контексты)` `ДобавитьТестовыйНабор` `СПараметрами` `СПараметрамиНаСервере/НаКлиенте` `ВТранзакции` `УдалениеТестовыхДанных` `Перед/После/Тег` | `ЮТТесты` |
| Утверждения | `Равно НеРавно Больше Заполнено НеЗаполнено ИмеетТип ЭтоИстина ЭтоЛожь ЭтоНеопределено Содержит ИмеетДлину[Больше] Свойство Метод ВыбрасываетИсключение ВСписке НачинаетсяС` | `ЮТУтверждения` |
| БД-утверждения | `СодержитЗаписи СодержитЗаписиСКодом/СНаименованием/СНомером СодержитТолькоОднуЗапись СодержитНесколькоЗаписей` | `ЮТУтвержденияИБ` |
| Мокито | `Обучение Прогон Проверить КоличествоВызовов Вызовы ЛюбойПараметр/Числовой/Строковый/Типизированный МассивПараметров Наблюдать Когда Вернуть ВыброситьИсключение Пропустить ВыполнитьМетод` | `Мокито` `МокитоОбучение` `МокитоПроверки` |
| Точка входа | `ОжидаетЧто ОжидаетЧтоТаблицаБазы Данные Предикат Варианты Контекст Пропустить Упал ОписаниеТекущегоТеста` | `ЮТест` |
| Предикаты | `Реквизит(602) Получить(44)` | `ЮТПредикаты` |
| Фикстуры | `СоздатьЭлемент СоздатьДокумент УстановитьЗначениеРеквизита КонструкторОбъекта КонструкторДвижений Фикция СлучайноеПоложительноеЧисло СлучайнаяДата СлучайноеЗначениеПеречисления СлучайнаяСсылка HTTPСервисЗапрос/Ответ/Соединение ADORecordSet СообщениеСервисаИнтеграции` | `ЮТТестовыеДанные` |
| Data-driven | `Варианты ДобавитьКомбинации СписокВариантов` | `ЮТКонструкторВариантов` |
| Дискавери | `ПодключаемыеМодулиПодсистемы` (регистрация по подсистеме `Модульные`) | `ЮТПодключаемыеМодулиСлужебный` + `Модульные.xml` |
**Все — существуют. Ни одного нового невалидного символа не введено.** PASS.

## В3. lint_skills — yaxunit-unit-testing: findings NONE (clean); BODY 367<500, DESC в бюджете, frontmatter восстановлен. PASS. (остаточные errors/warnings в отчёте — чужие скиллы, предсуществующие.)

## В4. Понятийная корректность (research-сверка)
- Разведён «smoke»: `ЮТДымовыеТесты` (functional runtime по всей конфе) ≠ рек.3-смоук (быстрый unit
  затронутого модуля) ≠ syntax-check (компиляция). Исправлено допущение из 01-architecture.
- Безопасный режим (офиц. run.md) — ломает И config.json И перехваты Мокито (подтверждено Агентом 3).
- Обогащение (METR/vrunner/Coverage41C/onec-docker) — с URL, adopt-статусом; Coverage41C→Sonar релевантен нам.

## Ограничение
Доки не исполняются (нет BSL-компиляции здесь) — верификация = сверка каждого символа с вендоренным
исходником (авторитет для развёрнутой версии) + research. Прогон живого YAxUnit-теста по новой методике —
за отдельной 1С-задачей на живой ИБ (среда пользователя).