# Возврат master в зелёное + разбор degraded + сабмодули

**Дата:** 2026-07-25 · **Тип:** CI-ремонт (многораундовый) · **Мандат:** «переходи к реализации и исправлению» по трём пунктам

## 1. Планирование

Три задачи из стартового баннера: (а) master красный, 50 ci-fail уведомлений; (б) 14 degraded-инструментов; (в) 524 незакоммиченных файла в сабмодулях. Гипотеза по (а) — состояние origin/master месячной давности (868 локальных коммитов не запушены), проверять локальным воспроизведением job'ов.

## 2. Дизайн (approve: мандат пользователя + [[feedback-repo-full-permission]] «чини CI сам, не спрашивай»)

- **CI:** чинить в ИСТОЧНИКЕ, не отключением проверок. Воспроизводить каждый упавший job локально, различая «сломанный код» и «рассинхрон инструмента с эволюцией репо».
- **degraded:** применить методику гл. 50.3 (парность по `tool_call_id` против фона 7%); by-design свойства → классификаторы, внешние причины → known-issue (ADR-055), малые выборки → без действий.
- **Сабмодули:** НЕ коммитить вслепую; сначала проверить пересечение с файлами своих задач.

## 3. Кодирование

**CI — 11 независимых корней.** Локально найдены 6: pre-commit правил 2610 файлов конфигурации KAT (нет в exclude, в отличие от `src/bsl/`); `validate_toc` считал 315 файлов орфанами (TOC ссылается на каталоги глав); `FILE_LEVEL_BANNERS` не знал баннера `⚠️ ARCHIVE`; 4 ошибки ruff; устаревший mypy-baseline; description скилла 1152 симв. при бюджете 1024. Ещё 5 видны только на раннере: `uv run pre-commit` (изолированное окружение без `kb_lint`), путь-литерал `C:/…` на POSIX, чтение gitignored `.mcp.json`, 4 Linux-специфичные mypy-ошибки, 2 research-кеша под `.gitignore: cache/`.

**degraded — 14 разобраны:** `update_database` → known-issue (MCP-таймаут при асинхронном завершении EDT); `AskUserQuestion` → `ASYNC_UNPAIRED_UNRELIABLE` + `CONTENT_VARIABLE_BUILTINS`; `search_in_code` → `CONTENT_VARIABLE_MCP_SUFFIXES` (by-design свойство ≠ поломка); 11 остальных — шум.

**Сабмодули:** 445 файлов, пересечение с моими задачами = 0 → не коммичено; факт зафиксирован в памяти.

## 4. Тестирование

- Локально: ruff `All checks passed`, 465 файлов отформатированы, compileall OK, TOC 0/0, pre-commit **12/12 Passed**, pytest `-m unit`: **2842 passed / 0 failed**, skill-lint 0 ошибок, mypy filter new:0.
- Новые тесты: `tests/unit/test_validate_toc.py` (3) + саботаж-проверка (возврат старой ветки краснит директорный тест); health-probe переведён на подмножество целей (9 тестов).
- **code-verify (read-only субагент): PARTIAL → 3 замечания исправлены** (скоуп баннера `ARCHIVE`, top-level exclude снимал gitleaks с `configuration/`, p95-ветка `AskUserQuestion`).
- CI: 6 коммитов `5c1164999 … 4708ace3e`, master запушен; финальный ран под наблюдением Monitor.

## Артефакты
Память: `project-ci-master-red-root-causes-260725`, `project-config-submodules-foreign-wip`. Доки: гл. 50.3 (таблица разбора degraded + правило «by-design → классификатор»).
