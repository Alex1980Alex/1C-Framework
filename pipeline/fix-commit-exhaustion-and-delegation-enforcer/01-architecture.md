# 01. Планирование

Пайплайн оформлен по факту выполнения (ADR-018): работа началась как диагностика вопроса
«в прошлой сессии возникала ошибка, в чём причина», и переросла в правки после явного «сделай».

## Вход

Вопрос пользователя об ошибке в предыдущей сессии + последующий мандат исправить обе найденные причины.

## Что установлено диагностикой (факты, не гипотезы)

**A. Обрыв сессии 41e93bb6 (2026-07-26 16:51).**
- Транскрипт кончается на `tool_use mcp__llm-rotation__llm_complete` (16:51:01.894), `tool_result` отсутствует.
- `data/hook-invocations.jsonl`: Pre для `llm_complete` в 16:51:02 есть, Post нет.
- Дочерний `claude-cli` (сессия 36dd751f) отработал успешно 16:51:07→16:51:55 и выдал OLD/NEW блоки.
- `.claude/cache/mcp-llm-rotation-calls.jsonl` и `data/llm-rotation-completions.jsonl`: последние записи 16:02, за 16:51 ничего.
- System log `Resource-Exhaustion-Detector` id 2004 в **16:51:11**: нехватка виртуальной памяти;
  крупнейшие - vmmemWSL 38.8 ГБ, sqlservr 19.4 ГБ, javaw 5.2 ГБ.
- Посторонние крэши в том же окне: svchost/CaptureService `0x800705af` (= ERROR_COMMITMENT_LIMIT), SnippingTool `0xc0000409`.
- У родительского `claude` (node) нет ни WER-записи, ни дампа.

Вывод: делегирование и таймауты ни при чём (48 с при потолке 300 с). Родитель умер от исчерпания commit charge,
триггер - спавн ВТОРОГО полного Claude Code провайдером `claude-cli-sonnet`.

**B. Хук `z-ai-delegation-enforcer` падает на каждом подходящем промпте.**
- `execute()` зовёт `self._delegation_level(...)`, метода в классе нет → `AttributeError`.
- 2607 записей в `.claude/hooks/cache/hook-errors.log`, первая 2026-05-23T19:02.
- Методы существовали в `ca61a74b0` (2026-05-16), потеряны мержем около 23.05 - тот же класс, что F821-stubs из PR #10.
- `BaseHook.run()` гасит исключение → отказ бесшумный.

## Цель

1. Снять острый риск повторного обрыва (запас commit был 4.2 ГБ из 129.1).
2. Восстановить хук так, чтобы он снова выполнял свою функцию, и закрыть регресс тестом.

## Ограничения

- `wsl --shutdown` роняет 13 живых контейнеров (Qdrant и TEI питают memory-хуки) - требуется восстановление и проверка.
- Правка MSSQL затрагивает инстанс с боевыми 1С-инфобазами → только обратимое `sp_configure`, без рестарта службы.
- Хук исполняется на КАЖДОМ промпте с таймаутом 3 с → латентность обязана быть измерена, а не предположена.
