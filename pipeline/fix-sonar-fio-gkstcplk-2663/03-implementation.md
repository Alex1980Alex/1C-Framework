# 03 Кодирование

Файл: `ИБTransportManagementDevelop/Конфигурация/src/CommonModules/гкс_ОбщегоНазначения/Module.bsl`
(`git diff --numstat`: +34 / -14, только строки 1001-1108; UTF-8 без BOM, LF сохранены).

Реализовано:
- `ФИОДляПечати`: `КоличествоСлов`, константа `МаксимумСловВПолномФИО`, `СтрШаблон("%1.", СтрСоединить(...))` для инициалов, `СтрШаблон("%1 %2", ...)` для результата.
- `РазобратьТокенНаименованияФИО`: константы `ПозицияВторогоСимвола`/`ПозицияПослеИнициалаСТочкой`, вызовы `ЭтоЗаглавнаяБуква(...)`.
- Добавлена приватная `Функция ЭтоЗаглавнаяБуква(Символ)` (регион `СлужебныеПроцедурыИФункции`, не Экспорт).

Скрипт правки (re-runnable, anchor-ассерты): scratchpad `edit_fio.py`.
Детали и таблица «замечание → фикс» + сообщение коммита — в [IMPLEMENTATION-PROGRESS.md § Round 2].

Не закоммичено: вся работа GKSTCPLK-2663 (Round 1 + Round 2) в рабочем дереве, коммит — за пользователем.
