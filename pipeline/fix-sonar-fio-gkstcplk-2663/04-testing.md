# 04 Тестирование — все PASS

1. **Поведение (live-диф):** развёрнутая старая `ФИОДляПечати` (оракул) vs инлайн новой логики через
   `execute_code` на 18 входах (слитные инициалы «О.В.Мирская», порядок «Инициалы Фамилия», пропущенная
   точка «Толочко Н.В», дефисные фамилии, 3+ инициала «А.Б.В. Иванов», не-ФИО/должность, лишние пробелы,
   пустая строка, не-строка) → **0 расхождений**. `СтрШаблон` проверен отдельно (`%1.`→«Л.Л.», `%1 %2`→«Мышук Т.Н.»).
2. **Статика (bsl-language-server):** 0 диагностик на строках 1001-1108 (в т.ч. 0 MagicNumber), 0 синтаксических ошибок.
3. **code-verify (behavior-preservation):** reviewer-субагент → **PASS** (доказал недостижимость дивергенции пустого массива).
4. **Sonar-дельта (ADR-037):** `run-sonar-analysis.ps1 -Project utp-ib` (CE SUCCESS) + API `utp-ib`:
   в регионе **0 MagicNumber, 0 BLOCKER/CRITICAL**, все 9 замечаний закрыты. Остаётся пред-существующий
   `TooManyReturns` (вне скоупа).

Примечание: репо-широкий completion-verify показывает FAIL из-за пред-существующей грязи в
сабмодулях MFM/SVETLY (не эта задача); per-project **utp-ib = pass=True**.
