# 01 Планирование — параметр КодВалютыПоУмолчанию (GKSTCPLK-2668)

Замечание (EDT/query-checker): «Не задано значение параметра "КодВалютыПоУмолчанию"» на
`ЕСТЬNULL(СправочникВалюта.Наименование, <<?>>&КодВалютыПоУмолчанию)` в
`Функция ТекстЗапросаДанныхДляПечати()`.

Файл: `ИБTransportManagementDevelop/Конфигурация/src/DataProcessors/гкс_ПечатьТТНСнятиеСХранения/ManagerModule.bsl`.

Диагностика: query-текст строится в отдельной функции `ТекстЗапросаДанныхДляПечати()`, а параметр
устанавливается в вызывающем методе (`ВывестиВТабличныйДокумент`, было L184: `УстановитьПараметр("КодВалютыПоУмолчанию", "BYN")`).
EDT анализирует запрос метод-локально и не видит кросс-методный set → флаг. Параметр использовался
ТОЛЬКО как значение-по-умолчанию в `ЕСТЬNULL` (3 места) и всегда = константа "BYN".

Скоуп: 1 файл, ТТН-only (сиблинг ТН валютной логики не содержит). Классификация: trivial.
