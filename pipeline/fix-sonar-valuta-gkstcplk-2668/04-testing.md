# 04 Тестирование — все PASS

1. **Синтаксис/поведение ISNULL (live, execute_query):** `ЕСТЬNULL(NULL, "BYN")`→"BYN", `ЕСТЬNULL("Доллар США", "BYN")`→"Доллар США". Строковый литерал в ЕСТЬNULL ведёт себя тождественно удалённому параметру (был `"BYN"`, Строка).
2. **Статика (bsl-ls):** 0 диагностик у изменённых строк (184/279/457/486-регионы). Пред-существующие InvalidCharacterInFile (тире в комментариях 34/209/823/824) — не на изменённых строках, вне скоупа.
3. **code-verify (behavior-preservation):** reviewer-субагент — **PASS**. 0 вхождений `КодВалютыПоУмолчанию`, тип сохранён, удаление set-строки не задело другие параметры, нет осиротевшего сеттера/параметра. Находка: SELECT (457) и СГРУППИРОВАТЬ ПО (486) изменены синхронно → агрегирующий запрос остаётся валидным.
4. **Sonar-дельта (utp-ib, CE SUCCESS):** `sonar_rescan_verify` **utp-ib pass=True** (0 BLOCKER/CRITICAL на изменённых строках). EDT-замечание «параметр не задан» устранено по построению (параметр удалён). `FieldsFromJoinsWithoutIsNull` L462 — пред-существующее (строка JOIN сдвинута -1 удалением, контент не менялся → не гейтится).

Примечание: overall split-verify = FAIL из-за пред-существующей грязи MFM/SVETLY (не эта задача); per-project utp-ib = pass=True.
