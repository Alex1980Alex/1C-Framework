# Pipeline: gks-ttn-kluchi-sreza-vt

Задача: вынести inline-подзапрос ключей (из условия `СрезПоследних`) в отдельную временную таблицу в запросе печати `гкс_ПечатьТТНСнятиеСХранения/ManagerModule.bsl`.
Источник: задача из чата (продолжение Sonar-фикса). Тип: refactor (behavior-preserving). Сложность: medium (1 файл).

## 1. Анализ / Планирование
- В `ТекстЗапросаДанныхДляПечати` условие виртуальной таблицы `СрезПоследних` содержало inline-подзапрос `(Номенклатура, ТочкаМаршрута) В (ВЫБРАТЬ ... ИЗ Регистрация ⋈ ВТ_Убыли)`.
- Требование: вынести подзапрос в отдельную ВТ.

## 2. Дизайн (approved)
- Новый стейтмент `ВЫБРАТЬ РАЗЛИЧНЫЕ Номенклатура, ТочкаМаршрута ПОМЕСТИТЬ ВТ_КлючиСреза ИЗ Регистрация ⋈ ВТ_Убыли`, размещён ПЕРЕД ВТ_Цены.
- Условие среза → ссылка на ВТ: `(Номенклатура, ТочкаМаршрута) В (ВЫБРАТЬ КлючиСреза.Номенклатура, КлючиСреза.ТочкаМаршрута ИЗ ВТ_КлючиСреза КАК КлючиСреза)`.
- Семантика отбора неизменна (то же множество пар; `РАЗЛИЧНЫЕ` не влияет на IN).
- Индексы пакета +1: Шапка 3→4, ТабличнаяЧасть 4→5.

## 3. Реализация
- Вставлен стейтмент ВТ_КлючиСреза; условие `СрезПоследних` ссылается на ВТ_КлючиСреза; индексы пакета обновлены; doc-комментарий пакета дополнен.
- Порядок стейтментов: ВТ_Убыли → ВТ_КлючиСреза → ВТ_Цены → ВТ_Шапка → выборка шапки[4] → выборка ТЧ[5].

## 4. Тестирование / Верификация
- `validate_query` на живой ИБ — **valid=true** (ВТ_КлючиСреза + ссылка в условии среза приняты парсером).
- code-verify (behavior-preservation) — **PASS**: эквивалентность отбора, индексы пакета верны (подсчётом стейтментов), порядок ВТ корректен, алиасы целы, остальной запрос не затронут, плейсхолдер ИСТИНА=ИСТИНА на месте.
- Применение в рабочей ИБ: «Обновить конфигурацию БД».
