# Pipeline: gks-ttn-snyatie-query-sonar

Задача: исправить замечания код-ревью (Sonar/EDT) в запросе печати `гкс_ПечатьТТНСнятиеСХранения/ManagerModule.bsl`.
Источник: файл `260623_Ошибки.md` (GKSTCPLK-2521). Тип: bug-fix/refactor запроса. Сложность: medium (1 файл, рерайт запроса).

## 1. Анализ / Планирование
4 типа находок на запросе `ТекстЗапросаДанныхДляПечати`:
- излишнее разыменование `Цены.ВидЗалоговойЦены.Валюта.Наименование`;
- строковый литерал `"BYN"` в тексте запроса;
- виртуальная таблица `гкс_ЗалоговыеЦены.СрезПоследних(&ДатаСреза)` без отбора в параметрах;
- соединение с виртуальной таблицей (оба алиаса `Цены`, `ЦеныШапка`) — требование переписать через ВТ.

## 2. Дизайн (approved)
Единый фикс — материализовать срез в ВТ:
- новый стейтмент `ПОМЕСТИТЬ ВТ_Цены`: `СрезПоследних(&ДатаСреза, (Номенклатура, ТочкаМаршрута) В (ключи убывших регистраций))` + ЛЕВОЕ СОЕДИНЕНИЕ `гкс_ВидыЗалоговыхЦен`→`Валюты` → `КодВалюты = ЕСТЬNULL(Валюта.Наименование, &КодВалютыПоУмолчанию)`;
- оба джойна (`Цены`, `ЦеныШапка`) → на `ВТ_Цены`;
- параметр `&КодВалютыПоУмолчанию` (= "BYN" в коде, вне текста запроса);
- сдвиг индексов пакета на +1 (Шапка 2→3, ТабличнаяЧасть 3→4).

## 3. Реализация
- `ТекстЗапросаДанныхДляПечати`: вставлен ВТ_Цены; джойны → ВТ_Цены; валюта в SELECT/GROUP BY → `Цены.КодВалюты`; doc-комментарий пакета дополнен ВТ_Цены.
- `ПолучитьДанныеДляПечати`: `УстановитьПараметр("КодВалютыПоУмолчанию", "BYN")`; индексы Шапка=3, ТабличнаяЧасть=4.

## 4. Тестирование / Верификация
- `validate_query` на живой ИБ — **valid=true** (подтверждена допустимость подзапроса к ВТ внутри условия `СрезПоследних` + соединения для валюты).
- code-verify (bug-fix + behavior-preservation) — **PASS**: все 4 находки устранены, индексы пакета верны (подсчётом стейтментов), набор колонок/`ОснованиеОтпуска`/LEFT-семантика/покрытие ключей сохранены, GROUP BY валиден, плейсхолдер `ИСТИНА = ИСТИНА` на месте в обеих точках.
- Применение в рабочей ИБ: «Обновить конфигурацию БД»; рекомендуется re-scan Sonar/EDT для подтверждения закрытия замечаний.
