# Pipeline: gks-zalog-nomenclature-guard

Задача: добавить измерение **Номенклатура** в guard `НайтиПротоколНаПериод` (РС `гкс_ЗалоговыеЦены`).
Источник: задача из чата (BSL-сигнатура). Сложность: medium (2 файла, без метаданных).

## 1. Анализ / Планирование
- РС `гкс_ЗалоговыеЦены` **уже** имеет измерения `ТочкаМаршрута, Номенклатура, ВидЗалоговойЦены` → метаданные не трогаем.
- guard-функция фильтровала срез без `Номенклатура` → слишком широк (ложно блокировал записи с другой номенклатурой).
- Единственный вызов: `Documents/гкс_УстановкаЗалоговыхЦен/ObjectModule.ПроверитьЕдинственностьПротоколаЗаМесяц`.
- `Номенклатура` — построчно (ТЧ `Товары`); `ТочкаМаршрута`/`ВидЗалоговойЦены` — шапка.

## 2. Дизайн (approved)
- Сигнатура: `(ТочкаМаршрута, Номенклатура, ВидЗалоговойЦены, НаДату, ТекущийРегистратор)` — порядок по измерениям.
- Каллер → цикл по ТЧ `Товары`, per-row вызов, skip пустой номенклатуры (зеркало фильтра движений `<> ПустаяСсылка`).
- Управляемая блокировка остаётся по `(ТочкаМаршрута + ВидЗалоговойЦены)` — надмножество ключа → race-safe (TOCTOU закрыт).
- Внутридокументные дубли номенклатуры по-прежнему ловит `ОбработкаПроверкиЗаполнения` (не дублируем).

## 3. Реализация
- `InformationRegisters/гкс_ЗалоговыеЦены/ManagerModule.bsl`: параметр `Номенклатура` + `И ЗалоговыеЦены.Номенклатура = &Номенклатура` + `УстановитьПараметр`; doc-комментарии.
- `Documents/гкс_УстановкаЗалоговыхЦен/ObjectModule.bsl`: цикл по ТЧ, перенумерация плейсхолдеров `%1..%5`, номенклатура в тексте сообщения; обновлены устаревшие комментарии о срезе.

## 4. Тестирование / Верификация
- code-verify (bug-fix-validation) — **PASS**: корень устранён, единственный вызов обновлён под сигнатуру, плейсхолдеры согласованы, race-safe, БСП-стандарты соблюдены, регрессий нет.
- Grep по конфигурации: ровно 2 вхождения функции (объявление + 1 вызов).
- Применение в рабочей ИБ: «Обновить конфигурацию БД».
