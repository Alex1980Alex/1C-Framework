# Pipeline — GKSTCPLK-2521: сверка паттерна залоговых цен с типовым УТ/ERP

Тип: анализ/документирование (не код-фича). Цель — подтвердить «паттерн УТ» для залоговых цен по живым базам и внести уточнения в ТЗ. Правки кода конфигурации НЕ делались.

## 1. План (pl-plan)
Снять реальную структуру типового механизма ценообразования (`ВидыЦен` + `УстановкаЦенНоменклатуры` + `ЦеныНоменклатуры25`) из живых баз УТ 11.5 (`Trade_11_5_27_52`) и ERP 2.5 (`Enterprise20_2_5_27_52`) и сверить с ТЗ/ANALYSIS GKSTCPLK-2521. Триггер — запрос пользователя «найди аналогичные паттерны для гкс_ЗалоговыеЦены / гкс_УстановкаЗалоговыхЦен / ВидЗалоговойЦены».

## 2. Дизайн (pl-design)
Источник истины — live MCP `1c-mcp-crud-trade` / `-erp`: `list_metadata_objects` → `get_metadata_structure` (3 объекта) → `execute_code` (РежимЗаписи/Периодичность). Решение: методику задачи НЕ менять, только уточнить ТЗ подтверждёнными фактами и закрыть оговорку §13.2 «структуру взяли косвенно».

## 3. Реализация (pl-code)
Выгрузка (УТ = ERP, идентичны):
- `ЦеныНоменклатуры25` = **Подчинение регистратору + периодичность Секунда**; регистратор `УстановкаЦенНоменклатуры`; `ВидЦены` — измерение; `Валюта`/`СтавкаНДС` — ресурсы.
- `УстановкаЦенНоменклатуры`: Проведение Разрешить / ОперПроведение Запретить; ТЧ `Товары2_5` (ВидЦены построчно); стоимость-с-НДС не хранится.
- `ВидыЦен`: политика цены (валюта/НДС/округление/способ/назначение).

Правки документов:
- `ТЗ-РАЗРАБОТКА_Расширение_ПаттернУТ.md`: §5.3 периодичность «Месяц»→`Секунда`; примечание помечено СУПЕРСЕДЕД; добавлен §20 (live-верификация).
- `ANALYSIS-REPORT.md`: добавлен §13.11 (подтверждение по живым базам).
- Кеш `1c-doc-research/cache/ut-pricing-mechanism-registrar-subordinate.md`: §2 живая верификация + поправка периодичности; `_index.json` last_verified → 2026-06-19.

## 4. Тест (pl-test)
- Кросс-подтверждение `WebSearch`: infostart 527518 / 1749405 (периодичность РС «в пределах секунды» / «по позиции регистратора»).
- Recall `unified_search` (прежние анализы GKSTCPLK-2521/2536).
- `capture_pattern` находки (pending, id 261fd7c8).
- Правки — только документация; регресс-кода конфигурации нет, EDT-валидация не требуется.
