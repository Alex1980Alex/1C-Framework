# Пайплайн (trivial) — GKSTCPLK-2566 рефакторинг условия ПФ «Акт расхождения веса»

Задача из чата (код-ревью заказчика): условие `Если ТипЗнч(ДанныеДополнительные) = Тип("Структура") И … Тогда`
слишком громоздкое — вынести часть условия в отдельный метод для читаемости.
Файл: `ИБTransportManagementDevelop/Конфигурация/src/DataProcessors/гкс_ПечатьАктРасхожденияВеса/ManagerModule.bsl`.

## 1. Планирование
- Тип: чистый extract-method рефакторинг (поведение не меняется), 1 файл, trivial.
- 1С-детект: BSL-код конфигурации, JIRA-код → 1С-задача (моя оценка).
- Recall: `search_patterns` (qdrant circular-import — недоступен) + grep по конвенции предикатов модуля.

## 2. Дизайн
- Вынести булево выражение ПОБИТОВО в чистую функцию-предикат `ЗаполненКладовщик(ДанныеДополнительные)`.
- Разместить в `#Область СлужебныеПроцедурыИФункции` рядом с вызывающей процедурой; БЕЗ `Экспорт` (внутримодульный вызов).
- Имя по конвенции модуля (`Это…/Есть…/Заполнен…`; ср. `ЕстьСвойствоВидЗерна`).
- Инвариант корректности: порядок операндов `И` сохранён (ленивость гарантирует безопасный доступ к `.Кладовщик`).

## 3. Реализация
- Точка вызова свёрнута в одну строку: `Если ЗаполненКладовщик(ДанныеДополнительные) Тогда`.
- Добавлена функция-предикат `ЗаполненКладовщик` с doc-комментарием (Параметры/Возвращаемое значение).
- 2 правки в `ManagerModule.bsl`; маркеры `// GKSTCPLK-2566` сохранены.

## 4. Тестирование
- `code-verify` режим behavior-preservation → adversarial reviewer → **PASS** (выражение перенесено побитово,
  short-circuit И сохранён, доступ к `.Кладовщик` безопасен, публичный API не изменён, побочных эффектов нет, стиль OK).
- Новые unit/BDD-тесты не вводятся: поведение идентично, тривиальный рефакторинг.
- Capture: skill-learning `capture_pattern` (bsl-pattern, pending). Research: WebSearch (v8std/BSLLS) + кеш-топик.

## Доработка 2 (тот же код-ревью GKSTCPLK-2566): убрать вложенный тернар
- Строка `ИзлишекНедостача = ?(Разница=0, …, ?(Разница>0, …, …))` (вложенный `?()` + длина 196) → плоский `Если/ИначеЕсли/Иначе`.
- Ветки эквивалентны: `=0 → "(нет расхождения)"`, `>0 → "(излишек)"`, иначе `"(недостача)"`. Соседний одиночный тернар `Разница = ?(…)` НЕ тронут (не вложенный, ревьюер просил только вложенный).
- Верификация: bsl_lint — `NestedTernaryOperator` + `LineLength 196` исчезли (6→4 диагностик, 0 на новом коде); code-verify behavior-preservation → **PASS**.
