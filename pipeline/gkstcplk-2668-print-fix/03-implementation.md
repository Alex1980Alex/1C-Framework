# GKSTCPLK-2668 - фикс индексов пакета в печати ТТН снятия с хранения

Тип: пост-имплементационный bug-fix в рамках GKSTCPLK-2668 (основной пайплайн - в папке задачи
`configuration/260304_GKSTCPLK-2182.../docs/260710_ GKSTCPLK-2668.../`).

Полный разбор, root cause, верификация на реальных данных и сообщение коммита:
см. `.../docs/260710_ GKSTCPLK-2668 Доработать механизм хранения залоговых цен/IMPLEMENTATION-PRINT-FIX.md`.

Кратко:
- Файл: `ИБTransportManagementDevelop/Конфигурация/src/DataProcessors/гкс_ПечатьТТНСнятиеСХранения/ManagerModule.bsl`, `ПолучитьДанныеДляПечати`.
- Корень: `ВыполнитьПакет()` даёт результат на каждый запрос вкл. ПОМЕСТИТЬ; жёсткие индексы протухли после расширения запроса до 8 ВТ → «Поле объекта не обнаружено (ДокументРегистрации)».
- Фикс: `Шапка = РезультатПакета.Количество()-2; ТабличнаяЧасть = ...-1` (две последние выборки).
- Верификация: печать по реальной регистрации формирует ТабДок (высота 67), без исключения; развёрнуто через update_database; code-verify PASS.

RECALL (unified_search) + RESEARCH (onec_search: Infostart/1С-доки) подтвердили механику ВыполнитьПакет.
