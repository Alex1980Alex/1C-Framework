# 01 — Планирование: усиление полноты 1С-артефактов

## Задача (пользователь, C)
Исправить тонкие ANALYSIS-REPORT/IMPLEMENTATION-PROGRESS задачи 2567 + усилить, чтобы пробел не повторился.

## Глубокий анализ — корень пробела
Конвенция НЕ отсутствует: `analyze-1c-task-v2` (§1-§11 + [REQ-N]/[MODIFIED]) и `implement-1c-task`
(IMPLEMENTATION-шаблон + Отклонения + коммит+МЕТАДАННЫЕ) ПОЛНОСТЬЮ документируют шаблоны. Корень —
**отсутствие принуждения**: run-1c-task делегирует, но (1) не встраивает чеклист секций, (2) ничто не
ВАЛИДИРУЕТ структуру, (3) H7-гард продвижения проверяет только длину (≥200), не структуру.

## Охват A+B+C+D
- A: перегенерировать 2 файла 2567 по канону.
- B: усилить SKILL run-1c-task (чеклист секций + «не конспектируй» + self-check валидатором).
- C: валидатор `lint_1c_artifacts.py` (де-факто core-секции, lenient, advisory).
- D: advisory-проводка валидатора в хук `pipeline-1c-advance` (нудж, не блок).
