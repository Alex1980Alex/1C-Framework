# Sonar JoinWithVirtualTable — гкс_ПечатьТТНСнятиеСХранения

Дефект SonarQube/BSL `JoinWithVirtualTable` («Не следует использовать соединения с виртуальными таблицами»).
Обработан через паттерн `/fix-sonar-task` (ADR-033). Trivial-кластер (1 issue, 1 файл).

## 1. Планирование
- Триаж: issue **real**, код кастомный (`гкс_`, in scope), `remediation_class = judgment` (переписывание запроса с сохранением семантики).
- Скан модуля: единственное прямое соединение с ВТ — `РегистрСведений.гкс_ЗалоговыеЦены.СрезПоследних(...)` в `ТекстЗапросаДанныхДляПечати()`, пакет `ВТ_ЦеныКандидаты` (стр. 257).
- Recall памяти ✅ (memory-ai/skill-learning; vector-memory был недоступен).
- Research ✅ ITS (использование временных таблиц) + Infostart.

## 2. Дизайн
- Расщепить пакет на два: **A** — материализация `СрезПоследних` в `ПОМЕСТИТЬ ВТ_СрезЗалоговыхЦен` без соединений + `ИНДЕКСИРОВАТЬ ПО Номенклатура, ТочкаМаршрута, ВидЗалоговойЦены`; **B** — те же соединения, источник = ВТ.
- Intended behavior: выборка `ВТ_ЦеныКандидаты` идентична. Approve получен.

## 3. Кодирование
- `write_module_source` (searchReplace, hash-guard) в `DataProcessors/гкс_ПечатьТТНСнятиеСХранения/ManagerModule.bsl`.
- BSL syntax check: passed. 1113 → 1132 строк.

## 4. Тестирование
- EDT `get_project_errors` по объекту: 0 новых ошибок (1 предсуществующая косметика doc-комментария, стр.75).
- Эквивалентность на реальных данных (execute_query): счётчики Старый=1/Новый=1, дельта мультимножеств по 10 колонкам **пуста** → бит-в-бит идентично.
- Структурно правило `JoinWithVirtualTable` сработать не может (нет прямого соединения с ВТ).
- **Открыто:** формальный re-scan Sonar заблокирован отсутствием `SONAR_TOKEN` (SonarQube UP, API 401).

## Capture
- skill-learning pattern `c1983878` (материализация ВТ).
- 1c-doc-research cache: `virtual-table-join-materialize-temp-table.md` + `_index.json`.
