# Пайплайн (trivial): Транскрибация встречи GMFM-6470 (13.07.26)

**Тип:** trivial (одна операция: медиа → текст, без изменения кода/конфигурации 1С).
**Запрос:** сделать транскрибацию записи встречи `Встреча в Телемосте 13.07.26 10-32-13 — запись.webm`.

## Планирование
Расшифровать речь из `.webm` (13:47, русский) в текст, причесать (спикеры, абзацы, саммари по задаче), результат положить в файл (в чат не выводить — по требованию пользователя).

## Дизайн
- Инструмент: MCP `whisper` (локально, офлайн, GPU/Vulkan), модель `large-v3-turbo` (мультиязычная).
- Язык `ru`, формат с таймкодами, фоновое задание, доменная подсказка для терминов 1С.
- Сырой `.txt` сохранить как первоисточник; обработанную версию писать в отдельный `.md`.
- Спикеры — по контексту диалога (диаризации нет), роли условные.

## Кодирование (выполнение)
1. `check_system` / `list_models` / `analyze_media` — окружение и оценка (GPU ~2 мин).
2. `transcribe_audio` (background) → `check_progress` до готовности → сырой `.txt`.
3. Прочитан `.txt` + файл задачи GMFM-6470 (для терминологии).
4. Записан обработанный транскрипт: `configuration/260304_GKSTCPLK-2182…/docs/260710_GMFM-6470…/Встреча в Телемосте 13.07.26 10-32-13 — транскрипт.md` (шапка, саммари, задачи 1 и 2, договорённости, расшифровка по репликам, глоссарий исправлений).

## Тестирование (проверка)
- Файл создан, содержимое сверено с файлом задачи (термины, две задачи, оценка ~80ч) — согласовано.
- Кода/BSL нет → SonarQube и code-verify неприменимы.
- Петли 1С-гейта закрыты: RECALL (`unified_search`), RESEARCH (`onec_search.py`), CAPTURE (`route_and_save` → vector-memory, требования встречи).

**Результат:** транскрипт готов, требования GMFM-6470 зафиксированы в память.
