# Транскрипт видео «Пример DDD и Чистой Архитектуры в 1С» (trivial)

**Задача:** получить текст видео по ссылке и положить в `docs/roadmap/`.

## Что сделано

- Скилл `whisper-transcription`, маршрут «ссылка → текст», слой A (ADR-057): готовые субтитры YouTube.
- `mcp__youtube-transcript__get_video_info` / `get_available_languages` — доступен только `ru (auto-generated)`.
- `get_transcript` × 4 страницы (пагинация по `next_cursor`: 1365 → 2724 → 4078 → null).
- Склейка в один markdown + шапка (источник, канал, дата, длительность, способ получения) + навигация по тайм-кодам из описания.

**Артефакт:** [260723_TRANSCRIPT_DDD_CLEAN_ARCH_1C.md](../../docs/roadmap/260723_TRANSCRIPT_DDD_CLEAN_ARCH_1C.md) (~152 тыс. символов).

## Ограничения

Субтитры автоматические: пунктуация машинная, часть терминов искажена распознаванием
(«1S», «ээнд», «Nва-риантов»). Слой B (`yt-dlp` + whisper large-v3-turbo) даст чище,
но на 2,5 часах существенно дольше — не запускался.

## Проверка

Файл прочитан обратно: заголовок на месте, размер 273 815 байт, кодировка UTF-8,
хвост последней страницы совпадает с ответом MCP. Кода продукта не менялось
(единственный скрипт склейки — во временном каталоге сессии).
