# 01 — Планирование: верификация PR #77 + ремедиация findings

## Контекст
`/verify #77` (ветка `fix/260608-unit-test-remediation` → `master`) завершён с
вердиктом **PASS** по runtime-поверхностям. Верификация вскрыла findings; по ним
выбраны 4 follow-up действия. Этот пайплайн — зонтик всего усилия по PR #77.

## Объём работ (follow-ups)
- **T1.** Синхронизировать тело PR #77: устаревшие SHA сабмодулей + честно отразить
  фактический объём (дифф ≫ описания). Outward-facing (текст на GitHub), обратимо.
- **T2.** Починить `.gitmodules`: stray-gitlink'и `tools/bsl-ls/tree-sitter-bsl-src`
  и `tools/multilspy-fork` без маппинга ломали `git submodule status`. Локально.
- **T3.** Довести этот пайплайн до полноценных 01–04 (текущий артефакт).
- **T4.** Разобрать раздутый scope PR. Требует переписывания истории / force-push в
  открытый PR → **gate на одобрение**.

## Ограничения / риски
- Локальный HEAD **ahead 48** от `origin/fix/260608` (не запушен); открытый PR
  отражает origin, не локаль → T1/T4 завязаны на этот разрыв.
- Рабочее дерево несёт pointer-drift двух 1С-конфиг-сабмодулей (авто-save churn).
- Force-push в чужой/общий PR — необратимо и outward-facing → по правилам сессии
  спрашивать (repo-full-permission НЕ покрывает force-push).
- Оба stray-gitlink'а — **пре-существуют на master**, в дифф PR #77 их нет.

## Классификация
medium (несколько файлов + git/PR-операции); T4 — отдельное решение пользователя.
