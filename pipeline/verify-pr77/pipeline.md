# Пайплайн (trivial): Верификация PR #77

Задача: `/verify #77` — проверка ветки `fix/260608-unit-test-remediation` → `master`
(ремедиация unit-тестов + райдер MCP cold-start + бамп 1С-сабмодулей). Это
**верификация**, а не разработка: продуктового кода не создавалось.

## 1. План
Проверить runtime-наблюдаемые исходные изменения, заявленные в теле PR:
- MCP cold-start (`src/mcp_server/server.py`): фоновый прогрев на старте.
- `src/api/routes/jobs.py`: отложенный импорт опц. зависимости `arq`.
- `src/pdf_framework/agents/plan_execute/{agent,planner}.py`: правки графа и промпта.
- Бамп gitlink двух 1С-сабмодулей (no runtime surface).
Тесты НЕ перезапускать (свидетельство автора, зона CI — по протоколу `/verify`).

## 2. Дизайн (метод)
- Запустить **реальный процесс** MCP-сервера, держать stdin открытым, читать лог
  прогрева (поверхность = старт процесса).
- Прочие правки прогнать через их поверхности из `.venv` python: import-поверхность
  `jobs.py`; репро бага/фикса промпта `planner.py`; компиляция графа через публичный
  конструктор `create_plan_execute_agent`.
- Сабмодули — `git submodule status` / `ls-tree` (резолв gitlink, без runtime).

## 3. Реализация
Продуктовый код **не менялся**. Единственная правка сессии — временный
драйвер `data/_verify_pr77.py` (verification harness), **удалён** после прогона.
Рабочее дерево по продукту чистое.

## 4. Тест (результаты)
Вердикт: **PASS** по проверенным runtime-поверхностям.
- MCP: `Lazy-init Components: starting` через +3 мс после старта транспорта (без
  tool-call), `ready in 7.82s` — прогрев S1 детачен от запроса и доходит до конца
  (против живого Qdrant). Клиентский `timeout: 600000` подтверждён в `.mcp.json`.
- `jobs.py`: после import `arq not in sys.modules` (раньше eager) → импорт отложен;
  все 4 маршрута `/jobs*` регистрируются.
- `planner.py`: старое `.format()` → `KeyError: '"steps"'` (репро бага); новое
  `.replace("{query}", …)` подставляет запрос и сохраняет JSON-пример.
- `agent.py`: публичный конструктор отдаёт скомпилированный граф; узел `executor`
  есть, фантомного `execute` нет.
- 1С-сабмодули: оба gitlink резолвятся на `heads/master` (dangling нет).

Findings (детально — в финальном отчёте сессии):
- ⚠ Фактический дифф (162 файла) ≫ описания PR (накоплен pipeline/skill-router/
  tdd-guard/memory и десятки хуков) — мерж принесёт всё это.
- ⚠ SHA сабмодулей в теле устарели (`a2a9a55`/`2396781` → факт `465963935`/`dc3695fc4`).
- ⚠ Пре-существующий (не из PR) gitlink `tools/bsl-ls/tree-sitter-bsl-src` без записи
  в `.gitmodules` ломает `git submodule status`.
- Env: 7.82s вместо ~510s, т.к. ML-стек уже прогрет; «холодный death-spiral» в этой
  среде не воспроизводим, half с `asyncio.shield` проверен по коду.
