# w4w5-decomposition - максимальная декомпозиция W4/W5 роадмапа 260708 (компактный, docs-only)

**Задача:** проанализировать нереализованные пункты роадмапа 260708 (A4/A5/A6/C2/C3/C4/C6), вписать в карту максимальную декомпозицию реализации.

**План+Дизайн:** инвентаризация кода ПЕРЕД декомпозицией (урок [[project-roadmap-audit-pattern]] - roadmap переоценивает scope; inventory audit до execution). Находки: sticky `debug_capture_mode:5129`, coverage hit-счётчики `_hit_counters:314`, metric-diff `debug_session_diff:3523`, A3-хелперы `find_method_range`/`find_assignment_lines:569`, logpoint-конвейер - УЖЕ в коде; обратного fqn→uuid нет; replay-снапшоты без переменных.

**Код:** новая секция §8 роадмапа (зеркало §7): §8.0 инвентаризация + 7 подсекций с шагами 0.5-1ч/якорями/live-планами на held-JOB/acceptance + §8.8 сводка (волны пересобраны по зависимостям: W4′=C3→C2→A5 ~10.5ч; W5′=C4→C6→A6→A4 ~14ч; итого ~24.5ч ≈ исходные 24ч). Обновлены W4/W5-строки §4 и легенда §3.

**Тест:** структурная сверка - каждый якорь из §8.0 верифицирован Grep/Read по факту кода (не по памяти); суммы часов шагов сходятся с итогами таблиц. Code-verify не требуется (docs-only, кода нет).
