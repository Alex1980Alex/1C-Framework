app:
  id: testdb
  format: DESIGNER
  base-path: "C:\\1С-Framework\\src\\bsl"

  source-set:
    - path: "."
      name: testdb
      type: "CONFIGURATION"
      purpose: [ "MAIN" ]
    - path: "exts/YAXUNIT"
      name: YAXUNIT
      type: "EXTENSION"
      purpose: [ "YAXUNIT" ]
    - path: "exts/Smoke"
      name: Smoke
      type: "EXTENSION"
      purpose: [ "TESTS" ]

  connection:
    connection-string: 'Srvr="KOMPUTER";Ref="TestDB";'
    user: "a.terletskiy@sodru.com"
    password: "Alex80Alex"

  platform-version: "8.3.27.1936"

  tools:
    builder: DESIGNER
    enterprise:
      additional-launch-keys:
        - "/TESTMANAGER"

  # Phase 8.7: Incremental dump configuration
  dump:
    type: INCREMENTAL
    path: "C:\\1С-Framework\\build\\dumps"
    checksum-algorithm: SHA-256
