# Оценка задачи: GKSTCPLK-2288 Доработать формирование для движения запасов

## Метод оценки: Подробная

## Таблица компонентов

| Тип | Имя | Синоним | Что сделать | Вид изменения |
|-----|-----|---------|-------------|---------------|
| Документ | гкс_ОснованиеДляДвиженияЗапасов | Основание для движения запасов | Добавить фильтр ПометкаУдаления в 2 запроса ManagerModule | Изменение |

## Декомпозиция

| # | Операция | Часы | Примечание |
|---|----------|------|------------|
| 1 | Анализ существующей логики (4 цепочки вызовов, 3 запроса) | 1 | Выполнен — ANALYSIS-REPORT.md |
| 2 | Модификация запроса `ПолучитьПоРегистрации` (стр. 112) — добавить `И НЕ ПометкаУдаления` | 0.5 | Простое добавление условия в WHERE |
| 3 | Модификация запроса `ДокументыОснованияДляДвиженияЗапасовПоФормированиюНомераПроб` (стр. 148) — добавить `И НЕ ПометкаУдаления` | 0.5 | Простое добавление условия в JOIN |
| 4 | Тестирование: создать регистрацию → сформировать документ → пометить на удаление → повторить формирование | 1 | Ручное тестирование по тест-плану (5 шагов) |
| 5 | Проверка отсутствия регрессии (непомеченные документы обрабатываются штатно) | 0.5 | Прогон штатного сценария |

## Коэффициенты сложности

| Фактор | Применимость | Коэффициент |
|--------|-------------|-------------|
| Незнакомая подсистема | Нет — знакомый модуль | × 1.0 |
| Сложная бизнес-логика | Нет — стандартная проверка | × 1.0 |
| Много зависимостей | Частично — 4 цепочки вызовов, но фикс в одном месте покрывает 3 из 4 | × 1.0 |

**Итоговый коэффициент: × 1.0** (нет усложняющих факторов)

## Итого: 3.5 часа

| Этап | Часы |
|------|------|
| Анализ | 1 |
| Разработка (2 строки кода) | 1 |
| Тестирование | 1.5 |
| **ВСЕГО** | **3.5** |

## Риски

- **Низкий**: создание дубля документа при повторном вызове (корректное поведение — помеченный документ не должен использоваться, создаётся новый)
- **Низкий**: на тестовой базе нет данных с ПометкаУдаления=Истина — баг воспроизводится только на продуктиве БАТМ

## Рекомендации

- Фикс минимальный (2 строки в 1 файле), безопасный, стандартный паттерн 1С
- Точки модификации независимы — можно применять в любом порядке
- Рекомендуется применить обе точки одним коммитом
- Правило 16 часов: задача значительно ниже лимита, декомпозиция не требуется
