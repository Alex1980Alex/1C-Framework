# GKSTCPLK-2288 — Прогресс реализации

## Статус: Завершено (тест на запросах PASS, ожидает обновления базы EDT)

## Выполненные точки модификации

### Точка 1: ПолучитьПоРегистрации — добавить фильтр ПометкаУдаления
- **Файл:** `Documents/гкс_ОснованиеДляДвиженияЗапасов/ManagerModule.bsl`
- **Действие:** Добавлено условие `И НЕ ОснованиеДляДвиженияЗапасов.ПометкаУдаления` в WHERE запроса
- **Строки:** 107-115 (было 106-112)
- **Валидация запросов:** validate_query OK, execute_query OK (0 помеченных документов в базе)
- **Ошибки EDT:** 0
- **Тест на данных:** регрессии нет (0 документов с ПометкаУдаления)
- **Отклонения от ANALYSIS-REPORT:** нет

### Точка 2: ДокументыОснованияДляДвиженияЗапасовПоФормированиюНомераПроб — добавить фильтр
- **Файл:** `Documents/гкс_ОснованиеДляДвиженияЗапасов/ManagerModule.bsl`
- **Действие:** Добавлено условие `И (НЕ ОснованиеДляДвиженияЗапасов.ПометкаУдаления)` в JOIN
- **Строки:** 152
- **Валидация запросов:** validate_query OK
- **Ошибки EDT:** 0
- **Тест на данных:** регрессии нет
- **Отклонения от ANALYSIS-REPORT:** нет

## Результаты тестирования

### Автоматические тесты (execute_code на живой базе, 2026-03-26)
- **Тест: новый запрос не находит помеченный документ** — **PASS**
  - Документ СоУТ-0000003 помечен на удаление → запрос с `И НЕ ПометкаУдаления` вернул пустой результат
- **Тест: старый запрос находил помеченный** — подтверждено (это был баг)
  - Запрос без фильтра возвращал помеченный документ с `ПометкаУдаления=Да`
- **Тест регрессии** — **PASS** — непомеченные документы обрабатываются как раньше
- **Очистка** — пометка удаления снята, данные восстановлены

### Ручные тесты (ожидают обновления базы из EDT)
- Тест 1 (пометить и перезаполнить через АРМ Приёмка транспорта): PENDING
- Тест 2 (создание нового документа вместо помеченного): PENDING
- Тест 3 (взвешивание + АктуализироватьДокументДвиженияЗапасов): PENDING

## Верификация кросс-зависимостей

- `ПолучитьПоРегистрации` вызывается в 4 местах (стр. 42, 74, 103, 268) — все корректно обрабатывают ПустаяСсылка()
- `ДокументыОснованияДляДвиженияЗапасовПоФормированиюНомераПроб` вызывается в 1 месте (стр. 264) — корректно обрабатывает Неопределено
- Изменения обратно совместимы — сигнатуры методов не менялись

## Открытые вопросы
- Для полного end-to-end теста нужно обновить базу из EDT (Ctrl+Shift+F11) и повторить сценарий через АРМ
