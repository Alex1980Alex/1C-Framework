# GKSTCPLK-2399 — Прогресс реализации

## Статус: ✅ Код реализован, ⏳ Ожидает ручного обновления базы и функционального тестирования

**Задача:** Разработать механизм приемки авто с другим порядком операции для ООО Инфида.
Добавить константу `гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто` по аналогии с ЖД-константой
и реализовать альтернативный порядок приёмки авто (Регистрация → ЛабАнализ → Взвешивание →
Силосы → Тара) при включённой опции.

**EDT Workspace:** `D:\WorkSpace\УправлениеТранспортомНаПЛК\УправлениеТранспортомНаПЛК`
**EDT Project name:** `УправлениеТранспортомНаПЛК`
**Application (база):** `TestDB`, id=`29edc536-8c91-4e37-ae0a-b557911e412a`

## Выполненные точки модификации

### ✅ Точка 1 [ADDED]: Константа `гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто`

- **Файл:** `src/Constants/гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто/гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто.mdo`
- **Действие:** создан новый EDT `.mdo` объект, Boolean, DataLockControlMode=Managed
- **Синоним (ru):** «Разрешить входной контроль до взвешивания (авто транспорт)»
- **Тултип (ru):** «Для авто транспорта при приемке разрешается оформлять Лабораторный анализ входного контроля до взвешивания авто»
- **UUID:** 6 новых UUID сгенерированы через `uuid.uuid4()`
- **EDT revalidate:** OK, `get_project_errors` = 0
- **Отклонения от ANALYSIS-REPORT:** нет

### ✅ Точка 2 [MODIFIED]: `Configuration.mdo`

- **Файл:** `src/Configuration/Configuration.mdo`
- **Действие:** добавлена запись `<constants>Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто</constants>` в алфавитном порядке (перед ЖД)
- **Строка вставки:** 1733 (после `гкс_ПутьКДокументамСАРА`)
- **Отклонения от ANALYSIS-REPORT:** нет (формат EDT `.mdo` вместо Designer `.xml` — эквивалентно)

### ✅ Точка 3 [MODIFIED]: Расширение функции `РазрешитьВходнойКонтрольДоВзвешивания`

- **Файл:** `src/CommonModules/гкс_ПриемкаТранспорта/Module.bsl`
- **Метод:** `Функция РазрешитьВходнойКонтрольДоВзвешивания(ТипРегистрации, ВидПеревозки) Экспорт`
- **Строки (было → стало):** 1143–1152 → 1144–1162 (строк кода: 9 → 19)
- **Действие:** заменён монолитный `Возврат ... И ЭтоЖДПеревозка И Константа.ЖД` на развилку по типу перевозки
- **Ветви:**
  - ЖД → `Константы.гкс_РазрешитьВходнойКонтрольДоВзвешиванияЖД.Получить()`
  - **Авто → `Константы.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто.Получить()`** ← новое
  - иное → `Ложь`
  - `ТипРегистрации ≠ Приемка` → `Ложь` (ранний exit)
- **Комментарии трассируемости:** `// GKSTCPLK-2399` на новых строках (1156, 1157, 1160)
- **EDT syntaxCheck:** passed
- **EDT get_project_errors (по модулю):** 0 ошибок
- **find_references Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто:** 11 ссылок, включая BSL строка 1158 ✓
- **Отклонения от ANALYSIS-REPORT:** нет

### ✅ Точка 4 [MODIFIED]: Docstring wrapper-функции

- **Файл:** `src/CommonModules/гкс_ПриемкаТранспорта/Module.bsl`
- **Метод:** `Функция РазрешитьВходнойКонтрольДоВзвешиванияПоРегистрации(ДокументРегистрации) Экспорт`
- **Действие:** комментарий-заголовок заменён с «Возвращает применение оформления ЖД вагонов без взвешивания» на нейтральный «Возвращает признак разрешения входного контроля до взвешивания по документу регистрации. Делегирует в РазрешитьВходнойКонтрольДоВзвешивания»
- **Тело функции:** не изменялось

### ✅ Точка 5 [MODIFIED]: `CommonForm.гкс_ОсновныеНастройки.Form.form`

- **Файл:** `src/CommonForms/гкс_ОсновныеНастройки/Form.form` (EDT формат `.form`, не Designer XML)
- **Действие:** добавлен `<items xsi:type="form:FormField">` с `<name>гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто</name>` **перед** существующим ЖД-чекбоксом в группе `ГруппаОбщиеНастройки` (визуально Авто выше ЖД)
- **id-поля новых элементов:** 272, 273 (context menu), 274 (tooltip) — проверено: max id в форме = 271
- **DataPath:** `НаборКонстант.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто`
- **Атрибуты формы:** не изменялись (`НаборКонстант: ConstantsSet` автоматически видит новую константу)
- **EDT revalidate:** OK
- **Отклонения от ANALYSIS-REPORT:** вставка ПЕРЕД ЖД (а не после) — для соответствия созвонной договорённости «рядом, визуально аналогично ЖД»

### ✅ Точка 6 [MODIFIED]: `Subsystem.гкс_ФункциональныеСодружество.ПриемкаТранспорта`

- **Файл:** `src/Subsystems/гкс_ФункциональныеСодружество/Subsystems/ПриемкаТранспорта/ПриемкаТранспорта.mdo`
- **Строка вставки:** 53 (перед `Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияЖД`)
- **Действие:** добавлен `<content>Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто</content>`
- **EDT revalidate:** OK

### ✅ Точка 7 [MODIFIED]: `Role.гкс_БазовыеПраваУправлениеТранспортомНаПЛК`

- **Файл:** `src/Roles/гкс_БазовыеПраваУправлениеТранспортомНаПЛК/Rights.rights` (EDT формат `.rights`)
- **Действие:** добавлен `<object>` для `Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто` перед существующим ЖД
- **Права:** `Read`, `View`, `ReadDataHistory`, `ViewDataHistory` = true (идентично ЖД)
- **EDT revalidate:** OK

### ✅ Точка 8 [MODIFIED]: `Role.гкс_БазовыеПраваАналитика`

- **Файл:** `src/Roles/гкс_БазовыеПраваАналитика/Rights.rights`
- **Действие:** добавлен `<object>` для `Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто` перед существующим ЖД
- **Права:** `Read`, `Update`, `View`, `Edit`, `ReadDataHistory`, `ViewDataHistory` = true (идентично ЖД)
- **EDT revalidate:** OK

### ✅ Точка 9 [MODIFIED]: Rename процедуры `ИнициализироватьРеквизитыПриемкиЖД`

- **Файл:** `src/DataProcessors/гкс_ПриемкаТранспорта/Forms/ОсновнаяФорма/Module.bsl`
- **Старое имя:** `ИнициализироватьРеквизитыПриемкиЖД` (misnomer — тело универсальное)
- **Новое имя:** `ИнициализироватьРеквизитыВходногоКонтроляДоВзвешивания`
- **Изменённые строки (4 вхождения):**
  - 968 — вызов в `ИнициализироватьРеквизитыФормы`
  - 1069 — объявление процедуры (`&НаСервере`, неэкспортная)
  - 3239 — вызов в `ВидПеревозкиПриИзмененииНаСервере`
  - 3253 — вызов в `ТипРегистрацииПриИзмененииНаСервере`
- **Верификация рефакторинга:**
  - `Grep "ИнициализироватьРеквизитыПриемкиЖД"` → **0 совпадений** ✓
  - `Grep "ИнициализироватьРеквизитыВходногоКонтроляДоВзвешивания"` → **4 совпадения в 1 файле** ✓
- **Комментарии трассируемости:** `// GKSTCPLK-2399` на всех 4 изменённых строках
- **EDT get_project_errors (по модулю):** 0 ошибок
- **EDT syntaxCheck:** passed

## Проверка кросс-зависимостей (Этап 5)

- `find_references Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто` → **11 ссылок**:
  - CommonForm.гкс_ОсновныеНастройки (2 ссылки: source + FormField)
  - Configuration.constants (1)
  - Constant-внутренние типы (4)
  - Role.гкс_БазовыеПраваАналитика.Rights (1)
  - Role.гкс_БазовыеПраваУправлениеТранспортомНаПЛК.Rights (1)
  - Subsystem.гкс_ФункциональныеСодружество.ПриемкаТранспорта (1)
  - CommonModules/гкс_ПриемкаТранспорта/Module.bsl строка 1158 ← наша новая ветка ✓

## Общие ошибки проекта EDT

**Pre-existing ошибки, не связанные с задачей (baseline):**
1. `ОбщийМодуль.гкс_ИнтеграцияСКверионКлиент` — `ПрочитатьJSON не определена [Web-клиент]` (pre-existing)
2. `Справочник.гкс_ВходящиеДанныеWarehouse.ФормаЭлемента` — `ОткрытьЗначение не определена [Web-клиент]` (pre-existing)

**Новых ошибок от реализации GKSTCPLK-2399: 0.** По изменённым модулям
(`CommonModule.гкс_ПриемкаТранспорта`, `DataProcessor.гкс_ПриемкаТранспорта`,
`Constant.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто`, `CommonForm.гкс_ОсновныеНастройки`,
`Subsystem.ПриемкаТранспорта`, `Role.*`) — `get_project_errors` = 0.

## Этап 2 (валидация запросов)

**N/A** — в задаче нет SQL-запросов. Все операции — работа с константой и перечислением
через `Константы.X.Получить()` и предикаты enum `ЭтоЖДПеревозка/ЭтоАвтоПеревозка`.

## Этап 4 (статический анализ)

- **EDT syntaxCheck** (встроенный в `write_module_source`): passed для всех BSL-записей
- **EDT get_project_errors** по изменённым модулям: 0 ошибок
- **bsl_analyze** (линтер через bsl-debug-server): пропущен — в изменениях только развилка по enum + вызовы `Константы.X.Получить()`, пошаговой отладки не требуется (нет сложной логики, циклов, вычислений)

## Результаты тестирования (Этап 6)

**Статус: ⏳ ОЖИДАЕТ РУЧНОГО ТЕСТИРОВАНИЯ**

### База 1С:Предприятие

`TestDB` в данный момент **НЕ обновлена** из EDT workspace.
`update_database` через EDT-MCP не выполнялся (cancel от пользователя).

Перед тестированием необходимо:
1. **В EDT выполнить «Обновить конфигурацию базы данных»** (или `update_database(incremental=true, autoRestructure=true)`).
2. Убедиться, что в `Константа.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто` появилась через
   `mcp__1c-mcp-crud__get_metadata(filter="Константа.гкс_РазрешитьВходнойКонтрольДоВзвешиванияАвто")`.

### Сценарии для ручного тестирования (из ANALYSIS-REPORT п. 7.2)

| Сценарий | Действие | Ожидание | Статус |
|---|---|---|---|
| **А** (регрессия ЖД) | Включить ЖД-константу, в АРМе ЖД → цепочка `Прибыл → Вес на въезде → Номер пробы → ...` | Старое поведение сохранено | ⏳ pending |
| **Б** (новое поведение Авто) | Включить Авто-константу, ВидПеревозки=Автомобиль, в статусе `Прибыл` — проверить что кнопка «Номер пробы» активна | Ранний отбор проб до взвешивания | ⏳ pending |
| **В** (Авто с выкл. константой) | Авто, константа Ложь → старый порядок (Взвешивание → ЛабАнализ) | Кнопка «Номер пробы» недоступна в `Прибыл` | ⏳ pending |
| **Г** (переключение ВидаПеревозки) | Обе константы Истина, ЖД↔Авто↔ЖД в АРМе | Реквизит формы `РазрешитьВходнойКонтрольДоВзвешивания` пересчитывается | ⏳ pending |
| **Д** (граничные условия) | Отгрузка+Авто+константа=Истина | Функция возвращает Ложь | ⏳ pending |
| **Д.2** (константа не установлена) | По умолчанию Ложь | Функция возвращает Ложь | ⏳ pending |

Для проверки результата после каждого сценария использовать:
```bsl
// Через mcp__1c-mcp-crud__execute_code
Результат = гкс_ПриемкаТранспорта.РазрешитьВходнойКонтрольДоВзвешивания(
    Перечисления.гкс_ТипРегистрации.Приемка,
    Перечисления.гкс_ТипыТранспортныхСредствДоставки.Автомобиль);
```

### YAxUnit Smoke (см. ANALYSIS-REPORT п. 7.4)

Автоматический smoke-прогон всех форм через `tools/yaxunit.json`
(`ДымовыеТесты.ОткрытиеФорм = true`) покроет:
- Открытие `CommonForm.гкс_ОсновныеНастройки` (поймает опечатки в DataPath, права)
- Открытие `DataProcessor.гкс_ПриемкаТранспорта.ОсновнаяФорма` (поймает падение в `ИнициализироватьРеквизитыВходногоКонтроляДоВзвешивания`)

**Запуск:** `mcp__mcp-onec-test-runner__run_all_tests` (после обновления базы).

## Отклонения от ANALYSIS-REPORT

1. **Формат файлов: EDT `.mdo`/`.form`/`.rights`** вместо Designer `.xml` — потому что работа ведётся в EDT workspace (`D:\WorkSpace\...`), а не в submodule Designer-экспорте. Семантически эквивалентно.
2. **CheckBoxField в форме — ПЕРЕД ЖД, не ПОСЛЕ.** Эстетика (Авто визуально выше) — согласно созвонной договорённости «рядом».
3. **Рефакторинг комментария трассируемости.** В ANALYSIS-REPORT изначально предполагалось оставить в заголовке процедуры комментарий `// GKSTCPLK-2399 (было ИнициализироватьРеквизитыПриемкиЖД)`. В реализации убран, потому что чек-лист верификации требовал `Grep "ИнициализироватьРеквизитыПриемкиЖД"` = 0. Трассируемость осталась через `// GKSTCPLK-2399` без упоминания старого имени (а также через git-историю коммита).

## Источники синхронизации EDT ↔ Designer (submodule)

**ВАЖНО:** Изменения сделаны в EDT workspace `D:\WorkSpace\УправлениеТранспортомНаПЛК\...`.
Submodule `D:\1С-Framework\src\projects\configuration\260304_GKSTCPLK-2182 .../src\` (Designer XML) НЕ обновлялся.

Для синхронизации выполнить:
- Через EDT: **Configuration → Export → Export configuration to files** (в Designer формат) → путь submodule.
- Или через `ring`: `ring edt dump` (если установлено).
- После дампа — git commit в submodule + update submodule ref в основном репозитории.

## Открытые вопросы

Нет. Все Q1, Q2, Q3 из ANALYSIS-REPORT были разрешены на этапе анализа:
- **Q1 → ДА**, переименование выполнено (точка 9)
- **Q2 → НЕТ**, `ПолныеПрава` покрывается через `setForNewObjects=true`
- **Q3 → ОТСУТСТВУЮЩИХ ТЕСТОВ НЕТ**, нужны новые артефакты (юнит+BDD) на этапе `/write-1c-tests`

## Следующие шаги

1. **Обновить базу данных** из EDT (Конфигурация → Обновить конфигурацию базы данных, или `update_database` MCP).
2. **Ручное тестирование** по сценариям А–Д (пользователь).
3. **Прогон YAxUnit** (`run_all_tests`).
4. **Дамп EDT → Designer формат** в submodule и commit.
5. `/write-1c-tests` — создать юнит-тесты + BDD-сценарий из шаблонов ANALYSIS-REPORT п. 7.4.
6. `/run-1c-tests` — baseline + прогон с новыми тестами.

---

- **Реализация выполнена:** 2026-04-14
- **Исполнитель:** Claude Opus 4.6 через /implement-1c-task v2.1.1
- **ANALYSIS-REPORT:** [ANALYSIS-REPORT.md](ANALYSIS-REPORT.md)
