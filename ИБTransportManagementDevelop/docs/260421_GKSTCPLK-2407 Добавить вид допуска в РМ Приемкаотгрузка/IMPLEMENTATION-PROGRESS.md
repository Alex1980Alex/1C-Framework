# GKSTCPLK-2407 — Прогресс реализации

## Статус: Код внесён, ожидает загрузки конфигурации в TestDB и ручного тестирования REQ-2

## Сводка

- **Pipeline**: `/implement-1c-task` v2.2.0, dual-write (EDT workspace + submodule).
- **Входные данные**: [ANALYSIS-REPORT.md](./ANALYSIS-REPORT.md) v1.0 (score 85/100).
- **Ответы пользователя на OQ** (полученные перед началом Этапа 3):
  - **OQ-1** (REQ-2): тест на `Srvr=KOMPUTER;Ref=TestDB` (копию БелАгроТерминала не используем).
  - **OQ-3**: Ветеринарный — по аналогии с Фумигационный/Фитосанитарный (без специфичной логики Претензионного, `ВскрытиеЗапрещено` не форсится в первичном заполнении).
  - **OQ-4 / R-3**: имя роли и регистрация в профиле — по шаблону (`гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК`, добавляется в `ДополнитьПрофильРолямиУправленияПретензионнымДопускомКВагонам`).
- **SDD-гейт**: обойдён (прямой `/implement-1c-task` с явного согласия пользователя — dual-write, без `/opsx:propose`).

## Отклонения от ANALYSIS-REPORT

| Пункт отчёта | Отклонение | Причина |
|---|---|---|
| Точка 3 § «СтруктураДоступов в `гкс_УправлениеДоступом`» | Фактическое место — `CommonModules/гкс_ПриемкаНаПЛКСервер/Module.bsl`, функция `ИнициализироватьСтруктуруДоступовПриемкаПЛК` (837-870) | `search_in_code` по `ДоступенДопускФумигационный` показал единственное совпадение именно в этом модуле; в `гкс_УправлениеДоступом` живёт только профиль ролей (Точка 4). Отчёт ошибочно указал модуль |
| Форма XML — dual-write | EDT-формат (`.form`, `.mdo`) и Designer-формат (`.xml`) **структурно разные**, поэтому XML-правки сделаны **только в submodule** | Dual-write в XML требует второго комплекта файлов в другой схеме — нерентабельно. EDT переиндексирует `.mdo` автоматически (подтверждено: после правки Enum `.mdo` `validate_query` начал принимать `Ветеринарный`). Но `Form.xml` → `Form.form` не синхронизируется — **пользователю нужно вручную импортировать submodule в EDT** или пересобрать проект EDT, если захочется видеть новые кнопки/колонки в среде разработки |
| Точка 14: заголовок PictureField | Колонка называется «Вт» (2 символа, как «Фм», «Фт», «Пр»), а не полное «Ветеринарный» | Сохранение единообразия с соседями |
| ID XML-элементов (Button/Command/PictureField) | Использованы 921-926 (ОсновнаяФорма) и 939-941 (ФормаЖДПеревозки) — по максимуму существующих +1 | UUID для форменных объектов не используются — у 1С int id внутри формы |
| bsl_analyze | Не применён | Отклоняет Designer-формат с BOM (`Неожиданный символ: ﻿`). Заменён на `EDT-MCP get_project_errors` (passed) и `syntaxCheck` в `write_module_source` (passed на каждом write) |

## Выполненные точки модификации

### Точка 1: [ADDED] EnumValue Ветеринарный
- **Файлы**:
  - `src/Enums/гкс_ДопускиКВскрытиюВагонов.xml` (submodule, Designer)
  - `src/Enums/гкс_ДопускиКВскрытиюВагонов/гкс_ДопускиКВскрытиюВагонов.mdo` (EDT)
- **UUID нового значения**: `2e8d5847-1e2a-46fd-a844-cac9536063c1`
- **Comment**: `GKSTCPLK-2407`
- **Проверка**: `mcp__1c-mcp-crud__execute_code` на TestDB пока возвращает старый список (3 значения) — базу нужно обновить; EDT после правки `.mdo` сразу увидел новое значение (подтверждено `validate_query`)

### Точка 2: [ADDED] Role гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК
- **Файлы**:
  - `src/Roles/гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК.xml` (submodule, Designer)
  - `src/Roles/гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК/гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК.mdo` + `Rights.rights` (EDT)
- **UUID**: `1e6bd11a-8469-4899-afc7-453b78886ba5`
- **Registration**:
  - `src/Configuration.xml` (submodule): добавлена строка `<Role>...Ветеринарный...</Role>` в алфавитном порядке (перед Претензионный)
  - `src/Configuration/Configuration.mdo` (EDT): добавлена строка `<roles>Role...Ветеринарный...</roles>` там же
- **Rights.rights**: минимальный (без гранулярных прав — «маркерная» роль, используется только через `РольДоступна()`)

### Точка 3: [MODIFIED] СтруктураДоступов — ключ ДоступенДопускВетеринарный
- **Файл**: `CommonModules/гкс_ПриемкаНаПЛКСервер/Ext/Module.bsl` (EDT: `.../Module.bsl`)
- **Функция**: `ИнициализироватьСтруктуруДоступовПриемкаПЛК` (строки 837-870)
- **Действие**: добавлен 4-й `СтруктураДоступов.Вставить("ДоступенДопускВетеринарный", РольДоступна("гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК"))`; расширена дизъюнкция `ДоступенДопускаКВскрытиюВагонов`
- **Syntax check**: passed (EDT)
- **Отклонение от отчёта**: см. сводку выше (модуль не тот, что указан в отчёте)

### Точка 4: [MODIFIED] Профиль — роль Ветеринарный
- **Файл**: `CommonModules/гкс_УправлениеДоступом/Ext/Module.bsl`
- **Процедура**: `ДополнитьПрофильРолямиУправленияПретензионнымДопускомКВагонам` (строки 869-880)
- **Действие**: добавлена 4-я `ОписаниеПрофиля.Роли.Добавить("гкс_ВозможностьМенятьДопускВетеринарныйВАРМеПриемкиНаПЛК")`
- **Syntax check**: passed
- **Побочный эффект (R-3)**: роль будет автоматически раздаваться всем профилям с «допуск»-ролями. Подтверждено пользователем (OQ-4).

### Точка 5: [MODIFIED] гкс_ПриемкаТранспорта — ТекущиеДопускиПоРегистрации + ЕстьДопускПоКонтролямСтатусов
- **Файл**: `CommonModules/гкс_ПриемкаТранспорта/Ext/Module.bsl`
- **ЕстьДопускПоКонтролямСтатусов** (339-347): добавлено `И Не ТекущиеДопуски.ВетеринарныйЗапрет`
- **ТекущиеДопускиПоРегистрации** (349-421):
  - Докстринг: добавлено `* ВетеринарныйЗапрет - Булево -`
  - Запрос: +4-й `ОБЪЕДИНИТЬ ВСЕ` блок с Ветеринарный, расширение всех SELECT до 4 колонок, добавлен `МАКСИМУМ(...ВетеринарныйЗапрет)`
  - Структура-результат: `"ФумигационныйЗапрет, ФитосанитарныйЗапрет, ПретензионныйЗапрет, ВетеринарныйЗапрет"` + 4 `Ложь`
- **validate_query**: PASS (после переиндексации Enum)
- **Syntax check**: passed

### Точка 6: [ADDED] Command ДопускВетеринарный (ОсновнаяФорма)
- **Файл**: `src/DataProcessors/гкс_ПриемкаТранспорта/Forms/ОсновнаяФорма/Ext/Form.xml`
- **ID**: 921, секция `<Commands>`
- **Action**: `ДопускВетеринарный`
- **Submodule only** (EDT format `Form.form` другой)

### Точка 7: [MODIFIED] Button в подменю «Допуски» (ОсновнаяФорма)
- **Файл**: тот же Form.xml
- **ID**: 922 (Button), 923 (ExtendedTooltip)
- **Connects to**: `Form.Command.ДопускВетеринарный`
- **Position**: после Button `СписокРегистрацииДопускПретензионный` в `<Popup name="ПодменюДопуски">`

### Точка 8: [MODIFIED] PictureField СписокРегистрацииВетеринарный
- **Файл**: тот же Form.xml
- **ID**: 924 (PictureField), 925 (ContextMenu), 926 (ExtendedTooltip)
- **DataPath**: `СписокРегистрации.Ветеринарный`
- **Title**: «Вт»
- **Picture**: `CommonPicture.ВниманиеКрасный` (как у соседей)

### Точка 9: [MODIFIED] DCS dynamic list (ОсновнаяФорма)
- **Файл**: тот же Form.xml
- 4 изменения:
  1. `<UseAlways>` +`<Field>СписокРегистрации.Ветеринарный</Field>`
  2. Query `ВТ_ДопускиКВскрытиюВагонов`: +4-й `МАКСИМУМ(ВЫБОР КОГДА ... = Ветеринарный ТОГДА ВскрытиеЗапрещено ИНАЧЕ ЛОЖЬ КОНЕЦ)`
  3. Final `ВЫБРАТЬ`: +`ЕСТЬNULL(ВТ_ДопускиКВскрытиюВагонов.Ветеринарный, ЛОЖЬ) КАК Ветеринарный`
  4. DCS schema: `<Field xsi:type="dcssch:DataSetFieldField">` для Ветеринарный с локализованным title

### Точка 10: [ADDED] Обработчик ДопускВетеринарный (ОсновнаяФорма)
- **Файл**: `.../ОсновнаяФорма/Ext/Form/Module.bsl`
- После процедуры `ДопускПретензионный` (751-756) добавлена процедура `ДопускВетеринарный` (&НаКлиенте), вызывающая `ИзменитьДопуск(ПредопределенноеЗначение("Перечисление.гкс_ДопускиКВскрытиюВагонов.Ветеринарный"))`
- Syntax check: passed

### Точка 11: [MODIFIED] Видимость кнопки в `ПодменюДопуски` (ОсновнаяФорма)
- `УстановитьВидимостьДоступностьКомандФормы` (1112-): +`Элементы.СписокРегистрацииДопускВетеринарный.Видимость = СтруктураДоступов.ДоступенДопускВетеринарный`
- Сводный ключ `ПодменюДопуски.Видимость` уже использует расширенный `ДоступенДопускаКВскрытиюВагонов` (см. Точку 3) — дополнительных правок не требуется

### Точка 12: [MODIFIED] REQ-2 фикс + Ветеринарный (ОсновнаяФорма)
- `УстановитьВидимостьЭлементовСпискаРегистрации` (1190-): заменено 3 строки `= ЭтоЖДПеревозка` на 4 строки с gate по роли `= ЭтоЖДПеревозка И СтруктураДоступов.ДоступенДопускХ`
- **REQ-2**: теперь пользователь без роли не видит колонку вообще; с ролью — пользовательская настройка «скрыть» через меню формы перестанет перетираться на каждом пересчёте
- Пользователь подтвердил поведение в ответе на OQ-1

### Точка 13: [MODIFIED] Доступность кнопки при смене режима (ОсновнаяФорма)
- `УстановитьДоступностьКомандУправленияДопусками` (1789-): +`Элементы.СписокРегистрацииДопускВетеринарный.Доступность = ЭтоПриемкаЖДТранспорта`

### Точка 14: [MODIFIED] ФормаЖДПеревозки — зеркало кнопки/команды/видимости
- **Module.bsl**:
  - +`ДопускВетеринарный(Команда)` после `ДопускПретензионный` (190-193)
  - `УстановитьВидимостьДоступностьКомандФормы` (386-): +`Элементы.СписокРегистрацииДопускВетеринарный.Видимость = СтруктураДоступов.ДоступенДопускВетеринарный`
  - `УстановитьДоступностьКомандУправленияДопусками` (497-): +`Элементы.СписокРегистрацииДопускВетеринарный.Доступность = ЭтоПриемкаТранспорта`
- **Form.xml**:
  - `<Popup name="ПодменюДопуски">`: +Button `СписокРегистрацииДопускВетеринарный` (id=939, ExtendedTooltip 940)
  - `<Commands>`: +Command `ДопускВетеринарный` (id=941)
- Syntax check: passed
- **PictureField не требуется** — в этой форме колонок нет (подтверждено анализом отчёта, grep на 2026-04-21)

### Точка 15: [MODIFIED] РегистрацияНаПЛК.ManagerModule — первичное заполнение
- **Файл**: `Documents/гкс_РегистрацияНаПЛК/Ext/ManagerModule.bsl`
- **Функция**: `ТекстЗапросаТаблицаСтатусыДопускаВагоновКВскрытию` (615-679 после правки)
- **Действие**: +4-й `ОБЪЕДИНИТЬ ВСЕ` с `ЗНАЧЕНИЕ(...Ветеринарный) КАК ДопускКВскрытию` в `ВТ_ПереченьДопусков`
- **CASE на строках 655-662** (специфика Претензионного) **не расширен** — Ветеринарный остаётся `ВскрытиеЗапрещено = ЛОЖЬ` по умолчанию (OQ-3: по аналогии с Фумигационный/Фитосанитарный)
- Syntax check: passed

### Точка 16: [NOT MODIFIED] Subsystem ПриемкаТранспорта
- **Файл**: `src/Subsystems/гкс_ФункциональныеСодружество/Subsystems/ПриемкаТранспорта.xml`
- **Проверено**: ссылается на `DataProcessor.гкс_ПриемкаТранспорта` целиком, на конкретные команды формы — нет
- **Действие**: не требуется

## EDT Project Errors (после всех правок)

Запуск `get_project_errors` с фильтром по 5 изменённым объектам:
- **CRITICAL**: 0
- **BLOCKER**: 0
- **ERRORS**: 0

Исторические WARNING / MINOR (pre-existing, не вызваны моими правками): `using-isinrole`, `constructor-function-return-section` (теперь упоминает `ДоступенДопускВетеринарный` в списке полей структуры — это ожидаемое расширение, не ошибка).

## Результаты тестирования

### Статический анализ
- ✅ `EDT write_module_source syntaxCheck`: passed на всех 11 BSL-записях
- ✅ `EDT validate_query` итоговый (с Ветеринарным): PASS, 0 ошибок
- ✅ `EDT get_project_errors ERRORS`: 0

### На живых данных (TestDB)
- ⏳ **Ожидает обновления конфигурации**: текущая TestDB содержит старую конфигурацию (3 значения перечисления). Для полного прогона тестов из §7 ANALYSIS-REPORT требуется:
  1. Загрузить изменённую конфигурацию в TestDB через Конфигуратор («Обновить конфигурацию БД») или через EDT (Deploy)
  2. Перезапустить MCP-подключение
  3. Выполнить тест-план 7.2 (Сценарии 1-5) и 7.3 (Регрессии)

### REQ-2 smoke (Точка 12 — gate видимости по роли)
- ✅ Код обеспечивает: без роли → колонка скрыта; с ролью → пользовательская настройка «скрыть» не перетирается (фикс `= ЭтоЖДПеревозка` → `= ЭтоЖДПеревозка И СтруктураДоступов.ДоступенДопускХ`)
- ⏳ Ручная проверка на TestDB — после обновления базы

## Открытые вопросы

- **Импорт в EDT**: XML-правки (Enum .xml, Role .xml, Configuration.xml, Form.xml) сделаны в Designer-формате submodule. EDT-формат `.mdo` синхронизирован только для Enum, Role, Configuration (4 файла). Form.xml → Form.form — **не синхронизирован** (разные схемы). Если пользователь собирается работать в EDT, нужно сделать Project → Update from configuration / переимпорт submodule
- **Кеш `1c-doc-research`**: в рамках этой задачи не обновлялся (домен 1С-платформы не был основным блокером — всё было прикладной конфигурацией)
- **OpenSpec**: поскольку пошли напрямую через `/implement-1c-task` без `/opsx:propose`, spec'ов в `openspec/changes/` нет. Если нужно ретроспективно — можно запустить `/opsx:propose gkstcplk-2407-veterinary-allowance` и заархивировать с уже реализованным кодом

## Чеклист завершения

- [x] Все 16 точек модификации реализованы (14 в коде, Точка 16 — not needed)
- [x] Каждый блок кода имеет комментарий `// GKSTCPLK-2407`
- [x] EDT `get_project_errors ERRORS` = 0
- [x] Все SQL-запросы прошли `validate_query`
- [x] `syntaxCheck` EDT: passed на всех 11 BSL-записях
- [ ] `bsl_analyze`: заменён на EDT syntax check (bsl_analyze падает на BOM submodule-файлов)
- [ ] Тест-план на живых данных — ожидает обновления TestDB
- [x] REQ-2 (gate по роли) — код внесён
- [x] IMPLEMENTATION-PROGRESS.md создан
- [x] Отклонения от ANALYSIS-REPORT зафиксированы
- [ ] Git commit — следующий шаг
