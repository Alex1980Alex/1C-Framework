# Дорожная карта VA BDD: ВозвратАвто

> Вид табло: `гкс_ВидыЭлектронныхТабло.ВозвратАвто`
> Путь: **неизвестен**
> Записей в TestDB: **0**
> Настройка: **НЕТ** (0 записей)

## Статус: CONFIRMED DEAD CODE (12.04.2026)

Проверка BSL кода (2185 файлов):
- **0 ссылок** на `ВозвратАвто` в BSL модулях
- **0 обработчиков** в `гкс_ИнтеграцияСЭлектроннымТаблоСервер` (обслуживает только 4 других вида)
- **0 настроек** в `гкс_НастройкаЭлектронногоТабло`
- **0 записей** в `гкс_ЭлектронныеТабло`
- Определен только в XML перечисления как значение

Синоним: "Возврат авто, не подходящего по качеству"

## Вывод

**Тесты НЕ нужны.** Значение перечисления не используется ни в одном модуле.
Вероятно — запланированная, но не реализованная функциональность (возврат ТС при непринятом качестве).

## Рекомендация

- Документировать как dead code в тест-плане
- Предложить удалить из перечисления (или пометить комментарием в метаданных)
- Если в будущем функционал возврата будет реализован — создать ROADMAP с тестами
