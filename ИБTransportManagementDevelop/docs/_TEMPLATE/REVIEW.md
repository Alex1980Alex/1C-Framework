# REVIEW — <Название задачи>

> Опционально. Запускается через subagent с `code-verify` skill в режиме
> `quality-review` или `bug-fix-validation` в зависимости от характера задачи.

## Безопасность

- [ ] Нет SQL-инъекций (параметры запросов передаются через `&`)
- [ ] Нет хардкода паролей / connection strings
- [ ] Права доступа RLS соблюдены
- [ ] Логирование без чувствительных данных

## Производительность

- [ ] Нет N+1 запросов
- [ ] Виртуальные таблицы регистров используются вместо подзапросов
- [ ] Индексы есть на ключевых реквизитах
- [ ] Большие выборки — через пакеты запросов

## Корректность

- [ ] Обработка проведения формирует движения корректно
- [ ] При отмене проведения движения автоматически удаляются
- [ ] Граничные случаи (пустые/null значения) обработаны
- [ ] Locale-зависимое поведение (даты, числа) корректно

## Стиль и поддерживаемость

- [ ] Префикс `гкс_` для нетиповых объектов
- [ ] Регионы модуля стандартные (см. `coding-conventions.md`)
- [ ] Комментарии только где логика неочевидна
- [ ] Удалены закомментированные блоки кода / TODO без owner'а

## Совместимость

- [ ] Совместимо с БСП 3.1.x
- [ ] Не ломает РИБ (планы обмена обновлены)
- [ ] Совместимо с расширениями (*.cfe не требуют пересборки)

## Вердикт

PASS / PARTIAL / FAIL

## Рекомендации

<Если PARTIAL/FAIL — конкретные исправления>
