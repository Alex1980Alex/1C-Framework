# Граф зависимостей: ФормаСписка

## Диаграмма

```mermaid
graph TD
    subgraph Модуль
        ФормаСписка["Модуль ФормаСписка"]
        Процедура_ПриСозданииНаСервере[("Процедура ПриСозданииНаСервере")]
        Процедура_СписокПриАктивизацииСтроки[("Процедура СписокПриАктивизацииСтроки")]
        Процедура_Подключаемый_ВыполнитьКоманду[("Процедура Подключаемый_ВыполнитьКоманду")]
        Процедура_Подключаемый_ПродолжитьВыполнениеКомандыНаСервере[("Процедура Подключаемый_ПродолжитьВыполнениеКомандыНаСервере")]
        Процедура_ВыполнитьКомандуНаСервере[("Процедура ВыполнитьКомандуНаСервере")]
        Процедура_Подключаемый_ОбновитьКоманды[("Процедура Подключаемый_ОбновитьКоманды")]
    end

    subgraph Общие модули
        Модуль_ПодключаемыеКоманды["Общий модуль ПодключаемыеКоманды"]
        Модуль_ПодключаемыеКомандыКлиент["Общий модуль ПодключаемыеКомандыКлиент"]
        Модуль_ПодключаемыеКомандыКлиентСервер["Общий модуль ПодключаемыеКомандыКлиентСервер"]
    end

    ФормаСписка --> Процедура_ПриСозданииНаСервере
    ФормаСписка --> Процедура_СписокПриАктивизацииСтроки
    ФормаСписка --> Процедура_Подключаемый_ВыполнитьКоманду
    ФормаСписка --> Процедура_Подключаемый_ПродолжитьВыполнениеКомандыНаСервере
    ФормаСписка --> Процедура_ВыполнитьКомандуНаСервере
    ФормаСписка --> Процедура_Подключаемый_ОбновитьКоманды

    Процедура_ПриСозданииНаСервере -->|ПриСозданииНаСервере| Модуль_ПодключаемыеКоманды
    Процедура_Подключаемый_ВыполнитьКоманду -->|ВыполнитьКоманду| Модуль_ПодключаемыеКоманды
    Процедура_Подключаемый_ПродолжитьВыполнениеКомандыНаСервере -->|НачатьОбновлениеКоманд| Модуль_ПодключаемыеКомандыКлиент
    Процедура_Подключаемый_ПродолжитьВыполнениеКомандыНаСервере -->|НачатьВыполнениеКоманды| Модуль_ПодключаемыеКомандыКлиент
    Процедура_ВыполнитьКомандуНаСервере -->|ОбновитьКоманды| Модуль_ПодключаемыеКомандыКлиентСервер
```

## Статистика

- **Процедур/функций:** 6
- **Экспортируемых:** 1
- **Внешних зависимостей:** 3
- **Внутренних вызовов:** 6

## Детали зависимостей

| Тип | Имя | Использований | Тип использования | Методы |
|-----|-----|---------------|-------------------|--------|
| module | ПодключаемыеКоманды | 2 | call | ПриСозданииНаСервере, ВыполнитьКоманду |
| module | ПодключаемыеКомандыКлиент | 2 | call | НачатьОбновлениеКоманд, НачатьВыполнениеКоманды |
| module | ПодключаемыеКомандыКлиентСервер | 1 | call | ОбновитьКоманды |

---
*Сгенерировано автоматически: 2026-03-09T01:59:06.108Z*
